---
phase: 89-ctf-schema-dimension-table
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - alembic/versions/j4k5l6m7n8o9_ctf_schema.py
  - configs/ctf_config.yaml
autonomous: true

must_haves:
  truths:
    - "dim_ctf_indicators table exists with SMALLSERIAL PK and 22 seeded indicator rows"
    - "ctf fact table exists with composite PK (id, venue_id, ts, base_tf, ref_tf, indicator_id, alignment_source)"
    - "ctf has FK references to dim_venues and dim_ctf_indicators"
    - "ix_ctf_lookup and ix_ctf_indicator indexes exist"
    - "configs/ctf_config.yaml defines tf_pairs, indicators by source, and composite params"
    - "alembic upgrade head runs without error"
  artifacts:
    - path: "alembic/versions/j4k5l6m7n8o9_ctf_schema.py"
      provides: "Alembic migration creating dim_ctf_indicators, ctf, indexes, seed data"
      min_lines: 100
    - path: "configs/ctf_config.yaml"
      provides: "Declarative CTF configuration with tf_pairs, indicators, composite_params"
      min_lines: 40
  key_links:
    - from: "alembic/versions/j4k5l6m7n8o9_ctf_schema.py"
      to: "dim_venues"
      via: "FK reference in ctf table"
      pattern: "REFERENCES public.dim_venues"
    - from: "alembic/versions/j4k5l6m7n8o9_ctf_schema.py"
      to: "dim_ctf_indicators"
      via: "FK reference in ctf table"
      pattern: "REFERENCES public.dim_ctf_indicators"
    - from: "configs/ctf_config.yaml"
      to: "dim_ctf_indicators"
      via: "indicator names match seeded rows"
      pattern: "name: rsi_14"
---

<objective>
Create the complete database foundation for cross-timeframe (CTF) feature infrastructure:
dimension table, fact table, seed data, indexes, and declarative YAML config.

Purpose: Phase 90 (CTF computation engine) depends on these tables and config existing.
This phase establishes the schema contract that all downstream CTF code will target.

Output: One Alembic migration file + one YAML config file. Migration runs cleanly via
`alembic upgrade head`.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/89-ctf-schema-dimension-table/89-RESEARCH.md

# Pattern reference -- most recent migration for exact style
@alembic/versions/i3j4k5l6m7n8_garch_tables.py

# YAML config pattern reference
@configs/cross_asset_config.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Alembic migration -- dim_ctf_indicators + ctf tables + seed data</name>
  <files>alembic/versions/j4k5l6m7n8o9_ctf_schema.py</files>
  <action>
Create Alembic migration file `alembic/versions/j4k5l6m7n8o9_ctf_schema.py`.

Revision metadata:
- revision: `j4k5l6m7n8o9`
- down_revision: `440fdfb3e8e1` (current head -- NOT `i3j4k5l6m7n8` as research says)
- Docstring: ASCII only, describe both tables and seed data

Follow the exact pattern from `i3j4k5l6m7n8_garch_tables.py`:
- `conn = op.get_bind()` then `conn.execute(text(...))`
- All SQL as inline text strings
- `IF NOT EXISTS` on all CREATE TABLE and CREATE INDEX
- ASCII-only comments (NO UTF-8 box-drawing characters)

upgrade() must execute these steps in order:

1. CREATE TABLE dim_ctf_indicators:
   ```sql
   CREATE TABLE IF NOT EXISTS public.dim_ctf_indicators (
       indicator_id    SMALLSERIAL PRIMARY KEY,
       indicator_name  TEXT NOT NULL UNIQUE,
       source_table    TEXT NOT NULL,
       source_column   TEXT NOT NULL,
       is_directional  BOOLEAN NOT NULL DEFAULT TRUE,
       is_active       BOOLEAN NOT NULL DEFAULT TRUE,
       description     TEXT,
       created_at      TIMESTAMPTZ NOT NULL DEFAULT now()
   );
   ```

2. Seed dim_ctf_indicators with exactly 22 rows using INSERT...ON CONFLICT DO NOTHING.
   Use a single INSERT with multiple VALUES tuples. The 22 indicators:

   From ta (11):
   - rsi_14, ta, rsi_14, FALSE
   - rsi_7, ta, rsi_7, FALSE
   - rsi_21, ta, rsi_21, FALSE
   - macd_12_26, ta, macd_12_26, TRUE
   - macd_hist_12_26_9, ta, macd_hist_12_26_9, TRUE
   - macd_8_17, ta, macd_8_17, TRUE
   - macd_hist_8_17_9, ta, macd_hist_8_17_9, TRUE
   - adx_14, ta, adx_14, FALSE
   - bb_width_20, ta, bb_width_20, FALSE
   - stoch_k_14, ta, stoch_k_14, FALSE
   - atr_14, ta, atr_14, FALSE

   From vol (7):
   - vol_parkinson_20, vol, vol_parkinson_20, FALSE
   - vol_gk_20, vol, vol_gk_20, FALSE
   - vol_rs_20, vol, vol_rs_20, FALSE
   - vol_log_roll_20, vol, vol_log_roll_20, FALSE
   - vol_parkinson_63, vol, vol_parkinson_63, FALSE
   - vol_gk_63, vol, vol_gk_63, FALSE
   - vol_log_roll_63, vol, vol_log_roll_63, FALSE

   From returns_bars_multi_tf_u (2):
   - ret_arith, returns_bars_multi_tf_u, ret_arith, TRUE
   - ret_log, returns_bars_multi_tf_u, ret_log, TRUE

   From features (2):
   - close_fracdiff, features, close_fracdiff, TRUE
   - sadf_stat, features, sadf_stat, FALSE

   Format: (indicator_name, source_table, source_column, is_directional)
   ON CONFLICT (indicator_name) DO NOTHING

3. CREATE TABLE ctf:
   ```sql
   CREATE TABLE IF NOT EXISTS public.ctf (
       id              INTEGER NOT NULL,
       venue_id        SMALLINT NOT NULL REFERENCES public.dim_venues(venue_id),
       ts              TIMESTAMPTZ NOT NULL,
       base_tf         TEXT NOT NULL,
       ref_tf          TEXT NOT NULL,
       indicator_id    SMALLINT NOT NULL
                       REFERENCES public.dim_ctf_indicators(indicator_id),
       alignment_source TEXT NOT NULL,
       ref_value       DOUBLE PRECISION,
       base_value      DOUBLE PRECISION,
       slope           DOUBLE PRECISION,
       divergence      DOUBLE PRECISION,
       agreement       DOUBLE PRECISION,
       crossover       DOUBLE PRECISION,
       computed_at     TIMESTAMPTZ NOT NULL DEFAULT now(),
       PRIMARY KEY (id, venue_id, ts, base_tf, ref_tf, indicator_id, alignment_source)
   );
   ```

4. Create indexes:
   ```sql
   CREATE INDEX IF NOT EXISTS ix_ctf_lookup
       ON public.ctf (id, base_tf, ref_tf, indicator_id, ts);
   CREATE INDEX IF NOT EXISTS ix_ctf_indicator
       ON public.ctf (indicator_id, base_tf);
   ```

downgrade() must drop in reverse order:
   ```python
   conn.execute(text("DROP TABLE IF EXISTS public.ctf"))
   conn.execute(text("DROP TABLE IF EXISTS public.dim_ctf_indicators"))
   ```

IMPORTANT: The down_revision is `440fdfb3e8e1` (the actual current head), NOT
`i3j4k5l6m7n8` as the research document states. The research was written before
the `440fdfb3e8e1` migration was added.
  </action>
  <verify>
Run `alembic upgrade head` from the project root. It must complete without errors.
Then verify the tables exist:
- `psql` or `python -c` query: SELECT count(*) FROM dim_ctf_indicators (expect 22)
- `psql` or `python -c` query: SELECT indicator_name, source_table FROM dim_ctf_indicators ORDER BY indicator_id (expect 22 rows)
- Verify ctf table exists (empty, but structurally correct): SELECT column_name FROM information_schema.columns WHERE table_name='ctf'
- Run `alembic downgrade -1` then `alembic upgrade head` to verify idempotency
  </verify>
  <done>
dim_ctf_indicators exists with 22 seeded rows. ctf fact table exists with correct PK,
FKs to dim_venues and dim_ctf_indicators, and both indexes. Migration is reversible.
alembic upgrade head passes cleanly from previous head (440fdfb3e8e1).
  </done>
</task>

<task type="auto">
  <name>Task 2: Declarative YAML config for CTF feature computation</name>
  <files>configs/ctf_config.yaml</files>
  <action>
Create `configs/ctf_config.yaml` following the style of `configs/cross_asset_config.yaml`
(comment header explaining purpose, shallow nesting, plain YAML).

Structure:

```yaml
# Cross-Timeframe (CTF) Feature Configuration
# Phase 89: Declarative config for CTF indicator computation.
# Changing values here changes behavior without code changes.

timeframe_pairs:
  - base_tf: "1D"
    ref_tfs: ["7D", "14D", "30D", "90D", "180D", "365D"]
  - base_tf: "7D"
    ref_tfs: ["30D", "90D", "180D", "365D"]
  - base_tf: "14D"
    ref_tfs: ["90D", "180D", "365D"]
  - base_tf: "30D"
    ref_tfs: ["180D", "365D"]

indicators:
  ta:
    - name: rsi_14
      column: rsi_14
      is_directional: false
    - name: rsi_7
      column: rsi_7
      is_directional: false
    - name: rsi_21
      column: rsi_21
      is_directional: false
    - name: macd_12_26
      column: macd_12_26
      is_directional: true
    - name: macd_hist_12_26_9
      column: macd_hist_12_26_9
      is_directional: true
    - name: macd_8_17
      column: macd_8_17
      is_directional: true
    - name: macd_hist_8_17_9
      column: macd_hist_8_17_9
      is_directional: true
    - name: adx_14
      column: adx_14
      is_directional: false
    - name: bb_width_20
      column: bb_width_20
      is_directional: false
    - name: stoch_k_14
      column: stoch_k_14
      is_directional: false
    - name: atr_14
      column: atr_14
      is_directional: false

  vol:
    - name: vol_parkinson_20
      column: vol_parkinson_20
      is_directional: false
    - name: vol_gk_20
      column: vol_gk_20
      is_directional: false
    - name: vol_rs_20
      column: vol_rs_20
      is_directional: false
    - name: vol_log_roll_20
      column: vol_log_roll_20
      is_directional: false
    - name: vol_parkinson_63
      column: vol_parkinson_63
      is_directional: false
    - name: vol_gk_63
      column: vol_gk_63
      is_directional: false
    - name: vol_log_roll_63
      column: vol_log_roll_63
      is_directional: false

  returns:
    source_table: returns_bars_multi_tf_u
    roll_filter: false
    indicators:
      - name: ret_arith
        column: ret_arith
        is_directional: true
      - name: ret_log
        column: ret_log
        is_directional: true

  features:
    - name: close_fracdiff
      column: close_fracdiff
      is_directional: true
    - name: sadf_stat
      column: sadf_stat
      is_directional: false

composite_params:
  slope_window: 5                # Bars for slope regression
  divergence_zscore_window: 63   # Bars for z-score normalization of divergence
```

Note the `returns` section uses a nested `indicators:` key because it has extra
metadata fields (`source_table`, `roll_filter`) that other source table sections
do not need (ta, vol, features source tables are inferred from the section key).
  </action>
  <verify>
Validate YAML syntax: `python -c "import yaml; yaml.safe_load(open('configs/ctf_config.yaml'))"` runs without error.
Verify the loaded dict has keys: timeframe_pairs (4 items), indicators (4 sub-keys: ta, vol, returns, features), composite_params (2 keys).
Count total indicators: ta=11, vol=7, returns.indicators=2, features=2 = 22 total.
  </verify>
  <done>
configs/ctf_config.yaml exists, parses without error, contains 4 timeframe pair groups,
22 indicators across 4 source table sections, and composite_params with slope_window=5
and divergence_zscore_window=63.
  </done>
</task>

</tasks>

<verification>
1. `alembic upgrade head` completes without error
2. `SELECT count(*) FROM dim_ctf_indicators` returns 22
3. `SELECT column_name FROM information_schema.columns WHERE table_name='ctf' ORDER BY ordinal_position` shows all expected columns
4. `SELECT indexname FROM pg_indexes WHERE tablename='ctf'` shows ix_ctf_lookup and ix_ctf_indicator
5. `python -c "import yaml; d=yaml.safe_load(open('configs/ctf_config.yaml')); assert len(d['timeframe_pairs'])==4; assert len(d['indicators']['ta'])==11"`
6. `alembic downgrade -1` then `alembic upgrade head` succeeds (idempotency)
</verification>

<success_criteria>
- dim_ctf_indicators table has 22 rows with correct indicator_name, source_table, source_column, is_directional values
- ctf table has correct PK, FKs, value columns, and computed_at default
- Both indexes (ix_ctf_lookup, ix_ctf_indicator) exist on ctf
- configs/ctf_config.yaml is valid YAML with 22 indicators, 4 tf_pair groups, composite_params
- Alembic chain is unbroken: 440fdfb3e8e1 -> j4k5l6m7n8o9
</success_criteria>

<output>
After completion, create `.planning/phases/89-ctf-schema-dimension-table/89-01-SUMMARY.md`
</output>
