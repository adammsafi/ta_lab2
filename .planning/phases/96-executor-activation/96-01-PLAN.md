---
phase: 96-executor-activation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - alembic/versions/o9p0q1r2s3t4_phase96_executor_activation.py
  - src/ta_lab2/executor/signal_reader.py
  - sql/signals/096_signals_macd_crossover.sql
  - sql/signals/097_signals_ama_momentum.sql
  - sql/signals/098_signals_ama_mean_reversion.sql
  - sql/signals/099_signals_ama_regime_conditional.sql
  - sql/executor/096_strategy_parity.sql
  - sql/executor/097_pnl_attribution.sql
autonomous: true

must_haves:
  truths:
    - "4 new signal tables exist in the database and accept INSERT"
    - "dim_executor_config accepts all 7 signal_type values without CHECK violation"
    - "dim_executor_config accepts sizing_mode='bl_weight' without CHECK violation"
    - "executor_run_log accepts status='halted' without CHECK violation"
    - "strategy_parity and pnl_attribution tables exist"
    - "SIGNAL_TABLE_MAP contains 7 entries and SignalReader validates all 7 table names"
    - "dim_signals contains rows for all 7 signal types (ema_crossover, rsi_mean_revert, atr_breakout, macd_crossover, ama_momentum, ama_mean_reversion, ama_regime_conditional)"
  artifacts:
    - path: "alembic/versions/o9p0q1r2s3t4_phase96_executor_activation.py"
      provides: "Alembic migration for Phase 96 schema changes"
      contains: "signals_macd_crossover"
    - path: "src/ta_lab2/executor/signal_reader.py"
      provides: "Updated SIGNAL_TABLE_MAP with 7 signal types"
      contains: "ama_regime_conditional"
  key_links:
    - from: "alembic migration"
      to: "dim_executor_config"
      via: "CHECK constraint widening"
      pattern: "chk_exec_config_signal_type"
    - from: "alembic migration"
      to: "dim_signals"
      via: "INSERT seed rows for 4 new signal types"
      pattern: "INSERT INTO.*dim_signals"
    - from: "signal_reader.py"
      to: "paper_executor.py"
      via: "SIGNAL_TABLE_MAP import"
      pattern: "SIGNAL_TABLE_MAP"
---

<objective>
Create the Alembic migration for all Phase 96 schema changes (4 new signal tables, 3 CHECK constraint widenings, 2 new reporting tables, 1 bug fix, dim_signals seed rows for new signal types) and update SIGNAL_TABLE_MAP in signal_reader.py to recognize all 7 signal types.

Purpose: This is the foundation plan. Every subsequent plan depends on these schema changes existing. The migration must run before any configs can be seeded or generators can write signals. Critically, dim_signals must contain rows for all 7 signal types or seed_executor_config.py will silently skip configs for missing signal types.

Output: Alembic migration file, 6 reference DDL files, updated signal_reader.py with 7-entry SIGNAL_TABLE_MAP.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/96-executor-activation/96-CONTEXT.md
@.planning/phases/96-executor-activation/96-RESEARCH.md

@sql/signals/060_signals_ema_crossover.sql
@src/ta_lab2/executor/signal_reader.py
@sql/executor/088_dim_executor_config.sql
@alembic/versions/n8o9p0q1r2s3_phase87_pipeline_wiring.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Alembic migration for all Phase 96 schema changes</name>
  <files>
    alembic/versions/o9p0q1r2s3t4_phase96_executor_activation.py
  </files>
  <action>
    Create a single Alembic migration that performs ALL Phase 96 schema changes:

    1. **Widen `chk_exec_config_signal_type`** on `dim_executor_config`:
       - DROP the existing constraint
       - CREATE new constraint allowing: `'ema_crossover', 'rsi_mean_revert', 'atr_breakout', 'macd_crossover', 'ama_momentum', 'ama_mean_reversion', 'ama_regime_conditional'`

    2. **Widen `chk_exec_config_sizing_mode`** on `dim_executor_config`:
       - DROP existing constraint
       - CREATE new constraint allowing: `'fixed_fraction', 'regime_adjusted', 'signal_strength', 'target_vol', 'bl_weight'`

    3. **Fix existing bug: widen `chk_exec_run_status`** on `executor_run_log`:
       - DROP existing constraint
       - CREATE new constraint allowing: `'running', 'success', 'failed', 'stale_signal', 'no_signals', 'halted'`

    4. **Seed `dim_signals` with rows for the 4 new signal types** (CRITICAL — without these rows, seed_executor_config.py silently skips all new configs because it resolves signal_name -> signal_id from dim_signals):
       - Query existing dim_signals to see what signal_name values exist for the original 3 types (ema_crossover, rsi_mean_revert, atr_breakout). Use `SELECT signal_id, signal_name FROM dim_signals` to understand the naming pattern.
       - INSERT rows for each new signal type. The `signal_name` values MUST match the `signal_name` field in `configs/executor_config_seed.yaml` (Plan 96-03). Use these signal_name values:
         - `macd_12_26_9_long` (signal_type: macd_crossover)
         - `ama_momentum_v1` (signal_type: ama_momentum)
         - `ama_mean_reversion_v1` (signal_type: ama_mean_reversion)
         - `ama_regime_conditional_v1` (signal_type: ama_regime_conditional)
       - Use INSERT...ON CONFLICT DO NOTHING (idempotent) keyed on signal_name (unique constraint).
       - Each row needs: `signal_name`, `signal_type`, `description`, `is_active` (default TRUE), `created_at` (DEFAULT now()).
       - Check the dim_signals table DDL for exact column names before inserting.

    5. **Create 4 new signal tables** (same schema as `signals_ema_crossover`):
       - `signals_macd_crossover`
       - `signals_ama_momentum`
       - `signals_ama_mean_reversion`
       - `signals_ama_regime_conditional`

       Each table must have:
       - PK: `(id, ts, signal_id)`
       - Columns: `id INTEGER NOT NULL`, `ts TIMESTAMPTZ NOT NULL`, `signal_id INTEGER NOT NULL`, `direction TEXT NOT NULL`, `position_state TEXT NOT NULL`, `entry_price NUMERIC NULL`, `entry_ts TIMESTAMPTZ NULL`, `exit_price NUMERIC NULL`, `exit_ts TIMESTAMPTZ NULL`, `pnl_pct NUMERIC NULL`, `feature_snapshot JSONB NULL`, `signal_version TEXT NULL`, `feature_version_hash TEXT NULL`, `params_hash TEXT NULL`, `executor_processed_at TIMESTAMPTZ NULL` (CRITICAL - needed for replay guard), `created_at TIMESTAMPTZ DEFAULT now()`
       - FK: `signal_id REFERENCES public.dim_signals(signal_id)`
       - Indexes: open positions partial index, backtest index, closed positions partial index (mirror signals_ema_crossover indexes)

    6. **Create `strategy_parity` table**:
       - `parity_id SERIAL PRIMARY KEY`
       - `computed_at TIMESTAMPTZ NOT NULL DEFAULT now()`
       - `strategy TEXT NOT NULL` (config_name from dim_executor_config)
       - `window_days INTEGER NOT NULL`
       - `live_sharpe_fill NUMERIC`, `live_sharpe_mtm NUMERIC`, `bt_sharpe NUMERIC`
       - `ratio_fill NUMERIC`, `ratio_mtm NUMERIC`
       - `n_fills INTEGER`, `n_mtm_days INTEGER`
       - Index: `(strategy, computed_at DESC)`

    7. **Create `pnl_attribution` table**:
       - `attr_id SERIAL PRIMARY KEY`
       - `computed_at TIMESTAMPTZ NOT NULL DEFAULT now()`
       - `period_start DATE NOT NULL`, `period_end DATE NOT NULL`
       - `asset_class TEXT NOT NULL` ('crypto', 'perp', 'all')
       - `benchmark TEXT NOT NULL` ('BTC', 'SPX', 'underlying', 'blended')
       - `total_pnl NUMERIC`, `beta_pnl NUMERIC`, `alpha_pnl NUMERIC`
       - `beta NUMERIC`, `sharpe_alpha NUMERIC`, `n_positions INTEGER`
       - Index: `(period_start, period_end, asset_class)`

    Revision chain: `Revises: n8o9p0q1r2s3` (the latest migration).
    Use `schema="public"` for all operations.
    Use ASCII-only comments (Windows cp1252 compatibility -- project convention).
    Follow the pattern in existing migrations (e.g., `n8o9p0q1r2s3_phase87_pipeline_wiring.py`).

    downgrade() should: drop all 6 new tables, restore original CHECK constraints, and DELETE the 4 new dim_signals rows (by signal_name).
  </action>
  <verify>
    Run `alembic heads` to verify single head, then `alembic upgrade head` to apply.
    Query: `SELECT table_name FROM information_schema.tables WHERE table_schema='public' AND table_name IN ('signals_macd_crossover','signals_ama_momentum','signals_ama_mean_reversion','signals_ama_regime_conditional','strategy_parity','pnl_attribution')` returns 6 rows.
    Test CHECK: `INSERT INTO dim_executor_config (config_name,signal_type,signal_id,sizing_mode) VALUES ('__test_macd','macd_crossover',1,'bl_weight') ON CONFLICT DO NOTHING` succeeds (then DELETE the test row).
    Verify dim_signals seeded: `SELECT signal_name FROM dim_signals WHERE signal_name IN ('macd_12_26_9_long','ama_momentum_v1','ama_mean_reversion_v1','ama_regime_conditional_v1')` returns 4 rows.
  </verify>
  <done>6 new tables exist in the database. 3 CHECK constraints widened. 4 new dim_signals rows seeded. Migration is reversible.</done>
</task>

<task type="auto">
  <name>Task 2: Update SIGNAL_TABLE_MAP and write reference DDL files</name>
  <files>
    src/ta_lab2/executor/signal_reader.py
    sql/signals/096_signals_macd_crossover.sql
    sql/signals/097_signals_ama_momentum.sql
    sql/signals/098_signals_ama_mean_reversion.sql
    sql/signals/099_signals_ama_regime_conditional.sql
    sql/executor/096_strategy_parity.sql
    sql/executor/097_pnl_attribution.sql
  </files>
  <action>
    1. **Update `signal_reader.py`**: Expand `SIGNAL_TABLE_MAP` to include all 7 types:
       ```python
       SIGNAL_TABLE_MAP: dict[str, str] = {
           "ema_crossover": "signals_ema_crossover",
           "rsi_mean_revert": "signals_rsi_mean_revert",
           "atr_breakout": "signals_atr_breakout",
           "macd_crossover": "signals_macd_crossover",
           "ama_momentum": "signals_ama_momentum",
           "ama_mean_reversion": "signals_ama_mean_reversion",
           "ama_regime_conditional": "signals_ama_regime_conditional",
       }
       ```
       The `_VALID_SIGNAL_TABLES` frozenset is auto-derived from `.values()` so it will update automatically.

    2. **Write reference DDL files** (for documentation, matching existing convention in `sql/signals/`):
       - `sql/signals/096_signals_macd_crossover.sql` — CREATE TABLE for signals_macd_crossover (same schema as 060)
       - `sql/signals/097_signals_ama_momentum.sql` — CREATE TABLE for signals_ama_momentum
       - `sql/signals/098_signals_ama_mean_reversion.sql` — CREATE TABLE for signals_ama_mean_reversion
       - `sql/signals/099_signals_ama_regime_conditional.sql` — CREATE TABLE for signals_ama_regime_conditional
       - `sql/executor/096_strategy_parity.sql` — CREATE TABLE for strategy_parity
       - `sql/executor/097_pnl_attribution.sql` — CREATE TABLE for pnl_attribution

       Each reference DDL file should include:
       - Header comment with table description
       - `CREATE TABLE IF NOT EXISTS` statement matching the Alembic migration columns exactly
       - Index definitions matching the Alembic migration
       - COMMENT ON TABLE/COLUMN as in the existing signal DDL files
       - Note: "Reference DDL -- actual migration is in alembic/versions/o9p0q1r2s3t4_..."
  </action>
  <verify>
    Run: `python -c "from ta_lab2.executor.signal_reader import SIGNAL_TABLE_MAP; assert len(SIGNAL_TABLE_MAP) == 7, f'Expected 7, got {len(SIGNAL_TABLE_MAP)}'; print('OK: 7 signal types in SIGNAL_TABLE_MAP')"` succeeds.
    Verify all 6 reference DDL files exist: `ls sql/signals/09*.sql sql/executor/09*.sql`.
    Run ruff check on signal_reader.py: `ruff check src/ta_lab2/executor/signal_reader.py`.
  </verify>
  <done>SIGNAL_TABLE_MAP has 7 entries. _VALID_SIGNAL_TABLES auto-validates all 7 table names. 6 reference DDL files written.</done>
</task>

</tasks>

<verification>
- `alembic current` shows the new revision as head
- All 6 new tables queryable via `SELECT COUNT(*) FROM <table>` (returns 0, no errors)
- CHECK constraint test INSERT for all 7 signal types succeeds
- CHECK constraint test INSERT for `sizing_mode='bl_weight'` succeeds
- CHECK constraint test INSERT for `status='halted'` into executor_run_log succeeds
- `SIGNAL_TABLE_MAP` has 7 entries when imported
- Reference DDL files exist in sql/ directories
- dim_signals contains rows for all 7 signal types (4 new + 3 existing)
</verification>

<success_criteria>
- 6 new database tables created via Alembic migration
- 3 CHECK constraints widened (signal_type, sizing_mode, executor_run_log status)
- 4 new dim_signals rows seeded (macd_crossover, ama_momentum, ama_mean_reversion, ama_regime_conditional)
- SIGNAL_TABLE_MAP updated to 7 entries in signal_reader.py
- All reference DDL files written
- Existing tests and imports still pass
</success_criteria>

<output>
After completion, create `.planning/phases/96-executor-activation/96-01-SUMMARY.md`
</output>
