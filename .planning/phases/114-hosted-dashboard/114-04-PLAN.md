---
phase: 114-hosted-dashboard
plan: 04
type: execute
wave: 2
depends_on: ["114-01"]
files_modified:
  - src/ta_lab2/dashboard/app.py
  - src/ta_lab2/dashboard/mobile.py
autonomous: true

must_haves:
  truths:
    - "Dashboard injects responsive CSS for mobile viewports (<768px)"
    - "Sidebar auto-collapses on mobile (Streamlit default, verified)"
    - "Data Refresh button in sidebar triggers sync_dashboard_to_vm when on VM"
    - "Charts and tables are usable on mobile screens"
  artifacts:
    - path: "src/ta_lab2/dashboard/mobile.py"
      provides: "Mobile CSS injection and refresh button logic"
      min_lines: 40
    - path: "src/ta_lab2/dashboard/app.py"
      provides: "Updated app.py importing mobile CSS"
  key_links:
    - from: "src/ta_lab2/dashboard/app.py"
      to: "src/ta_lab2/dashboard/mobile.py"
      via: "import and call inject_mobile_css()"
      pattern: "from.*mobile import|inject_mobile_css"
---

<objective>
Add mobile responsive CSS and a data refresh button to the dashboard for hosted deployment.

Purpose: The hosted dashboard must be usable from mobile browsers. A refresh button lets users trigger a data sync on-demand.
Output: Mobile CSS module and updated app.py with refresh integration.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/114-hosted-dashboard/114-RESEARCH.md
@.planning/phases/114-hosted-dashboard/114-CONTEXT.md
@src/ta_lab2/dashboard/app.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create mobile CSS module</name>
  <files>src/ta_lab2/dashboard/mobile.py</files>
  <action>
Create mobile.py with a single function inject_mobile_css() that uses st.markdown(unsafe_allow_html=True) to inject responsive CSS.

CSS rules within @media (max-width: 768px):
- Stacked columns: `.stHorizontalBlock { flex-direction: column !important; }` (Streamlit columns stack vertically)
- Smaller font: `.stMarkdown { font-size: 14px; }` and `.stMetric label { font-size: 12px; }`
- Full-width charts: `.stPlotlyChart, .stVegaLiteChart { width: 100% !important; overflow-x: auto; }`
- Table horizontal scroll: `.stDataFrame { overflow-x: auto; }`
- Reduce padding: `.block-container { padding: 0.5rem 1rem !important; }`
- Metric values: `.stMetric [data-testid="stMetricValue"] { font-size: 1.2rem; }`

Also add general (non-mobile) CSS improvements:
- `.stDataFrame td, .stDataFrame th { white-space: nowrap; }` (prevent table cell wrapping)

The function should be called once in app.py, before the navigation runs.
  </action>
  <verify>
    python -c "from ta_lab2.dashboard.mobile import inject_mobile_css; print('import ok')"
  </verify>
  <done>
    mobile.py exists with inject_mobile_css() containing @media (max-width: 768px) responsive rules.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update app.py with mobile CSS and refresh button</name>
  <files>src/ta_lab2/dashboard/app.py</files>
  <action>
Update app.py with two additions:

1. Import and call inject_mobile_css() after st.set_page_config() but before sidebar:
   ```python
   from ta_lab2.dashboard.mobile import inject_mobile_css
   inject_mobile_css()
   ```

2. Add a "Sync Data from Local" button in the sidebar (below the existing "Refresh Now" button):
   - Only show if running on the VM (detect via: socket.gethostname() or os.environ.get("DASHBOARD_ENV") == "vm")
   - Use environment variable approach: check for DASHBOARD_ENV=vm (set in streamlit.service EnvironmentFile)
   - On click, run sync_dashboard_to_vm.py as a subprocess (the script is bidirectional -- on VM it would need to reverse SSH back, which is complex)
   - SIMPLER APPROACH: Just show a st.info() saying "Run sync from local PC: python -m ta_lab2.scripts.etl.sync_dashboard_to_vm" with a "Last synced" timestamp from VM's sync_log table
   - Query: SELECT MAX(synced_at) FROM hyperliquid.sync_log WHERE source = 'dashboard_sync' (or similar)
   - If no sync record, show "Never synced"

Do NOT remove or modify any existing sidebar functionality. The "Refresh Now" button (cache clear) stays as-is.
  </action>
  <verify>
    python -c "import ast; ast.parse(open('src/ta_lab2/dashboard/app.py').read()); print('syntax ok')"
    grep "inject_mobile_css" src/ta_lab2/dashboard/app.py
    grep "DASHBOARD_ENV" src/ta_lab2/dashboard/app.py
  </verify>
  <done>
    app.py imports and calls inject_mobile_css().
    app.py shows "Last synced" info on VM environment.
    Existing sidebar functionality preserved.
  </done>
</task>

</tasks>

<verification>
- `python -c "from ta_lab2.dashboard.mobile import inject_mobile_css"` succeeds
- app.py has inject_mobile_css() call before sidebar
- app.py has DASHBOARD_ENV check for VM-specific sidebar content
- ruff check + ruff format pass on both files
</verification>

<success_criteria>
Dashboard has responsive CSS for mobile viewports and shows last-sync timestamp when running on VM.
</success_criteria>

<output>
After completion, create `.planning/phases/114-hosted-dashboard/114-04-SUMMARY.md`
</output>
