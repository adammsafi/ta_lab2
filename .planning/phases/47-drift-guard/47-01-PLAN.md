---
phase: 47-drift-guard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - alembic/versions/XXXX_drift_guard.py
  - sql/drift/094_cmc_drift_metrics.sql
  - sql/drift/095_v_drift_summary.sql
autonomous: true

must_haves:
  truths:
    - "cmc_drift_metrics table exists with correct PK and columns"
    - "v_drift_summary materialized view exists with unique index"
    - "dim_risk_state has drift_paused, drift_paused_at, drift_paused_reason, drift_auto_escalate_after_days columns"
    - "dim_risk_limits has drift threshold columns"
    - "cmc_risk_events CHECK constraint includes 3 new drift event types"
    - "cmc_risk_events trigger_source CHECK constraint includes drift_monitor"
    - "cmc_executor_run_log has data_snapshot JSONB column"
  artifacts:
    - path: "alembic/versions/XXXX_drift_guard.py"
      provides: "Alembic migration for all Phase 47 DDL changes"
      contains: "cmc_drift_metrics"
    - path: "sql/drift/094_cmc_drift_metrics.sql"
      provides: "Reference DDL for cmc_drift_metrics table"
      contains: "CREATE TABLE"
    - path: "sql/drift/095_v_drift_summary.sql"
      provides: "Reference DDL for v_drift_summary materialized view"
      contains: "CREATE MATERIALIZED VIEW"
  key_links:
    - from: "alembic/versions/XXXX_drift_guard.py"
      to: "dim_risk_state"
      via: "ALTER TABLE ADD COLUMN"
      pattern: "drift_paused"
    - from: "alembic/versions/XXXX_drift_guard.py"
      to: "cmc_risk_events"
      via: "DROP CONSTRAINT + ADD CONSTRAINT"
      pattern: "chk_risk_events_type"
---

<objective>
Create the Alembic migration and reference DDL for all Phase 47 database schema changes: new cmc_drift_metrics table, v_drift_summary materialized view, extensions to dim_risk_state (drift pause columns), dim_risk_limits (drift threshold columns), cmc_risk_events (3 new event types + drift_monitor trigger source), and cmc_executor_run_log (data_snapshot JSONB).

Purpose: Establish the database foundation that all subsequent drift guard plans depend on.
Output: One Alembic migration file + two reference DDL files in sql/drift/.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/47-drift-guard/47-CONTEXT.md
@.planning/phases/47-drift-guard/47-RESEARCH.md
@alembic/versions/b5178d671e38_risk_controls.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create reference DDL files for drift tables</name>
  <files>
    sql/drift/094_cmc_drift_metrics.sql
    sql/drift/095_v_drift_summary.sql
  </files>
  <action>
Create `sql/drift/` directory. Write two reference DDL files (ASCII-only comments, no box-drawing characters):

**094_cmc_drift_metrics.sql:**
- CREATE TABLE IF NOT EXISTS public.cmc_drift_metrics with columns:
  - metric_id UUID PRIMARY KEY DEFAULT gen_random_uuid()
  - metric_date DATE NOT NULL
  - created_at TIMESTAMPTZ NOT NULL DEFAULT now()
  - config_id INTEGER NOT NULL (FK concept to dim_executor_config)
  - asset_id INTEGER NOT NULL
  - signal_type TEXT NOT NULL
  - pit_replay_run_id UUID NULL (FK concept to cmc_backtest_runs)
  - cur_replay_run_id UUID NULL
  - paper_trade_count INTEGER NOT NULL DEFAULT 0
  - replay_trade_count INTEGER NOT NULL DEFAULT 0
  - unmatched_paper INTEGER NOT NULL DEFAULT 0
  - unmatched_replay INTEGER NOT NULL DEFAULT 0
  - paper_cumulative_pnl NUMERIC NULL
  - replay_pit_cumulative_pnl NUMERIC NULL
  - replay_cur_cumulative_pnl NUMERIC NULL
  - absolute_pnl_diff NUMERIC NULL
  - data_revision_pnl_diff NUMERIC NULL
  - tracking_error_5d NUMERIC NULL
  - tracking_error_30d NUMERIC NULL
  - paper_sharpe NUMERIC NULL
  - replay_sharpe NUMERIC NULL
  - sharpe_divergence NUMERIC NULL
  - threshold_breach BOOLEAN NOT NULL DEFAULT FALSE
  - drift_pct_of_threshold NUMERIC NULL
  - attr_baseline_pnl NUMERIC NULL
  - attr_fee_delta NUMERIC NULL
  - attr_slippage_delta NUMERIC NULL
  - attr_timing_delta NUMERIC NULL
  - attr_data_revision_delta NUMERIC NULL
  - attr_sizing_delta NUMERIC NULL
  - attr_regime_delta NUMERIC NULL
  - attr_unexplained NUMERIC NULL
  - CONSTRAINT uq_drift_metrics_scope UNIQUE (metric_date, config_id, asset_id)
- Three indexes: idx_drift_metrics_date (metric_date DESC), idx_drift_metrics_config (config_id, metric_date DESC), idx_drift_metrics_breach (threshold_breach, metric_date DESC) WHERE threshold_breach = TRUE

**095_v_drift_summary.sql:**
- CREATE MATERIALIZED VIEW IF NOT EXISTS public.v_drift_summary with aggregations: days_monitored, breach_count, avg/max tracking_error_5d, avg_absolute_pnl_diff, avg_sharpe_divergence, last_metric_date, current_tracking_error_5d
- GROUP BY config_id, asset_id, signal_type
- CREATE UNIQUE INDEX IF NOT EXISTS uq_drift_summary ON v_drift_summary (config_id, asset_id) -- required for REFRESH CONCURRENTLY
- Add COMMENT ON TABLE for both

ASCII-only in all SQL comments. Use `encoding='utf-8'` pattern note in file header comment.
  </action>
  <verify>
Files exist at sql/drift/094_cmc_drift_metrics.sql and sql/drift/095_v_drift_summary.sql. No non-ASCII characters in either file. Both contain valid SQL.
  </verify>
  <done>
Two reference DDL files in sql/drift/ with complete table and view definitions, all ASCII comments, correct column types and constraints.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Alembic migration for all Phase 47 DDL</name>
  <files>alembic/versions/XXXX_drift_guard.py</files>
  <action>
FIRST: Run `python -m alembic history` to verify current head is b5178d671e38 (risk_controls). Use this as down_revision.

Create Alembic migration `drift_guard` with revision generated by alembic or manual hex ID. The migration must perform ALL of these operations in upgrade():

**1. Create cmc_drift_metrics table** (use op.create_table matching the reference DDL). Include all columns, PK, UNIQUE constraint. Then create the 3 indexes via op.create_index.

**2. Create v_drift_summary materialized view** via op.execute() with raw SQL (Alembic has no native materialized view op). Include the unique index on (config_id, asset_id) for REFRESH CONCURRENTLY support.

**3. Extend dim_risk_state** with 4 new columns via op.execute() raw SQL:
```sql
ALTER TABLE public.dim_risk_state
  ADD COLUMN IF NOT EXISTS drift_paused BOOLEAN NOT NULL DEFAULT FALSE,
  ADD COLUMN IF NOT EXISTS drift_paused_at TIMESTAMPTZ NULL,
  ADD COLUMN IF NOT EXISTS drift_paused_reason TEXT NULL,
  ADD COLUMN IF NOT EXISTS drift_auto_escalate_after_days INTEGER NOT NULL DEFAULT 7;
```

**4. Extend dim_risk_limits** with drift threshold columns via op.execute():
```sql
ALTER TABLE public.dim_risk_limits
  ADD COLUMN IF NOT EXISTS drift_tracking_error_threshold_5d NUMERIC NULL DEFAULT 0.015,
  ADD COLUMN IF NOT EXISTS drift_tracking_error_threshold_30d NUMERIC NULL DEFAULT 0.005,
  ADD COLUMN IF NOT EXISTS drift_window_days INTEGER NULL DEFAULT 5;
```
Then UPDATE the existing seed row to set drift thresholds:
```sql
UPDATE public.dim_risk_limits SET drift_tracking_error_threshold_5d = 0.015, drift_tracking_error_threshold_30d = 0.005, drift_window_days = 5 WHERE asset_id IS NULL AND strategy_id IS NULL;
```

**5. Extend cmc_risk_events CHECK constraints** via op.execute():
- DROP CONSTRAINT chk_risk_events_type
- ADD CONSTRAINT chk_risk_events_type with all existing 10 types PLUS: 'drift_pause_activated', 'drift_pause_disabled', 'drift_escalated'
- DROP CONSTRAINT chk_risk_events_source
- ADD CONSTRAINT chk_risk_events_source with existing 4 sources PLUS: 'drift_monitor'

**6. Extend cmc_executor_run_log** with data_snapshot column:
```sql
ALTER TABLE public.cmc_executor_run_log ADD COLUMN IF NOT EXISTS data_snapshot JSONB NULL;
```

**downgrade()** must reverse all operations in reverse order:
- DROP COLUMN data_snapshot from cmc_executor_run_log
- Restore original CHECK constraints on cmc_risk_events (DROP + ADD with original lists)
- DROP drift columns from dim_risk_limits
- DROP drift columns from dim_risk_state
- DROP MATERIALIZED VIEW v_drift_summary
- DROP TABLE cmc_drift_metrics

CRITICAL: ASCII-only in all docstrings and comments. Use `encoding='utf-8'` note. All CHECK constraint values must exactly match the existing ones in b5178d671e38 plus the new additions.
  </action>
  <verify>
Run `python -m alembic history` to confirm new migration appears as head with correct down_revision chain. Run `python -m alembic upgrade head` to apply migration successfully. Verify tables/columns exist by running quick SQL check.
  </verify>
  <done>
Alembic migration applies cleanly. cmc_drift_metrics table, v_drift_summary view, and all ALTER TABLE extensions are live in the database. `alembic current` shows the new revision as head.
  </done>
</task>

</tasks>

<verification>
1. `python -m alembic history` shows drift_guard migration in chain after risk_controls
2. `python -m alembic upgrade head` completes without errors
3. SQL verification: `SELECT column_name FROM information_schema.columns WHERE table_name='cmc_drift_metrics'` returns expected columns
4. SQL verification: `SELECT drift_paused FROM dim_risk_state WHERE state_id=1` returns FALSE
5. SQL verification: `SELECT drift_tracking_error_threshold_5d FROM dim_risk_limits LIMIT 1` returns 0.015
6. Reference DDL files exist in sql/drift/
</verification>

<success_criteria>
- Alembic migration applies and downgrades cleanly
- All 7 schema changes are present (new table, materialized view, 4 ALTER TABLE extensions, 1 CHECK constraint extension)
- Reference DDL matches migration content
- No non-ASCII characters in any file
</success_criteria>

<output>
After completion, create `.planning/phases/47-drift-guard/47-01-SUMMARY.md`
</output>
