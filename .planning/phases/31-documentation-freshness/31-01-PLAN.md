---
phase: 31-documentation-freshness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - README.md
  - mkdocs.yml
  - docs/index.md
  - docs/DESIGN.md
  - docs/deployment.md
  - docs/ops/update_price_histories_and_emas.md
autonomous: true

must_haves:
  truths:
    - "pyproject.toml version is 0.8.0"
    - "README.md heading says v0.8.0"
    - "mkdocs.yml site_name says v0.8.0"
    - "docs/index.md heading says v0.8.0"
    - "docs/DESIGN.md version header says 0.8.0"
    - "docs/deployment.md version header says 0.8.0"
    - "No [TODO:] placeholders remain in docs/ops/"
    - "No aspirational alembic commands remain in README.md or docs/index.md"
    - "No reference to black formatter remains in docs/index.md"
  artifacts:
    - path: "pyproject.toml"
      provides: "Canonical version source"
      contains: 'version = "0.8.0"'
    - path: "README.md"
      provides: "User-facing version"
      contains: "v0.8.0"
    - path: "mkdocs.yml"
      provides: "Docs site version"
      contains: "v0.8.0"
  key_links:
    - from: "pyproject.toml"
      to: "README.md"
      via: "version string consistency"
      pattern: "0\\.8\\.0"
    - from: "pyproject.toml"
      to: "mkdocs.yml"
      via: "version string consistency"
      pattern: "0\\.8\\.0"
---

<objective>
Bump all version strings to 0.8.0 and eliminate every stale reference (aspirational alembic commands, black formatter, outdated callouts, [TODO:] placeholders) across documentation files.

Purpose: DOCS-01 and DOCS-03 — version consistency and stale reference cleanup are the foundation that must land before mkdocs nav fixes (Plan 03) touch these same files.
Output: All 7 files updated with correct version and no stale content.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-documentation-freshness/31-CONTEXT.md
@.planning/phases/31-documentation-freshness/31-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Version bump to 0.8.0 across all version-bearing files</name>
  <files>
    pyproject.toml
    README.md
    mkdocs.yml
    docs/index.md
    docs/DESIGN.md
    docs/deployment.md
  </files>
  <action>
    Update version strings in these 6 files:

    1. **pyproject.toml** line 7: `version = "0.5.0"` -> `version = "0.8.0"`

    2. **README.md**:
       - Line 1: `# ta_lab2 v0.5.0` -> `# ta_lab2 v0.8.0`
       - Lines 7-9: Replace `> **v0.5.0 Ecosystem Reorganization Complete**` callout block with:
         `> **v0.8.0 Polish and Hardening**`
         `> Production-hardened infrastructure with automated stats/QA pipeline, blocking CI lint, and fresh documentation.`
       - Lines ~71, ~122: Replace any `v0.5.0 reorganization` phrasing with `v0.8.0` or remove the qualifier where it reads awkwardly.
       - Lines ~543-555: Update `Latest Release: v0.5.0 (2026-02-04)` changelog section to `Latest Release: v0.8.0 (2026-02-22)` with a brief summary: "Stats/QA orchestration wired into daily refresh, ruff lint blocking in CI, mypy on features/regimes, documentation freshness pass."

    3. **mkdocs.yml** line 1: `site_name: ta_lab2 v0.4.0` -> `site_name: ta_lab2 v0.8.0`

    4. **docs/index.md** line 1: `# ta_lab2 v0.4.0` -> `# ta_lab2 v0.8.0`

    5. **docs/DESIGN.md**:
       - Line 3: `Version: 0.4.0` -> `Version: 0.8.0`
       - Line ~509 (footer): `*Version: 0.4.0 release candidate*` -> `*Version: 0.8.0*`

    6. **docs/deployment.md**:
       - Line 3: `Version: 0.4.0` -> `Version: 0.8.0`
       - Line ~962 (footer): `*Version: 0.4.0 release candidate*` -> `*Version: 0.8.0*`

    Do NOT change historical changelog entries (e.g., `docs/api/orchestrator.md` line 559 mentioning v0.4.0 as a historical fact).
  </action>
  <verify>
    Run these checks:
    ```
    grep -n 'version = "0.8.0"' pyproject.toml
    head -1 README.md | grep "v0.8.0"
    grep "site_name" mkdocs.yml | grep "v0.8.0"
    head -1 docs/index.md | grep "v0.8.0"
    grep "Version:" docs/DESIGN.md | head -1 | grep "0.8.0"
    grep "Version:" docs/deployment.md | head -1 | grep "0.8.0"
    ```
    All 6 commands must produce matching output.
  </verify>
  <done>All 6 files show version 0.8.0. No file still contains 0.4.0 or 0.5.0 in a version-position (header, footer, site_name). Historical changelog entries are preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Remove stale references and resolve TODO placeholders</name>
  <files>
    README.md
    docs/index.md
    docs/ops/update_price_histories_and_emas.md
  </files>
  <action>
    **Aspirational alembic removal:**

    1. **README.md** lines ~463-476: Delete the entire `### Database Migrations` subsection that contains alembic commands (`alembic upgrade head`, `alembic revision --autogenerate`, `alembic history`). Delete from the `### Database Migrations` heading through the end of that subsection. Do NOT leave a placeholder.

    2. **docs/index.md** lines ~400-412: Delete the entire `### Database Migrations` subsection with the same 3 alembic commands. Same pattern as README.

    **Aspirational black removal:**

    3. **docs/index.md** line ~391: In the Code Quality section, replace `black src/ tests/` with `ruff format src/` (ruff is the actual formatter since Phase 30). The surrounding `ruff check src/ --fix` line is already correct — leave it.

    **TODO placeholder resolution in ops doc:**

    4. **docs/ops/update_price_histories_and_emas.md**:
       - Line 191: Replace `[TODO: fill in canonical post-load row-count check SQL here]` with:
         `sql/metrics/current_vs_snapshot_rowcount_comparisson.sql`
       - Line 310: Replace `[TODO: insert path/filename, e.g. refresh_cmc_ema_daily_stats.py]` with:
         `python -m ta_lab2.scripts.emas.stats.multi_tf.refresh_ema_multi_tf_stats`
       - Line 367: Replace `[TODO: insert path/filename, e.g. refresh_cmc_ema_multi_tf_stats.py]` with:
         `python -m ta_lab2.scripts.emas.stats.multi_tf_cal.refresh_ema_multi_tf_cal_stats`
       - Line 419: Replace `[TODO: insert path/filename, e.g. refresh_cmc_ema_multi_tf_cal_stats.py]` with:
         `python -m ta_lab2.scripts.emas.stats.multi_tf_cal_anchor.refresh_ema_multi_tf_cal_anchor_stats`
  </action>
  <verify>
    ```
    # No alembic commands in README or index
    grep -c "alembic" README.md  # Should be 0
    grep -c "alembic" docs/index.md  # Should be 0

    # No black references in index
    grep -c "black src" docs/index.md  # Should be 0

    # No TODO placeholders in ops doc
    grep -c "\[TODO:" docs/ops/update_price_histories_and_emas.md  # Should be 0

    # Verify ruff format is present
    grep "ruff format" docs/index.md  # Should match
    ```
  </verify>
  <done>Zero alembic commands in README.md and docs/index.md. Zero black references in docs/index.md (replaced with ruff format). Zero [TODO:] placeholders in docs/ops/update_price_histories_and_emas.md (all 4 resolved with actual script paths).</done>
</task>

</tasks>

<verification>
- `grep -rn "0\.4\.0\|0\.5\.0" pyproject.toml README.md mkdocs.yml docs/index.md docs/DESIGN.md docs/deployment.md` returns no version-position matches (historical changelog entries in other files are OK)
- `grep -rn "\[TODO:" docs/` returns zero matches
- `grep -rn "alembic" README.md docs/index.md` returns zero matches
- `grep -rn "black src" docs/index.md` returns zero matches
</verification>

<success_criteria>
1. All 6 version-bearing files show 0.8.0
2. Zero [TODO:] placeholders in docs/ops/
3. Zero aspirational alembic commands in README.md and docs/index.md
4. Zero black formatter references in docs/index.md
5. Historical changelog entries (e.g., v0.4.0 initial release) preserved untouched
</success_criteria>

<output>
After completion, create `.planning/phases/31-documentation-freshness/31-01-SUMMARY.md`
</output>
