---
phase: 31-documentation-freshness
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/diagrams/data_flow.mmd
autonomous: true

must_haves:
  truths:
    - "Main pipeline diagram shows the full data flow from price_histories7 through stats/QA"
    - "Diagram includes actual DB table names, not just conceptual labels"
    - "A secondary detail diagram shows the 6-variant structure for bars and EMAs"
    - "Mermaid syntax renders correctly (valid flowchart/graph definitions)"
  artifacts:
    - path: "docs/diagrams/data_flow.mmd"
      provides: "Pipeline architecture visualization"
      min_lines: 50
  key_links: []
---

<objective>
Replace the obsolete v0.5.0 file-migration diagram with two accurate v0.8.0 pipeline diagrams: (1) main data flow from price_histories7 through stats/QA, and (2) variant detail showing the 6-table structure for bars/EMAs.

Purpose: DOCS-02 — the pipeline diagram is the single most useful artifact for understanding how data flows through the system. The existing diagram shows file migration history, not the data pipeline.
Output: `docs/diagrams/data_flow.mmd` with two Mermaid diagrams in a single file.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-documentation-freshness/31-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create main pipeline flow diagram and variant detail diagram</name>
  <files>docs/diagrams/data_flow.mmd</files>
  <action>
    Replace the entire contents of `docs/diagrams/data_flow.mmd` with TWO Mermaid diagrams separated by a markdown comment.

    **Diagram 1: Main Pipeline Flow (top-down flowchart)**

    Show the 7-stage data pipeline with actual table names:

    Stage 1 - Source: `cmc_price_histories7` (raw OHLCV from CoinMarketCap)
    Stage 2 - Bars: `cmc_price_bars_multi_tf_u` (unified bars, 6 variants consolidated)
    Stage 3a - Bar Returns: `cmc_returns_bars_multi_tf_u` (unified bar returns)
    Stage 3b - EMAs: `cmc_ema_multi_tf_u` (unified EMAs, 6 variants consolidated)
    Stage 3c - EMA Returns: `cmc_returns_ema_multi_tf_u` (unified EMA returns)
    Stage 4 - Features: Three parallel inputs merge into `cmc_features` (112-col feature store):
      - `cmc_vol` (volatility: Parkinson, GK, Rogers-Satchell) -- reads from bars
      - `cmc_ta` (technical: RSI, MACD, Bollinger, ATR, ADX) -- reads from bars
      - Plus bar returns + EMA values joined in
    Stage 5 - Regimes: `cmc_regimes` + `cmc_regime_flips` + `cmc_regime_stats` + `cmc_regime_comovement`
    Stage 6 - Signals: `cmc_signals_ema_crossover`, `cmc_signals_rsi_mean_revert`, `cmc_signals_atr_breakout`
      - Read from `cmc_features` + `cmc_ema_multi_tf_u` (LEFT JOIN for EMA data)
    Stage 7 - Backtest: `cmc_backtest_runs`, `cmc_backtest_trades`, `cmc_backtest_metrics`
    Stage 8 - Stats/QA: `cmc_bar_stats`, `cmc_features_stats`, etc. (6 stats runners)

    Use `flowchart TD` (top-down). Group related tables in subgraph blocks. Use arrows to show data dependencies. Color-code stages:
    - Source: red/pink
    - Bars/Returns: blue
    - EMAs: green
    - Features: orange
    - Regimes: purple
    - Signals: teal
    - Backtest: gold
    - Stats: gray

    **Diagram 2: Table Variant Detail**

    Show the 6-variant structure that exists for both bars and EMAs:
    ```
    Base table (multi_tf)
      + Calendar ISO (cal_iso)
      + Calendar US (cal_us)
      + Anchor ISO (cal_anchor_iso)
      + Anchor US (cal_anchor_us)
      = Unified _u table (sync via INSERT ON CONFLICT DO NOTHING)
    ```

    Show this pattern for all 4 table families:
    1. Price Bars: `cmc_price_bars_multi_tf` + 4 calendar variants -> `cmc_price_bars_multi_tf_u`
    2. Bar Returns: `cmc_returns_bars_multi_tf` + 4 calendar variants -> `cmc_returns_bars_multi_tf_u`
    3. EMA Values: `cmc_ema_multi_tf` + 4 calendar variants -> `cmc_ema_multi_tf_u`
    4. EMA Returns: `cmc_returns_ema_multi_tf` + 4 calendar variants -> `cmc_returns_ema_multi_tf_u`

    Use `flowchart LR` (left-right) for this diagram. Use a subgraph per table family.

    **File structure:**
    Put both diagrams in the same `.mmd` file. Since `.mmd` files typically contain one diagram, separate them clearly with comments:
    ```
    %% === DIAGRAM 1: Main Pipeline Flow ===
    %% Render this block as a standalone diagram

    flowchart TD
      ...

    %% === DIAGRAM 2: Table Variant Detail ===
    %% Render this block as a standalone diagram

    flowchart LR
      ...
    ```

    NOTE: Some Mermaid renderers only support one diagram per file. If that's the case, put the main pipeline flow as the primary diagram in `data_flow.mmd` and create a second file `docs/diagrams/table_variants.mmd` for the variant detail. Use your judgment — if a single file works cleanly, prefer it; if separation is cleaner for rendering, split.

    Keep the diagrams clean and readable. Use short node IDs (e.g., `PH7`, `BARS_U`, `EMAS_U`) with longer labels inside the node text. Avoid overcrowding — this is a reference diagram, not a wall of text.
  </action>
  <verify>
    1. File exists and has content: `wc -l docs/diagrams/data_flow.mmd` (should be 50+ lines)
    2. Contains key table names: `grep -c "cmc_price_histories7\|cmc_features\|cmc_regimes\|cmc_backtest" docs/diagrams/data_flow.mmd` (should be >= 4)
    3. Contains flowchart declaration: `grep -c "flowchart" docs/diagrams/data_flow.mmd` (should be >= 1)
    4. No old migration content remains: `grep -c "ProjectTT\|Data_Tools\|fredtools2" docs/diagrams/data_flow.mmd` (should be 0)
  </verify>
  <done>docs/diagrams/data_flow.mmd contains accurate v0.8.0 pipeline diagram(s) showing the full bars -> EMAs -> features -> regimes -> signals -> backtest -> stats flow with actual DB table names. Old v0.5.0 migration diagram is completely replaced.</done>
</task>

</tasks>

<verification>
- `docs/diagrams/data_flow.mmd` exists with 50+ lines of Mermaid content
- Diagram includes all 7 pipeline stages (source, bars, EMAs, features, regimes, signals, backtest)
- All major table names present: cmc_price_histories7, cmc_price_bars_multi_tf_u, cmc_ema_multi_tf_u, cmc_features, cmc_regimes, cmc_signals_*, cmc_backtest_*
- Variant detail shows the 6-variant -> unified pattern for all 4 table families
- No references to old v0.5.0 file migration content
</verification>

<success_criteria>
1. Main pipeline diagram accurately shows v0.7.0+ data flow topology
2. Table variant detail diagram shows the 4 families x 6 variants structure
3. All node labels use actual DB table names
4. Mermaid syntax is valid (flowchart TD/LR declarations, proper arrow syntax)
5. Old migration diagram content completely removed
</success_criteria>

<output>
After completion, create `.planning/phases/31-documentation-freshness/31-02-SUMMARY.md`
</output>
