---
phase: 51-perps-readiness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - alembic/versions/XXXX_perps_readiness.py
  - sql/perps/095_cmc_funding_rates.sql
  - sql/perps/096_cmc_margin_config.sql
  - sql/perps/097_cmc_perp_positions.sql
autonomous: true

must_haves:
  truths:
    - "cmc_funding_rates table exists with (venue, symbol, ts, tf) PK and CHECK constraints for venue and tf"
    - "cmc_margin_config table exists with venue-specific tiered margin rates for BTC/ETH"
    - "cmc_perp_positions table exists for perp position tracking separate from spot cmc_positions"
    - "Alembic migration upgrades and downgrades cleanly"
    - "cmc_risk_events CHECK constraints extended: chk_risk_events_type includes liquidation event types, chk_risk_events_source includes 'margin_monitor'"
  artifacts:
    - path: "alembic/versions/XXXX_perps_readiness.py"
      provides: "Alembic migration creating all Phase 51 tables"
      contains: "cmc_funding_rates"
    - path: "sql/perps/095_cmc_funding_rates.sql"
      provides: "Reference DDL for funding rates table"
      contains: "CREATE TABLE"
    - path: "sql/perps/096_cmc_margin_config.sql"
      provides: "Reference DDL for margin config dimension table"
      contains: "CREATE TABLE"
    - path: "sql/perps/097_cmc_perp_positions.sql"
      provides: "Reference DDL for perp positions table"
      contains: "CREATE TABLE"
  key_links:
    - from: "alembic/versions/XXXX_perps_readiness.py"
      to: "alembic chain"
      via: "down_revision detected dynamically from alembic heads"
      pattern: "down_revision"
    - from: "alembic/versions/XXXX_perps_readiness.py"
      to: "cmc_risk_events"
      via: "DROP + ADD CONSTRAINT for chk_risk_events_source with 'margin_monitor'"
      pattern: "chk_risk_events_source"
---

<objective>
Create the database schema for Phase 51 perps readiness: cmc_funding_rates (multi-venue funding history), cmc_margin_config (venue-specific tiered margin rates), and cmc_perp_positions (perp position tracking with margin state). Also extend cmc_risk_events CHECK constraints with new liquidation event types AND 'margin_monitor' trigger source.

Purpose: All subsequent plans depend on these tables existing. Schema-first approach ensures the data model is locked before any ingestion or computation code.
Output: One Alembic migration + 3 reference SQL files in sql/perps/.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/51-perps-readiness/51-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reference DDL files for all Phase 51 tables</name>
  <files>
    sql/perps/095_cmc_funding_rates.sql
    sql/perps/096_cmc_margin_config.sql
    sql/perps/097_cmc_perp_positions.sql
  </files>
  <action>
    Create `sql/perps/` directory and write 3 reference DDL files (ASCII-only, no box-drawing chars for Windows cp1252 compatibility).

    **095_cmc_funding_rates.sql:**
    - PK: (venue, symbol, ts, tf)
    - Columns: venue TEXT NOT NULL, symbol TEXT NOT NULL, ts TIMESTAMPTZ NOT NULL, tf TEXT NOT NULL, funding_rate NUMERIC NOT NULL, mark_price NUMERIC (nullable), raw_tf TEXT, ingested_at TIMESTAMPTZ NOT NULL DEFAULT now()
    - CHECK: venue IN ('binance','hyperliquid','bybit','dydx','aevo','aster','lighter')
    - CHECK: tf IN ('1h','4h','8h','1d')
    - Indexes: (symbol, ts DESC), (venue, symbol, ts DESC)
    - COMMENT ON TABLE explaining purpose and PK semantics

    **096_cmc_margin_config.sql:**
    - PK: (venue, symbol, tier)
    - Columns: venue TEXT NOT NULL, symbol TEXT NOT NULL, tier INTEGER NOT NULL, notional_floor NUMERIC NOT NULL DEFAULT 0, notional_cap NUMERIC, initial_margin_rate NUMERIC NOT NULL, maintenance_margin_rate NUMERIC NOT NULL, max_leverage INTEGER, updated_at TIMESTAMPTZ NOT NULL DEFAULT now()
    - CHECK: venue IN ('binance','hyperliquid','bybit','dydx','aevo','aster','lighter')
    - This is a dimension/config table -- seed with known Binance BTC/ETH tiers (Tier 1: 0-50K, IM 0.8%, MM 0.4%, max lev 125; Tier 2: 50K-250K, IM 1%, MM 0.5%, max lev 100; Tier 3: 250K-1M, IM 2%, MM 1%, max lev 50) and Hyperliquid BTC/ETH (single tier: IM = 1/max_leverage, MM = 1/(2*max_leverage), max_leverage=50)
    - COMMENT explaining purpose

    **097_cmc_perp_positions.sql:**
    - PK: (venue, symbol, strategy_id)
    - Columns: venue TEXT NOT NULL, symbol TEXT NOT NULL, strategy_id INTEGER NOT NULL DEFAULT 0, side TEXT NOT NULL (CHECK IN 'long','short','flat'), quantity NUMERIC NOT NULL DEFAULT 0, avg_entry_price NUMERIC, mark_price NUMERIC, unrealized_pnl NUMERIC, margin_mode TEXT NOT NULL DEFAULT 'isolated' (CHECK IN 'isolated','cross'), leverage NUMERIC NOT NULL DEFAULT 1, allocated_margin NUMERIC NOT NULL DEFAULT 0, maintenance_margin NUMERIC, margin_utilization NUMERIC, liquidation_price NUMERIC, updated_at TIMESTAMPTZ NOT NULL DEFAULT now()
    - CHECK: venue IN ('binance','hyperliquid','bybit','dydx','aevo','aster','lighter')
    - This is separate from cmc_positions (spot) to avoid touching the existing spot position CHECK constraint
    - COMMENT explaining why separate from cmc_positions
  </action>
  <verify>
    All 3 SQL files exist in sql/perps/ directory. Each file contains CREATE TABLE, PRIMARY KEY, CHECK constraints, and COMMENT. No non-ASCII characters in any file. Run: `python -c "open('sql/perps/095_cmc_funding_rates.sql', encoding='ascii').read(); print('ASCII OK')"` for each file.
  </verify>
  <done>Three reference DDL files created with correct schemas, constraints, and seed data.</done>
</task>

<task type="auto">
  <name>Task 2: Alembic migration for all Phase 51 tables + risk event extension</name>
  <files>
    alembic/versions/XXXX_perps_readiness.py
  </files>
  <action>
    First, detect the current Alembic head dynamically: run `cd C:/Users/asafi/Downloads/ta_lab2 && python -m alembic heads` (or parse the latest migration file). Use that as down_revision.

    Create a single Alembic migration file that:

    1. **cmc_funding_rates** -- create table matching 095 DDL. Use op.create_table() for the table, then op.execute() for CHECK constraints (venue, tf), indexes, and COMMENT.

    2. **cmc_margin_config** -- create table matching 096 DDL. Use op.create_table(), then op.execute() for CHECK, COMMENT. Seed with known Binance BTC/ETH tiers and Hyperliquid BTC/ETH tiers using INSERT...ON CONFLICT DO NOTHING for idempotency.

    3. **cmc_perp_positions** -- create table matching 097 DDL. Use op.create_table(), then op.execute() for CHECK constraints (venue, side, margin_mode), COMMENT.

    4. **Extend cmc_risk_events type CHECK constraint** -- Drop existing `chk_risk_events_type` CHECK, recreate with all existing event types PLUS new: 'liquidation_warning', 'liquidation_critical', 'margin_alert'. Follow Phase 47 drift_guard drop+recreate pattern exactly. The current type values are: 'kill_switch_activated', 'kill_switch_disabled', 'position_cap_scaled', 'position_cap_blocked', 'daily_loss_stop_triggered', 'circuit_breaker_tripped', 'circuit_breaker_reset', 'override_created', 'override_applied', 'override_reverted', 'drift_signal', 'drift_attribution', 'drift_escalation'. Add the 3 new types to this list.

    5. **Extend cmc_risk_events source CHECK constraint** -- Drop existing `chk_risk_events_source` CHECK, recreate with all existing sources PLUS 'margin_monitor'. The current source values are: 'manual', 'daily_loss_stop', 'circuit_breaker', 'system', 'drift_monitor'. Add 'margin_monitor' to this list. Follow the same Phase 47 drop+recreate pattern.

    6. **Extend dim_risk_limits** -- Add column `margin_alert_threshold NUMERIC DEFAULT 1.5` and `liquidation_kill_threshold NUMERIC DEFAULT 1.1` via op.add_column() if not exists.

    downgrade() must:
    - Drop cmc_funding_rates, cmc_margin_config, cmc_perp_positions
    - Restore original cmc_risk_events type CHECK constraint (without liquidation event types -- restore to the 13 drift_guard types)
    - Restore original cmc_risk_events source CHECK constraint (without 'margin_monitor' -- restore to the 5 drift_guard sources)
    - Drop added dim_risk_limits columns

    CRITICAL: ASCII-only in migration file. No box-drawing characters. Use plain `--` comments.
    CRITICAL: Use `op.execute()` with raw SQL strings for CHECK constraints and seed data. All SQL must use CAST(:param AS ...) pattern or direct string values (no bind params in op.execute for simple DDL).
  </action>
  <verify>
    Run: `cd C:/Users/asafi/Downloads/ta_lab2 && python -m alembic upgrade head` -- must complete without errors.
    Run: `cd C:/Users/asafi/Downloads/ta_lab2 && python -m alembic downgrade -1` -- must complete without errors.
    Run: `cd C:/Users/asafi/Downloads/ta_lab2 && python -m alembic upgrade head` again to leave DB in upgraded state.
    Run: `cd C:/Users/asafi/Downloads/ta_lab2 && python -m alembic history | head -5` -- shows new migration at head.
  </verify>
  <done>Alembic migration creates all 3 tables, seeds margin config, extends risk event types AND trigger sources (including 'margin_monitor'), and adds margin threshold columns to dim_risk_limits. Upgrade and downgrade both succeed cleanly.</done>
</task>

</tasks>

<verification>
- `SELECT COUNT(*) FROM information_schema.tables WHERE table_name IN ('cmc_funding_rates','cmc_margin_config','cmc_perp_positions')` returns 3
- `SELECT COUNT(*) FROM cmc_margin_config` returns seed rows (at least 6: 3 Binance BTC tiers + 1 Hyperliquid BTC + similar for ETH)
- `alembic current` shows Phase 51 migration as head
- All reference SQL files parse correctly (no encoding errors)
- Verify chk_risk_events_source includes 'margin_monitor': `SELECT conname, consrc FROM pg_constraint WHERE conname = 'chk_risk_events_source'` should show 'margin_monitor' in the constraint
</verification>

<success_criteria>
All 3 Phase 51 tables exist in PostgreSQL with correct PKs, CHECK constraints, indexes, and comments. Margin config seeded with Binance and Hyperliquid tiers. Risk events CHECK extended with liquidation types AND 'margin_monitor' trigger source. dim_risk_limits has margin threshold columns. Alembic upgrade/downgrade cycle clean.
</success_criteria>

<output>
After completion, create `.planning/phases/51-perps-readiness/51-01-SUMMARY.md`
</output>
