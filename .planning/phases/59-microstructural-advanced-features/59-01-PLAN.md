---
phase: 59-microstructural-advanced-features
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - sql/migration/add_microstructure_to_features.sql
  - sql/migration/create_cmc_codependence.sql
autonomous: true

must_haves:
  truths:
    - "cmc_features table has 9 new microstructure columns"
    - "cmc_codependence table exists with correct PK"
    - "Migration is idempotent (re-runnable without error)"
  artifacts:
    - path: "sql/migration/add_microstructure_to_features.sql"
      provides: "ALTER TABLE for 9 microstructure columns"
      contains: "ADD COLUMN IF NOT EXISTS close_fracdiff"
    - path: "sql/migration/create_cmc_codependence.sql"
      provides: "New pairwise codependence table DDL"
      contains: "CREATE TABLE IF NOT EXISTS public.cmc_codependence"
  key_links:
    - from: "sql/migration/add_microstructure_to_features.sql"
      to: "public.cmc_features"
      via: "ALTER TABLE ADD COLUMN IF NOT EXISTS"
      pattern: "ALTER TABLE public.cmc_features"
    - from: "sql/migration/create_cmc_codependence.sql"
      to: "public.cmc_codependence"
      via: "CREATE TABLE IF NOT EXISTS"
      pattern: "CREATE TABLE IF NOT EXISTS public.cmc_codependence"
---

<objective>
Create SQL migration scripts to add 9 microstructure columns to `cmc_features` and create the new `cmc_codependence` table for pairwise asset dependency measures.

Purpose: All downstream feature computation scripts need these columns/tables to exist before they can write data. This is the schema foundation for MICRO-01 through MICRO-05.
Output: Two SQL migration files, both executed against the database.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/milestones/v1.0.0-ROADMAP.md
@.planning/phases/59-microstructural-advanced-features/59-RESEARCH.md

# Existing DDL for reference
@sql/views/050_cmc_features.sql
@sql/regimes/084_cmc_regime_comovement.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SQL migration for cmc_features microstructure columns</name>
  <files>sql/migration/add_microstructure_to_features.sql</files>
  <action>
Create `sql/migration/add_microstructure_to_features.sql` that adds 9 columns to `public.cmc_features` using `ALTER TABLE ... ADD COLUMN IF NOT EXISTS`:

Columns to add:
- `close_fracdiff DOUBLE PRECISION` -- FFD-transformed close price (MICRO-01)
- `close_fracdiff_d DOUBLE PRECISION` -- the d parameter used per asset (MICRO-01)
- `kyle_lambda DOUBLE PRECISION` -- Kyle (1985) price impact (MICRO-02)
- `amihud_lambda DOUBLE PRECISION` -- Amihud (2002) illiquidity ratio (MICRO-02)
- `hasbrouck_lambda DOUBLE PRECISION` -- Hasbrouck (2009) signed price impact (MICRO-02)
- `sadf_stat DOUBLE PRECISION` -- Rolling 63-bar ADF t-statistic (MICRO-03)
- `sadf_is_explosive BOOLEAN DEFAULT FALSE` -- True when sadf_stat > 1.5 (MICRO-03)
- `entropy_shannon DOUBLE PRECISION` -- Shannon entropy of 50-bar return window (MICRO-04)
- `entropy_lz DOUBLE PRECISION` -- Lempel-Ziv complexity normalized (MICRO-04)

IMPORTANT: Do NOT use UTF-8 box-drawing characters (U+2550 etc.) in SQL comments. Use plain ASCII dashes/equals for section dividers. This prevents Windows cp1252 encoding errors per project MEMORY.md.

Use a single ALTER TABLE statement with multiple ADD COLUMN IF NOT EXISTS clauses (matching existing migration pattern in `sql/migration/alter_cmc_features_redesign.sql`).

Add a header comment with date, purpose, and which requirements each column maps to.
  </action>
  <verify>
Run the migration: `python -c "from ta_lab2.config import TARGET_DB_URL; from sqlalchemy import create_engine, text; e = create_engine(TARGET_DB_URL); c = e.connect(); c.execute(text(open('sql/migration/add_microstructure_to_features.sql', encoding='utf-8').read())); c.commit(); c.close(); print('OK')"` -- should print OK without error.

Verify columns exist: `python -c "from ta_lab2.config import TARGET_DB_URL; from sqlalchemy import create_engine, text; e = create_engine(TARGET_DB_URL); r = e.connect().execute(text(\"SELECT column_name FROM information_schema.columns WHERE table_name='cmc_features' AND column_name LIKE '%fracdiff%' OR column_name LIKE '%lambda%' OR column_name LIKE '%sadf%' OR column_name LIKE '%entropy%' ORDER BY column_name\")); print([row[0] for row in r])"` -- should list all 9 new columns.

Run migration a second time to confirm idempotency (no error on re-run).
  </verify>
  <done>9 new columns exist in cmc_features table; migration is idempotent</done>
</task>

<task type="auto">
  <name>Task 2: Create cmc_codependence table DDL and execute</name>
  <files>sql/migration/create_cmc_codependence.sql</files>
  <action>
Create `sql/migration/create_cmc_codependence.sql` with:

```sql
CREATE TABLE IF NOT EXISTS public.cmc_codependence (
    id_a            INTEGER NOT NULL,
    id_b            INTEGER NOT NULL,
    tf              TEXT NOT NULL,
    window_bars     INTEGER NOT NULL,
    computed_at     TIMESTAMPTZ NOT NULL DEFAULT now(),

    pearson_corr    DOUBLE PRECISION,
    distance_corr   DOUBLE PRECISION,
    mutual_info     DOUBLE PRECISION,
    variation_of_info DOUBLE PRECISION,

    n_obs           INTEGER,

    PRIMARY KEY (id_a, id_b, tf, window_bars, computed_at)
);
```

Add indexes:
- `idx_cmc_codependence_pair` on `(id_a, id_b, tf)` for pair lookups
- `idx_cmc_codependence_computed` on `(computed_at DESC)` for latest snapshot queries

Add COMMENT ON TABLE and key COMMENT ON COLUMN statements explaining the table purpose and PK design (pairwise asset metrics, computed_at retains historical snapshots).

IMPORTANT: No UTF-8 box-drawing characters in comments (use plain ASCII).

Execute the migration against the database.
  </action>
  <verify>
Run migration: `python -c "from ta_lab2.config import TARGET_DB_URL; from sqlalchemy import create_engine, text; e = create_engine(TARGET_DB_URL); c = e.connect(); c.execute(text(open('sql/migration/create_cmc_codependence.sql', encoding='utf-8').read())); c.commit(); c.close(); print('OK')"` -- should print OK.

Verify table exists: `python -c "from ta_lab2.config import TARGET_DB_URL; from sqlalchemy import create_engine, text; e = create_engine(TARGET_DB_URL); r = e.connect().execute(text(\"SELECT column_name FROM information_schema.columns WHERE table_name='cmc_codependence' ORDER BY ordinal_position\")); print([row[0] for row in r])"` -- should list all columns.

Confirm idempotent on second run.
  </verify>
  <done>cmc_codependence table exists with PK (id_a, id_b, tf, window_bars, computed_at), two indexes, and comments</done>
</task>

</tasks>

<verification>
- Both SQL files exist and contain no UTF-8 box-drawing characters
- `cmc_features` has 9 new columns (close_fracdiff, close_fracdiff_d, kyle_lambda, amihud_lambda, hasbrouck_lambda, sadf_stat, sadf_is_explosive, entropy_shannon, entropy_lz)
- `cmc_codependence` table exists with correct schema
- Both migrations are idempotent (run twice without error)
</verification>

<success_criteria>
- Both migration files created and executed
- All 9 microstructure columns queryable in cmc_features
- cmc_codependence table queryable with correct PK
- No encoding errors on Windows
</success_criteria>

<output>
After completion, create `.planning/phases/59-microstructural-advanced-features/59-01-SUMMARY.md`
</output>
