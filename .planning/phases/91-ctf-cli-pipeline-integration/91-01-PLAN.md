---
phase: 91-ctf-cli-pipeline-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - alembic/versions/k5l6m7n8o9p0_ctf_state.py
  - configs/ctf_config.yaml
  - pyproject.toml
autonomous: true

must_haves:
  truths:
    - "ctf_state table exists in PostgreSQL with PK (id, venue_id, base_tf, ref_tf, indicator_id, alignment_source)"
    - "ctf_config.yaml has 6 base TFs (1D, 2D, 3D, 7D, 14D, 30D) with appropriate ref_tfs"
    - "tqdm is listed in pyproject.toml core dependencies"
    - "alembic upgrade head succeeds without errors"
  artifacts:
    - path: "alembic/versions/k5l6m7n8o9p0_ctf_state.py"
      provides: "Alembic migration for ctf_state table"
      contains: "ctf_state"
    - path: "configs/ctf_config.yaml"
      provides: "Expanded CTF timeframe pair configuration"
      contains: "2D"
    - path: "pyproject.toml"
      provides: "tqdm added to core dependencies"
      contains: "tqdm"
  key_links:
    - from: "alembic/versions/k5l6m7n8o9p0_ctf_state.py"
      to: "alembic/versions/j4k5l6m7n8o9_ctf_schema.py"
      via: "down_revision chain"
      pattern: 'down_revision.*j4k5l6m7n8o9'
---

<objective>
Create the ctf_state Alembic migration table, expand ctf_config.yaml with 2D/3D base timeframes, and add tqdm to project dependencies.

Purpose: These are prerequisites for the standalone CLI (Plan 02) and pipeline integration (Plan 03). The state table enables incremental refresh. The YAML expansion adds the user-requested additional base timeframes. tqdm enables progress bars.

Output: Alembic migration file, updated YAML config, updated pyproject.toml.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/91-ctf-cli-pipeline-integration/91-CONTEXT.md
@.planning/phases/91-ctf-cli-pipeline-integration/91-RESEARCH.md

# Key reference files
@alembic/versions/j4k5l6m7n8o9_ctf_schema.py
@configs/ctf_config.yaml
@pyproject.toml
@src/ta_lab2/scripts/features/feature_state_manager.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Alembic migration for ctf_state table</name>
  <files>alembic/versions/k5l6m7n8o9p0_ctf_state.py</files>
  <action>
Create a new Alembic migration file that creates the `ctf_state` table for incremental CTF refresh tracking.

Migration metadata:
- Revision ID: `k5l6m7n8o9p0`
- Down revision: `j4k5l6m7n8o9` (CTF schema migration -- current Alembic head)
- Description: "CTF state table for incremental refresh tracking"

Table schema for `public.ctf_state`:
```sql
CREATE TABLE public.ctf_state (
    id                  INTEGER         NOT NULL,
    venue_id            SMALLINT        NOT NULL DEFAULT 1,
    base_tf             TEXT            NOT NULL,
    ref_tf              TEXT            NOT NULL,
    indicator_id        SMALLINT        NOT NULL,
    alignment_source    TEXT            NOT NULL DEFAULT 'multi_tf',
    last_ts             TIMESTAMPTZ     NULL,
    row_count           INTEGER         NULL,
    updated_at          TIMESTAMPTZ     NOT NULL DEFAULT now(),
    PRIMARY KEY (id, venue_id, base_tf, ref_tf, indicator_id, alignment_source)
);
```

Add a FK to dim_ctf_indicators: `FOREIGN KEY (indicator_id) REFERENCES public.dim_ctf_indicators(indicator_id)`.

Add a comment on the table: `-- Incremental refresh state for CTF features. Tracks last computed timestamp per scope.`

Use ASCII-only comments in the migration file (Windows cp1252 compatibility -- project convention).

Implement both `upgrade()` and `downgrade()` functions. downgrade drops the table.

Follow the exact pattern from `j4k5l6m7n8o9_ctf_schema.py` for imports and structure (using `op.execute(text(...))` for raw SQL, NOT op.create_table).
  </action>
  <verify>
Run `cd C:/Users/asafi/Downloads/ta_lab2 && python -m alembic upgrade head` -- should succeed without errors.
Run `cd C:/Users/asafi/Downloads/ta_lab2 && python -m alembic history | head -5` -- should show k5l6m7n8o9p0 as head.
Run `cd C:/Users/asafi/Downloads/ta_lab2 && python -c "from sqlalchemy import create_engine, text; from ta_lab2.config import TARGET_DB_URL; e=create_engine(TARGET_DB_URL); r=e.connect().execute(text(\"SELECT column_name FROM information_schema.columns WHERE table_name='ctf_state' ORDER BY ordinal_position\")); print([row[0] for row in r])"` -- should list all columns.
  </verify>
  <done>ctf_state table exists in PostgreSQL with correct PK and all columns. Alembic head is k5l6m7n8o9p0.</done>
</task>

<task type="auto">
  <name>Task 2: Expand YAML config and add tqdm dependency</name>
  <files>configs/ctf_config.yaml, pyproject.toml</files>
  <action>
1. **Expand ctf_config.yaml** -- Add `2D` and `3D` as new base_tf entries. Insert them between the existing `1D` and `7D` entries. Appropriate ref_tfs for each:

```yaml
- base_tf: "2D"
  ref_tfs: ["7D", "14D", "30D", "90D", "180D", "365D"]
- base_tf: "3D"
  ref_tfs: ["7D", "14D", "30D", "90D", "180D", "365D"]
```

Keep ALL existing entries unchanged (1D, 7D, 14D, 30D). The result is 6 base TFs total.

2. **Add tqdm to pyproject.toml** -- Add `"tqdm>=4.60"` to the `dependencies` list in `[project]`. Insert it alphabetically (after `SQLAlchemy`, before the closing bracket). The user explicitly requested tqdm-style progress bars in the CONTEXT.md.

Do NOT modify any other files. Do NOT change the `indicators` or `composite_params` sections of the YAML.
  </action>
  <verify>
Run `cd C:/Users/asafi/Downloads/ta_lab2 && python -c "import yaml; cfg=yaml.safe_load(open('configs/ctf_config.yaml')); tfs=[p['base_tf'] for p in cfg['timeframe_pairs']]; print(tfs); assert len(tfs)==6; assert '2D' in tfs; assert '3D' in tfs"` -- should print 6 base TFs including 2D and 3D.
Run `cd C:/Users/asafi/Downloads/ta_lab2 && python -c "import tqdm; print(tqdm.__version__)"` -- should print version.
Grep pyproject.toml for `tqdm` -- should find it in dependencies.
  </verify>
  <done>ctf_config.yaml has 6 base TFs (1D, 2D, 3D, 7D, 14D, 30D). tqdm is in pyproject.toml core dependencies and importable.</done>
</task>

</tasks>

<verification>
- `alembic upgrade head` succeeds
- `ctf_state` table exists with correct schema
- YAML has 6 base TFs
- tqdm importable
</verification>

<success_criteria>
- ctf_state table created via Alembic with PK (id, venue_id, base_tf, ref_tf, indicator_id, alignment_source)
- ctf_config.yaml expanded from 4 to 6 base TFs (2D, 3D added)
- tqdm listed in pyproject.toml dependencies
</success_criteria>

<output>
After completion, create `.planning/phases/91-ctf-cli-pipeline-integration/91-01-SUMMARY.md`
</output>
