---
phase: 38-feature-experimentation
plan: "05"
type: execute
wave: 3
depends_on: ["38-03", "38-04"]
files_modified:
  - tests/test_experiments.py
autonomous: true

must_haves:
  truths:
    - "FeatureRegistry loads YAML, expands params, validates expressions, and detects duplicates"
    - "resolve_experiment_dag returns topological order and raises CycleError on cycles"
    - "ExperimentRunner._compute_feature dispatches inline and dotpath modes correctly"
    - "check_bh_gate rejects noise p-values and passes signal p-values"
    - "PromotionRejectedError is raised when BH gate fails"
    - "BH correction handles NaN p-values without crashing"
    - "All 3 CLI scripts accept --help without errors"
  artifacts:
    - path: "tests/test_experiments.py"
      provides: "Unit tests for registry, DAG, BH gate, runner compute, and promoter logic"
      min_lines: 100
  key_links:
    - from: "tests/test_experiments.py"
      to: "src/ta_lab2/experiments/registry.py"
      via: "import and test"
      pattern: "from ta_lab2\\.experiments"
    - from: "tests/test_experiments.py"
      to: "src/ta_lab2/experiments/promoter.py"
      via: "import and test"
      pattern: "PromotionRejectedError|check_bh_gate"
---

<objective>
Write unit tests for the feature experimentation framework and verify all CLI scripts work end-to-end.

Purpose: Validates that FEAT-01 through FEAT-05 work correctly in isolation (unit tests) and together (CLI integration). Tests the critical paths: YAML loading, parameter expansion, BH gate logic, inline/dotpath compute dispatch, and error handling.

Output: `tests/test_experiments.py` with comprehensive test coverage for the experiments subpackage.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/38-feature-experimentation/38-RESEARCH.md

# Prior plan SUMMARYs
@.planning/phases/38-feature-experimentation/38-02-SUMMARY.md
@.planning/phases/38-feature-experimentation/38-03-SUMMARY.md
@.planning/phases/38-feature-experimentation/38-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write unit tests for feature experimentation framework</name>
  <files>tests/test_experiments.py</files>
  <action>
Create `tests/test_experiments.py` with pytest tests covering:

**FeatureRegistry tests:**
1. `test_load_yaml_basic` -- Load a minimal YAML (write to tmp_path), verify features are parsed correctly.
2. `test_expand_params` -- Create YAML with params sweep (period: [5, 14]), verify expansion produces 2 variants with correct names.
3. `test_validate_inline_expression_valid` -- Verify ast.parse accepts valid pandas expressions.
4. `test_validate_inline_expression_invalid` -- Verify ValueError on syntax error.
5. `test_duplicate_names_raise` -- Create YAML with two features that expand to same name, verify ValueError.
6. `test_invalid_lifecycle_raises` -- Create YAML with lifecycle: "invalid", verify ValueError.
7. `test_list_experimental_filters` -- Create YAML with experimental + promoted entries, verify list_experimental returns only experimental.
8. `test_yaml_digest_changes` -- Verify digest changes when expression changes.

**DAG resolver tests:**
9. `test_dag_no_deps` -- Features with no depends_on return in some order (all are roots).
10. `test_dag_linear` -- A->B->C returns [A, B, C] order.
11. `test_dag_cycle_raises` -- A->B->A raises graphlib.CycleError.

**BH gate tests:**
12. `test_bh_gate_rejects_noise` -- DataFrame with ic_p_value=[0.32, 0.45, 0.67] returns (False, ...).
13. `test_bh_gate_passes_signal` -- DataFrame with ic_p_value=[0.001, 0.01, 0.04] returns (True, ...) with at least one passing.
14. `test_bh_gate_handles_nan` -- DataFrame with ic_p_value=[0.001, NaN, 0.04] does not crash, NaN rows get NaN in ic_p_value_bh.
15. `test_bh_gate_all_nan` -- DataFrame with all NaN p-values returns (False, ...).
16. `test_bh_gate_min_pass_rate` -- With min_pass_rate=0.5 and only 1/3 passing, returns False.

**Compute dispatch tests (mock-based, no DB):**
17. `test_compute_feature_inline` -- Create a DataFrame with 'close' column, spec with inline expression "close * 2", verify result is close * 2.
18. `test_compute_feature_dotpath` -- Mock importlib to return a function, verify it's called with input_df.

**PromotionRejectedError tests:**
19. `test_promotion_rejected_error_message` -- Verify the error includes reason string.

**CLI help tests:**
20. `test_run_experiment_help` -- subprocess.run python -m ta_lab2.scripts.experiments.run_experiment --help, verify exit code 0.
21. `test_promote_feature_help` -- subprocess.run python -m ta_lab2.scripts.experiments.promote_feature --help, verify exit code 0.
22. `test_purge_experiment_help` -- subprocess.run python -m ta_lab2.scripts.experiments.purge_experiment --help, verify exit code 0.

For tests that need a FeatureRegistry, write a minimal YAML to tmp_path and load it. For tests that need ExperimentRunner._compute_feature, instantiate runner with mocked engine/registry and call the method directly. For BH gate tests, instantiate FeaturePromoter with mocked engine and call check_bh_gate directly.

Use `pytest.raises` for expected exceptions. Use `@pytest.fixture` for shared YAML content.

IMPORTANT:
- Tests must NOT require a live database connection (mock DB operations).
- Use tmp_path fixture for YAML files.
- Import from `ta_lab2.experiments` (not relative imports).
  </action>
  <verify>
Run `python -m pytest tests/test_experiments.py -v` -- all tests should pass. Check count of tests (expect ~20).
  </verify>
  <done>
20+ tests covering registry loading, parameter expansion, expression validation, DAG resolution, BH gate logic, compute dispatch, and CLI help. All pass without DB connection.
  </done>
</task>

<task type="auto">
  <name>Task 2: End-to-end CLI verification</name>
  <files></files>
  <action>
Run verification commands to confirm the full Phase 38 framework works:

1. Verify Alembic migration chain: `alembic -c alembic/alembic.ini history` -- confirm new migration appears after c3b718c2d088.
2. Verify registry loads: `python -c "from ta_lab2.experiments import FeatureRegistry; r = FeatureRegistry('configs/experiments/features.yaml'); r.load(); print(r.list_experimental())"`.
3. Verify all CLI --help: run each of the 3 CLI scripts with --help.
4. Verify imports: `python -c "from ta_lab2.experiments import FeatureRegistry, ExperimentRunner, FeaturePromoter, PromotionRejectedError, resolve_experiment_dag; print('All imports OK')"`.
5. Run full test suite: `python -m pytest tests/test_experiments.py -v --tb=short`.

If any verification fails, fix the issue in the relevant file before marking complete.

Do NOT run alembic upgrade head or any commands that modify the live database -- the migration is verified by structure only.
  </action>
  <verify>
All 5 verification commands exit 0. Test suite passes with 20+ tests.
  </verify>
  <done>
Phase 38 framework is structurally complete: migration chains correctly, registry loads YAML, all CLI scripts parse arguments, all imports resolve, and test suite passes.
  </done>
</task>

</tasks>

<verification>
- `python -m pytest tests/test_experiments.py -v` passes all tests
- All 3 CLI scripts accept --help without errors
- All exports from ta_lab2.experiments resolve
- Alembic history shows correct chain
</verification>

<success_criteria>
- 20+ tests covering critical paths (registry, DAG, BH gate, compute, CLI)
- Zero test failures
- No DB connection required for tests
- Full structural verification of Phase 38 artifacts
</success_criteria>

<output>
After completion, create `.planning/phases/38-feature-experimentation/38-05-SUMMARY.md`
</output>
