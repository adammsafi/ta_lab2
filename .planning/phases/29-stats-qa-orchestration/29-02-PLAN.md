---
phase: 29-stats-qa-orchestration
plan: 02
type: execute
wave: 2
depends_on: ["29-01"]
files_modified:
  - src/ta_lab2/scripts/stats/__init__.py
  - src/ta_lab2/scripts/stats/run_all_stats_runners.py
  - src/ta_lab2/scripts/run_daily_refresh.py
autonomous: true

must_haves:
  truths:
    - "python -m ta_lab2.scripts.run_daily_refresh --all executes bars -> EMAs -> regimes -> stats in sequence"
    - "python -m ta_lab2.scripts.run_daily_refresh --stats runs only stats runners as standalone"
    - "Stats runner FAIL status (from DB query) halts the pipeline and triggers Telegram alert"
    - "Stats runner WARN status continues execution but logs a Telegram alert"
    - "All 6 stats runners are called and run to completion even if some FAIL"
  artifacts:
    - path: "src/ta_lab2/scripts/stats/__init__.py"
      provides: "Package marker for stats module"
    - path: "src/ta_lab2/scripts/stats/run_all_stats_runners.py"
      provides: "Stats orchestrator calling all 6 runners + DB aggregate query + Telegram alerting"
      exports: ["main", "run_all_stats", "query_stats_status"]
      min_lines: 150
    - path: "src/ta_lab2/scripts/run_daily_refresh.py"
      provides: "--stats flag, run_stats_runners() function, pipeline gate after stats"
      contains: "run_stats"
  key_links:
    - from: "src/ta_lab2/scripts/run_daily_refresh.py"
      to: "src/ta_lab2/scripts/stats/run_all_stats_runners.py"
      via: "subprocess.run with -m ta_lab2.scripts.stats.run_all_stats_runners"
      pattern: "stats.*run_all_stats_runners"
    - from: "src/ta_lab2/scripts/stats/run_all_stats_runners.py"
      to: "6 stats runner modules"
      via: "subprocess.run with -m for each runner"
      pattern: "subprocess\\.run.*-m"
    - from: "src/ta_lab2/scripts/stats/run_all_stats_runners.py"
      to: "stats tables in PostgreSQL"
      via: "SQL query for aggregate PASS/WARN/FAIL counts"
      pattern: "SELECT.*status.*COUNT"
    - from: "src/ta_lab2/scripts/stats/run_all_stats_runners.py"
      to: "ta_lab2.notifications.telegram"
      via: "send_alert for FAIL/WARN"
      pattern: "telegram\\.send_alert"
---

<objective>
Create the stats runner orchestrator and wire it into run_daily_refresh.py as the final pipeline stage with FAIL/WARN pipeline gating.

Purpose: STAT-01 (stats in --all pipeline) and STAT-03 (pipeline gates on FAIL). The daily refresh must validate data quality automatically -- stats runners execute as the final stage and FAIL status halts the pipeline before anyone uses bad data.

Output: New `run_all_stats_runners.py` orchestrator + `--stats` flag on `run_daily_refresh.py` + pipeline gate logic.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-stats-qa-orchestration/29-CONTEXT.md
@.planning/phases/29-stats-qa-orchestration/29-RESEARCH.md
@.planning/phases/29-stats-qa-orchestration/29-01-SUMMARY.md

Key codebase files (read these for patterns):
@src/ta_lab2/scripts/run_daily_refresh.py
@src/ta_lab2/scripts/run_all_audits.py
@src/ta_lab2/notifications/telegram.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create stats runner orchestrator</name>
  <files>
    src/ta_lab2/scripts/stats/__init__.py
    src/ta_lab2/scripts/stats/run_all_stats_runners.py
  </files>
  <action>
    Create `src/ta_lab2/scripts/stats/__init__.py` (empty file).

    Create `src/ta_lab2/scripts/stats/run_all_stats_runners.py` following the structural pattern of `run_all_audits.py` but using `-m` module invocation for each runner:

    1. Define a `StatsScript` dataclass (like AuditScript) with fields: name, module, description, extra_args (list[str]).

    2. Define ALL_STATS_SCRIPTS list with all 6 runners. Call each runner directly via `-m module` (do NOT call the intermediate EMA stats orchestrator):
       - bars: module="ta_lab2.scripts.bars.stats.refresh_price_bars_stats", extra_args=[]
       - ema_multi_tf: module="ta_lab2.scripts.emas.stats.multi_tf.refresh_ema_multi_tf_stats", extra_args=[]
       - ema_cal: module="ta_lab2.scripts.emas.stats.multi_tf_cal.refresh_ema_multi_tf_cal_stats", extra_args=[]
       - ema_cal_anchor: module="ta_lab2.scripts.emas.stats.multi_tf_cal_anchor.refresh_ema_multi_tf_cal_anchor_stats", extra_args=[]
       - returns_ema: module="ta_lab2.scripts.returns.stats.refresh_returns_ema_stats", extra_args=["--families", "all"]
       - features: module="ta_lab2.scripts.features.stats.refresh_cmc_features_stats", extra_args=[]

    3. Create `run_stats_script()` function following `run_audit_script()` pattern:
       - Build cmd: [sys.executable, "-m", script.module] + script.extra_args
       - Add --full-refresh, --db-url, --log-level args as appropriate
       - Use TIMEOUT_STATS = 3600 (1 hour) for all stats runners
       - Catch subprocess.TimeoutExpired separately from generic Exception
       - Return ComponentResult (reuse the same dataclass from run_all_audits or define locally)

    4. Create `query_stats_status(engine, window_hours=2)` function:
       - Query all 6 stats tables for PASS/WARN/FAIL counts from the last window_hours
       - Tables: price_bars_multi_tf_stats, ema_multi_tf_stats, ema_multi_tf_cal_stats, ema_multi_tf_cal_anchor_stats, returns_ema_stats, cmc_features_stats
       - SQL: `SELECT status, COUNT(*) as n FROM {table} WHERE checked_at >= NOW() - INTERVAL '{window_hours} hours' GROUP BY status`
       - Return dict[str, dict[str, int]] mapping table_name -> {status: count}
       - Use sqlalchemy.text() for parameterized queries
       - Use get_engine() or create_engine from resolve_db_url

    5. Create `send_stats_alerts(status_results, failed_runners, warn_runners)` function:
       - Import from ta_lab2.notifications.telegram
       - Check is_configured() first
       - On FAIL: send_alert with severity="critical", title="Daily Refresh: Stats FAILED", message listing which runners have FAIL rows
       - On WARN (no FAIL): send_alert with severity="warning", title="Daily Refresh: Stats WARN", message listing which runners have WARN rows

    6. Create `run_all_stats(db_url, verbose, dry_run, full_refresh=True)` function:
       - Run ALL 6 stats scripts sequentially (run to completion even if some crash)
       - Collect ComponentResults
       - After all complete: if any crashed (returncode != 0), note as FAIL
       - Query DB via query_stats_status() for aggregate PASS/WARN/FAIL
       - Determine overall status: FAIL if any table has FAIL rows OR any runner crashed, WARN if any table has WARN rows, PASS otherwise
       - Send Telegram alerts via send_stats_alerts()
       - Return (overall_status: str, results: list[ComponentResult], db_status: dict)

    7. Create `main()` with argparse:
       - --db-url, --verbose, --dry-run, --full-refresh, --log-level
       - Call run_all_stats()
       - Exit 0 if PASS/WARN, exit 1 if FAIL (this is the subprocess exit code for run_daily_refresh.py to check)
       - Print summary matching run_all_audits.py pattern

    CRITICAL: Stats runners exit 0 even when tests produce FAIL rows. The orchestrator must query the DB AFTER all runners complete to determine aggregate FAIL/WARN/PASS status. Do NOT rely solely on subprocess return codes for data quality status.
  </action>
  <verify>
    Run: `python -c "from ta_lab2.scripts.stats.run_all_stats_runners import main, run_all_stats, query_stats_status"` -- imports without error.
    Run: `python -m ta_lab2.scripts.stats.run_all_stats_runners --dry-run` -- prints 6 runners in dry-run mode, exits 0.
  </verify>
  <done>run_all_stats_runners.py exists, is importable, and dry-run mode lists all 6 stats runners without errors.</done>
</task>

<task type="auto">
  <name>Task 2: Wire --stats flag into run_daily_refresh.py with pipeline gating</name>
  <files>
    src/ta_lab2/scripts/run_daily_refresh.py
  </files>
  <action>
    Modify run_daily_refresh.py to add stats as the 4th pipeline stage:

    1. Add --stats argument to argparse:
       ```python
       p.add_argument("--stats", action="store_true", help="Run stats runners only")
       ```

    2. Update --all help text:
       ```python
       help="Run bars then EMAs then regimes then stats (full refresh)"
       ```

    3. Update validation to include --stats:
       ```python
       if not (args.bars or args.emas or args.regimes or args.stats or args.all):
           p.error("Must specify --bars, --emas, --regimes, --stats, or --all")
       ```

    4. Add `run_stats = args.stats or args.all` alongside existing flags.

    5. Add components.append("stats") when run_stats is True.

    6. Add TIMEOUT_STATS = 3600 to run_daily_refresh.py at the module level alongside the other TIMEOUT constants (TIMEOUT_BARS, TIMEOUT_EMAS, TIMEOUT_REGIMES were added by Plan 01; add TIMEOUT_STATS in the same block).

    7. Create `run_stats_runners(args, db_url)` function following the run_regime_refresher() pattern:
       - Build cmd: [sys.executable, "-m", "ta_lab2.scripts.stats.run_all_stats_runners"]
       - Pass --db-url, --verbose, --dry-run as appropriate
       - Use TIMEOUT_STATS = 3600
       - Catch subprocess.TimeoutExpired
       - Return ComponentResult

    8. Wire stats after regimes in main():
       ```python
       # Run stats if requested (final stage -- after bars, EMAs, regimes)
       if run_stats:
           stats_result = run_stats_runners(args, db_url)
           results.append(("stats", stats_result))

           # Pipeline gate: stats FAIL means data quality issues
           if not stats_result.success:
               print("\n[PIPELINE GATE] Stats runners reported FAIL -- data quality check failed")
               print("Review stats tables for specific failures before using this data")
               # Don't check continue_on_error -- stats FAIL is always terminal
               return 1
       ```

    IMPORTANT: The `return 1` after the pipeline gate print statements is MANDATORY. Without it, the pipeline continues past a FAIL status, defeating the purpose of the gate. The stats FAIL gate always halts (even with --continue-on-error), because the whole point of stats is to validate data quality. If stats say data is bad, continuing is worse than stopping.

    IMPORTANT: The stats subprocess (run_all_stats_runners.py) already handles Telegram alerting internally. The run_daily_refresh.py wrapper just needs to check the exit code (non-zero = FAIL status from DB query).

    9. Update the docstring/epilog examples to include --stats usage:
       ```
       # Run stats only
       python run_daily_refresh.py --stats

       # Full daily refresh (bars -> EMAs -> regimes -> stats)
       python run_daily_refresh.py --all --ids all
       ```
  </action>
  <verify>
    Run: `python -m ta_lab2.scripts.run_daily_refresh --stats --dry-run` -- prints stats component, dry-run mode.
    Run: `python -m ta_lab2.scripts.run_daily_refresh --all --ids 1 --dry-run` -- prints bars, EMAs, regimes, AND stats components.
    Run: `python -m ta_lab2.scripts.run_daily_refresh --help` -- shows --stats in help output.
    Run: `grep -n "return 1" src/ta_lab2/scripts/run_daily_refresh.py` -- confirms the pipeline gate halt exists after the PIPELINE GATE print.
  </verify>
  <done>
    run_daily_refresh.py has --stats flag, TIMEOUT_STATS constant, stats runs as final stage in --all, pipeline gate halts on FAIL status with `return 1`, Telegram alerts sent on FAIL/WARN.
    STAT-01 satisfied: stats wired into --stats and --all.
    STAT-03 satisfied: FAIL halts pipeline with alert, WARN continues with alert.
  </done>
</task>

</tasks>

<verification>
1. `python -m ta_lab2.scripts.stats.run_all_stats_runners --dry-run` exits 0, lists all 6 runners
2. `python -m ta_lab2.scripts.run_daily_refresh --stats --dry-run` exits 0, shows stats component
3. `python -m ta_lab2.scripts.run_daily_refresh --all --ids 1 --dry-run` shows 4 components: bars, EMAs, regimes, stats
4. `python -m ta_lab2.scripts.run_daily_refresh --help` shows --stats flag
5. `grep -n "query_stats_status" src/ta_lab2/scripts/stats/run_all_stats_runners.py` confirms DB query function exists
6. `grep -n "telegram" src/ta_lab2/scripts/stats/run_all_stats_runners.py` confirms Telegram alerting wired
7. `grep -n "return 1" src/ta_lab2/scripts/run_daily_refresh.py` confirms pipeline gate halt after stats FAIL
</verification>

<success_criteria>
- run_all_stats_runners.py calls all 6 stats runners via subprocess, queries DB for aggregate status, sends Telegram alerts
- run_daily_refresh.py --stats runs stats as standalone
- run_daily_refresh.py --all runs stats as 4th/final stage
- FAIL status from stats halts pipeline with `return 1` (non-zero exit from run_all_stats_runners.py)
- WARN status continues but triggers Telegram alert
- All stats runners run to completion before aggregate determination (no early bail)
- TIMEOUT_STATS = 3600 defined as module-level constant in run_daily_refresh.py
</success_criteria>

<output>
After completion, create `.planning/phases/29-stats-qa-orchestration/29-02-SUMMARY.md`
</output>
