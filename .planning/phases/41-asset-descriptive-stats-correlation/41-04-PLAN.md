---
phase: 41-asset-descriptive-stats-correlation
plan: 04
type: execute
wave: 3
depends_on: ["41-02", "41-03"]
files_modified:
  - src/ta_lab2/scripts/desc_stats/run_all_desc_stats_refreshes.py
  - src/ta_lab2/scripts/run_daily_refresh.py
autonomous: true

must_haves:
  truths:
    - "run_all_desc_stats_refreshes.py orchestrates asset stats then correlation as sequential subprocesses"
    - "run_daily_refresh.py --all executes desc_stats stage AFTER AMAs and BEFORE regimes"
    - "run_daily_refresh.py --desc-stats runs desc stats in isolation"
    - "--dry-run propagates correctly to desc stats subprocess"
  artifacts:
    - path: "src/ta_lab2/scripts/desc_stats/run_all_desc_stats_refreshes.py"
      provides: "Desc stats orchestrator subprocess"
      exports: ["main"]
    - path: "src/ta_lab2/scripts/run_daily_refresh.py"
      provides: "Daily refresh with desc_stats stage inserted"
      contains: "desc_stats"
  key_links:
    - from: "src/ta_lab2/scripts/run_daily_refresh.py"
      to: "src/ta_lab2/scripts/desc_stats/run_all_desc_stats_refreshes.py"
      via: "subprocess.run call"
      pattern: "run_all_desc_stats_refreshes"
    - from: "src/ta_lab2/scripts/desc_stats/run_all_desc_stats_refreshes.py"
      to: "src/ta_lab2/scripts/desc_stats/refresh_cmc_asset_stats.py"
      via: "subprocess.run call"
      pattern: "refresh_cmc_asset_stats"
    - from: "src/ta_lab2/scripts/desc_stats/run_all_desc_stats_refreshes.py"
      to: "src/ta_lab2/scripts/desc_stats/refresh_cmc_cross_asset_corr.py"
      via: "subprocess.run call"
      pattern: "refresh_cmc_cross_asset_corr"
---

<objective>
Create the desc stats orchestrator script and wire it into run_daily_refresh.py as a new pipeline stage between AMAs and regimes.

Purpose: This satisfies DESC-05 and CORR-03 -- daily pipeline integration with --desc-stats standalone flag.
Output: Orchestrator + updated daily refresh script with correct pipeline ordering.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41-asset-descriptive-stats-correlation/41-RESEARCH.md

# Key files being modified
@src/ta_lab2/scripts/run_daily_refresh.py

# Pattern reference: AMA orchestrator
@src/ta_lab2/scripts/amas/run_all_ama_refreshes.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create run_all_desc_stats_refreshes.py orchestrator</name>
  <files>src/ta_lab2/scripts/desc_stats/run_all_desc_stats_refreshes.py</files>
  <action>
Create the orchestrator that runs both desc stats scripts sequentially via subprocess. Follow the pattern of `run_all_ama_refreshes.py` for structure.

**Sequential order:** Asset stats first, then correlation (correlation may depend on stats being fresh for regime integration later, and asset stats is independent).

**Subprocess calls:**
1. `python -m ta_lab2.scripts.desc_stats.refresh_cmc_asset_stats` with forwarded args
2. `python -m ta_lab2.scripts.desc_stats.refresh_cmc_cross_asset_corr` with forwarded args

**CLI (argparse):**
- `--ids` (forward to both subscripts, default "all")
- `--tf` (forward to both)
- `--windows` (forward to both, default "30,60,90,252")
- `--full-rebuild` (forward to both)
- `--dry-run` (forward to both)
- `--continue-on-error` (if asset stats fails and this flag is set, still run correlation)
- `--workers` (forward to both, default 4)
- `--db-url` (forward to both)
- `--verbose` (forward + stream output)

**Timeout:** 1800s (30 min) per subscript.

**Flow:**
1. Parse args
2. Run asset stats subprocess with timeout
3. Check result -- if failed and not --continue-on-error, exit 1
4. Run correlation subprocess with timeout
5. Print summary (both results)
6. Exit 0 if both succeeded, 1 otherwise

**Error handling:** Match existing patterns -- catch subprocess.TimeoutExpired separately from generic Exception. Print stdout/stderr on failure.

Module invocable as `-m ta_lab2.scripts.desc_stats.run_all_desc_stats_refreshes`.
  </action>
  <verify>
```
python -m ta_lab2.scripts.desc_stats.run_all_desc_stats_refreshes --ids 1 --tf 1D --dry-run
```
Should show both subscript commands that would run.
  </verify>
  <done>
run_all_desc_stats_refreshes.py orchestrates asset stats then correlation as sequential subprocesses with proper arg forwarding and error handling.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire desc_stats into run_daily_refresh.py</name>
  <files>src/ta_lab2/scripts/run_daily_refresh.py</files>
  <action>
Modify run_daily_refresh.py to add a desc_stats stage between AMAs and regimes.

**Changes to make:**

1. Add timeout constant:
   ```python
   TIMEOUT_DESC_STATS = 3600  # 1 hour -- asset stats + correlation computation
   ```

2. Add `--desc-stats` CLI flag:
   ```python
   p.add_argument("--desc-stats", action="store_true",
                  help="Run descriptive stats refresh only (asset stats + correlation)")
   ```

3. Add `run_desc_stats_refresher(args, db_url, parsed_ids)` function following the exact same pattern as `run_regime_refresher`. Key details:
   - Script path: `Path(__file__).parent / "desc_stats" / "run_all_desc_stats_refreshes.py"` -- NO, use `-m` invocation: `[sys.executable, "-m", "ta_lab2.scripts.desc_stats.run_all_desc_stats_refreshes"]`
   - Forward args: --ids (formatted from parsed_ids), --db-url, --verbose, --dry-run, --continue-on-error
   - Forward --workers as --workers if args.num_processes is set
   - Timeout: TIMEOUT_DESC_STATS
   - Component name: "desc_stats"

4. Update validation check (line ~973-978): Add `args.desc_stats` to the `not (args.bars or args.emas or ...)` check.

5. Update `run_desc_stats` determination:
   ```python
   run_desc_stats = args.desc_stats or args.all
   ```

6. Add to components string building.

7. **CRITICAL: Insert desc_stats stage AFTER AMAs, BEFORE regimes in the main() flow.**
   The current flow is: bars -> EMAs -> AMAs -> regimes -> stats.
   New flow must be: bars -> EMAs -> AMAs -> **desc_stats** -> regimes -> stats.

   Insert after the AMA block (around line 1096-1102) and before the regime block (around line 1104):
   ```python
   # Run desc stats if requested (after AMAs, before regimes)
   if run_desc_stats:
       desc_result = run_desc_stats_refresher(args, db_url, parsed_ids)
       results.append(("desc_stats", desc_result))

       if not desc_result.success and not args.continue_on_error:
           print("\n[STOPPED] Desc stats failed, stopping execution")
           print("(Use --continue-on-error to run remaining components)")
           return 1
   ```

8. Update docstring and epilog examples to include --desc-stats.

9. Update the --all help text to include desc_stats in the list.
  </action>
  <verify>
```
python -m ta_lab2.scripts.run_daily_refresh --desc-stats --ids 1 --tf 1D --dry-run
```
Should show desc stats would execute.

```
python -m ta_lab2.scripts.run_daily_refresh --all --ids 1 --dry-run
```
Should show all stages including desc_stats between AMAs and regimes.
  </verify>
  <done>
run_daily_refresh.py has --desc-stats flag and runs desc_stats stage between AMAs and regimes in --all mode.
  </done>
</task>

</tasks>

<verification>
- `python -m ta_lab2.scripts.desc_stats.run_all_desc_stats_refreshes --ids 1 --tf 1D --dry-run` shows both subscripts
- `python -m ta_lab2.scripts.run_daily_refresh --all --ids 1 --dry-run` shows bars, EMAs, AMAs, desc_stats, regimes, stats in that order
- `python -m ta_lab2.scripts.run_daily_refresh --desc-stats --ids 1 --dry-run` runs desc_stats alone
- Pipeline ordering is correct: desc_stats after AMAs, before regimes
</verification>

<success_criteria>
- Orchestrator runs asset stats then correlation sequentially
- run_daily_refresh.py --all includes desc_stats between AMAs and regimes
- --desc-stats standalone flag works
- --dry-run propagates to desc stats subprocess
- Error handling follows existing component patterns
</success_criteria>

<output>
After completion, create `.planning/phases/41-asset-descriptive-stats-correlation/41-04-SUMMARY.md`
</output>
