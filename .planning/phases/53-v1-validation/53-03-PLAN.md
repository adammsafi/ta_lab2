---
phase: 53-v1-validation
plan: 03
type: execute
wave: 3
depends_on: ["53-01"]
files_modified:
  - src/ta_lab2/scripts/validation/run_kill_switch_exercise.py
autonomous: false

must_haves:
  truths:
    - "Kill switch exercise script executes an 8-step protocol with timestamped evidence"
    - "Manual trigger uses activate_kill_switch() with 'V1 EXERCISE:' prefix in reason"
    - "Re-enable uses re_enable_trading() with operator + reason (manual, NOT automatic)"
    - "Auto-trigger test temporarily lowers daily_loss_pct_threshold, then restores"
    - "Exercise produces Markdown evidence document with latency measurements"
    - "Exercise uses existing event types (kill_switch_activated/disabled), no new DB types"
  artifacts:
    - path: "src/ta_lab2/scripts/validation/run_kill_switch_exercise.py"
      provides: "Kill switch exercise protocol CLI with manual + automatic trigger test"
      min_lines: 200
  key_links:
    - from: "src/ta_lab2/scripts/validation/run_kill_switch_exercise.py"
      to: "ta_lab2.risk.kill_switch"
      via: "activate_kill_switch, re_enable_trading, get_kill_switch_status"
      pattern: "from ta_lab2\\.risk\\.kill_switch import"
    - from: "src/ta_lab2/scripts/validation/run_kill_switch_exercise.py"
      to: "dim_risk_state, dim_risk_limits, cmc_risk_events, cmc_orders"
      via: "SQLAlchemy text() queries for state snapshots"
      pattern: "text\\("
---

<objective>
Create the kill switch exercise protocol script -- the engineered end-to-end test of manual and automatic kill switch triggers with full evidence collection.

Purpose: This script satisfies VAL-04 (kill switch tested manually and automatically). It follows an 8-step protocol: pre-snapshot, manual trigger, validate, re-enable, engineer auto-trigger, validate, restore, produce evidence document. The script is interactive (requires operator input for re-enable steps) and produces a timestamped Markdown evidence file.

Output: `src/ta_lab2/scripts/validation/run_kill_switch_exercise.py` CLI.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/53-v1-validation/53-CONTEXT.md
@.planning/phases/53-v1-validation/53-RESEARCH.md
@src/ta_lab2/risk/kill_switch.py
@src/ta_lab2/risk/risk_engine.py
@src/ta_lab2/scripts/risk/kill_switch_cli.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Kill switch exercise protocol script</name>
  <files>
    src/ta_lab2/scripts/validation/run_kill_switch_exercise.py
  </files>
  <action>
Create `run_kill_switch_exercise.py` -- an interactive protocol script implementing the 8-step kill switch exercise.

**Project root:** `_PROJECT_ROOT = Path(__file__).resolve().parents[4]`

**Engine:** `_get_engine()` with NullPool, from `ta_lab2.db.config.resolve_db_url`.

**Imports from existing code:**
```python
from ta_lab2.risk.kill_switch import (
    activate_kill_switch, re_enable_trading, get_kill_switch_status
)
```

**`ExerciseStep` dataclass:**
- `step_num: int`
- `name: str`
- `timestamp: str` (ISO format)
- `result: str` (description of what happened)
- `passed: bool`

**`KillSwitchExercise` class:**

Constructor: `__init__(self, engine: Engine, operator: str)`

Stores `self._steps: list[ExerciseStep]` for evidence collection.

**`run_exercise(self) -> str`** -- returns path to evidence document. Implements:

**STEP 1: Pre-exercise snapshot**
- Query dim_risk_state: capture trading_state, halted_at, drift_paused
- Query cmc_orders: count orders in 'created' or 'submitted' status
- Query cmc_risk_events: capture MAX(event_ts)
- Record T0 = datetime.now(timezone.utc)
- Log step: "Pre-state: trading_state={}, open_orders={}, last_event={}"
- Verify trading_state == 'active'. If not, print WARNING and ask operator to confirm continue.

**STEP 2: Manual kill switch activation**
- Call `activate_kill_switch(self._engine, reason="V1 EXERCISE: manual kill switch test", trigger_source="manual")`
- Record T1 = datetime.now(timezone.utc)
- Compute manual_latency = (T1 - T0).total_seconds()
- Log step: "Manual trigger fired, latency={manual_latency:.3f}s"

**STEP 3: Validate manual trigger effects**
- Query dim_risk_state: verify trading_state='halted', halted_reason contains 'V1 EXERCISE'
- Query cmc_orders: verify no orders in 'created'/'submitted' status
- Query cmc_risk_events: verify new row with event_type='kill_switch_activated' and reason containing 'V1 EXERCISE'
- Log each check as sub-step with PASS/FAIL

**STEP 4: Manual re-enable**
- Print to stdout: `\n>>> Kill switch activated. Verify effects above.`
- Print: `>>> Press ENTER to re-enable trading...`
- Call `input()` to wait for operator
- Call `re_enable_trading(self._engine, reason="V1 EXERCISE: manual test complete", operator=self._operator)`
- Verify trading_state returns to 'active'
- Log step

**STEP 5: Engineer automatic trigger**
- Query current daily_loss_pct_threshold from dim_risk_limits (WHERE asset_id IS NULL AND strategy_id IS NULL)
- Store original_threshold
- Update to 0.001 (0.1%) via `UPDATE dim_risk_limits SET daily_loss_pct_threshold = 0.001 WHERE asset_id IS NULL AND strategy_id IS NULL`
- Print: `>>> Threshold lowered to 0.1%. Waiting for automatic trigger...`
- Print: `>>> The daily loss stop will trigger on the next RiskEngine check cycle.`
- Print: `>>> You may need to run the executor or wait for the next scheduled run.`
- Print: `>>> Press ENTER once the automatic trigger has fired (check dim_risk_state)...`
- Call `input()` to wait
- Record T5 = datetime.now(timezone.utc)
- Log step

**STEP 6: Validate automatic trigger effects**
- Query dim_risk_state: verify trading_state='halted'
- Query cmc_risk_events: check for event with trigger_source containing 'daily_loss_stop' (or event_type containing appropriate value)
- Log each check

**STEP 7: Restore thresholds + re-enable**
- `UPDATE dim_risk_limits SET daily_loss_pct_threshold = :original WHERE asset_id IS NULL AND strategy_id IS NULL`
- Verify restored via SELECT
- Print: `>>> Thresholds restored. Press ENTER to re-enable trading...`
- Call `input()`
- Call `re_enable_trading(self._engine, reason="V1 EXERCISE: auto-trigger test complete", operator=self._operator)`
- Verify trading_state='active'
- Log step

**STEP 8: Produce evidence document**
- Call `_generate_evidence_report()` which writes Markdown to `reports/validation/kill_switch_exercise/ks_exercise_{date}.md`
- Return path

**`_generate_evidence_report(self) -> str`:**

Markdown structure:
```markdown
# Kill Switch Exercise: {date}

## Summary
- Exercise type: Manual + Automatic (daily loss stop)
- Date: {date}
- Operator: {operator}
- Outcome: PASS / FAIL

## Manual Trigger Evidence
| Step | Timestamp | Result |
...

**Manual trigger latency: {X}ms**
**Target: < 5 seconds | Actual: {X}ms | Status: PASS/FAIL**

## Automatic Trigger Evidence
| Step | Timestamp | Result |
...

## Threshold Restoration Evidence
- Original threshold: {X}%
- Test threshold: 0.1%
- Restored at: {ts}
- Confirmed via: SELECT

## VAL-04 Gate Assessment
- Manual trigger: PASS/FAIL
- Automatic trigger: PASS/FAIL
- Recovery: PASS/FAIL
- Overall VAL-04: PASS/FAIL
```

All file writes use `encoding="utf-8"`.

**CLI argparse:**
- `--operator` (required): operator name for re_enable_trading
- `--db-url` (optional)
- `--output-dir` (optional, default `reports/validation/kill_switch_exercise`)
- `--skip-auto` (optional flag): skip the automatic trigger test (Steps 5-6) if operator only wants manual test

Invocation: `python -m ta_lab2.scripts.validation.run_kill_switch_exercise --operator asafi`

**Key constraints:**
- Use EXISTING event types only (kill_switch_activated, kill_switch_disabled). Do NOT create new event types.
- All exercise-related events use "V1 EXERCISE:" prefix in reason string for filtering.
- Re-enable is ALWAYS manual (operator + reason). Never implement auto-resume for kill switch.
- The script is interactive and prints prompts to stdout. It is NOT designed for automated/background execution.
  </action>
  <verify>
    python -m ta_lab2.scripts.validation.run_kill_switch_exercise --help
  </verify>
  <done>
    Kill switch exercise CLI shows help with --operator required arg. Script implements 8-step protocol with evidence collection. Uses existing kill_switch API. Tags exercise events with "V1 EXERCISE:" prefix. Produces timestamped Markdown evidence document.
  </done>
</task>

</tasks>

<verification>
- `python -m ta_lab2.scripts.validation.run_kill_switch_exercise --help` -- shows usage with --operator required
- Script imports activate_kill_switch, re_enable_trading, get_kill_switch_status from ta_lab2.risk.kill_switch
- No new event types defined -- uses existing kill_switch_activated / kill_switch_disabled
- Evidence document path follows reports/validation/kill_switch_exercise/ convention
</verification>

<success_criteria>
1. CLI accepts --operator (required) and --skip-auto (optional)
2. 8-step protocol implemented with ExerciseStep evidence collection
3. Manual trigger via activate_kill_switch() with "V1 EXERCISE:" reason prefix
4. Re-enable via re_enable_trading() with operator parameter (never automatic)
5. Auto-trigger test temporarily lowers threshold, waits for operator, restores
6. Markdown evidence document generated with latency measurements and VAL-04 assessment
</success_criteria>

<output>
After completion, create `.planning/phases/53-v1-validation/53-03-SUMMARY.md`
</output>
