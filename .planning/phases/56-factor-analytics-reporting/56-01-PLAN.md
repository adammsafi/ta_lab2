---
phase: 56-factor-analytics-reporting
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - alembic/versions/XXXX_add_rank_ic_to_ic_results.py
  - alembic/versions/XXXX_add_mae_mfe_to_trades.py
  - alembic/versions/XXXX_add_mc_ci_to_metrics.py
  - alembic/versions/XXXX_add_cs_norms_to_features.py
  - sql/migration/add_factor_analytics_columns.sql
autonomous: true

must_haves:
  truths:
    - "cmc_ic_results has a rank_ic NUMERIC column"
    - "cmc_backtest_trades has mae and mfe NUMERIC columns"
    - "cmc_backtest_metrics has mc_sharpe_lo, mc_sharpe_hi, mc_sharpe_median, mc_n_samples columns"
    - "cmc_backtest_runs has a tearsheet_path TEXT column"
    - "cmc_features has 6 CS-norm columns (ret_arith_cs_zscore, ret_arith_cs_rank, rsi_14_cs_zscore, rsi_14_cs_rank, vol_parkinson_20_cs_zscore, vol_parkinson_20_cs_rank)"
    - "All 4 migrations are reversible: alembic downgrade to pre-Phase-56 head drops all 13 columns without error"
  artifacts:
    - path: "alembic/versions/*_add_rank_ic_to_ic_results.py"
      provides: "rank_ic column migration with backfill"
    - path: "alembic/versions/*_add_mae_mfe_to_trades.py"
      provides: "mae/mfe columns on cmc_backtest_trades"
    - path: "alembic/versions/*_add_mc_ci_to_metrics.py"
      provides: "Monte Carlo CI columns on cmc_backtest_metrics + tearsheet_path on cmc_backtest_runs"
    - path: "alembic/versions/*_add_cs_norms_to_features.py"
      provides: "6 cross-sectional normalization columns on cmc_features"
    - path: "sql/migration/add_factor_analytics_columns.sql"
      provides: "Reference DDL for all Phase 56 schema changes"
  key_links:
    - from: "alembic/versions/*_add_rank_ic_to_ic_results.py"
      to: "public.cmc_ic_results"
      via: "op.add_column + backfill UPDATE"
      pattern: "rank_ic.*ic"
    - from: "alembic/versions/*_add_cs_norms_to_features.py"
      to: "public.cmc_features"
      via: "op.add_column for 6 columns"
      pattern: "cs_zscore|cs_rank"
---

<objective>
Create all Alembic migrations for Phase 56 schema changes: rank_ic on cmc_ic_results, mae/mfe on cmc_backtest_trades, Monte Carlo CI on cmc_backtest_metrics, tearsheet_path on cmc_backtest_runs, and 6 cross-sectional normalization columns on cmc_features.

Purpose: Schema must exist before any data-writing code in Plans 04-07 can run. Grouping all migrations in one plan ensures a single `alembic upgrade head` makes all columns available.
Output: 4 Alembic migration files + 1 reference DDL file. All columns added as nullable (no data loss risk).
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/56-factor-analytics-reporting/56-RESEARCH.md

# Existing migration pattern reference
@alembic/versions/30eac3660488_perps_readiness.py
# Existing schemas being altered
@sql/backtests/071_cmc_backtest_trades.sql
@sql/backtests/072_cmc_backtest_metrics.sql
@sql/backtests/070_cmc_backtest_runs.sql
@sql/features/080_cmc_ic_results.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 4 Alembic migration files</name>
  <files>
    alembic/versions/XXXX_add_rank_ic_to_ic_results.py
    alembic/versions/XXXX_add_mae_mfe_to_trades.py
    alembic/versions/XXXX_add_mc_ci_to_metrics.py
    alembic/versions/XXXX_add_cs_norms_to_features.py
  </files>
  <action>
    Generate 4 Alembic migration files using `alembic revision -m "..."` for each. Chain them sequentially (each `down_revision` points to the previous). The head revision is currently `30eac3660488` (perps_readiness).

    Migration 1: `add_rank_ic_to_ic_results`
    - Add `rank_ic NUMERIC` nullable column to `public.cmc_ic_results`
    - Backfill: `UPDATE public.cmc_ic_results SET rank_ic = ic WHERE rank_ic IS NULL`
    - downgrade: drop `rank_ic` column

    Migration 2: `add_mae_mfe_to_trades`
    - Add `mae NUMERIC` nullable to `public.cmc_backtest_trades`
    - Add `mfe NUMERIC` nullable to `public.cmc_backtest_trades`
    - downgrade: drop both columns

    Migration 3: `add_mc_ci_to_metrics`
    - Add `mc_sharpe_lo NUMERIC`, `mc_sharpe_hi NUMERIC`, `mc_sharpe_median NUMERIC`, `mc_n_samples INTEGER` nullable to `public.cmc_backtest_metrics`
    - Add `tearsheet_path TEXT` nullable to `public.cmc_backtest_runs`
    - downgrade: drop all 5 columns

    Migration 4: `add_cs_norms_to_features`
    - Add 6 nullable DOUBLE PRECISION columns to `public.cmc_features`:
      `ret_arith_cs_zscore`, `ret_arith_cs_rank`,
      `rsi_14_cs_zscore`, `rsi_14_cs_rank`,
      `vol_parkinson_20_cs_zscore`, `vol_parkinson_20_cs_rank`
    - downgrade: drop all 6 columns

    Follow existing migration pattern from `30eac3660488_perps_readiness.py`. Use `sa.Column` with `sa.Numeric()` for NUMERIC and `sa.Float()` for DOUBLE PRECISION. Use `schema="public"` in all `op.add_column` / `op.drop_column` calls.

    CRITICAL: Use ASCII-only comments in all migration files (no box-drawing chars like ===). Windows UTF-8 issue documented in MEMORY.md.

    CRITICAL: Each migration's `downgrade()` must be a complete inverse of `upgrade()`. Verify each downgrade drops exactly the columns that upgrade adds. This ensures the migration chain is fully reversible.
  </action>
  <verify>
    Run `alembic upgrade head` and verify no errors. Then verify columns exist:
    ```
    python -c "
    from sqlalchemy import create_engine, text
    from ta_lab2.db import get_engine
    e = get_engine()
    with e.connect() as c:
        # Check rank_ic
        r = c.execute(text(\"SELECT column_name FROM information_schema.columns WHERE table_name='cmc_ic_results' AND column_name='rank_ic'\")).fetchone()
        assert r, 'rank_ic missing'
        # Check mae/mfe
        r = c.execute(text(\"SELECT column_name FROM information_schema.columns WHERE table_name='cmc_backtest_trades' AND column_name='mae'\")).fetchone()
        assert r, 'mae missing'
        # Check mc columns
        r = c.execute(text(\"SELECT column_name FROM information_schema.columns WHERE table_name='cmc_backtest_metrics' AND column_name='mc_sharpe_lo'\")).fetchone()
        assert r, 'mc_sharpe_lo missing'
        # Check tearsheet_path
        r = c.execute(text(\"SELECT column_name FROM information_schema.columns WHERE table_name='cmc_backtest_runs' AND column_name='tearsheet_path'\")).fetchone()
        assert r, 'tearsheet_path missing'
        # Check CS-norm columns
        r = c.execute(text(\"SELECT column_name FROM information_schema.columns WHERE table_name='cmc_features' AND column_name='ret_arith_cs_zscore'\")).fetchone()
        assert r, 'ret_arith_cs_zscore missing'
    print('All Phase 56 columns verified')
    "
    ```

    Then verify downgrade path works:
    ```
    # Downgrade one step and verify tearsheet_path is gone, then re-upgrade
    alembic downgrade -1
    python -c "
    from ta_lab2.db import get_engine
    from sqlalchemy import text
    e = get_engine()
    with e.connect() as c:
        r = c.execute(text(\"SELECT column_name FROM information_schema.columns WHERE table_name='cmc_features' AND column_name='ret_arith_cs_zscore'\")).fetchone()
        assert r is None, 'CS-norm column should be gone after downgrade'
    print('Downgrade verified: CS-norm columns removed')
    "
    alembic upgrade head
    ```
  </verify>
  <done>All 4 migrations applied successfully. 13 new columns exist across 5 tables: rank_ic (1), mae+mfe (2), mc_sharpe_lo/hi/median+mc_n_samples (4), tearsheet_path (1), and 6 CS-norm columns. Existing data unaffected (all nullable). Downgrade path verified.</done>
</task>

<task type="auto">
  <name>Task 2: Create reference DDL for Phase 56 columns</name>
  <files>sql/migration/add_factor_analytics_columns.sql</files>
  <action>
    Create a reference DDL file documenting all Phase 56 schema additions. This is documentation only (not executed -- Alembic handles the actual migration). Include ALTER TABLE statements and COMMENT ON statements for each new column. Use ASCII-only comments.

    Document:
    - cmc_ic_results.rank_ic: "Spearman rank IC (explicit label for existing Spearman IC in 'ic' column)"
    - cmc_backtest_trades.mae: "Maximum Adverse Excursion: worst intra-trade return vs entry price"
    - cmc_backtest_trades.mfe: "Maximum Favorable Excursion: best intra-trade return vs entry price"
    - cmc_backtest_metrics.mc_sharpe_lo/hi/median: "Monte Carlo 95% CI bounds and median for Sharpe ratio (N=1000 resamples)"
    - cmc_backtest_metrics.mc_n_samples: "Number of Monte Carlo resamples used"
    - cmc_backtest_runs.tearsheet_path: "File path to QuantStats HTML tear sheet"
    - cmc_features CS-norm columns: "Cross-sectional z-score/rank computed via PARTITION BY (ts, tf)"
  </action>
  <verify>File exists and contains all 13 column definitions. `cat sql/migration/add_factor_analytics_columns.sql | grep -c "ADD COLUMN"` returns 13.</verify>
  <done>Reference DDL file created with all 13 Phase 56 column definitions and COMMENT ON documentation.</done>
</task>

</tasks>

<verification>
- `alembic upgrade head` completes without error
- `alembic current` shows the latest Phase 56 migration as head
- All 13 new columns exist in the database (verified via information_schema query)
- Existing data in all 5 tables is unaffected
- Downgrade path verified: `alembic downgrade -1` removes last migration's columns cleanly
</verification>

<success_criteria>
All Phase 56 schema changes applied via Alembic. 13 new nullable columns across 5 tables. Reference DDL documents all changes. Migration chain is fully reversible.
</success_criteria>

<output>
After completion, create `.planning/phases/56-factor-analytics-reporting/56-01-SUMMARY.md`
</output>
