---
phase: 36-psr-purged-k-fold
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/ta_lab2/backtests/psr.py
  - tests/backtests/test_psr.py
autonomous: true

must_haves:
  truths:
    - "compute_psr(returns, sr_star=0) returns float in [0, 1] for valid returns"
    - "compute_psr returns NaN when n < 30 and logs a warning"
    - "compute_psr logs a warning when n < 100"
    - "compute_psr handles zero-std returns: returns 0.5 when sr_star==0, returns 0.0 when sr_star>0"
    - "compute_dsr with full SR list produces value <= raw best Sharpe's PSR"
    - "compute_dsr with n_trials approximation produces value <= raw best Sharpe's PSR"
    - "min_trl returns n_obs (bars) and calendar_days for valid strategies"
    - "min_trl returns inf when sr_hat <= sr_star"
    - "PSR variance formula uses Pearson kurtosis (fisher=False), not excess kurtosis"
  artifacts:
    - path: "src/ta_lab2/backtests/psr.py"
      provides: "PSR/DSR/MinTRL/expected_max_sr formulas"
      exports: ["compute_psr", "compute_dsr", "min_trl", "expected_max_sr"]
      min_lines: 120
    - path: "tests/backtests/test_psr.py"
      provides: "Comprehensive tests for PSR module"
      min_lines: 80
  key_links:
    - from: "src/ta_lab2/backtests/psr.py"
      to: "scipy.stats"
      via: "import norm, skew, kurtosis"
      pattern: "from scipy.stats import"
    - from: "src/ta_lab2/backtests/psr.py"
      to: "kurtosis(.*fisher=False)"
      via: "Pearson kurtosis convention"
      pattern: "fisher=False"
---

<objective>
Implement the Lopez de Prado PSR/DSR/MinTRL formula module using TDD (Red-Green-Refactor).

Purpose: PSR-02 through PSR-05 require the full formulas with exact scipy kurtosis convention. TDD ensures the critical kurtosis trap (Pearson vs Fisher) is caught by tests before implementation.

Output: `src/ta_lab2/backtests/psr.py` with 4 exported functions + comprehensive test suite.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-psr-purged-k-fold/36-RESEARCH.md
@.planning/phases/36-psr-purged-k-fold/36-CONTEXT.md

@src/ta_lab2/backtests/metrics.py
</context>

<feature>
  <name>PSR/DSR/MinTRL formulas (Lopez de Prado)</name>
  <files>src/ta_lab2/backtests/psr.py, tests/backtests/test_psr.py</files>
  <behavior>
    PSR formula behavior:
    - compute_psr(normal_returns_n10000_mean0.01_std0.1, sr_star=0) -> value > 0.99 (strong positive SR)
    - compute_psr(normal_returns_n20, sr_star=0) -> NaN (n < 30 guard) + UserWarning
    - compute_psr(normal_returns_n50, sr_star=0) -> valid float in [0,1] + UserWarning (n < 100)
    - compute_psr(zero_returns_n500, sr_star=0) -> 0.5 (zero-std guard: SR undefined, but z=0 when sr_star=0)
    - compute_psr(zero_returns_n500, sr_star=0.05) -> 0.0 (zero-std guard: SR=0 < sr_star, cannot beat benchmark)
    - compute_psr(constant_nonzero_returns_n500, sr_star=0) -> 0.5 (zero-std guard: std=0, same logic as zero returns)
    - For normal returns: var_sr approximately equals (1 + SR^2/2)/(T-1) (Pearson kurtosis validation)
    - compute_psr(returns, sr_star=0.05) -> lower than compute_psr(returns, sr_star=0) (higher benchmark)

    DSR formula behavior:
    - compute_dsr(best_returns, sr_estimates=[list of N SR values]) -> float <= compute_psr(best_returns, sr_star=0)
    - compute_dsr(best_returns, n_trials=100) -> float <= compute_psr(best_returns, sr_star=0) (approximation mode)
    - compute_dsr(best_returns, sr_estimates=None, n_trials=None) -> raises ValueError
    - expected_max_sr([0.5, 0.3, -0.1, 0.8, 0.2]) -> positive float (expected max of 5 trials)

    MinTRL formula behavior:
    - min_trl(strong_positive_returns, sr_star=0) -> dict with n_obs > 0, calendar_days > 0
    - min_trl(returns_where_sr_leq_sr_star) -> dict with n_obs=inf, calendar_days=inf
    - min_trl(returns, target_psr=0.99) -> higher n_obs than min_trl(returns, target_psr=0.95)
    - Result dict has keys: n_obs, calendar_days, sr_hat, target_psr
  </behavior>
  <implementation>
    Create `src/ta_lab2/backtests/psr.py` with:

    1. `compute_psr(returns, sr_star=0.0) -> float`:
       - Guard: n < 30 -> NaN + warnings.warn; n < 100 -> warnings.warn
       - ZERO-STD GUARD (must come before SR calculation):
         If std(returns, ddof=1) == 0.0:
           - If sr_star == 0.0: return 0.5  (z = 0 -> CDF = 0.5; SR is undefined but equal to benchmark)
           - If sr_star > 0.0: return 0.0   (SR = 0 which is < sr_star; cannot beat positive benchmark)
           - If sr_star < 0.0: return 1.0   (SR = 0 which is > sr_star; always beats negative benchmark)
       - sr_hat = mean(returns) / std(returns, ddof=1)
       - gamma_3 = scipy.stats.skew(returns)
       - gamma_4 = scipy.stats.kurtosis(returns, fisher=False)  # PEARSON kurtosis CRITICAL
       - var_sr = (1 - gamma_3 * sr_hat + (gamma_4 - 1)/4 * sr_hat^2) / (n - 1)
       - Guard: var_sr <= 0 -> NaN
       - z = (sr_hat - sr_star) / sqrt(var_sr)
       - return norm.cdf(z)

    2. `expected_max_sr(sr_estimates, expected_mean=0.0) -> float`:
       - N = len(sr_estimates)
       - euler_gamma = 0.5772156649
       - std_sr = np.std(sr_estimates - expected_mean, ddof=1)
       - e_max = expected_mean + std_sr * ((1-gamma)*norm.ppf(1-1/N) + gamma*norm.ppf(1-1/(N*e)))

    3. `compute_dsr(best_trial_returns, sr_estimates=None, n_trials=None, sr_star_override=None) -> float`:
       - Exact mode: benchmark = expected_max_sr(sr_estimates)
       - Approx mode: benchmark via Bailey formula with assumed std=1
       - Call compute_psr(best_trial_returns, sr_star=benchmark)

    4. `min_trl(returns, sr_star=0.0, target_psr=0.95, freq_per_year=365) -> dict`:
       - Guard: sr_hat <= sr_star -> return inf values
       - v_factor = 1 - gamma_3*sr_hat + (gamma_4-1)/4*sr_hat^2
       - z_threshold = norm.ppf(target_psr)
       - n_obs = (z_threshold * sqrt(v_factor) / (sr_hat - sr_star))^2 + 1
       - calendar_days = round(ceil(n_obs) / freq_per_year * 365)

    Follow TDD: write failing tests first (RED), implement to pass (GREEN), refactor if needed.
  </implementation>
</feature>

<verification>
- `pytest tests/backtests/test_psr.py -v` passes all tests
- Kurtosis validation test confirms var_sr matches (1 + SR^2/2)/(T-1) for normal returns
- DSR always produces value <= raw PSR (deflation property)
- MinTRL returns inf for sr_hat <= sr_star
- Zero-std guard: compute_psr(np.zeros(500), sr_star=0) returns exactly 0.5
- Zero-std guard: compute_psr(np.zeros(500), sr_star=0.05) returns exactly 0.0
- Zero-std guard: compute_psr(np.full(500, 0.01), sr_star=0) returns exactly 0.5 (constant nonzero returns also have std=0)
- All functions handle edge cases (empty returns, zero std, negative sr_star) without crashing
</verification>

<success_criteria>
- PSR-02: compute_psr matches Lopez de Prado formula exactly using scipy skew/kurtosis with Pearson convention
- PSR-03: NaN guard at n < 30, warning at n < 100, zero-std guard returns deterministic values, configurable sr_star parameter
- PSR-04: compute_dsr deflates correctly in both exact and approximate modes
- PSR-05: min_trl returns both bar count and calendar days with configurable target_psr
- All tests pass with pytest
</success_criteria>

<output>
After completion, create `.planning/phases/36-psr-purged-k-fold/36-02-SUMMARY.md`
</output>
