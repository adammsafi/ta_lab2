---
phase: 36-psr-purged-k-fold
plan: 05
type: execute
wave: 2
depends_on: ["36-01"]
files_modified:
  - src/ta_lab2/scripts/run_daily_refresh.py
  - src/ta_lab2/scripts/alembic_utils.py
autonomous: true

must_haves:
  truths:
    - "run_daily_refresh.py checks Alembic migration status on startup and warns if behind head"
    - "Migration check does NOT auto-upgrade (warn only, print instructions)"
    - "Migration check uses NullPool and does not hold connections"
    - "is_alembic_head() returns True when DB is at head, False when behind"
  artifacts:
    - path: "src/ta_lab2/scripts/alembic_utils.py"
      provides: "Alembic migration status checker"
      exports: ["is_alembic_head", "check_migration_status"]
      min_lines: 30
    - path: "src/ta_lab2/scripts/run_daily_refresh.py"
      provides: "Migration check wired into daily refresh startup"
      contains: "check_migration_status"
  key_links:
    - from: "src/ta_lab2/scripts/run_daily_refresh.py"
      to: "src/ta_lab2/scripts/alembic_utils.py"
      via: "import check_migration_status"
      pattern: "from ta_lab2.scripts.alembic_utils import"
    - from: "src/ta_lab2/scripts/alembic_utils.py"
      to: "alembic"
      via: "MigrationContext + ScriptDirectory"
      pattern: "from alembic"
---

<objective>
Create an Alembic migration status utility and wire it into run_daily_refresh.py startup.

Purpose: CONTEXT decision says "Migration auto-check on startup: run_daily_refresh.py checks alembic current vs head, warns or auto-upgrades." RESEARCH pitfall 6 says warn-only, not auto-upgrade, to avoid leaving DB in inconsistent state.

Output: New alembic_utils.py module + modified run_daily_refresh.py with startup migration check.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-psr-purged-k-fold/36-RESEARCH.md

@src/ta_lab2/scripts/run_daily_refresh.py
@alembic/env.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create alembic_utils.py migration checker</name>
  <files>src/ta_lab2/scripts/alembic_utils.py</files>
  <action>
Create `src/ta_lab2/scripts/alembic_utils.py` with two functions:

1. `is_alembic_head(db_url: str, ini_path: str = "alembic.ini") -> bool`:
   - Create Alembic Config from ini_path
   - Get head revision from ScriptDirectory.from_config(cfg).get_current_head()
   - Create engine with NullPool (matching project pattern)
   - Get current DB revision via MigrationContext.configure(conn).get_current_revision()
   - Return current == head

2. `check_migration_status(db_url: str, ini_path: str = "alembic.ini") -> bool`:
   - Wraps is_alembic_head with logging
   - If at head: logger.info("[MIGRATION] Database is at Alembic head: {head_rev}")
   - If behind: logger.warning("[MIGRATION] Database at revision {current_rev}, head is {head_rev}. Run: alembic upgrade head")
   - If error (e.g., alembic.ini not found, DB unreachable): logger.warning with the error message, return False. Do NOT crash -- the migration check is advisory.
   - Returns True if at head, False otherwise

Imports needed:
```python
from alembic.config import Config
from alembic.script import ScriptDirectory
from alembic.runtime.migration import MigrationContext
from sqlalchemy import create_engine, pool
import logging
import os
```

Handle the case where alembic.ini path needs resolution relative to project root. Use `os.path.join(os.path.dirname(__file__), "..", "..", "..", "alembic.ini")` to find alembic.ini from the scripts directory. Normalize with os.path.abspath.

Wrap the entire function body in try/except to prevent migration check failures from crashing the daily refresh pipeline.
  </action>
  <verify>
`python -c "from ta_lab2.scripts.alembic_utils import check_migration_status; print('OK')"`
  </verify>
  <done>
alembic_utils.py provides check_migration_status that returns True/False and logs warnings when behind head. Handles errors gracefully without crashing.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire migration check into run_daily_refresh.py</name>
  <files>src/ta_lab2/scripts/run_daily_refresh.py</files>
  <action>
Modify `run_daily_refresh.py` to check migration status at startup:

1. Add import: `from ta_lab2.scripts.alembic_utils import check_migration_status`

2. In the main execution flow (after argument parsing, before any refresh steps), add:
   ```python
   # Check Alembic migration status (advisory, non-blocking)
   if not args.dry_run:
       db_url = resolve_db_url()
       if not check_migration_status(db_url):
           logger.warning(
               "[MIGRATION] Pending migrations detected. "
               "Run 'alembic upgrade head' before next refresh."
           )
   ```

   Place this AFTER `resolve_db_url()` is available but BEFORE any refresh steps run. Look for where the script starts executing steps (likely after argument parsing and ID resolution).

3. The check must NOT block execution. It warns and continues. This matches the RESEARCH recommendation: "The startup check should WARN and print instructions, not auto-upgrade."

4. Skip the check in dry-run mode (no DB connection needed for dry run).

IMPORTANT: Do NOT modify any other behavior of run_daily_refresh.py. Only add the migration check call.
  </action>
  <verify>
`python -m ta_lab2.scripts.run_daily_refresh --dry-run --all --ids 1` still works (no migration check in dry-run).
`python -c "from ta_lab2.scripts.run_daily_refresh import main; print('import OK')"` -- verify import doesn't crash.
  </verify>
  <done>
run_daily_refresh.py checks Alembic migration status on startup. Warns if behind head. Does not block execution. Skipped in dry-run mode.
  </done>
</task>

</tasks>

<verification>
- `python -c "from ta_lab2.scripts.alembic_utils import is_alembic_head, check_migration_status"` imports cleanly
- check_migration_status returns True when DB is at Alembic head
- check_migration_status returns False and logs WARNING when DB is behind
- run_daily_refresh.py --dry-run still works without DB connection
- Migration check does not crash if alembic.ini is missing or DB is unreachable
</verification>

<success_criteria>
- Migration check integrated into daily refresh startup
- Advisory-only (warn, never auto-upgrade)
- Graceful error handling (try/except around entire check)
- NullPool used for DB connection
</success_criteria>

<output>
After completion, create `.planning/phases/36-psr-purged-k-fold/36-05-SUMMARY.md`
</output>
