---
phase: 86-portfolio-construction-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - alembic/versions/l6m7n8o9p0q1_phase86_portfolio_pipeline.py
  - src/ta_lab2/analysis/stop_calibration.py
  - src/ta_lab2/scripts/portfolio/calibrate_stops.py
  - src/ta_lab2/portfolio/stop_ladder.py
autonomous: true

must_haves:
  truths:
    - "stop_calibrations table exists with PK (id, strategy) and columns sl_p25/sl_p50/sl_p75/tp_p50/tp_p75/n_trades/calibrated_at"
    - "dim_executor_config has target_annual_vol NUMERIC column with CHECK > 0 when not NULL"
    - "calibrate_stops.py CLI reads MAE/MFE from backtest_trades and writes percentile-based stop levels to stop_calibrations"
    - "StopLadder.from_db_calibrations() classmethod seeds per_asset_overrides from stop_calibrations table"
    - "Assets with fewer than 30 trades get no calibration row (global defaults apply)"
  artifacts:
    - path: "alembic/versions/l6m7n8o9p0q1_phase86_portfolio_pipeline.py"
      provides: "Alembic migration creating stop_calibrations table and adding target_annual_vol to dim_executor_config"
      contains: "stop_calibrations"
    - path: "src/ta_lab2/analysis/stop_calibration.py"
      provides: "MAE/MFE percentile stop level computation"
      exports: ["calibrate_stops_from_mae_mfe", "MIN_TRADES_FOR_CALIBRATION"]
    - path: "src/ta_lab2/scripts/portfolio/calibrate_stops.py"
      provides: "CLI script to run stop calibration and persist to DB"
    - path: "src/ta_lab2/portfolio/stop_ladder.py"
      provides: "Extended StopLadder with from_db_calibrations classmethod"
      contains: "from_db_calibrations"
  key_links:
    - from: "src/ta_lab2/analysis/stop_calibration.py"
      to: "backtest_trades"
      via: "SQL JOIN backtest_runs on run_id"
      pattern: "backtest_trades.*backtest_runs"
    - from: "src/ta_lab2/scripts/portfolio/calibrate_stops.py"
      to: "src/ta_lab2/analysis/stop_calibration.py"
      via: "import calibrate_stops_from_mae_mfe"
      pattern: "from ta_lab2\\.analysis\\.stop_calibration import"
    - from: "src/ta_lab2/portfolio/stop_ladder.py"
      to: "stop_calibrations"
      via: "SQL query in from_db_calibrations"
      pattern: "stop_calibrations"
---

<objective>
Create the database schema extensions and MAE/MFE stop calibration pipeline for Phase 86.

Purpose: Provides the infrastructure for data-driven stop levels (replacing static YAML config) and the target_annual_vol column needed by Plan 02's GARCH sizing mode. The stop calibration pipeline reads bake-off trade MAE/MFE data and computes percentile-based stop/TP levels per (asset, strategy) pair.

Output: Alembic migration, stop calibration analysis module, CLI script, and StopLadder DB-seeding classmethod.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/86-portfolio-construction-pipeline/86-RESEARCH.md
@.planning/phases/86-portfolio-construction-pipeline/86-CONTEXT.md
@src/ta_lab2/portfolio/stop_ladder.py
@src/ta_lab2/analysis/mae_mfe.py
@alembic/versions/k5l6m7n8o9p0_ctf_state.py
@configs/portfolio.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Alembic migration + stop calibration analysis module</name>
  <files>
    alembic/versions/l6m7n8o9p0q1_phase86_portfolio_pipeline.py
    src/ta_lab2/analysis/stop_calibration.py
  </files>
  <action>
    **Alembic migration** (`l6m7n8o9p0q1_phase86_portfolio_pipeline.py`):
    - down_revision = "k5l6m7n8o9p0" (the ctf_state migration, current head)
    - revision = "l6m7n8o9p0q1"
    - Creates `stop_calibrations` table:
      ```sql
      CREATE TABLE public.stop_calibrations (
          id              INTEGER NOT NULL,
          strategy        TEXT    NOT NULL,
          sl_p25          NUMERIC,
          sl_p50          NUMERIC,
          sl_p75          NUMERIC,
          tp_p50          NUMERIC,
          tp_p75          NUMERIC,
          n_trades        INTEGER,
          calibrated_at   TIMESTAMPTZ DEFAULT now(),
          PRIMARY KEY (id, strategy)
      );
      ```
    - Adds column to dim_executor_config:
      ```sql
      ALTER TABLE public.dim_executor_config
        ADD COLUMN target_annual_vol NUMERIC DEFAULT NULL;
      ALTER TABLE public.dim_executor_config
        ADD CONSTRAINT chk_target_annual_vol_positive
        CHECK (target_annual_vol IS NULL OR target_annual_vol > 0);
      ```
    - downgrade: DROP TABLE stop_calibrations, DROP COLUMN target_annual_vol (and the CHECK constraint)

    **Stop calibration module** (`src/ta_lab2/analysis/stop_calibration.py`):
    - `MIN_TRADES_FOR_CALIBRATION = 30` constant
    - `calibrate_stops_from_mae_mfe(engine, asset_id, strategy, signal_id) -> dict | None`:
      - Queries `backtest_trades bt JOIN backtest_runs br ON bt.run_id = br.run_id`
      - WHERE `br.asset_id = :asset_id AND br.signal_id = :signal_id AND bt.mae IS NOT NULL AND bt.mfe IS NOT NULL`
      - If fewer than MIN_TRADES_FOR_CALIBRATION rows, return None
      - Compute: sl_p25 = np.nanpercentile(abs(mae), 25), sl_p50 = 50th, sl_p75 = 75th
      - Compute: tp_p50 = np.nanpercentile(mfe, 50), tp_p75 = 75th
      - Return dict with keys: sl_p25, sl_p50, sl_p75, tp_p50, tp_p75, n_trades
    - `persist_calibrations(engine, calibrations: list[dict]) -> int`:
      - Upsert to stop_calibrations using ON CONFLICT (id, strategy) DO UPDATE
      - Each dict has: id, strategy, sl_p25..tp_p75, n_trades
      - Returns count of rows written
    - Follow project patterns: `from __future__ import annotations`, logging, sqlalchemy text(), NullPool for engines
    - Use numpy for percentiles (`np.nanpercentile`)
    - ASCII-only file header comment (no box-drawing chars)
  </action>
  <verify>
    - `python -c "from ta_lab2.analysis.stop_calibration import calibrate_stops_from_mae_mfe, MIN_TRADES_FOR_CALIBRATION, persist_calibrations; print('OK')"` succeeds
    - `cd alembic && python -c "import versions.l6m7n8o9p0q1_phase86_portfolio_pipeline"` succeeds (or verify via `alembic history` if DB is available)
    - `ruff check src/ta_lab2/analysis/stop_calibration.py alembic/versions/l6m7n8o9p0q1_phase86_portfolio_pipeline.py` passes
  </verify>
  <done>
    stop_calibrations table schema defined in migration. dim_executor_config gains target_annual_vol column. stop_calibration.py exports calibrate_stops_from_mae_mfe with MIN_TRADES_FOR_CALIBRATION=30 gate and persist_calibrations for DB upsert.
  </done>
</task>

<task type="auto">
  <name>Task 2: calibrate_stops CLI script + StopLadder.from_db_calibrations()</name>
  <files>
    src/ta_lab2/scripts/portfolio/calibrate_stops.py
    src/ta_lab2/portfolio/stop_ladder.py
  </files>
  <action>
    **CLI script** (`src/ta_lab2/scripts/portfolio/calibrate_stops.py`):
    - Follows project CLI pattern (argparse, _resolve_db_url from refresh_utils or inline, NullPool engine, _setup_logging)
    - `--ids` (all or comma-separated), `--tf` (default 1D), `--dry-run`, `--db-url`, `--verbose`
    - Main logic:
      1. Load all distinct (asset_id, signal_id, strategy) combinations from `backtest_runs` (or `strategy_bakeoff_results` if backtest_runs is sparse -- check both)
      2. For each combination, call `calibrate_stops_from_mae_mfe(engine, asset_id, strategy, signal_id)`
      3. Collect all non-None results into a list
      4. Call `persist_calibrations(engine, calibrations)` (skip in dry-run mode)
      5. Print summary: total combinations checked, calibrations written, skipped (< 30 trades)
    - Usage: `python -m ta_lab2.scripts.portfolio.calibrate_stops --ids all --dry-run`
    - CRITICAL: Use `from ta_lab2.scripts.refresh_utils import resolve_db_url` if available, otherwise inline the resolution

    **StopLadder extension** (modify `src/ta_lab2/portfolio/stop_ladder.py`):
    - Add classmethod `from_db_calibrations(cls, engine, config: dict | None = None) -> StopLadder`:
      1. Construct a StopLadder instance normally: `ladder = cls(config=config)`
      2. Query all rows from `stop_calibrations` table
      3. For each row, build per_asset_overrides key as `f"{row.id}:{row.strategy}"` (most-specific combined key format)
      4. Map calibration percentiles to stop ladder format:
         - sl_stops = [sl_p25, sl_p50, sl_p75] (tight to wide)
         - tp_stops = [tp_p50, tp_p75] (conservative to aggressive -- 2 tiers, not 3, because we only have p50/p75)
         - sl_sizes = [0.33, 0.33, 0.34] (equal weight across 3 SL tiers)
         - tp_sizes = [0.50, 0.50] (equal weight across 2 TP tiers)
      5. Validate tiers and set into `ladder._per_asset[combined_key]`
      6. Log count of overrides loaded
      7. Return ladder
    - Import sqlalchemy.text at the top of the classmethod (or use existing import if present)
    - Keep the existing __init__ and all other methods unchanged
    - Add `from sqlalchemy import text` import (or guard in the method body to avoid import at module level if preferred -- follow the pattern used by other portfolio modules)
  </action>
  <verify>
    - `python -c "from ta_lab2.portfolio.stop_ladder import StopLadder; print(hasattr(StopLadder, 'from_db_calibrations'))"` prints True
    - `python -c "from ta_lab2.scripts.portfolio.calibrate_stops import main; print('OK')"` succeeds
    - `ruff check src/ta_lab2/scripts/portfolio/calibrate_stops.py src/ta_lab2/portfolio/stop_ladder.py` passes
  </verify>
  <done>
    calibrate_stops.py CLI reads backtest_trades MAE/MFE, computes percentile stops, and writes to stop_calibrations table. StopLadder.from_db_calibrations() seeds per-asset-strategy overrides from the DB at runtime, overriding static YAML defaults for calibrated assets.
  </done>
</task>

</tasks>

<verification>
- Alembic migration file imports cleanly and has correct down_revision (k5l6m7n8o9p0)
- stop_calibration.py exports are importable
- calibrate_stops.py CLI is importable and parseable
- StopLadder.from_db_calibrations classmethod exists
- All files pass ruff check
</verification>

<success_criteria>
- stop_calibrations table defined in migration with PK (id, strategy)
- dim_executor_config.target_annual_vol column defined with CHECK constraint
- calibrate_stops_from_mae_mfe returns None for assets with < 30 trades
- calibrate_stops.py CLI can be invoked with --dry-run
- StopLadder.from_db_calibrations() loads DB calibrations into per_asset_overrides using "{id}:{strategy}" key format
</success_criteria>

<output>
After completion, create `.planning/phases/86-portfolio-construction-pipeline/86-01-SUMMARY.md`
</output>
