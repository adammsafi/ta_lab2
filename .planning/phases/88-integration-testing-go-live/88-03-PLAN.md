---
phase: 88-integration-testing-go-live
plan: 03
type: execute
wave: 2
depends_on: ["88-01", "88-02"]
files_modified:
  - docs/guides/operations/02_daily_pipeline.md
  - docs/guides/operations/04_paper_trading_and_risk.md
  - docs/guides/operations/07_path_to_production.md
  - docs/CHANGELOG.md
  - .planning/milestones/v1.2.0-REQUIREMENTS.md
autonomous: true

must_haves:
  truths:
    - "Part 2 (daily pipeline) documents the 21-stage pipeline including GARCH, stop calibration, portfolio allocation stages"
    - "Part 4 (paper trading) documents parity check --pnl-correlation-threshold flag and burn-in protocol"
    - "Part 7 (path to production) documents burn-in protocol with daily report command and success/stop criteria"
    - "CHANGELOG.md has a v1.2.0 section with all new features from Phases 80-88"
    - "v1.2.0-REQUIREMENTS.md exists and lists all success criteria for milestone audit"
  artifacts:
    - path: "docs/guides/operations/02_daily_pipeline.md"
      provides: "Updated daily pipeline documentation covering v1.2.0 stages"
      contains: "garch"
    - path: "docs/guides/operations/04_paper_trading_and_risk.md"
      provides: "Updated paper trading docs with parity threshold + burn-in"
      contains: "pnl-correlation-threshold"
    - path: "docs/guides/operations/07_path_to_production.md"
      provides: "Updated production path with burn-in protocol"
      contains: "burn-in"
    - path: "docs/CHANGELOG.md"
      provides: "v1.2.0 changelog entry"
      contains: "[1.2.0]"
    - path: ".planning/milestones/v1.2.0-REQUIREMENTS.md"
      provides: "v1.2.0 milestone requirements for audit"
      contains: "v1.2.0"
  key_links:
    - from: "docs/guides/operations/04_paper_trading_and_risk.md"
      to: "src/ta_lab2/scripts/executor/run_parity_check.py"
      via: "documents CLI usage"
      pattern: "pnl-correlation-threshold"
    - from: "docs/guides/operations/07_path_to_production.md"
      to: "src/ta_lab2/scripts/integration/smoke_test.py"
      via: "documents CLI usage"
      pattern: "smoke_test"
---

<objective>
Update operations manual for v1.2.0 pipeline, create CHANGELOG entry, and prepare v1.2.0 milestone requirements document.

Purpose: The runbooks must reflect the v1.2.0 pipeline (GARCH, stop calibration, portfolio allocation, new dashboard pages, parity check extension, burn-in protocol) so a future operator can run the system without the original author. The requirements doc enables the milestone audit that gates the v1.2.0 tag.

Output: Updated Parts 2/4/7 of the operations manual, v1.2.0 CHANGELOG entry, v1.2.0-REQUIREMENTS.md.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/88-integration-testing-go-live/88-CONTEXT.md
@.planning/phases/88-integration-testing-go-live/88-RESEARCH.md

# Plans 01-02 summaries (these will exist by Wave 2)
@.planning/phases/88-integration-testing-go-live/88-01-SUMMARY.md
@.planning/phases/88-integration-testing-go-live/88-02-SUMMARY.md

# Existing docs to update
@docs/guides/operations/02_daily_pipeline.md
@docs/guides/operations/04_paper_trading_and_risk.md
@docs/guides/operations/07_path_to_production.md
@docs/CHANGELOG.md

# Prior milestone requirements for format reference
@.planning/milestones/v1.1.0-REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update operations manual Parts 2, 4, 7 for v1.2.0</name>
  <files>
    docs/guides/operations/02_daily_pipeline.md
    docs/guides/operations/04_paper_trading_and_risk.md
    docs/guides/operations/07_path_to_production.md
  </files>
  <action>
Read each file fully before modifying. Make INCREMENTAL additions -- do not rewrite existing content. Add v1.2.0-specific sections alongside existing material.

**Part 2 (02_daily_pipeline.md) additions:**

1. Update the pipeline DAG diagram (or add a "v1.2.0 Extended Pipeline" section) to reflect the 21-stage order from `run_daily_refresh.py --all`:
   - Add stages 15-18 that are new in v1.2.0: garch, calibrate_stops, portfolio, executor
   - Mention stages 17-18 only run with `--paper-start`
   - Document `--continue-on-error` flag behavior

2. Add a subsection for each new v1.2.0 stage:
   - **GARCH Forecasts** (`scripts/garch/refresh_garch_forecasts.py`): What it does (fits 4 GARCH variants per asset, writes to garch_forecasts + garch_diagnostics), timeout (30 min), key flags (--continue-on-error makes it non-fatal), tables written, troubleshooting (convergence failures logged, carry-forward fallback)
   - **Stop Calibration** (`scripts/portfolio/calibrate_stops.py`): MAE/MFE-based stop ladder calibration, writes to stop_calibrations, timeout 5 min, uses bakeoff winners
   - **Portfolio Allocation** (`scripts/portfolio/refresh_portfolio_allocations.py`): Black-Litterman MV/CVaR/HRP weights, writes to portfolio_allocations, requires ic_results data

3. Add note about `--paper-start DATE` being REQUIRED for drift stage to run

**Part 4 (04_paper_trading_and_risk.md) additions:**

1. Add a "Parity Verification" subsection (or update existing):
   - Document `--pnl-correlation-threshold` flag: what it does, default (0.99), burn-in usage (0.90)
   - Document `--bakeoff-winners` mode: auto-discovery from strategy_bakeoff_results
   - Document the KNOWN data gap: bakeoff results are in strategy_bakeoff_results, not backtest_trades. Parity checker will warn "No backtest trades found" -- this is expected, not a bug.
   - Full command example:
     ```bash
     python -m ta_lab2.scripts.executor.run_parity_check \
         --bakeoff-winners --start 2025-01-01 --end 2025-12-31 \
         --slippage-mode fixed --pnl-correlation-threshold 0.90
     ```

2. Add a "Signal Anomaly Gate" subsection documenting the Phase 87 pre-execution signal validation gate

**Part 7 (07_path_to_production.md) additions:**

1. Add a "v1.2.0 Burn-In Protocol" section with:
   - Purpose: 7-day sustained paper trading to validate full pipeline before live
   - Daily command:
     ```bash
     python -m ta_lab2.scripts.run_daily_refresh \
         --all --ids all --paper-start YYYY-MM-DD --continue-on-error
     ```
   - Daily report command:
     ```bash
     python -m ta_lab2.scripts.integration.daily_burn_in_report \
         --burn-in-start YYYY-MM-DD
     ```
   - Smoke test command (pre-burn-in):
     ```bash
     python -m ta_lab2.scripts.integration.smoke_test
     ```
   - Success criteria: No kill switch triggers, no drift pauses, PnL not catastrophically negative (not -20% or worse), pipeline ran every day
   - Early stop criteria: ONLY if kill switch or drift pause fires
   - Monitoring: Dashboard for deep dives, daily_burn_in_report for status, Telegram alerts for critical events

2. Update Gate 1 (Research -> Paper) criteria to mention v1.2.0 additions:
   - Smoke test passing
   - Parity check with r >= 0.90 threshold
   - GARCH forecasts generating without persistent convergence failures

Keep all additions in the same tone and formatting style as existing content. Use the same heading levels, code block style, and tip/warning callout patterns already present in the files.
  </action>
  <verify>
1. Each file still has valid markdown structure (no broken headers, lists, code blocks)
2. Grep for "garch" in Part 2 confirms GARCH documentation added
3. Grep for "pnl-correlation-threshold" in Part 4 confirms parity threshold documented
4. Grep for "burn-in" in Part 7 confirms burn-in protocol documented
5. Grep for "smoke_test" in Part 7 confirms smoke test documented
  </verify>
  <done>
Parts 2, 4, 7 of operations manual updated with v1.2.0 pipeline stages, parity threshold flag, burn-in protocol, and smoke test commands. Existing content preserved. New sections match existing tone and formatting.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create CHANGELOG entry and v1.2.0 requirements document</name>
  <files>
    docs/CHANGELOG.md
    .planning/milestones/v1.2.0-REQUIREMENTS.md
  </files>
  <action>
**CHANGELOG.md:**

Read the full file first. Add a `## [1.2.0] - Unreleased` section between `## [Unreleased]` and `## [1.0.0]` (or update `## [Unreleased]` to become `## [1.2.0] - Unreleased`). Follow the Keep a Changelog format already used in the file.

Content (derive from roadmap phases 80-92):

### Added
- **Feature Selection (Phase 80)**: IC-IR analysis of 107 features, 20 promoted to active tier, AMA dominance finding, per-asset IC variation analysis
- **GARCH Volatility (Phase 81)**: 4-variant GARCH engine (GARCH/EGARCH/GJR-GJRGARCH/FIGARCH), blend forecasting, carry-forward fallback, `garch_forecasts` + `garch_diagnostics` tables
- **Strategy Bake-Off (Phase 82)**: Multi-strategy evaluation with AMA features, per-asset IC weighting, Hyperliquid cost scenarios, CPCV/PKF cross-validation
- **Dashboard Research Pages (Phase 83)**: Strategy-first view, signal monitor, asset hub with candlestick charts, sidebar reorganization
- **Dashboard Data Pages (Phase 84)**: Hyperliquid perps page, AMA inspector, regime dashboard, IC results landing
- **Dashboard Operations Pages (Phase 85)**: Stats monitoring, drawdown tracking, operational health
- **Portfolio Pipeline (Phase 86)**: Stop calibration (MAE/MFE), Black-Litterman portfolio allocation, parity check --bakeoff-winners mode
- **Pipeline Alert Wiring (Phase 87)**: IC staleness monitor, signal anomaly gate, pipeline_run_log dead-man switch, Telegram alert integration
- **Integration Testing (Phase 88)**: End-to-end smoke test CLI, daily burn-in report, parity check --pnl-correlation-threshold extension
- **CTF Schema (Phase 89)**: Cross-timeframe feature dimension table, fact table, YAML config
- **CTF Computation (Phase 90)**: Slope, divergence, z-score, agreement composites across timeframes
- **CTF Pipeline (Phase 91)**: Incremental refresh with state tracking, CLI interface, pipeline integration
- **CTF Feature Selection (Phase 92)**: IC sweep for CTF features, pruning, comparison with AMA features

### Changed
- Daily refresh pipeline expanded from 15 to 21 stages (added GARCH, stop calibration, portfolio, executor, drift stages)
- Parity checker now accepts configurable --pnl-correlation-threshold (default 0.99, burn-in uses 0.90)
- Operations manual updated for v1.2.0 pipeline (Parts 2, 4, 7)
- Dashboard reorganized: Analysis (Research + Markets) + Operations groups

**v1.2.0-REQUIREMENTS.md:**

Read `.planning/milestones/v1.1.0-REQUIREMENTS.md` for format reference. Create v1.2.0-REQUIREMENTS.md following the same structure.

Content should include requirements derived from the v1.2.0 roadmap success criteria. Group by phase range:

Feature Selection & GARCH (Phases 80-81):
- REQ-01: dim_feature_selection has 15-25 active features with IC-IR >= 1.0
- REQ-02: garch_forecasts table populated for all tracked assets
- REQ-03: garch_forecasts_latest materialized view refreshable

Strategy & Portfolio (Phases 82, 86):
- REQ-04: strategy_bakeoff_results populated with CPCV results
- REQ-05: stop_calibrations table populated from MAE/MFE analysis
- REQ-06: portfolio_allocations table populated with MV weights

Dashboard (Phases 83-85):
- REQ-07: All dashboard pages load without error
- REQ-08: Strategy-first, signal monitor, asset hub pages functional
- REQ-09: Hyperliquid perps, AMA inspector pages functional

Pipeline & Alerts (Phase 87):
- REQ-10: pipeline_run_log populated after daily refresh
- REQ-11: signal_anomaly_log populated by anomaly gate
- REQ-12: IC staleness monitor runs without error

CTF Infrastructure (Phases 89-92):
- REQ-13: ctf table has computed features
- REQ-14: dim_ctf_feature_selection has active/watch/archive tiers

Integration & Go-Live (Phase 88):
- REQ-15: Smoke test passes (exit 0) on current data
- REQ-16: Parity check --bakeoff-winners --pnl-correlation-threshold 0.90 runs
- REQ-17: Daily burn-in report generates and displays status
- REQ-18: Operations manual Parts 2, 4, 7 updated for v1.2.0
- REQ-19: CHANGELOG.md has v1.2.0 entry

Each requirement should have: ID, description, verification command or query, and phase source.
  </action>
  <verify>
1. CHANGELOG.md has a `[1.2.0]` section header
2. CHANGELOG.md still has existing `[1.0.0]` section (not overwritten)
3. v1.2.0-REQUIREMENTS.md exists in .planning/milestones/
4. v1.2.0-REQUIREMENTS.md has REQ-01 through REQ-19
5. `ruff check` on any Python files is N/A (these are markdown files)
  </verify>
  <done>
CHANGELOG.md has v1.2.0 entry with Added/Changed sections covering all Phases 80-92. v1.2.0-REQUIREMENTS.md exists with 19 requirements grouped by phase range, each with verification method. Existing CHANGELOG content preserved.
  </done>
</task>

</tasks>

<verification>
1. Parts 2, 4, 7 of operations manual contain v1.2.0 additions
2. CHANGELOG.md has v1.2.0 section with all phases documented
3. v1.2.0-REQUIREMENTS.md has 19 requirements with verification methods
4. No existing content in any file was deleted or overwritten
5. All markdown files have valid structure
</verification>

<success_criteria>
- Operations manual documents GARCH, stop calibration, portfolio allocation, parity threshold, burn-in protocol, smoke test
- CHANGELOG has v1.2.0 entry covering Phases 80-92
- v1.2.0-REQUIREMENTS.md has 19 requirements ready for milestone audit
- Existing content in all files preserved
</success_criteria>

<output>
After completion, create `.planning/phases/88-integration-testing-go-live/88-03-SUMMARY.md`
</output>
