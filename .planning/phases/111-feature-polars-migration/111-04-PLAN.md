---
phase: 111-feature-polars-migration
plan: 04
type: execute
wave: 4
depends_on: ["111-03"]
files_modified:
  - src/ta_lab2/scripts/features/microstructure_feature.py
  - src/ta_lab2/scripts/features/daily_features_view.py
  - src/ta_lab2/scripts/features/run_all_feature_refreshes.py
autonomous: true

must_haves:
  truths:
    - "microstructure sub-phase with use_polars=True produces identical output to pandas path"
    - "unified features assembly (daily_features_view) unchanged -- it is pure SQL, no migration needed"
    - "run_all_feature_refreshes.py accepts --use-polars flag and passes it to all sub-phases"
  artifacts:
    - path: "src/ta_lab2/scripts/features/microstructure_feature.py"
      provides: "MicrostructureFeature with polars outer loop"
      contains: "use_polars"
    - path: "src/ta_lab2/scripts/features/run_all_feature_refreshes.py"
      provides: "CLI --use-polars flag for orchestrator"
      contains: "--use-polars"
  key_links:
    - from: "src/ta_lab2/scripts/features/run_all_feature_refreshes.py"
      to: "src/ta_lab2/scripts/features/vol_feature.py"
      via: "passes use_polars config"
      pattern: "use_polars"
    - from: "src/ta_lab2/scripts/features/run_all_feature_refreshes.py"
      to: "src/ta_lab2/scripts/features/ta_feature.py"
      via: "passes use_polars config"
      pattern: "use_polars"
    - from: "src/ta_lab2/scripts/features/microstructure_feature.py"
      to: "src/ta_lab2/features/polars_feature_ops.py"
      via: "import polars_sorted_groupby"
      pattern: "from ta_lab2.features.polars_feature_ops import"
---

<objective>
Migrate microstructure sub-phase to polars + wire --use-polars flag into orchestrator.

Purpose: Microstructure sub-phase uses numba/numpy kernels internally (find_min_d, frac_diff_ffd, kyle_lambda, etc.), so the polars value is marginal -- only the outer per-asset groupby loop benefits from polars-based sorting. Research noted this but it should still be migrated for consistency. The unified features assembly (daily_features_view.py) is pure SQL (INSERT INTO...SELECT...JOIN) and needs no migration -- just document as no-op. The orchestrator (run_all_feature_refreshes.py) needs a --use-polars CLI flag that propagates to all sub-phase configs.

Output:
- MicrostructureFeature polars outer loop
- --use-polars flag in orchestrator CLI
- daily_features_view confirmed as no-op (SQL assembly)
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/111-feature-polars-migration/111-CONTEXT.md
@.planning/phases/111-feature-polars-migration/111-RESEARCH.md
@.planning/phases/111-feature-polars-migration/111-01-SUMMARY.md
@.planning/phases/111-feature-polars-migration/111-02-SUMMARY.md
@.planning/phases/111-feature-polars-migration/111-03-SUMMARY.md

# Key source files
@src/ta_lab2/scripts/features/microstructure_feature.py
@src/ta_lab2/scripts/features/daily_features_view.py
@src/ta_lab2/scripts/features/run_all_feature_refreshes.py
@src/ta_lab2/features/polars_feature_ops.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate microstructure outer loop + document unified assembly no-op</name>
  <files>
    src/ta_lab2/scripts/features/microstructure_feature.py
  </files>
  <action>
  Modify MicrostructureFeature.compute_features() to add a polars path when self.config.use_polars is True.

  When use_polars=True:
  1. Import: `from ta_lab2.features.polars_feature_ops import polars_sorted_groupby`
  2. Extract the existing per-group computation into `_compute_micro_single_group(df_id: pd.DataFrame) -> pd.DataFrame`:
     This function contains the ENTIRE body of the current inner loop for a single (id, venue_id) group:
     - Sort by ts, extract close/volume numpy arrays
     - Guard n < 30
     - MICRO-01: find_min_d + frac_diff_ffd
     - MICRO-02: kyle_lambda, amihud_lambda, hasbrouck_lambda
     - MICRO-03: rolling_adf
     - MICRO-04: rolling_entropy
     - Return df_id with all columns added
     Move the guard check inside: if n < 30, return empty DataFrame.
  3. Call: `df_features = polars_sorted_groupby(df, ["id", "venue_id"], "ts", _compute_micro_single_group)`
  4. The numpy/numba kernels inside _compute_micro_single_group remain UNCHANGED -- they operate on numpy arrays extracted from the pandas group. Polars only does the outer sort.

  When use_polars=False: Keep existing pandas groupby loop EXACTLY as-is.

  After the branch: add tf metadata as before.

  Also modify the CLI main() function to accept --use-polars flag:
  ```python
  parser.add_argument("--use-polars", action="store_true", default=False,
                       help="Use polars for outer groupby loop (faster sort)")
  ```
  Pass to MicrostructureConfig: `config = MicrostructureConfig(tf=args.tf, ..., use_polars=args.use_polars)`

  daily_features_view.py: NO CHANGES NEEDED. It is pure SQL (INSERT INTO...SELECT...JOIN from price_bars, returns, vol, ta tables). Document in the summary that this is confirmed as a no-op for polars migration.
  </action>
  <verify>
    - `python -c "
from ta_lab2.scripts.features.microstructure_feature import MicrostructureFeature, MicrostructureConfig
c = MicrostructureConfig(use_polars=True)
assert c.use_polars is True
print('MicrostructureConfig use_polars OK')
"`
    - `python -m ta_lab2.scripts.features.microstructure_feature --help` shows --use-polars flag
    - ruff check passes on microstructure_feature.py
    - If DB available: run regression comparison for id=1, tf=1D. All microstructure columns max diff = 0.0 (numba kernels are deterministic; only the sort order changed)
  </verify>
  <done>
    MicrostructureFeature supports use_polars=True. Numba kernels unchanged. CLI has --use-polars flag. daily_features_view.py confirmed as no-op (pure SQL).
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire --use-polars into run_all_feature_refreshes orchestrator</name>
  <files>
    src/ta_lab2/scripts/features/run_all_feature_refreshes.py
  </files>
  <action>
  Modify run_all_feature_refreshes.py to accept and propagate the --use-polars flag.

  1. Add CLI argument to the argparse parser:
     ```python
     parser.add_argument("--use-polars", action="store_true", default=False,
                          help="Use polars acceleration for feature computation (faster sort + rolling ops)")
     ```

  2. In the refresh functions that create VolatilityConfig, TAConfig, etc., pass `use_polars=args.use_polars`:
     - Find where VolatilityConfig is instantiated and add use_polars
     - Find where TAConfig is instantiated and add use_polars
     - Find where MicrostructureConfig is instantiated and add use_polars
     - CycleStatsConfig and RollingExtremesConfig similarly

  3. The orchestrator creates configs and passes them to feature classes. Trace through the code to find EVERY place a FeatureConfig subclass is created and ensure use_polars is propagated. This may involve:
     - Direct config creation in refresh functions
     - Config creation in helper/wrapper functions
     - The sequential/parallel execution paths must both propagate the flag

  4. Add a log line at startup: `logger.info(f"Polars acceleration: {'enabled' if args.use_polars else 'disabled'}")`

  5. CS norms (refresh_cs_norms) and unified features (daily_features_view) do NOT need the flag -- they are pure SQL. Skip them when propagating.

  6. CTF (refresh_ctf) will be handled in Plan 05 -- do NOT add use_polars to CTF in this plan.
  </action>
  <verify>
    - `python -m ta_lab2.scripts.features.run_all_feature_refreshes --help` shows --use-polars flag
    - ruff check passes on run_all_feature_refreshes.py
    - Verify: `python -c "
import ast, sys
with open('src/ta_lab2/scripts/features/run_all_feature_refreshes.py') as f:
    src = f.read()
assert 'use_polars' in src, 'use_polars not found in orchestrator'
print('use_polars found in orchestrator')
"`
  </verify>
  <done>
    run_all_feature_refreshes.py accepts --use-polars flag. Flag propagated to VolatilityConfig, TAConfig, MicrostructureConfig, CycleStatsConfig, RollingExtremesConfig. CS norms and unified assembly unaffected (pure SQL). CTF deferred to Plan 05.
  </done>
</task>

</tasks>

<verification>
- MicrostructureFeature use_polars=True produces identical output to pandas path
- run_all_feature_refreshes.py --use-polars flag accepted and propagated
- daily_features_view confirmed as no-op (SQL assembly)
- All modified files pass ruff check
</verification>

<success_criteria>
- Microstructure polars path: max absolute difference = 0.0 vs pandas path (numba kernels are deterministic)
- Orchestrator --use-polars flag works and propagates to all computation sub-phases
- 6 of 8 sub-phases migrated (cycle_stats, rolling_extremes, vol, ta, microstructure, unified=no-op)
- CS norms confirmed as no-op
</success_criteria>

<output>
After completion, create `.planning/phases/111-feature-polars-migration/111-04-SUMMARY.md`
</output>
