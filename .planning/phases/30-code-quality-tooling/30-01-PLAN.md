---
phase: 30-code-quality-tooling
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - src/ta_lab2/features/vol.py
  - src/ta_lab2/regimes/labels.py
  - src/ta_lab2/resample.py
  - src/ta_lab2/signals/breakout_atr.py
  - src/ta_lab2/scripts/research/queries/opt_cf_ema.py
  - src/ta_lab2/scripts/research/queries/opt_cf_ema_refine.py
  - src/ta_lab2/scripts/research/queries/opt_cf_generic.py
  - src/ta_lab2/tools/data_tools/context/ask_project.py
  - src/ta_lab2/tools/data_tools/memory/memory_bank_rest.py
  - src/ta_lab2/features/m_tf/polars_helpers.py
  - src/ta_lab2/tools/data_tools/export/extract_kept_chats_from_keepfile.py
autonomous: true

must_haves:
  truths:
    - "ruff check src/ exits 0 with zero violations"
    - "ruff format --check src/ exits 0 with no files needing reformatting"
    - "No E741, E722, F401, F841, E721, F821, F811, E401, or invalid-syntax violations remain"
  artifacts:
    - path: "pyproject.toml"
      provides: "[tool.ruff] global section with target-version = py312"
      contains: "target-version"
  key_links:
    - from: "pyproject.toml [tool.ruff]"
      to: "ruff check src/"
      via: "target-version = py312 eliminates 3 invalid-syntax violations"
      pattern: 'target-version = "py312"'
---

<objective>
Fix all ruff lint violations and reformat the entire codebase so that both `ruff check src/` and `ruff format --check src/` exit 0. This is the prerequisite for removing the `|| true` escape hatch in CI (done in Plan 30-02).

Purpose: Achieve the zero-violation state required before CI can enforce ruff as a hard gate.
Output: A codebase with zero ruff lint violations and consistent formatting across all 359 Python files.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-code-quality-tooling/30-CONTEXT.md
@.planning/phases/30-code-quality-tooling/30-RESEARCH.md
@pyproject.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add [tool.ruff] global section and fix all lint violations</name>
  <files>
    pyproject.toml
    src/ta_lab2/features/vol.py
    src/ta_lab2/regimes/labels.py
    src/ta_lab2/resample.py
    src/ta_lab2/signals/breakout_atr.py
    src/ta_lab2/scripts/research/queries/opt_cf_ema.py
    src/ta_lab2/scripts/research/queries/opt_cf_ema_refine.py
    src/ta_lab2/scripts/research/queries/opt_cf_generic.py
    src/ta_lab2/tools/data_tools/context/ask_project.py
    src/ta_lab2/tools/data_tools/memory/memory_bank_rest.py
    src/ta_lab2/features/m_tf/polars_helpers.py
    src/ta_lab2/tools/data_tools/export/extract_kept_chats_from_keepfile.py
  </files>
  <action>
    **Step 1 -- Add [tool.ruff] global section to pyproject.toml.**

    Insert a new `[tool.ruff]` section ABOVE the existing `[tool.ruff.lint]` section (currently at line 152). Add exactly:

    ```toml
    [tool.ruff]
    target-version = "py312"
    line-length = 88
    ```

    This eliminates 3 invalid-syntax violations (f-string backslash in daily_features_view.py, 2x except* in execution.py) by telling ruff the codebase targets Python 3.12.

    **Step 2 -- Run safe auto-fixes.**

    ```bash
    ruff check src/ --fix
    ```

    This fixes 6 violations: E401 x5 (multiple imports on one line in opt_cf_ema.py, opt_cf_ema_refine.py, opt_cf_ema_sensitivity.py, opt_cf_generic.py, old_run_btc_pipeline.py) and F811 x1 (redefined unused in dbtool.py).

    **Step 3 -- Run unsafe auto-fixes after reviewing the diff.**

    First preview: `ruff check src/ --fix --unsafe-fixes --diff`
    Then apply: `ruff check src/ --fix --unsafe-fixes`

    This fixes 9 F841 (unused variable) violations across splitters.py, trend.py, btc_pipeline.py, regime_inspect.py, conflict.py, ask_project export, finetuning_data_generator.py, all_plots.py (x2). Review the diff to confirm each deleted assignment is genuinely unused.

    **Step 4 -- Manual fix E741 violations (9 total): rename `l` to `lo`.**

    In each file below, rename the variable `l` (ambiguous -- looks like `1`) to `lo` (standard finance abbreviation for "low"). Rename ALL occurrences of the variable within each function/scope, not just the assignment.

    - `src/ta_lab2/features/vol.py` -- 3 violations (lines ~42, ~63, ~82). These are in volatility estimator functions (Garman-Klass, Rogers-Satchell, ATR). The pattern is `o, h, l, c = ...` -- rename to `o, h, lo, c = ...` and update all references to `l` within those functions to `lo`.
    - `src/ta_lab2/regimes/labels.py` -- 4 violations (lines ~138, ~166, ~194, ~220). These are lambda expressions or list comprehensions using `l` for low price. Read each usage context and rename `l` to `lo`.
    - `src/ta_lab2/resample.py` -- 1 violation (line ~37). Rename `l` to `lo`.
    - `src/ta_lab2/signals/breakout_atr.py` -- 1 violation (line ~35). Rename `l` to `lo`.

    **Step 5 -- Manual fix E722 violations (3 total): bare except to except Exception.**

    - `src/ta_lab2/scripts/research/queries/opt_cf_ema.py` -- change `except:` to `except Exception:`
    - `src/ta_lab2/scripts/research/queries/opt_cf_ema_refine.py` -- change `except:` to `except Exception:`
    - `src/ta_lab2/scripts/research/queries/opt_cf_generic.py` -- change `except:` to `except Exception:`

    **Step 6 -- Manual fix F401 violations (2 total): remove unused imports.**

    - `src/ta_lab2/tools/data_tools/context/ask_project.py` -- remove unused `ChatCompletionMessageParam` import
    - `src/ta_lab2/tools/data_tools/memory/memory_bank_rest.py` -- remove unused `requests` import

    **Step 7 -- Manual fix E721 (1 violation): fix type comparison.**

    - `src/ta_lab2/features/m_tf/polars_helpers.py` line 179 -- change `agg_func == list` to `agg_func is list`

    **Step 8 -- Manual fix F821 (1 violation): add missing import.**

    - `src/ta_lab2/tools/data_tools/export/extract_kept_chats_from_keepfile.py` line 81 -- add `from typing import Any` at the top of the file (or in the existing typing imports block)

    **Step 9 -- Verify zero violations.**

    ```bash
    ruff check src/
    ```

    This MUST exit 0 with "All checks passed!" or equivalent zero-violation output.
  </action>
  <verify>
    Run `ruff check src/` and confirm exit code 0 with zero violations.
    Run `ruff check src/ --statistics` and confirm empty output (no rule violations).
  </verify>
  <done>
    `ruff check src/` exits 0. All 34 original violations eliminated: 3 via target-version, 6 via safe auto-fix, 9 via unsafe auto-fix, 16 via manual fixes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Run ruff format on entire codebase</name>
  <files>106 files across src/ (automated by ruff format)</files>
  <action>
    **Step 1 -- Run ruff format on the entire src/ directory.**

    ```bash
    ruff format src/
    ```

    This will reformat 106 files (out of 359 total). The reformatting is purely cosmetic (whitespace, line breaks, quote style) and does not change any logic.

    **Step 2 -- Verify format check passes.**

    ```bash
    ruff format --check src/
    ```

    This MUST exit 0, confirming all files conform to ruff's formatting rules.

    **Step 3 -- Run tests to confirm nothing broke.**

    ```bash
    python -m pytest tests/ -q --tb=short 2>&1 | head -50
    ```

    Tests should pass. If any test fails, investigate whether the formatting change inadvertently broke something (extremely unlikely with ruff format, but verify).
  </action>
  <verify>
    Run `ruff format --check src/` and confirm exit code 0.
    Run `ruff check src/` again to confirm lint still passes after formatting.
  </verify>
  <done>
    `ruff format --check src/` exits 0. All 106 files reformatted. `ruff check src/` still exits 0 (no new violations introduced by formatting).
  </done>
</task>

</tasks>

<verification>
Run both commands in sequence and confirm both exit 0:

```bash
ruff check src/ && echo "LINT OK" || echo "LINT FAIL"
ruff format --check src/ && echo "FORMAT OK" || echo "FORMAT FAIL"
```

Both must print OK. This is the prerequisite state for Plan 30-02 to remove the CI escape hatch.
</verification>

<success_criteria>
1. `ruff check src/` exits 0 with zero violations
2. `ruff format --check src/` exits 0 with no files needing reformatting
3. pyproject.toml has `[tool.ruff]` section with `target-version = "py312"` and `line-length = 88`
4. All E741 variables renamed from `l` to `lo` (9 occurrences across 4 files)
5. All E722 bare excepts replaced with `except Exception:` (3 files)
6. Unused imports removed (2 files), type comparison fixed (1 file), missing import added (1 file)
</success_criteria>

<output>
After completion, create `.planning/phases/30-code-quality-tooling/30-01-SUMMARY.md`
</output>
