---
phase: 67-macro-regime-classifier
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - alembic/versions/XXXX_macro_regime_tables.py
autonomous: true

must_haves:
  truths:
    - "cmc_macro_regimes table exists with PK (date, profile) and columns for per-dimension labels, regime_key, macro_state"
    - "cmc_macro_hysteresis_state table exists with PK (profile, dimension) for persisting tracker state across runs"
    - "Alembic migration chains correctly from Phase 66 head (or Phase 65 head if Phase 66 not yet applied)"
  artifacts:
    - path: "alembic/versions/XXXX_macro_regime_tables.py"
      provides: "DDL for cmc_macro_regimes and cmc_macro_hysteresis_state"
      contains: "cmc_macro_regimes"
  key_links:
    - from: "alembic/versions/XXXX_macro_regime_tables.py"
      to: "cmc_macro_regimes"
      via: "op.create_table"
      pattern: "create_table.*cmc_macro_regimes"
---

<objective>
Create Alembic migration for the two Phase 67 tables: `cmc_macro_regimes` (daily macro regime labels with composite key and bucketed state) and `cmc_macro_hysteresis_state` (persistent hysteresis tracker state for incremental resume).

Purpose: Foundation tables must exist before the classifier module or CLI can write results. The migration also validates the Alembic chain by discovering the actual current head at runtime.
Output: One Alembic migration file with upgrade/downgrade for both tables.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/67-macro-regime-classifier/67-CONTEXT.md
@.planning/phases/67-macro-regime-classifier/67-RESEARCH.md

Key reference files:
@alembic/versions/a1b2c3d4e5f6_fred_macro_features.py (Phase 65 migration pattern)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Discover Alembic head and create migration</name>
  <files>alembic/versions/XXXX_macro_regime_tables.py</files>
  <action>
1. Run `alembic heads` to discover the current migration head. Phase 66 may or may not have added its migration yet. Use whatever the actual current head is as `down_revision`. Do NOT hardcode a planned Phase 66 revision ID -- use the confirmed head.

2. Generate the migration file. You can use `alembic revision --autogenerate -m "macro_regime_tables"` as a starting point, but the tables must be hand-written (not auto-detected) since they are new tables with no model.

3. The migration must create two tables in the PUBLIC schema (not fred schema):

**Table 1: `cmc_macro_regimes`**
- `date` DATE NOT NULL
- `profile` TEXT NOT NULL, server_default='default'
- `monetary_policy` TEXT nullable (dimension label: Hiking/Holding/Cutting)
- `liquidity` TEXT nullable (dimension label: Strongly_Expanding/Expanding/Neutral/Contracting/Strongly_Contracting)
- `risk_appetite` TEXT nullable (dimension label: RiskOff/Neutral/RiskOn)
- `carry` TEXT nullable (dimension label: Unwind/Stress/Stable)
- `regime_key` TEXT nullable (composite: e.g. 'Cutting-Expanding-RiskOn-Stable')
- `macro_state` TEXT nullable (bucketed: favorable/constructive/neutral/cautious/adverse)
- `regime_version_hash` TEXT nullable (config hash for provenance)
- `ingested_at` TIMESTAMP WITH TIME ZONE NOT NULL, server_default=now()
- PK: (date, profile)
- Index: idx_cmc_macro_regimes_date on (date DESC)
- Index: idx_cmc_macro_regimes_state on (macro_state) -- for downstream policy lookups

**Table 2: `cmc_macro_hysteresis_state`**
- `profile` TEXT NOT NULL
- `dimension` TEXT NOT NULL (one of: monetary_policy, liquidity, risk_appetite, carry)
- `current_label` TEXT nullable
- `pending_label` TEXT nullable
- `pending_count` INTEGER NOT NULL, server_default=0
- `updated_at` TIMESTAMP WITH TIME ZONE NOT NULL, server_default=now()
- PK: (profile, dimension)

4. The downgrade function must drop both tables in reverse order (hysteresis_state first, then macro_regimes).

5. Follow the exact import and style pattern from `a1b2c3d4e5f6_fred_macro_features.py`: use `from alembic import op` and `import sqlalchemy as sa`, docstring with phase reference, revision identifiers at top.

6. Use a descriptive revision message: "macro_regime_tables" in the Alembic revision.
  </action>
  <verify>
Run `alembic check` or `alembic heads` to confirm the new migration is recognized and chains correctly. Run `alembic upgrade head --sql` to verify the generated SQL is valid (dry-run SQL output, no actual DB change needed in CI).
  </verify>
  <done>
Migration file exists in alembic/versions/, chains from the correct head, creates both tables with correct PKs and indexes, and `alembic heads` shows a single head (no branch).
  </done>
</task>

</tasks>

<verification>
- `alembic heads` shows exactly one head (the new migration)
- `alembic upgrade head --sql` produces valid DDL for both tables
- PK constraints are (date, profile) for cmc_macro_regimes and (profile, dimension) for cmc_macro_hysteresis_state
- Both tables are in public schema (no schema= argument)
</verification>

<success_criteria>
- Alembic migration file exists and is syntactically valid Python
- Migration chains from correct current head (discovered at runtime, not hardcoded from plan docs)
- Both tables have correct columns, types, PKs, and indexes
- Downgrade drops both tables cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/67-macro-regime-classifier/67-01-SUMMARY.md`
</output>
