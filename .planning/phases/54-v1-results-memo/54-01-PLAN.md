---
phase: 54-v1-results-memo
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ta_lab2/scripts/analysis/generate_v1_memo.py
autonomous: true

must_haves:
  truths:
    - "Running `python -m ta_lab2.scripts.analysis.generate_v1_memo --backtest-only` produces a valid V1_MEMO.md with Executive Summary, Build Narrative, and Methodology sections fully rendered"
    - "All subsequent sections (Results, Failure Modes, Research Tracks, Key Takeaways, V2 Roadmap, Appendix) appear as placeholder stubs in the output"
    - "The build timeline table shows correct milestone dates and plan counts computed at generation time from .planning/MILESTONES.md"
  artifacts:
    - path: "src/ta_lab2/scripts/analysis/generate_v1_memo.py"
      provides: "V1 memo generator script with skeleton, data loading, chart infrastructure, CLI, and sections 1-2 fully implemented"
      min_lines: 400
    - path: "reports/v1_memo/V1_MEMO.md"
      provides: "Partial memo document with Executive Summary, Build Narrative, and Methodology sections"
      contains: "# V1 Results Memo"
  key_links:
    - from: "src/ta_lab2/scripts/analysis/generate_v1_memo.py"
      to: "reports/bakeoff/*.csv"
      via: "CSV loading functions"
      pattern: "pd\\.read_csv.*bakeoff"
    - from: "src/ta_lab2/scripts/analysis/generate_v1_memo.py"
      to: ".planning/MILESTONES.md"
      via: "file read for build timeline"
      pattern: "MILESTONES\\.md"
---

<objective>
Build the complete generate_v1_memo.py script skeleton with CLI, data loading infrastructure, chart helpers, and fully implement the Executive Summary, Build Narrative (Section 1), and Methodology (Section 2 / MEMO-01) sections. All other memo sections are created as stub functions returning placeholder text so the script runs end-to-end.

Purpose: Establish the generator framework and deliver the narrative-heavy sections (MEMO-01) that rely on existing CSV artifacts and milestone data rather than DB queries.
Output: A runnable generator that produces reports/v1_memo/V1_MEMO.md with the first 2 sections fully written and remaining sections as placeholders.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/54-v1-results-memo/54-CONTEXT.md
@.planning/phases/54-v1-results-memo/54-RESEARCH.md
@.planning/MILESTONES.md

# Reference generator patterns
@src/ta_lab2/scripts/analysis/generate_bakeoff_scorecard.py
@src/ta_lab2/scripts/analysis/generate_tail_risk_policy.py

# Data sources for methodology section
@reports/bakeoff/STRATEGY_SELECTION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create generate_v1_memo.py with full skeleton and sections 1-2</name>
  <files>src/ta_lab2/scripts/analysis/generate_v1_memo.py</files>
  <action>
Create `src/ta_lab2/scripts/analysis/generate_v1_memo.py` following the established generator pattern from `generate_bakeoff_scorecard.py`.

**Script structure (top to bottom):**

1. **Module docstring** describing usage:
   - `python -m ta_lab2.scripts.analysis.generate_v1_memo`
   - `python -m ta_lab2.scripts.analysis.generate_v1_memo --backtest-only`
   - `python -m ta_lab2.scripts.analysis.generate_v1_memo --no-charts`

2. **Imports**: pathlib, argparse, logging, datetime, pandas, plotly.graph_objects, json, yaml, re

3. **Path constants**:
   - `_PROJECT_ROOT = Path(__file__).resolve().parents[4]` (scripts/analysis/ depth)
   - `_OUTPUT_DIR = _PROJECT_ROOT / "reports" / "v1_memo"`
   - `_CHARTS_DIR = _OUTPUT_DIR / "charts"`
   - `_DATA_DIR = _OUTPUT_DIR / "data"`
   - `_MEMO_PATH = _OUTPUT_DIR / "V1_MEMO.md"`
   - `_BAKEOFF_DIR = _PROJECT_ROOT / "reports" / "bakeoff"`
   - `_LOSS_LIMITS_DIR = _PROJECT_ROOT / "reports" / "loss_limits"`
   - `_TAIL_RISK_DIR = _PROJECT_ROOT / "reports" / "tail_risk"`
   - `_MILESTONES_PATH = _PROJECT_ROOT / ".planning" / "MILESTONES.md"`
   - `_STATE_PATH = _PROJECT_ROOT / ".planning" / "STATE.md"`

4. **Helpers** (copy pattern from scorecard):
   - `_get_engine(db_url)` with NullPool
   - `_save_chart(fig, filename, charts_dir)` with HTML fallback (try PNG via kaleido, fall back to HTML)
   - `_format_table(headers, rows)` for GFM Markdown tables
   - `_safe_read_text(path)` that returns empty string on missing file, always uses encoding="utf-8"
   - `_safe_read_csv(path)` that returns empty DataFrame on missing file

5. **Data loading functions**:
   - `load_bakeoff_artifacts()` -> dict of DataFrames from reports/bakeoff/*.csv
   - `load_milestone_stats()` -> dict with plan counts, dates, durations parsed from .planning/MILESTONES.md and .planning/STATE.md
   - `load_strategy_selection()` -> dict parsed from STRATEGY_SELECTION.md (strategy names, parameters, rationale)
   - `load_policy_documents()` -> dict of Markdown strings from reports/loss_limits/ and reports/tail_risk/

6. **Fully implemented section functions**:

   a. `_section_executive_summary(milestone_data, bakeoff)` -> str
      - 1-page executive summary: what was built, key findings, V1 gate outcomes
      - Use f-string interpolation with `milestone_data['total_plans']` for the plan count (do NOT hardcode "250+")
      - Reference the milestone count dynamically from `milestone_data`
      - State the two selected strategies and their performance
      - State the MaxDD gate failure and mitigation posture
      - All numeric references to plans, milestones, hours must come from milestone_data dict

   b. `_section_build_narrative(milestone_data)` -> str
      - "## 1. Build Narrative"
      - AI-accelerated development story: GSD workflow, Claude as planner+executor
      - Milestone timeline table (from MILESTONES.md data, NOT hardcoded)
      - Key architectural decisions summary (data architecture, regime pipeline, research tooling)
      - Development velocity stats (plans/day, avg duration)
      - Build timeline chart reference (build_timeline.html generated in Plan 02)

   c. `_section_methodology(bakeoff, strategy_data)` -> str
      - "## 2. Methodology"
      - "### Data Sources" subsection: CoinMarketCap API, BTC (id=1) and ETH (id=2), 2010-2025 daily bars, 109 timeframes, 4.1M price bar rows, 14.8M EMA rows
      - "### Strategy Descriptions" subsection: ema_trend(17,77) and ema_trend(21,50), EMA crossover mechanics, long-only
      - "### Parameter Selection Process" subsection: IC feature sweep (97 features) -> walk-forward bake-off (PurgedKFold, 5 folds) -> composite scoring (4 weighting schemes) -> robustness check (top-2 in 3+/4 schemes)
      - "### Fee and Slippage Assumptions" subsection: 16 bps maker fee + configurable slippage (0/5/10/25 bps), 12-scenario cost matrix from Phase 42
      - Use data from bakeoff CSVs for specific numbers (feature count, strategy count, fold count)

7. **Stub section functions** (return placeholder text, implemented in Plans 02 and 03):
   - `_section_results(...)` -> "## 3. Results\n\n_To be completed in Plan 02._\n"
   - `_section_failure_modes(...)` -> "## 4. Failure Modes\n\n_To be completed in Plan 02._\n"
   - `_section_research_tracks(...)` -> "## 5. Research Track Answers\n\n_To be completed in Plan 03._\n"
   - `_section_key_takeaways(...)` -> "## 6. Key Takeaways\n\n_To be completed in Plan 03._\n"
   - `_section_v2_roadmap(...)` -> "## 7. V2 Roadmap\n\n_To be completed in Plan 03._\n"
   - `_section_appendix(...)` -> "## Appendix\n\n_To be completed in Plan 03._\n"

8. **`build_memo()` function**: Assembles all sections, writes V1_MEMO.md with encoding="utf-8". Creates output directories (reports/v1_memo/, charts/, data/). Returns the full content string.

9. **`main()` with argparse**:
   - `--output-dir` (default: reports/v1_memo/)
   - `--backtest-only` flag (skip paper trading DB queries)
   - `--no-charts` flag (skip chart generation)
   - `--db-url` (optional DB connection override)
   - `--dry-run` (print what would be generated without writing)

**Critical conventions to follow:**
- `encoding="utf-8"` on ALL file reads and writes (Windows pitfall)
- NullPool for DB engine
- parents[4] for project root from scripts/analysis/
- HTML charts as primary format (no kaleido dependency)
- All CSV reads wrapped in try/except returning empty DataFrame

**load_milestone_stats() implementation detail:**
Read .planning/MILESTONES.md and .planning/STATE.md to extract:
- Per-milestone: name, ship date, phase count, plan count, total hours
- Overall: total plans completed (from STATE.md "Total plans completed: N" line), average duration, total execution time
- Parse using regex on known patterns (e.g., `r"Total plans completed: (\d+)"`)
- Do NOT hardcode plan counts -- derive from file content
  </action>
  <verify>
Run: `python -m ta_lab2.scripts.analysis.generate_v1_memo --backtest-only --no-charts --dry-run`
Expected: Script runs without errors, prints section summary. No DB connection needed.

Then run without --dry-run:
`python -m ta_lab2.scripts.analysis.generate_v1_memo --backtest-only --no-charts`
Expected: Creates reports/v1_memo/V1_MEMO.md with Executive Summary, Build Narrative, and Methodology sections fully rendered; other sections show placeholder text.
  </verify>
  <done>
generate_v1_memo.py exists, is importable, CLI works. V1_MEMO.md generated with:
- Executive Summary containing dynamic plan count from milestone_data (not hardcoded "250+")
- Build Narrative with milestone timeline table (non-hardcoded)
- Methodology section covering data sources, strategy descriptions, parameter selection, and fee assumptions
- All other sections present as stubs
  </done>
</task>

</tasks>

<verification>
- `python -m ta_lab2.scripts.analysis.generate_v1_memo --backtest-only --no-charts` completes without error
- `reports/v1_memo/V1_MEMO.md` exists with `# V1 Results Memo` header
- Milestone timeline table shows dates from MILESTONES.md (not hardcoded)
- Executive summary plan count is interpolated from milestone_data, not hardcoded "250+"
- Methodology section references real data (97 features, 4 weighting schemes, etc.)
- No `encoding` calls missing utf-8 parameter
</verification>

<success_criteria>
- generate_v1_memo.py runs end-to-end with --backtest-only flag
- V1_MEMO.md has fully rendered Executive Summary, Build Narrative, and Methodology
- Script follows established generator pattern (section-function decomposition)
- Plan count in memo matches STATE.md dynamically
</success_criteria>

<output>
After completion, create `.planning/phases/54-v1-results-memo/54-01-SUMMARY.md`
</output>
