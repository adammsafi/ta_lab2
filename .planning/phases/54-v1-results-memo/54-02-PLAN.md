---
phase: 54-v1-results-memo
plan: 02
type: execute
wave: 2
depends_on: ["54-01"]
files_modified:
  - src/ta_lab2/scripts/analysis/generate_v1_memo.py
autonomous: true

must_haves:
  truths:
    - "Running `python -m ta_lab2.scripts.analysis.generate_v1_memo --backtest-only` produces V1_MEMO.md with fully rendered Results (Section 3) and Failure Modes (Section 4) sections"
    - "Backtest metrics table shows Sharpe, MaxDD, Calmar (CAGR/abs(MaxDD)), win rate, avg trade, turnover for both ema_trend strategies queried via strategy_name='ema_trend' with params_json JSONB filter"
    - "Benchmark comparison includes buy-and-hold BTC, buy-and-hold ETH, 50/50 index, and risk-free rate"
    - "MaxDD gate failure root cause analysis is present with full narrative (what failed, why, ensemble attempt, accepted posture)"
    - "Paper trading sections gracefully degrade to placeholder text when Phase 53 data is unavailable"
    - "At least 2 Plotly HTML charts guaranteed under --backtest-only (benchmark_comparison.html, per_fold_sharpe.html); up to 5 with full DB access"
  artifacts:
    - path: "src/ta_lab2/scripts/analysis/generate_v1_memo.py"
      provides: "Generator with sections 3 (Results/MEMO-02) and 4 (Failure Modes/MEMO-03) fully implemented"
      min_lines: 700
    - path: "reports/v1_memo/V1_MEMO.md"
      provides: "Memo with sections 1-4 fully rendered, sections 5-7 as stubs"
      contains: "## 3. Results"
    - path: "reports/v1_memo/charts/"
      provides: "Plotly HTML companion charts"
  key_links:
    - from: "src/ta_lab2/scripts/analysis/generate_v1_memo.py"
      to: "strategy_bakeoff_results DB table"
      via: "SQL query for walk-forward fold detail"
      pattern: "strategy_bakeoff_results"
    - from: "src/ta_lab2/scripts/analysis/generate_v1_memo.py"
      to: "cmc_backtest_metrics DB table"
      via: "SQL query for per-run metrics joined via cmc_backtest_runs"
      pattern: "cmc_backtest_metrics.*JOIN.*cmc_backtest_runs"
    - from: "src/ta_lab2/scripts/analysis/generate_v1_memo.py"
      to: "cmc_price_bars_multi_tf_u DB table"
      via: "SQL query for benchmark buy-and-hold returns"
      pattern: "cmc_price_bars_multi_tf_u"
---

<objective>
Implement the Results (Section 3 / MEMO-02) and Failure Modes (Section 4 / MEMO-03) sections in generate_v1_memo.py, replacing the stub functions from Plan 01. Add all DB query functions for backtest metrics, paper trading metrics, and benchmark data. Generate companion Plotly HTML charts.

Purpose: Deliver the quantitative core of the memo -- the actual performance numbers, benchmark comparisons, trade statistics, failure analysis, and stress test results that make the memo useful.
Output: Updated generate_v1_memo.py with sections 3-4 producing full quantitative content; 2+ Plotly HTML charts in reports/v1_memo/charts/.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/54-v1-results-memo/54-CONTEXT.md
@.planning/phases/54-v1-results-memo/54-RESEARCH.md

# Prior plan summary
@.planning/phases/54-v1-results-memo/54-01-SUMMARY.md

# Reference for DB table schemas and query patterns
@src/ta_lab2/scripts/analysis/generate_bakeoff_scorecard.py
@src/ta_lab2/scripts/analysis/run_bakeoff_scoring.py

# DB schema reference for correct column names
@sql/backtests/070_cmc_backtest_runs.sql
@sql/backtests/072_cmc_backtest_metrics.sql

# Bakeoff orchestrator for strategy_bakeoff_results INSERT pattern
@src/ta_lab2/backtests/bakeoff_orchestrator.py

# Strategy selection data
@reports/bakeoff/STRATEGY_SELECTION.md
@reports/bakeoff/BAKEOFF_SCORECARD.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Results section (MEMO-02) with DB queries and charts</name>
  <files>src/ta_lab2/scripts/analysis/generate_v1_memo.py</files>
  <action>
Read the existing generate_v1_memo.py from Plan 01. Replace the `_section_results()` stub with a fully implemented section. Add all supporting data loading and chart functions.

**CRITICAL: Correct DB table usage and column names**

The two selected strategies are identified as follows in the DB:
- `strategy_bakeoff_results`: `strategy_name = 'ema_trend'` with `params_json` JSONB column. Filter using `params_json @> '{"fast": 17, "slow": 77}'::jsonb` and `params_json @> '{"fast": 21, "slow": 50}'::jsonb`. The table also has `params_str` as an alias in some queries (`params_json::text AS params_str`). Do NOT use "ema_trend_17_77" as a literal -- that is a display label, not a DB value.
- `cmc_backtest_runs`: `signal_type = 'ema_crossover'` (NOT 'ema_trend'). JOIN with `cmc_backtest_metrics` via `run_id`.
- `cmc_backtest_metrics`: Has `calmar_ratio` column (CAGR / max_drawdown). Do NOT expect `mar_ratio` -- it is called `calmar_ratio` in this table.

**There is NO `regime_breakdown_json` column** in `strategy_bakeoff_results`. The bakeoff orchestrator INSERT statement confirms the columns are: strategy_name, asset_id, tf, params_json, cost_scenario, cv_method, n_folds, embargo_bars, sharpe_mean, sharpe_std, max_drawdown_mean, max_drawdown_worst, total_return_mean, cagr_mean, trade_count_total, turnover, psr, dsr, psr_n_obs, pbo_prob, fold_metrics_json.

**New data loading functions to add:**

1. `load_backtest_metrics(engine)` -> pd.DataFrame
   - Primary source: `strategy_bakeoff_results` for walk-forward aggregated metrics
   - Query: `SELECT strategy_name, params_json::text AS params_str, sharpe_mean, max_drawdown_mean, max_drawdown_worst, cagr_mean, trade_count_total, turnover, psr, dsr FROM strategy_bakeoff_results WHERE strategy_name = 'ema_trend' AND params_json IN (CAST('{"fast": 17, "slow": 77}' AS jsonb), CAST('{"fast": 21, "slow": 50}' AS jsonb))`
   - Compute MAR (Calmar) inline: `mar = cagr_mean / abs(max_drawdown_worst)` -- do NOT expect a pre-computed `mar_ratio` column in this table
   - Secondary: JOIN `cmc_backtest_runs` (WHERE `signal_type = 'ema_crossover'`) with `cmc_backtest_metrics` for per-run detail including `calmar_ratio`, `win_rate`, `avg_win`, `avg_loss`
   - Wrap in try/except, return empty DataFrame on failure

2. `load_walkforward_folds(engine)` -> pd.DataFrame
   - Query `strategy_bakeoff_results` for fold-level detail
   - Filter: `strategy_name = 'ema_trend'` with `params_json` JSONB filter (same as above)
   - Parse `fold_metrics_json` using json.loads
   - Return: strategy_name, fold_id, sharpe, max_dd, n_trades per fold
   - Compute per-fold MAR inline: `fold_mar = fold_cagr / abs(fold_max_dd)` if cagr is available per fold
   - Wrap in try/except

3. `load_trade_stats(engine)` -> pd.DataFrame
   - Query `cmc_backtest_trades` JOIN `cmc_backtest_runs` WHERE `signal_type = 'ema_crossover'`
   - Compute: win_rate, avg_winner, avg_loser, max_consecutive_losses, avg_holding_period_bars
   - GROUP BY strategy display label, asset_id
   - Wrap in try/except

4. `load_benchmark_returns(engine, start_date, end_date)` -> pd.DataFrame
   - Query `cmc_price_bars_multi_tf_u` for BTC (id=1) and ETH (id=2), tf='1D'
   - Compute buy-and-hold returns, 50/50 index, and risk-free cumulative return (5% annual)
   - Note: column is `"timestamp"` (PostgreSQL reserved word, double-quote in SQL)
   - Wrap in try/except

5. `load_paper_metrics(engine)` -> pd.DataFrame
   - Query `cmc_drift_metrics` for paper trading data (paper_cumulative_pnl, replay_cumulative_pnl, tracking_error_5d, tracking_error_30d)
   - Wrap in try/except -- this is Phase 53 data, may be empty

6. `load_paper_fills(engine)` -> pd.DataFrame
   - Query `cmc_fills` for trade-level paper fills
   - Wrap in try/except -- Phase 53 data

**New chart functions to add:**

1. `_chart_equity_curve_overlay(paper_df, charts_dir)` -> str
   - Paper vs replay cumulative P&L per config_id
   - Returns "" if paper_df is empty (this chart requires paper trading data -- it will NOT be generated under --backtest-only)
   - Returns relative chart path string

2. `_chart_benchmark_comparison(backtest_metrics, benchmark_df, charts_dir)` -> str
   - Bar chart: Sharpe, total return, max DD for strategies vs benchmarks
   - Grouped bars, one group per metric
   - This chart can be generated from bakeoff data alone (no paper data needed)

3. `_chart_per_fold_sharpe(fold_df, charts_dir)` -> str
   - Box plot or bar chart of per-fold Sharpe for each strategy
   - Shows OOS variability
   - This chart can be generated from bakeoff data alone (no paper data needed)

4. `_chart_drawdown_comparison(backtest_metrics, charts_dir)` -> str
   - Drawdown time series for both strategies
   - Computed from daily returns if available, or summary stats

**_section_results() implementation:**

```
## 3. Results

### 3.1 Backtest Results
[Strategy-level metrics table: Sharpe, MaxDD, Calmar (computed as cagr_mean / abs(max_drawdown_worst)), win rate, turnover, total return]
[Per-fold walk-forward table showing OOS consistency]
[Chart: per_fold_sharpe.html]

### 3.2 Per-Asset Breakdown
[BTC vs ETH metrics if data supports per-asset split]
[Table with per-asset Sharpe, MaxDD, win rate]

### 3.3 Per-Regime Breakdown
[NOTE: Per-regime breakdown is NOT available in V1 -- regime labels exist in `cmc_regimes` but per-regime fold-level metrics were not persisted in the bake-off results. The `strategy_bakeoff_results` table does not have a `regime_breakdown_json` column.]

Render this subsection as:
"Per-regime performance breakdown is not available in V1. While regime labels are computed and stored in `cmc_regimes` (Phase 27), the walk-forward bake-off did not persist per-regime fold statistics. This is a V2 enhancement: re-run bake-off with regime-conditioned splits to capture bull/bear/sideways performance."

Optionally, if DB is available, query `cmc_regime_stats` for aggregate regime duration/frequency as supplemental context (how much time was spent in each regime).

### 3.4 Benchmark Comparison
[Buy-hold BTC, ETH, 50/50 index, risk-free rate]
[Chart: benchmark_comparison.html]
[Table comparing strategy returns vs benchmarks]

### 3.5 Paper Trading Results
[Graceful degradation if Phase 53 data unavailable]
[If available: paper P&L, tracking error, trade count]

### 3.6 Backtest vs Paper Comparison
[Graceful degradation if Phase 53 data unavailable]
[If available: side-by-side metrics table + equity curve overlay chart]
[Chart: equity_curve_overlay.html -- only generated when paper data exists]

### 3.7 Trade-Level Statistics
[Win rate, avg winner/loser, max consecutive losses, avg holding period]
[Notable best/worst trades if data available]
```

**Critical implementation notes:**
- Every DB query function wraps in try/except returning empty DataFrame
- Paper trading sections check `if paper_df.empty:` and render placeholder
- Benchmark start/end dates derived from backtest metrics date range
- Use `_format_table()` for all tables
- Charts saved as HTML via `_save_chart()`
- encoding="utf-8" on all file operations
- Use `calmar_ratio` from `cmc_backtest_metrics`, compute MAR inline from `strategy_bakeoff_results` as `cagr_mean / abs(max_drawdown_worst)`
  </action>
  <verify>
Run: `python -m ta_lab2.scripts.analysis.generate_v1_memo --backtest-only`
Expected: V1_MEMO.md now has fully rendered Section 3 (Results) with backtest metrics tables, benchmark comparison, and trade stats. Paper trading subsections show graceful degradation text. Section 3.3 acknowledges per-regime breakdown is not available in V1.

Verify charts exist:
`ls reports/v1_memo/charts/`
Expected: At least benchmark_comparison.html and per_fold_sharpe.html (equity_curve_overlay.html only if paper data exists).
  </verify>
  <done>Section 3 (Results/MEMO-02) fully implemented with backtest metrics from strategy_bakeoff_results (using correct strategy_name='ema_trend' + params_json filter), walk-forward folds, benchmark comparison, trade stats, per-regime graceful degradation note, and graceful degradation for paper trading data. MAR computed inline as cagr_mean/abs(max_drawdown_worst).</done>
</task>

<task type="auto">
  <name>Task 2: Implement Failure Modes section (MEMO-03) with stress tests</name>
  <files>src/ta_lab2/scripts/analysis/generate_v1_memo.py</files>
  <action>
Replace the `_section_failure_modes()` stub with a fully implemented section.

**New data loading functions:**

1. `load_risk_events(engine)` -> pd.DataFrame
   - Query `cmc_risk_events` for kill switch triggers, circuit breaker events
   - Wrap in try/except (Phase 53 data, may be empty)

2. `_compute_stress_test_returns(engine, strategy_signals_source)` -> pd.DataFrame
   - For each historical crash period, compute strategy returns:
     - 2018 Bear: 2018-01-06 to 2018-12-15 (BTC -83%)
     - COVID Crash: 2020-02-20 to 2020-03-23 (BTC -53% in 1 month)
     - 2022 Bear: 2022-04-01 to 2022-11-21 (BTC -77%, LUNA/FTX)
   - Query `cmc_price_bars_multi_tf_u` for price data in each window
   - Compute buy-hold drawdown for each window as baseline
   - If backtest trade data exists for these windows, extract actual strategy performance
   - Wrap in try/except
   - **Graceful degradation when DB is unavailable:** If `_compute_stress_test_returns()` returns an empty DataFrame (DB unavailable or query fails), render a note in the memo: "Stress test price data requires a database connection. Run without `--backtest-only` for computed stress test results. The crash periods analyzed would be: 2018 Bear (Jan-Dec 2018, BTC -83%), COVID Crash (Feb-Mar 2020, BTC -53%), 2022 Bear (Apr-Nov 2022, BTC -77%)."

**New chart function:**

1. `_chart_stress_test_results(stress_df, charts_dir)` -> str
   - Grouped bar chart: max drawdown per crash event, strategy vs buy-hold
   - Color-coded by severity
   - Returns "" if stress_df is empty (no chart generated without data)

**_section_failure_modes() implementation:**

```
## 4. Failure Modes

### 4.1 MaxDD Gate Failure: Root Cause Analysis

**What Failed:** Both selected strategies (ema_trend 17/77 and 21/50) fail the V1 MaxDD gate of <= 15%. Worst-fold drawdowns reach 70-75%.

**Why:** EMA crossover strategies are trend-following. During extended bear markets (2018, 2022), the strategy enters long positions on false crossover signals, each resulting in losses. The structural problem is that crypto bear markets last 12-18 months -- longer than any reasonable stop-loss can accommodate without being triggered repeatedly.

**What We Tried:** Ensemble blend of both strategies using majority-vote logic. The blend also fails because both strategies lose during the same macro bear market regimes -- diversification requires uncorrelated signals, and both EMA strategies are correlated (rho > 0.85).

**Accepted Risk Posture:** Deploy at 10% position fraction (not 50% from backtest) with 15% portfolio drawdown circuit breaker. This caps real-world max loss at approximately 10% * 75% = 7.5% of portfolio in worst case, within the 15% gate spirit.

[This subsection should be narrative, not just a table]

### 4.2 Stress Test Results

[If stress_df has data: Table with per-crash-event max drawdown for each strategy + buy-hold benchmark]
[If stress_df has data: Chart: stress_test_results.html]
[If stress_df is EMPTY (DB unavailable): Render graceful degradation note listing the crash periods that would be analyzed and stating DB connection is required]
[Narrative: what happens during 2x vol scenarios, 50% drawdown scenarios, correlation spike]

### 4.3 Drift Analysis

[If Phase 53 drift data available: narrative about drift severity, tracking error trends]
[If not: placeholder noting drift guard is implemented but not yet exercised]

### 4.4 Risk Events During Paper Trading

[If cmc_risk_events has data: list of circuit breaker triggers, kill switch exercises]
[If not: placeholder noting risk controls are implemented but not yet triggered in live paper]

### 4.5 Lessons Learned from Failure Analysis

[Bulleted summary: 4-6 key lessons from the failure analysis]
- Trend-following alone cannot survive crypto macro bears
- Position sizing is the primary risk lever, not signal quality
- Ensemble requires uncorrelated strategies (RSI + EMA, not EMA + EMA)
- etc.
```

**Critical implementation notes:**
- Stress test dates are constants (historical fact, safe to hardcode)
- Stress test computation uses DB price data, NOT hardcoded returns
- When DB is unavailable and stress_df is empty, render a descriptive note listing the crash periods and stating that a DB connection is needed -- do NOT crash or leave the section blank
- MaxDD root cause analysis is primarily narrative text assembled from known Phase 42 findings
- Risk events section gracefully degrades if Phase 53 tables empty
- Use STRATEGY_SELECTION.md for MaxDD gate failure details
  </action>
  <verify>
Run: `python -m ta_lab2.scripts.analysis.generate_v1_memo --backtest-only`
Expected: V1_MEMO.md Section 4 (Failure Modes) fully rendered with:
- MaxDD root cause analysis narrative
- Stress test section with either computed results (if DB available) or graceful degradation note listing crash periods
- Graceful degradation for drift analysis and risk events
- Charts in reports/v1_memo/charts/ (stress_test_results.html only if DB available and data returned)
  </verify>
  <done>Section 4 (Failure Modes/MEMO-03) fully implemented with MaxDD root cause, stress tests with graceful degradation when DB unavailable, drift analysis placeholder, risk events placeholder, and lessons learned.</done>
</task>

</tasks>

<verification>
- `python -m ta_lab2.scripts.analysis.generate_v1_memo --backtest-only` completes without error
- V1_MEMO.md sections 3 and 4 contain real quantitative data (not placeholders)
- Backtest metrics queried via `strategy_name = 'ema_trend'` with `params_json` JSONB filter (not "ema_trend_17_77" literals)
- Section 3.3 per-regime breakdown shows graceful degradation note (no regime_breakdown_json column exists)
- MAR/Calmar computed correctly: from `calmar_ratio` column in cmc_backtest_metrics, or inline as `cagr_mean / abs(max_drawdown_worst)` from strategy_bakeoff_results
- Paper trading subsections show graceful degradation text (not crashes)
- Stress test section degrades gracefully when DB unavailable (descriptive note, not blank)
- At least 2 chart HTML files exist in reports/v1_memo/charts/ (benchmark_comparison.html, per_fold_sharpe.html)
- No encoding errors on Windows (all file ops use utf-8)
</verification>

<success_criteria>
- MEMO-02 (Results) covers: backtest metrics, walk-forward folds, per-asset breakdown, per-regime graceful degradation, benchmarks, paper comparison (with degradation), trade stats
- MEMO-03 (Failure Modes) covers: MaxDD root cause, ensemble failure, stress tests (with DB-unavailable degradation), drift analysis, risk events
- All DB queries use correct column names (strategy_name='ema_trend', params_json JSONB, calmar_ratio, signal_type='ema_crossover')
- All DB queries wrapped in try/except with empty DataFrame fallback
- Charts generated as HTML in reports/v1_memo/charts/
</success_criteria>

<output>
After completion, create `.planning/phases/54-v1-results-memo/54-02-SUMMARY.md`
</output>
