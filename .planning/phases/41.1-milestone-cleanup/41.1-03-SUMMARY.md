---
phase: 41.1-milestone-cleanup
plan: "03"
subsystem: ui
tags: [streamlit, dashboard, research-explorer, cv, cross-validation, ic, rolling-ic]

# Dependency graph
requires:
  - phase: 39-04
    provides: Research Explorer page skeleton with IC decay and regime charts
  - phase: 38-01
    provides: PurgedKFoldSplitter and CPCVSplitter in ta_lab2.backtests.cv
  - phase: 37-01
    provides: compute_rolling_ic and build_rolling_ic_chart in analysis.ic / dashboard.charts
affects:
  - future notebook users calling fold_boundaries
  - Research Explorer dashboard users (rolling IC chart now visible)

provides:
  - Rolling IC chart section in Research Explorer (between IC Decay and Regime Analysis)
  - load_feature_close_series cached query for feature+close data loading
  - fold_boundaries as public API (was _fold_boundaries, private)
  - Corrected CLI reference: ta_lab2.scripts.analysis.run_ic_eval

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Input sanitization via alphanumeric/underscore check before interpolating column name into SQL"
    - "Use close_series_for_ic alias to avoid shadowing close_series used later in same page"

key-files:
  created: []
  modified:
    - src/ta_lab2/dashboard/pages/3_research_explorer.py
    - src/ta_lab2/dashboard/queries/research.py
    - src/ta_lab2/backtests/cv.py
    - notebooks/02_evaluate_features.ipynb

key-decisions:
  - "fold_boundaries made public (no leading underscore) — it is called from notebooks and should be part of the public API"
  - "load_feature_close_series uses alphanumeric+underscore check before SQL interpolation to prevent injection"
  - "Rolling IC uses close_series_for_ic alias to avoid shadowing the close_series variable used in Regime Analysis section"

patterns-established:
  - "Column name sanitization pattern: all(c.isalnum() or c == '_' for c in col_name) before SQL f-string interpolation"

# Metrics
duration: 4min
completed: 2026-02-24
---

# Phase 41.1 Plan 03: Tech Debt Cleanup (CLI Ref, Rolling IC, Public CV API) Summary

**Rolling IC chart added to Research Explorer, stale CLI reference fixed to ta_lab2.scripts.analysis.run_ic_eval, and fold_boundaries promoted to public API from _fold_boundaries.**

## Performance

- **Duration:** 4 min
- **Started:** 2026-02-24T19:33:55Z
- **Completed:** 2026-02-24T19:38:09Z
- **Tasks:** 2
- **Files modified:** 4

## Accomplishments

- Fixed stale CLI help text: `ta_lab2.scripts.ic.run_ic_evaluation` -> `ta_lab2.scripts.analysis.run_ic_eval` (correct module path that actually exists)
- Added `load_feature_close_series()` cached query function to research.py for loading feature+close pairs used by rolling IC
- Inserted Rolling IC chart section in Research Explorer between IC Decay and Regime Analysis, with IC-IR and IC-IR t-stat metrics
- Renamed `_fold_boundaries` -> `fold_boundaries` in cv.py (all 3 call sites updated), making it importable as public API
- Updated `notebooks/02_evaluate_features.ipynb` to import and call `fold_boundaries` (public name)

## Task Commits

Each task was committed atomically:

1. **Task 1: Fix stale CLI ref and add rolling IC chart to Research Explorer** - `9c36199f` (feat - landed in docs(41.1-01) commit due to stash pop timing)
2. **Task 2: Make _fold_boundaries public and fix notebook import** - `0476b2a4` (feat)

## Files Created/Modified

- `src/ta_lab2/dashboard/pages/3_research_explorer.py` - Fixed CLI ref, added rolling IC chart section with compute_rolling_ic + build_rolling_ic_chart + IC-IR metrics
- `src/ta_lab2/dashboard/queries/research.py` - Added load_feature_close_series() cached query
- `src/ta_lab2/backtests/cv.py` - Renamed _fold_boundaries -> fold_boundaries; updated all 3 call sites
- `notebooks/02_evaluate_features.ipynb` - Import and usage updated to fold_boundaries (public name)

## Decisions Made

- Used `close_series_for_ic` variable name (not `close_series`) in Rolling IC section to avoid shadowing the `close_series` variable loaded later in Regime Analysis. This is a safety measure since Streamlit pages execute top-to-bottom and variable shadowing would break the regime chart.
- Column name sanitization (`all(c.isalnum() or c == '_' for c in feature_col)`) in `load_feature_close_series` before SQL interpolation — necessary because feature_col comes from user selection (selectbox) not a parameterized query, so injection prevention is required.
- `_fold_sizes` kept private (no rename) — it is purely an implementation helper with no external call sites.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

- Pre-commit hook (ruff-format) reformatted one long line in 3_research_explorer.py (split `load_feature_close_series(engine, selected_id, selected_tf, selected_feature)` across multiple lines). Accepted the reformatted version.
- Pre-commit hook (end-of-file-fixer + mixed-line-ending) fixed the notebook file on second commit attempt. Re-staged and committed cleanly.
- Task 1 dashboard changes landed in a prior commit (9c36199f `docs(41.1-01)`) due to git stash pop timing during pre-commit retry cycle. Content is correct and verified.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Tech debt items 4, 5, 6 from v0.9.0 audit are now closed
- Research Explorer rolling IC chart is wired end-to-end (requires populated cmc_features + cmc_ic_results)
- `fold_boundaries` is now a stable public API — safe to reference from notebooks, scripts, or CLI tools
- No blockers for v1.0.0 planning

---
*Phase: 41.1-milestone-cleanup*
*Completed: 2026-02-24*
