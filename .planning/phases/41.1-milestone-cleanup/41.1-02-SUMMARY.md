---
phase: 41.1-milestone-cleanup
plan: "02"
subsystem: ui
tags: [streamlit, dashboard, experiments, ic, bh-correction, feature-experiments]

# Dependency graph
requires:
  - phase: 41.1-milestone-cleanup-01
    provides: AMA feature improvements that may generate cmc_feature_experiments rows

provides:
  - Experiments dashboard page showing cmc_feature_experiments results
  - Cached query module for experiment data access
  - Navigation registration for Experiments section in app.py

affects:
  - Future dashboard enhancements (can extend experiments.py with more charts)
  - v1.0.0 milestone (dashboard now covers all core data tables)

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "st.cache_data(ttl=300) with _engine underscore-prefix for all query functions"
    - "CheckboxColumn for boolean significance flags in st.dataframe"
    - "FILTER WHERE clause in aggregate SQL for BH significance count"
    - "Grouped bar chart via st.bar_chart() for horizon breakdown"

key-files:
  created:
    - src/ta_lab2/dashboard/queries/experiments.py
    - src/ta_lab2/dashboard/pages/5_experiments.py
  modified:
    - src/ta_lab2/dashboard/app.py

key-decisions:
  - "Used :material/experiment: Material icon for Experiments nav group"
  - "BH significance displayed as CheckboxColumn (st.column_config) in detail table"
  - "Summary aggregation uses FILTER WHERE (PostgreSQL syntax) for concise n_significant count"

patterns-established:
  - "Query module pattern: one file per page in queries/, functions accept _engine with underscore"
  - "Page pattern: header -> engine -> summary section -> detail section (with selectbox) -> chart"

# Metrics
duration: 2min
completed: 2026-02-24
---

# Phase 41.1 Plan 02: Experiments Dashboard Page Summary

**Streamlit Experiments page with BH-significance highlighting, per-feature detail table, and horizon breakdown chart wired to cmc_feature_experiments via 3 cached query functions**

## Performance

- **Duration:** 2 min
- **Started:** 2026-02-24T19:33:18Z
- **Completed:** 2026-02-24T19:34:46Z
- **Tasks:** 2
- **Files modified:** 3

## Accomplishments

- Created `queries/experiments.py` with 3 cached query functions: `load_experiment_results`, `load_experiment_feature_names`, `load_experiment_summary`
- Created `pages/5_experiments.py` with summary table, per-feature detail (BH significance as CheckboxColumn), CSV download, and horizon breakdown bar chart
- Registered Experiments nav group in `app.py` with `5_experiments.py` and updated sidebar caption

## Task Commits

Each task was committed atomically:

1. **Task 1: Create experiment query module and dashboard page** - `4dd2b297` (feat)
2. **Task 2: Register experiments page in app.py navigation** - `0ea4bac6` (feat)

**Plan metadata:** (to be committed below)

## Files Created/Modified

- `src/ta_lab2/dashboard/queries/experiments.py` - 3 cached query functions for cmc_feature_experiments
- `src/ta_lab2/dashboard/pages/5_experiments.py` - Experiments dashboard page with summary, detail, and significance chart
- `src/ta_lab2/dashboard/app.py` - Added Experiments nav group and updated sidebar caption

## Decisions Made

- Used `st.column_config.CheckboxColumn` for the `significant` column in the detail table to give a clear visual cue for BH-significant features.
- Used PostgreSQL `FILTER (WHERE ic_p_value_bh < 0.05)` clause in the aggregate summary query for conciseness over a CASE WHEN approach.
- Icon `:material/experiment:` used for the Experiments nav group (matches Streamlit Material icon library).

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

Pre-commit hooks (ruff-format + mixed-line-ending) reformatted the files after first commit attempt. Re-staged and committed successfully on second attempt.

## User Setup Required

None - no external service configuration required. The page reads from `cmc_feature_experiments` which is already in the database schema.

## Next Phase Readiness

- Experiments page is live and navigable from the dashboard sidebar.
- Table renders an info message gracefully if `cmc_feature_experiments` is empty.
- Ready for 41.1-03 and subsequent milestone cleanup plans.

---
*Phase: 41.1-milestone-cleanup*
*Completed: 2026-02-24*
