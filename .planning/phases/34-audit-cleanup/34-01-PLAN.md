---
phase: 34-audit-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/operations/DAILY_REFRESH.md
  - docs/CHANGELOG.md
  - src/ta_lab2/scripts/run_daily_refresh.py
  - CONTRIBUTING.md
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "DAILY_REFRESH.md documents --regimes, --stats, --weekly-digest flags and 4-stage execution order"
    - "CHANGELOG.md 0.8.0 section includes Phase 32 (runbooks) and Phase 33 (alembic) entries"
    - "run_daily_refresh.py argparse declares --no-telegram so the flag can be forwarded to weekly_digest subprocess"
    - "CONTRIBUTING.md recommends Python 3.12 matching CI and ruff target-version"
  artifacts:
    - path: "docs/operations/DAILY_REFRESH.md"
      provides: "Up-to-date orchestrator documentation"
      contains: "--regimes"
    - path: "docs/CHANGELOG.md"
      provides: "Complete v0.8.0 changelog"
      contains: "Phase 32"
    - path: "src/ta_lab2/scripts/run_daily_refresh.py"
      provides: "--no-telegram argparse declaration"
      contains: "no-telegram"
    - path: "CONTRIBUTING.md"
      provides: "Current Python version recommendation"
      contains: "3.12"
  key_links:
    - from: "DAILY_REFRESH.md flags table"
      to: "run_daily_refresh.py argparse"
      via: "flag names must match"
      pattern: "--(regimes|stats|weekly-digest)"
    - from: "run_daily_refresh.py argparse --no-telegram"
      to: "run_weekly_digest getattr"
      via: "argparse feeds getattr lookup"
      pattern: "add_argument.*no-telegram"
---

<objective>
Close 4 tech debt items identified by the v0.8.0 milestone audit. All items are small documentation or code edits with no architectural impact.

Purpose: Ensure docs, changelog, CLI args, and version recommendations accurately reflect the shipped v0.8.0 system.
Output: 4 files updated with correct, current information.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Files to modify
@docs/operations/DAILY_REFRESH.md
@docs/CHANGELOG.md
@src/ta_lab2/scripts/run_daily_refresh.py
@CONTRIBUTING.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update DAILY_REFRESH.md to reflect current orchestrator</name>
  <files>docs/operations/DAILY_REFRESH.md</files>
  <action>
Update DAILY_REFRESH.md to accurately document the current run_daily_refresh.py capabilities added in Phase 29 (stats/QA orchestration) and Phase 27 (regimes). Specific changes:

1. **Quick Start section**: Change "Full daily refresh (bars + EMAs)" comment to "Full daily refresh (bars + EMAs + regimes + stats)".

2. **Unified Script flags table**: Add these missing flags after the existing entries:
   - `--regimes` - Run regime refresher only
   - `--stats` - Run stats runners only (data quality check)
   - `--weekly-digest` - Run weekly QC digest (standalone, does not combine with pipeline flags)
   - `--no-regime-hysteresis` - Disable hysteresis smoothing in regime refresher

3. **Execution Order section**: Add two new subsections after the EMAs subsection:

   ### Regimes (refresh_cmc_regimes.py)
   After EMAs complete. Reads bars + EMAs, runs L0-L2 labeling, resolves policy, writes to cmc_regimes/flips/stats/comovement tables.

   ### Stats (run_all_stats_runners.py)
   Final stage. Runs 6 stats runners that check data quality across all pipeline tables. FAIL status halts the pipeline and sends a Telegram alert. WARN status continues with alert logged.

4. **Unified Script description**: Change "--all - Run bars then EMAs" to "--all - Run bars then EMAs then regimes then stats (full refresh)".

5. **Summary example output**: Update the example summary to show 4 components (bars, emas, regimes, stats) instead of 2.

6. **Telegram Alerts section**: Add a note that stats runners send Telegram alerts on FAIL/WARN, and that the weekly digest can be invoked via `--weekly-digest`.

Do NOT change the separate scripts section (bars/EMAs), troubleshooting, cron setup, performance, or other sections that remain accurate.
  </action>
  <verify>
Grep the updated file for "--regimes", "--stats", "--weekly-digest" to confirm all three new flags are documented. Grep for "regimes" in the execution order section. Verify the file has no broken markdown by checking heading structure.
  </verify>
  <done>DAILY_REFRESH.md documents all current flags (--regimes, --stats, --weekly-digest, --no-regime-hysteresis) and describes the 4-stage execution order (bars -> EMAs -> regimes -> stats).</done>
</task>

<task type="auto">
  <name>Task 2: Add Phase 32 and Phase 33 entries to CHANGELOG.md</name>
  <files>docs/CHANGELOG.md</files>
  <action>
Add two entries to the existing `[0.8.0] - 2026-02-22` section in docs/CHANGELOG.md. Insert them in the `### Added` subsection after the existing entries:

- **Runbooks (Phase 32)**: Operational runbooks for regime pipeline, backtest pipeline, new-asset onboarding SOP, and disaster recovery guide in `docs/operations/`
- **Alembic migrations (Phase 33)**: Alembic framework bootstrapped with `alembic/` directory, baseline no-op revision (`25f2b3c90f65`), legacy SQL migration catalog in `sql/migration/CATALOG.md`, and schema change workflow documented in CONTRIBUTING.md

Do NOT change the date, do NOT change any other sections. Just add the two bullet points.
  </action>
  <verify>
Grep docs/CHANGELOG.md for "Phase 32" and "Phase 33" to confirm both entries exist under the [0.8.0] section.
  </verify>
  <done>CHANGELOG.md [0.8.0] section includes entries for Phase 32 (runbooks) and Phase 33 (alembic migrations).</done>
</task>

<task type="auto">
  <name>Task 3: Add --no-telegram argparse declaration to run_daily_refresh.py</name>
  <files>src/ta_lab2/scripts/run_daily_refresh.py</files>
  <action>
Add an argparse declaration for `--no-telegram` in the `main()` function's argument parser. Place it near the other execution options (after `--no-regime-hysteresis` or after `--staleness-hours`). The declaration should be:

```python
p.add_argument(
    "--no-telegram",
    action="store_true",
    help="Suppress Telegram delivery for weekly digest (passed through to weekly_digest subprocess)",
)
```

This makes the existing `getattr(args, "no_telegram", False)` on line 552 work correctly when the flag is passed via CLI. Currently argparse silently ignores the flag because it is not declared.

Do NOT change any other code. The `getattr` pattern in `run_weekly_digest` already handles forwarding correctly once argparse knows about the flag.
  </action>
  <verify>
Run: `python -m ta_lab2.scripts.run_daily_refresh --help` and confirm `--no-telegram` appears in the help output. Also run: `python -m ta_lab2.scripts.run_daily_refresh --weekly-digest --no-telegram --dry-run` and confirm it exits 0 without error (dry-run avoids DB).
  </verify>
  <done>`python -m ta_lab2.scripts.run_daily_refresh --help` shows --no-telegram flag, and `--weekly-digest --no-telegram --dry-run` exits 0.</done>
</task>

<task type="auto">
  <name>Task 4: Update CONTRIBUTING.md Python version recommendation to 3.12</name>
  <files>CONTRIBUTING.md</files>
  <action>
Two edits in CONTRIBUTING.md:

1. Line 7: Change "Python 3.11 recommended." to "Python 3.12 recommended."
2. Line 49: Change "Python version: 3.11 (match the repo's `pyproject.toml` / tooling)." to "Python version: 3.12 (match the repo's `pyproject.toml` / tooling)."

These two lines are the only places "3.11" appears. The change aligns with the CI matrix (which tests 3.12) and ruff target-version (py312) established in Phase 30.

Do NOT change anything else in the file.
  </action>
  <verify>
Grep CONTRIBUTING.md for "3.11" to confirm zero matches. Grep for "3.12" to confirm exactly two matches (the recommendation line and the code style line).
  </verify>
  <done>CONTRIBUTING.md recommends Python 3.12 in both the dev setup section and the code style section, with zero remaining references to 3.11.</done>
</task>

</tasks>

<verification>
After all 4 tasks complete:
1. `grep -c "regimes\|stats\|weekly-digest" docs/operations/DAILY_REFRESH.md` returns a count >= 6
2. `grep "Phase 32\|Phase 33" docs/CHANGELOG.md` shows both entries under [0.8.0]
3. `python -m ta_lab2.scripts.run_daily_refresh --help` includes --no-telegram
4. `grep "3.11" CONTRIBUTING.md` returns no matches; `grep "3.12" CONTRIBUTING.md` returns 2 matches
</verification>

<success_criteria>
- DAILY_REFRESH.md documents --regimes, --stats, --weekly-digest flags and 4-stage execution order (bars -> EMAs -> regimes -> stats)
- CHANGELOG.md 0.8.0 section includes Phase 32 (runbooks) and Phase 33 (alembic) entries
- run_daily_refresh.py accepts --no-telegram flag via argparse and forwards it to weekly_digest subprocess
- CONTRIBUTING.md recommends Python 3.12 with zero remaining references to 3.11
</success_criteria>

<output>
After completion, create `.planning/phases/34-audit-cleanup/34-01-SUMMARY.md`
</output>
