---
phase: 80-ic-analysis-feature-selection
plan: 05
type: execute
wave: 4
depends_on: ["80-03", "80-04"]
files_modified:
  - configs/feature_selection.yaml
autonomous: false

must_haves:
  truths:
    - "User has reviewed the active feature set and confirmed it makes sense"
    - "The final feature_selection.yaml reflects any user adjustments"
    - "dim_feature_selection table matches the final YAML"
  artifacts:
    - path: "configs/feature_selection.yaml"
      provides: "Final validated feature selection config"
      contains: "active:"
  key_links:
    - from: "configs/feature_selection.yaml"
      to: "dim_feature_selection table"
      via: "save_to_db (re-run if adjusted)"
      pattern: "feature_selection"
---

<objective>
Verify the feature selection output with the user and finalize the YAML config.

Purpose: Feature selection is a domain judgment — the statistical pipeline produces the ranking, but the user should confirm the active set makes intuitive sense before it becomes the input to downstream phases (expression engine, regime router, bake-off). This is the quality gate for the entire phase.

Output: User-verified configs/feature_selection.yaml ready for Phase 82+ consumption.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/80-ic-analysis-feature-selection/80-CONTEXT.md
@.planning/phases/80-ic-analysis-feature-selection/80-03-SUMMARY.md
@.planning/phases/80-ic-analysis-feature-selection/80-04-SUMMARY.md
@configs/feature_selection.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate feature selection summary report</name>
  <files></files>
  <action>
  Read configs/feature_selection.yaml and produce a human-readable summary for the user to review. Print to console:

  1. **Active Features** (the core set that will drive downstream):
     - Table: name, IC-IR mean, pass rate, stationarity, Ljung-Box flag, monotonicity score, rationale
     - Count and coverage assessment

  2. **Conditional Features** (regime specialists):
     - Table: name, specialist regimes, IC-IR in-regime, rationale
     - Which regimes they serve

  3. **Watch Features** (not active, but worth monitoring):
     - Count and top 5 by IC-IR

  4. **Archive Features** (not selected):
     - Count only

  5. **Concordance Summary** (from Plan 04):
     - Read the concordance CSV if it exists
     - Report Spearman rho between IC-IR and MDA
     - List high-confidence features (both methods agree)

  6. **Cluster Report**:
     - Which active features are in the same cluster (correlated)
     - Whether the best-per-cluster was selected

  7. **Recommendation**:
     - Total active + conditional features vs the 15-25 target
     - Any anomalies (e.g., all vol features archive, all RSI active)
     - Suggested next steps

  This report is printed to console for the user to review before the checkpoint.
  </action>
  <verify>The summary report prints cleanly with all sections populated from the YAML and concordance data.</verify>
  <done>Human-readable summary report generated from feature_selection.yaml and concordance CSV.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
  Complete feature selection pipeline:
  - IC-IR ranking from ic_results (112+ features ranked by predictive power)
  - ADF/KPSS stationarity tests on top features
  - Ljung-Box autocorrelation tests on rolling IC series
  - Quintile monotonicity scores
  - Regime-conditional IC analysis (universal vs specialist features)
  - IC-IR vs MDA concordance analysis with feature clustering
  - Tiered feature selection config (configs/feature_selection.yaml)
  - Database mirror (dim_feature_selection table)
  </what-built>
  <how-to-verify>
  1. Review the summary report printed above
  2. Check configs/feature_selection.yaml — does the active set make intuitive sense?
     - Are the expected strong features (e.g., momentum, mean-reversion indicators) in the active tier?
     - Are obviously noisy features (e.g., raw returns without smoothing) in watch/archive?
     - Do conditional features have clear regime associations?
  3. If any features seem miscategorized:
     - "promote X" — move feature X to a higher tier
     - "demote X" — move feature X to a lower tier
     - "adjust cutoff to 0.25" — re-run with different IC-IR threshold
  4. Verify the feature count: active + conditional should be in the 15-25 range
  5. Check that the concordance report shows reasonable agreement between IC-IR and MDA

  After review, the YAML will be committed as the source of truth for downstream phases.
  </how-to-verify>
  <resume-signal>Type "approved" to accept the feature set as-is, or describe adjustments (e.g., "promote vol_30d to active", "re-run with cutoff 0.25", "move all MACD variants to watch").</resume-signal>
</task>

</tasks>

<verification>
1. User has reviewed and approved (or adjusted) the feature selection
2. configs/feature_selection.yaml reflects the final approved set
3. dim_feature_selection table matches the final YAML
4. Active + conditional tier count is in the 15-25 range (or user accepted a different count with rationale)
</verification>

<success_criteria>
- User has explicitly approved the feature selection output
- The YAML config is finalized and ready for downstream consumption by Phase 82 (expression engine, regime router, bake-off)
- Any user adjustments have been applied to both YAML and DB
</success_criteria>

<output>
After completion, create `.planning/phases/80-ic-analysis-feature-selection/80-05-SUMMARY.md`
</output>
