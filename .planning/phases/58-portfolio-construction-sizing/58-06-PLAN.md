---
phase: 58-portfolio-construction-sizing
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ta_lab2/scripts/portfolio/run_portfolio_backtest.py
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "TurnoverTracker.track() called inside _run_backtest() per-rebalance loop, accumulating turnover_pct and cost_pct"
    - "Per-strategy statistics print gross_return, turnover_cost, and net_return separately (decomposed cost reporting)"
    - "Signal probabilities loaded from cmc_meta_label_results.trade_probability per asset per rebalance, varying across assets"
    - "BetSizer receives varying probabilities so different assets get different position scales"
  artifacts:
    - path: "src/ta_lab2/scripts/portfolio/run_portfolio_backtest.py"
      provides: "TurnoverTracker wiring + real signal probabilities"
      contains: "TurnoverTracker"
  key_links:
    - from: "run_portfolio_backtest.py"
      to: "TurnoverTracker"
      via: "from ta_lab2.portfolio import TurnoverTracker"
      pattern: "tracker\\.track\\("
    - from: "run_portfolio_backtest.py"
      to: "cmc_meta_label_results"
      via: "SQL query for trade_probability"
      pattern: "cmc_meta_label_results"
---

<objective>
Wire TurnoverTracker into the portfolio backtest loop and replace hardcoded 0.6 signal probabilities with real trade_probability values from cmc_meta_label_results.

Purpose: Close PORT-03 (turnover tracking absent from backtest output) and PORT-04 (hardcoded uniform probability defeats bet sizing demonstration). These are the two gaps in run_portfolio_backtest.py identified by verification.

Output: Updated run_portfolio_backtest.py with decomposed cost reporting (gross/turnover/net) and probability-varying bet sizing.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/58-portfolio-construction-sizing/58-VERIFICATION.md
@.planning/phases/58-portfolio-construction-sizing/58-05-SUMMARY.md
@.planning/phases/58-portfolio-construction-sizing/58-CONTEXT.md

Key files:
@src/ta_lab2/scripts/portfolio/run_portfolio_backtest.py
@src/ta_lab2/portfolio/cost_tracker.py
@src/ta_lab2/portfolio/__init__.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire TurnoverTracker into _run_backtest and add decomposed cost reporting</name>
  <files>src/ta_lab2/scripts/portfolio/run_portfolio_backtest.py</files>
  <action>
Modify _run_backtest() to track turnover costs per rebalance period:

1. Add TurnoverTracker to the import from ta_lab2.portfolio at line 193 (inside _strategy_topk_dropout is wrong scope -- add a top-of-function import or pass tracker in).

2. In _run_backtest() function (line 318):
   - Create a TurnoverTracker instance at the start: `tracker = TurnoverTracker(config)`.
   - Inside the rebalance block (around line 374-383), after `current_weights = new_weights`:
     - Call `tracker.track(ts=ts, old_weights=old_weights_before, new_weights=current_weights, gross_return=period_return, portfolio_value=1.0)`.
     - To do this, save `old_weights_before = dict(current_weights)` BEFORE the strategy_fn call.
   - Change the return value from `pd.Series` to a tuple `(pd.Series, TurnoverTracker)` so the caller can access cost history.

3. In run_backtest() function (line 415), update the call to _run_backtest() to unpack the tuple:
   - `rets, tracker = _run_backtest(prices, strat, config)`
   - Store trackers per strategy: `trackers[strat] = tracker`

4. In the "Per-Strategy Statistics" print block (line 527-540), add decomposed cost reporting:
   - Compute cumulative turnover cost from tracker.history: `sum(r["cost_pct"] for r in tracker.history)`
   - Compute gross return (existing total_return) and net return (gross - cumulative_cost)
   - Print: `gross_ret=+X.XX%  turnover_cost=X.XX%  net_ret=+X.XX%  ann_vol=X.XX%  max_dd=X.XX%  n_periods=N`

5. Do NOT change the Sharpe comparison section -- it should continue using the raw (gross) returns for Sharpe calculation so the delta comparison remains apples-to-apples. The decomposed cost appears only in the per-strategy stats block.

IMPORTANT: The period_return computation (lines 389-396) happens AFTER the rebalance decision. The tracker.track() call should use the PREVIOUS period's gross_return (or 0.0 for the first period). Since tracker.track() needs gross_return for the period when the rebalance happened, call it right after computing period_return with the gross_return for that period. The key is: track the weight change that occurred at the START of this period, paired with the gross return earned DURING this period.

Pattern: Save old weights -> rebalance -> compute period return -> track(old, new, gross_return).
  </action>
  <verify>
    - `python -c "from ta_lab2.scripts.portfolio.run_portfolio_backtest import _run_backtest; print('import OK')"` succeeds
    - grep for "TurnoverTracker" in run_portfolio_backtest.py shows at least 3 occurrences (import, instantiation, track call)
    - grep for "turnover_cost" in run_portfolio_backtest.py shows it appears in the stats print block
    - grep for "net_ret" in run_portfolio_backtest.py shows decomposed reporting
  </verify>
  <done>
    TurnoverTracker instantiated in _run_backtest, track() called on each rebalance period, and per-strategy stats print gross_ret, turnover_cost, and net_ret separately.
  </done>
</task>

<task type="auto">
  <name>Task 2: Replace hardcoded 0.6 probability with real trade_probability from DB</name>
  <files>src/ta_lab2/scripts/portfolio/run_portfolio_backtest.py</files>
  <action>
Replace the hardcoded `probs = {a: 0.6 for a in held_weights}` (line 216) with real signal probabilities:

1. Add a module-level helper function `_load_signal_probabilities(engine, tf: str) -> pd.DataFrame`:
   - Query cmc_meta_label_results for the latest trade_probability per asset_id:
     ```sql
     SELECT DISTINCT ON (asset_id)
       asset_id AS id, trade_probability, ts
     FROM cmc_meta_label_results
     WHERE trade_probability IS NOT NULL
     ORDER BY asset_id, ts DESC
     ```
   - Return DataFrame with columns [id, trade_probability].
   - If the table does not exist or returns 0 rows, return an empty DataFrame and log a warning.
   - Wrap the query in try/except to handle the case where cmc_meta_label_results has not been populated yet (Phase 57 may not have been run on this DB).

2. In run_backtest() (line 415), after loading prices and before running strategies:
   - Call `prob_df = _load_signal_probabilities(engine, tf)` once.
   - Convert to a dict: `signal_probs_map = dict(zip(prob_df["id"], prob_df["trade_probability"]))` if not empty, else empty dict.
   - Pass `signal_probs_map` to _run_backtest() as a new parameter.

3. Update _run_backtest() signature to accept `signal_probs: dict[int, float] | None = None`.

4. Update _strategy_topk_dropout() signature to accept `signal_probs: dict[int, float] | None = None`.

5. In _strategy_topk_dropout() (line 215-216), replace the hardcoded line:
   ```python
   # OLD: probs = {a: 0.6 for a in held_weights}
   # NEW: Use real probabilities, fall back to 0.6 if unavailable
   default_prob = 0.6
   probs = {a: signal_probs.get(a, default_prob) if signal_probs else default_prob
            for a in held_weights}
   ```

6. In _run_backtest(), pass signal_probs through to _strategy_topk_dropout:
   ```python
   new_weights, new_holdings = strategy_fn(
       window, config, current_holdings, signal_probs=signal_probs
   )
   ```
   For other strategy functions (_strategy_fixed_sizing, _strategy_equal_weight, _strategy_per_asset), add **kwargs to their signatures so they accept and ignore signal_probs.

7. Update the strategy comment at line 215 from "stand-in for live signal confidence" to:
   "Real trade_probability from cmc_meta_label_results; falls back to 0.6 if unavailable."

IMPORTANT: Use try/except around the DB query. If cmc_meta_label_results doesn't exist, log a warning and fall back to empty dict (which triggers 0.6 fallback). This ensures backward compatibility.
  </action>
  <verify>
    - `python -c "from ta_lab2.scripts.portfolio.run_portfolio_backtest import _load_signal_probabilities; print('import OK')"` succeeds
    - grep for "cmc_meta_label_results" in run_portfolio_backtest.py shows the query
    - grep for "trade_probability" in run_portfolio_backtest.py shows usage
    - grep for "0.6" in run_portfolio_backtest.py shows it only as a fallback default, not as the primary value
    - The line `signal_probs = {a: 0.6 for a in held_weights}` no longer exists
  </verify>
  <done>
    Signal probabilities loaded from cmc_meta_label_results per asset. BetSizer receives varying probabilities so high-confidence assets get larger position scales. Falls back gracefully to 0.6 when meta-label data is unavailable.
  </done>
</task>

</tasks>

<verification>
1. `python -c "from ta_lab2.scripts.portfolio.run_portfolio_backtest import run_backtest; print('module loads')"` -- no import errors
2. grep confirms TurnoverTracker is instantiated and track() is called in _run_backtest
3. grep confirms "turnover_cost" and "net_ret" appear in the stats printing section
4. grep confirms "cmc_meta_label_results" query exists with trade_probability
5. grep confirms the old hardcoded `{a: 0.6 for a in held_weights}` pattern is gone
6. No `signal_probs = {a: 0.6` pattern remains -- 0.6 only appears as `default_prob = 0.6` fallback
</verification>

<success_criteria>
- TurnoverTracker.track() called per rebalance period in _run_backtest()
- Per-strategy stats print decomposed: gross_ret, turnover_cost, net_ret
- Signal probabilities sourced from cmc_meta_label_results with graceful fallback
- BetSizer receives per-asset varying probabilities
- Module imports cleanly with no errors
</success_criteria>

<output>
After completion, create `.planning/phases/58-portfolio-construction-sizing/58-06-SUMMARY.md`
</output>
