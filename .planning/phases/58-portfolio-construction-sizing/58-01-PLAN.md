---
phase: 58-portfolio-construction-sizing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - alembic/versions/XXXX_portfolio_tables.py
  - configs/portfolio.yaml
  - src/ta_lab2/portfolio/__init__.py
  - pyproject.toml
autonomous: true

must_haves:
  truths:
    - "Alembic migration creates cmc_portfolio_allocations table with correct schema"
    - "configs/portfolio.yaml is loadable and contains all optimizer/rebalancing/sizing config"
    - "PyPortfolioOpt is installable and importable"
    - "portfolio package is importable from ta_lab2"
  artifacts:
    - path: "alembic/versions/XXXX_portfolio_tables.py"
      provides: "cmc_portfolio_allocations DDL via Alembic"
      contains: "cmc_portfolio_allocations"
    - path: "configs/portfolio.yaml"
      provides: "Portfolio optimizer configuration"
      contains: "optimizer"
    - path: "src/ta_lab2/portfolio/__init__.py"
      provides: "Package init for portfolio module"
    - path: "pyproject.toml"
      provides: "PyPortfolioOpt dependency declaration"
      contains: "PyPortfolioOpt"
  key_links:
    - from: "alembic/versions/XXXX_portfolio_tables.py"
      to: "alembic chain"
      via: "down_revision points to latest existing migration"
      pattern: "down_revision.*=.*\"[a-f0-9]+\""
---

<objective>
Create the foundation for the portfolio construction module: Alembic migration for the
cmc_portfolio_allocations table, YAML config file, PyPortfolioOpt dependency, and the
portfolio package skeleton.

Purpose: All subsequent plans depend on the DB table existing, the config being loadable,
and PyPortfolioOpt being available. This is the common infrastructure layer.

Output: Alembic migration, configs/portfolio.yaml, src/ta_lab2/portfolio/__init__.py,
updated pyproject.toml.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/58-portfolio-construction-sizing/58-CONTEXT.md
@.planning/phases/58-portfolio-construction-sizing/58-RESEARCH.md
@alembic/versions/b5178d671e38_risk_controls.py
@pyproject.toml
@configs/default.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Alembic migration + YAML config + pyproject.toml</name>
  <files>
    alembic/versions/XXXX_portfolio_tables.py
    configs/portfolio.yaml
    pyproject.toml
  </files>
  <action>
1. Create Alembic migration for `cmc_portfolio_allocations` table. Find the current HEAD
   revision by reading the latest migration file in alembic/versions/ and use its revision ID
   as down_revision. Use `alembic revision --autogenerate` or create manually following the
   existing pattern (see b5178d671e38_risk_controls.py). Use ASCII-only comments (Windows
   cp1252 compatibility).

   Schema (from 58-RESEARCH.md):
   ```
   cmc_portfolio_allocations:
     alloc_id       UUID PRIMARY KEY DEFAULT gen_random_uuid()
     ts             TIMESTAMPTZ NOT NULL
     optimizer      TEXT NOT NULL          -- 'mv', 'cvar', 'hrp', 'bl'
     is_active      BOOLEAN NOT NULL DEFAULT FALSE
     regime_label   TEXT
     asset_id       INTEGER NOT NULL
     weight         NUMERIC NOT NULL       -- raw optimizer weight
     final_weight   NUMERIC                -- after bet sizing
     signal_score   NUMERIC
     condition_number NUMERIC
     config_snapshot  JSONB
     created_at     TIMESTAMPTZ DEFAULT now()

     UNIQUE (ts, optimizer, asset_id)
   ```
   Add indexes:
   - idx_portfolio_alloc_ts on (ts DESC)
   - idx_portfolio_alloc_active on (ts, is_active) WHERE is_active

   The downgrade() should drop the table and indexes.

2. Create `configs/portfolio.yaml` with the full config template from 58-RESEARCH.md:
   - optimizer section (lookback_days: 180, max_position_pct: 0.15, max_gross_exposure: 1.5, cvar_beta: 0.95, condition_number_threshold: 1000)
   - regime_routing (bear: cvar, stable: mv, uncertain: hrp, default: hrp)
   - rebalancing (mode: time_based, frequency: 1D, drift_threshold: 0.05, turnover_penalty: false, fee_bps: 10.0)
   - bet_sizing (mode: optimizer_first, w_parameter: 2.0, min_confidence: 0.2)
   - topk_selection (topk: 10, n_drop: 2)
   - black_litterman (tau: 0.05, use_idzorek: true, min_view_confidence: 0.2, max_view_confidence: 0.8, views_source: all_active)
   - risk_integration (mode: constraints_in_optimizer)
   - cash_management (redistribute_first: true, yield_instruments: [])

3. Update `pyproject.toml` to add a `portfolio` optional dependency group:
   ```toml
   portfolio = [
     "PyPortfolioOpt>=1.5.6",
   ]
   ```
   Add it after the existing `viz` group. Pin to >=1.5.6 (allows 1.6.0+).

4. Run `pip install -e ".[portfolio]"` to install PyPortfolioOpt and verify import works.
  </action>
  <verify>
    - `python -c "from pypfopt import EfficientFrontier, EfficientCVaR, HRPOpt, BlackLittermanModel; print('OK')"` prints OK
    - `python -c "import yaml; yaml.safe_load(open('configs/portfolio.yaml'))"` loads without error
    - `alembic heads` shows the new migration as head
    - `alembic upgrade head` applies the migration (creates cmc_portfolio_allocations)
  </verify>
  <done>cmc_portfolio_allocations table exists in DB, configs/portfolio.yaml loadable, PyPortfolioOpt importable</done>
</task>

<task type="auto">
  <name>Task 2: Portfolio package skeleton</name>
  <files>
    src/ta_lab2/portfolio/__init__.py
  </files>
  <action>
Create `src/ta_lab2/portfolio/__init__.py` with docstring describing the module purpose
and placeholder imports that will be filled as subsequent plans create the modules:

```python
"""Portfolio construction and position sizing.

Modules:
    optimizer       -- MV, CVaR, HRP portfolio optimization wrappers
    black_litterman -- BL allocation with market cap prior and signal views
    bet_sizing      -- Probability-based position scaling
    topk_selector   -- TopkDropout asset selection with turnover control
    rebalancer      -- Time/signal/threshold rebalancing scheduler
    cost_tracker    -- Turnover cost decomposition and tracking
"""
```

Also create a minimal config loader utility function in the __init__.py that reads
configs/portfolio.yaml using yaml.safe_load and returns a dict. This will be used by
all other modules:

```python
def load_portfolio_config(path: str = "configs/portfolio.yaml") -> dict:
    """Load portfolio configuration from YAML file."""
    import yaml
    from pathlib import Path
    with open(Path(path), "r") as f:
        return yaml.safe_load(f)
```
  </action>
  <verify>
    - `python -c "from ta_lab2.portfolio import load_portfolio_config; cfg = load_portfolio_config(); print(cfg['optimizer']['lookback_days'])"` prints 180
  </verify>
  <done>ta_lab2.portfolio package importable with config loader returning valid config dict</done>
</task>

</tasks>

<verification>
- `alembic upgrade head` succeeds
- `python -c "from pypfopt import EfficientFrontier; print('PyPortfolioOpt OK')"` passes
- `python -c "from ta_lab2.portfolio import load_portfolio_config; print(load_portfolio_config()['regime_routing'])"` shows regime mapping
- configs/portfolio.yaml is valid YAML with all sections present
</verification>

<success_criteria>
1. Alembic migration creates cmc_portfolio_allocations with UUID PK, UNIQUE(ts, optimizer, asset_id), and both indexes
2. configs/portfolio.yaml contains all 8 config sections (optimizer, regime_routing, rebalancing, bet_sizing, topk_selection, black_litterman, risk_integration, cash_management)
3. PyPortfolioOpt >=1.5.6 installed and importable
4. ta_lab2.portfolio package importable with load_portfolio_config() returning valid dict
</success_criteria>

<output>
After completion, create `.planning/phases/58-portfolio-construction-sizing/58-01-SUMMARY.md`
</output>
