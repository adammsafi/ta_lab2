---
phase: 70-cross-asset-aggregation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - alembic/versions/e1f2a3b4c5d6_cross_asset_aggregation_tables.py
  - configs/cross_asset_config.yaml
autonomous: true

must_haves:
  truths:
    - "Three new tables exist in PostgreSQL: cmc_cross_asset_agg, cmc_funding_rate_agg, crypto_macro_corr_regimes"
    - "cmc_macro_regimes table has a crypto_macro_corr TEXT column"
    - "All thresholds are YAML-configurable, not hardcoded"
  artifacts:
    - path: "alembic/versions/e1f2a3b4c5d6_cross_asset_aggregation_tables.py"
      provides: "DDL for 3 new tables + ALTER TABLE for crypto_macro_corr column"
      contains: "cmc_cross_asset_agg"
    - path: "configs/cross_asset_config.yaml"
      provides: "All XAGG thresholds and configuration"
      contains: "high_corr_threshold"
  key_links:
    - from: "alembic/versions/e1f2a3b4c5d6_cross_asset_aggregation_tables.py"
      to: "d5e6f7a8b9c0"
      via: "down_revision chain"
      pattern: "down_revision.*d5e6f7a8b9c0"
    - from: "configs/cross_asset_config.yaml"
      to: "src/ta_lab2/macro/cross_asset.py"
      via: "load_cross_asset_config() reads YAML at runtime"
      pattern: "cross_asset_config\\.yaml"
---

<objective>
Create the database foundation for Phase 70 Cross-Asset Aggregation: Alembic migration with 3 new tables (cmc_cross_asset_agg, cmc_funding_rate_agg, crypto_macro_corr_regimes), ALTER TABLE to add crypto_macro_corr column to cmc_macro_regimes, and YAML configuration file for all thresholds.

Purpose: Plans 02 and 03 cannot execute without these tables and config existing.
Output: Alembic migration file + YAML config file. Tables deployable via `alembic upgrade head`.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/70-cross-asset-aggregation/70-CONTEXT.md
@.planning/phases/70-cross-asset-aggregation/70-RESEARCH.md

# Pattern references
@alembic/versions/d5e6f7a8b9c0_macro_regime_tables.py
@configs/macro_regime_config.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Alembic migration for 3 new tables + ALTER TABLE</name>
  <files>alembic/versions/e1f2a3b4c5d6_cross_asset_aggregation_tables.py</files>
  <action>
Create Alembic migration with revision ID `e1f2a3b4c5d6`, down_revision `d5e6f7a8b9c0`.

Table 1: `cmc_cross_asset_agg` (crypto-only market-wide metrics per date)
- PK: `(date)` as DATE NOT NULL
- Columns: `btc_eth_corr_30d` FLOAT, `avg_pairwise_corr_30d` FLOAT, `high_corr_flag` BOOLEAN, `n_assets` INTEGER, `ingested_at` TIMESTAMPTZ NOT NULL DEFAULT now()
- Index: `idx_cmc_cross_asset_agg_date` on `date DESC`

Table 2: `cmc_funding_rate_agg` (aggregate funding rate signal per symbol per date)
- PK: `(date, symbol)` where date is DATE NOT NULL, symbol is TEXT NOT NULL
- Columns: `avg_funding_rate` FLOAT, `vwap_funding_rate` FLOAT (nullable), `n_venues` INTEGER, `zscore_30d` FLOAT, `zscore_90d` FLOAT, `venues_included` TEXT, `ingested_at` TIMESTAMPTZ NOT NULL DEFAULT now()
- Index: `idx_cmc_funding_rate_agg_symbol` on `(symbol, date DESC)`

Table 3: `crypto_macro_corr_regimes` (crypto-macro correlation per asset per macro variable)
- PK: `(date, asset_id, macro_var)` where date is DATE NOT NULL, asset_id is INTEGER NOT NULL, macro_var is TEXT NOT NULL
- Columns: `corr_60d` FLOAT, `prev_corr_60d` FLOAT, `sign_flip_flag` BOOLEAN DEFAULT FALSE, `corr_regime` TEXT, `ingested_at` TIMESTAMPTZ NOT NULL DEFAULT now()
- Index: `idx_crypto_macro_corr_regimes_asset` on `(asset_id, date DESC)`
- Index: `idx_crypto_macro_corr_regimes_flip` on `(sign_flip_flag)` WHERE `sign_flip_flag = TRUE` (partial index for quick sign-flip queries)

ALTER TABLE: Add `crypto_macro_corr` TEXT column to `cmc_macro_regimes` (nullable, no default -- labels: 'correlated', 'decorrelated', 'flipping').

NOTE on naming: `cmc_cross_asset_agg` and `cmc_funding_rate_agg` use `cmc_` prefix because their source assets come from cmc_price_histories7/tvc_price_histories and cmc_funding_rates respectively. `crypto_macro_corr_regimes` drops the prefix because it mixes crypto with non-crypto (macro) data per CONTEXT.md naming rule.

Follow the exact pattern from `d5e6f7a8b9c0_macro_regime_tables.py`: import `op` and `sa` from alembic/sqlalchemy, define `upgrade()` and `downgrade()` functions. Downgrade should `op.drop_table()` in reverse order and `op.drop_column('cmc_macro_regimes', 'crypto_macro_corr')`.
  </action>
  <verify>
Run `python -m alembic check` to verify no pending migrations conflict. Then run `python -m alembic upgrade head` to apply. Verify with SQL: `SELECT table_name FROM information_schema.tables WHERE table_name IN ('cmc_cross_asset_agg', 'cmc_funding_rate_agg', 'crypto_macro_corr_regimes')` returns 3 rows. Verify column: `SELECT column_name FROM information_schema.columns WHERE table_name = 'cmc_macro_regimes' AND column_name = 'crypto_macro_corr'` returns 1 row.
  </verify>
  <done>All 3 tables exist in PostgreSQL with correct PKs, indexes, and column types. cmc_macro_regimes has new crypto_macro_corr TEXT column. Alembic history is clean with no branch conflicts.</done>
</task>

<task type="auto">
  <name>Task 2: YAML configuration for cross-asset thresholds</name>
  <files>configs/cross_asset_config.yaml</files>
  <action>
Create `configs/cross_asset_config.yaml` following the same pattern as `configs/macro_regime_config.yaml` (top-level sections, no nested profiles for now).

```yaml
# Cross-Asset Aggregation Configuration
# Phase 70: All thresholds for cross-asset signal computation.
# Changing any threshold here changes behavior without code changes (XAGG).

cross_asset:
  btc_eth_corr_window: 30        # Rolling window (calendar days) for BTC/ETH correlation
  avg_pairwise_corr_window: 30   # Rolling window for average pairwise correlation
  high_corr_threshold: 0.7       # Above this = macro-driven market (XAGG-02)
  min_assets_for_avg: 3          # Minimum assets with data to compute avg pairwise corr

crypto_macro:
  corr_window: 60                # Rolling window (calendar days) for crypto-macro correlation
  sign_flip_threshold: 0.3       # Magnitude threshold: >0.3 to <-0.3 (or vice versa) = sign flip
  macro_vars:                    # Macro variables for correlation (from fred.fred_macro_features)
    - vix                        # Maps to vixcls column
    - dxy                        # Maps to dtwexbgs column
    - hy_oas                     # Maps to bamlh0a0hym2 column
    - net_liquidity              # Maps to net_liquidity column
  # Column mapping: macro_var label -> fred_macro_features column name
  macro_var_columns:
    vix: vixcls
    dxy: dtwexbgs
    hy_oas: bamlh0a0hym2
    net_liquidity: net_liquidity

funding_agg:
  zscore_windows: [30, 90]       # Rolling windows for z-score computation
  venues:                        # Venues to include in aggregate
    - binance
    - hyperliquid
    - bybit
    - dydx
    - aevo
    - aster

portfolio_override:
  high_corr_blend_factor: 0.3    # When high_corr_flag=True, blend covariance toward 1.0 by this factor
  enabled: true                  # Master switch for portfolio covariance override

telegram:
  sign_flip_alerts: true         # Send Telegram alert on crypto-macro sign flip
```
  </action>
  <verify>Run `python -c "import yaml; c = yaml.safe_load(open('configs/cross_asset_config.yaml')); assert 'cross_asset' in c; assert 'crypto_macro' in c; assert 'funding_agg' in c; assert c['cross_asset']['high_corr_threshold'] == 0.7; print('Config OK')"` succeeds.</verify>
  <done>YAML config file exists with all thresholds for XAGG-01 through XAGG-04 plus portfolio override and Telegram alert settings. All values match CONTEXT.md decisions.</done>
</task>

</tasks>

<verification>
1. `python -m alembic current` shows revision `e1f2a3b4c5d6`
2. All 3 tables exist and accept INSERT statements
3. `cmc_macro_regimes.crypto_macro_corr` column accepts TEXT values
4. YAML config loads without errors and contains all required sections
</verification>

<success_criteria>
- Alembic migration applied cleanly, head is `e1f2a3b4c5d6`
- 3 new tables with correct schemas are queryable
- ALTER TABLE column added to cmc_macro_regimes
- YAML config has all 6 top-level sections with correct values
</success_criteria>

<output>
After completion, create `.planning/phases/70-cross-asset-aggregation/70-01-SUMMARY.md`
</output>
