---
phase: 32-runbooks
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/operations/NEW_ASSET_ONBOARDING.md
  - docs/operations/DISASTER_RECOVERY.md
  - mkdocs.yml
autonomous: true

must_haves:
  truths:
    - "An operator can onboard a new crypto asset end-to-end using only NEW_ASSET_ONBOARDING.md"
    - "An operator can restore from backup or rebuild the database from scratch using only DISASTER_RECOVERY.md"
    - "The onboarding SOP uses ETH (id=2) as the worked example with verification queries at each step"
    - "The DR guide covers two scenarios: DB restore from backup and full rebuild from scratch"
    - "All 4 new runbooks plus existing operations docs appear in the mkdocs nav under an Operations section"
  artifacts:
    - path: "docs/operations/NEW_ASSET_ONBOARDING.md"
      provides: "New-asset onboarding SOP"
      contains: "dim_assets"
    - path: "docs/operations/DISASTER_RECOVERY.md"
      provides: "Disaster recovery guide"
      contains: "pg_dump"
    - path: "mkdocs.yml"
      provides: "Updated nav with Operations section"
      contains: "Operations"
  key_links:
    - from: "mkdocs.yml"
      to: "docs/operations/REGIME_PIPELINE.md"
      via: "nav entry"
      pattern: "REGIME_PIPELINE"
    - from: "mkdocs.yml"
      to: "docs/operations/BACKTEST_PIPELINE.md"
      via: "nav entry"
      pattern: "BACKTEST_PIPELINE"
    - from: "mkdocs.yml"
      to: "docs/operations/NEW_ASSET_ONBOARDING.md"
      via: "nav entry"
      pattern: "NEW_ASSET_ONBOARDING"
    - from: "mkdocs.yml"
      to: "docs/operations/DISASTER_RECOVERY.md"
      via: "nav entry"
      pattern: "DISASTER_RECOVERY"
---

<objective>
Write the New-Asset Onboarding SOP and Disaster Recovery guide (RUNB-03, RUNB-04), then add all operations docs to the mkdocs nav.

Purpose: Complete the runbook set and make all operations docs discoverable via the docs site.
Output: Two markdown runbooks + updated mkdocs.yml nav with Operations section
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# CRITICAL: Read this file FIRST — it contains all verified commands, flags, SQL queries, and content
@.planning/phases/32-runbooks/32-RESEARCH.md

# User's vision and decisions for runbook tone/structure
@.planning/phases/32-runbooks/32-CONTEXT.md

# Format reference
@docs/operations/DAILY_REFRESH.md

# Current nav structure to update
@mkdocs.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write NEW_ASSET_ONBOARDING.md SOP (RUNB-03)</name>
  <files>docs/operations/NEW_ASSET_ONBOARDING.md</files>
  <action>
Read the RUNB-03 section of `.planning/phases/32-runbooks/32-RESEARCH.md` thoroughly — it contains the exact 6-step process with commands, verification queries, and timing estimates.

Read the CONTEXT.md decisions: full end-to-end walkthrough using ETH (id=2) as example, each step has exact command + verification query, timing estimates per step, brief "Removing an asset" section at end.

Create `docs/operations/NEW_ASSET_ONBOARDING.md` with these sections:

1. **Quick Start** — The 6 commands in order for the impatient operator (just the commands, no explanation):
   - INSERT into dim_assets
   - Ingest price_histories7
   - `run_daily_refresh --bars --ids 2`
   - `run_daily_refresh --emas --ids 2`
   - Feature refresh + regime refresh
   - `run_daily_refresh --all --ids 2` (verify incremental)

2. **Prerequisites** — DB up, TARGET_DB_URL, Python environment, source data (CMC JSON/CSV) available for the asset.

3. **Step-by-Step Walkthrough (ETH, id=2)** — Tutorial-first tone. Each of the 6 steps as a subsection with:
   - What this step does (1-2 sentences)
   - Exact command(s) to run
   - Verification query to confirm success
   - Expected results / row counts where available
   - Timing estimate
   - Use all content from research RUNB-03 Steps 1-6

   **Step 1: Register in dim_assets** (~30 seconds)
   - SQL INSERT with ON CONFLICT DO NOTHING
   - Note about is_active column (check if exists, set if present)
   - Verification: SELECT from dim_assets

   **Step 2: Ingest price_histories7** (2-5 min)
   - Python upsert_cmc_history function call
   - Verification: MIN/MAX timeclose + COUNT
   - Note about ETH data starting ~2015-08-07

   **Step 3: Build multi-TF bars** (5-10 min)
   - `run_daily_refresh --bars --ids 2 --verbose`
   - Lists all 6 bar builders that run
   - Verification: 1D bar count + multi-TF coverage query

   **Step 4: Compute EMAs** (5-15 min)
   - `run_daily_refresh --emas --ids 2 --verbose`
   - Lists all 4 EMA refreshers
   - Verification: EMA coverage by tf/period query

   **Step 5: Compute features and regimes** (3-7 min)
   - Two commands: feature refresh then regime refresh
   - Verification: cmc_features row count + regime_inspect

   **Step 6: Verify incremental refresh** (2-5 min)
   - `run_daily_refresh --all --ids 2 --verbose`
   - Verification: asset_data_coverage query

4. **Total Time Estimate** — Sum of per-step estimates (18-43 minutes for one asset with full history).

5. **Removing an Asset** — The ordered DELETE queries from research (FK-aware order). Include comment about which deletes are "just derived data" vs "source data" vs "registry".

6. **See Also** — Links to DAILY_REFRESH.md, STATE_MANAGEMENT.md.

Tone: Tutorial-first — this is a walkthrough, not a reference. The reader follows along step by step.
  </action>
  <verify>
Verify the file exists and contains key sections:
- `ls docs/operations/NEW_ASSET_ONBOARDING.md`
- Confirm 6 steps are present with ETH (id=2) examples
- Confirm each step has command + verification query + timing
- Confirm "Removing an Asset" section exists with DELETE queries
  </verify>
  <done>
docs/operations/NEW_ASSET_ONBOARDING.md exists as a complete 6-step onboarding SOP using ETH (id=2) as the example, with verification queries at every step, timing estimates, and a removal section.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write DISASTER_RECOVERY.md guide (RUNB-04)</name>
  <files>docs/operations/DISASTER_RECOVERY.md</files>
  <action>
Read the RUNB-04 section of `.planning/phases/32-runbooks/32-RESEARCH.md` thoroughly — it contains the complete schema creation order (12 steps), rebuild pipeline, recovery time estimates, and all SQL/bash commands.

Read the CONTEXT.md decisions: two scenarios (DB loss, full rebuild), manual pg_dump/pg_restore, recommend automation, "rebuild from scratch" means re-ingest from CoinMarketCap source.

Create `docs/operations/DISASTER_RECOVERY.md` with these sections:

1. **Overview** — Brief statement that this covers two DR scenarios. No automated backup exists yet — this documents manual procedures.

2. **Prerequisites** — Environment variables (TARGET_DB_URL with fallback chain: TARGET_DB_URL -> DB_URL -> MARKETDATA_DB_URL), Python environment setup, PostgreSQL client tools (psql, pg_dump, pg_restore, createdb).

3. **Scenario 1: Restore from Backup** — Reference-first tone.
   - **Creating a Backup** (while DB is healthy): pg_dump command with gzip, recommended schedule (daily cron).
   - **Restoring from Backup**: gunzip + psql restore command.
   - **Verification**: 5 COUNT queries for key tables (price_histories7, bars, emas, features, regimes).
   - **After Restore**: Run incremental refresh to catch up (`run_daily_refresh --all`).
   - **Alembic note**: If Alembic is set up (Phase 33), run `alembic stamp head` after restore to mark the migration state as current.
   - Note: Recommend setting up automated daily backups as a future improvement.

4. **Scenario 2: Full Rebuild from Scratch** — The nuclear option.
   - **When to use**: Complete DB loss with no backup, or intentional clean-slate rebuild.
   - **Phase A: Schema Creation** — All 12 steps from research with exact `psql -f` commands for every SQL file in dependency order. Include the Windows encoding note (use psql directly, not Python open() with default encoding).
   - **Phase B: Source Data Ingest** — The upsert_cmc_history function call pattern. Note this is per-asset and requires CMC data files.
   - **Phase C: Derived Data Rebuild** — All rebuild commands from research in order: bars -> EMAs -> features -> regimes -> signals. Note about backtests (no automated full-rebuild script, run per config).
   - **Phase D: Verification** — The UNION ALL count query from research for key tables.

5. **Recovery Time Estimates** — Table from research with scenario, estimate, and notes for each phase (restore from backup, schema recreation, price ingest, bars, EMAs, features, regimes, signals).

6. **SQL File Reference** — Directory tree showing sql/ structure from research (ddl/, features/, lookups/, regimes/, signals/, backtests/, views/, migration/).

7. **See Also** — Links to DAILY_REFRESH.md, NEW_ASSET_ONBOARDING.md, STATE_MANAGEMENT.md.

Tone: Reference-first with clear step numbering. This is a crisis document — clarity and correctness over prose. Every command must be copy-pasteable.
  </action>
  <verify>
Verify the file exists and contains key sections:
- `ls docs/operations/DISASTER_RECOVERY.md`
- Confirm two scenarios are documented
- Confirm Phase A has all 12 schema creation steps with psql commands
- Confirm recovery time estimates table exists
- Confirm pg_dump/pg_restore commands are present
- Confirm "alembic stamp head" is mentioned
  </verify>
  <done>
docs/operations/DISASTER_RECOVERY.md exists covering two DR scenarios (restore from backup and full rebuild from scratch), with 12-step schema creation, derived data rebuild pipeline, recovery time estimates, and the alembic stamp head note.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add Operations section to mkdocs nav</name>
  <files>mkdocs.yml</files>
  <action>
Read the current `mkdocs.yml` file.

Add an "Operations" section to the nav between "Components" and "Deployment" (or wherever it fits logically — after the design/components sections but before reference/changelog). The Operations section should include all 6 operations docs:

```yaml
  - Operations:
    - Daily Refresh: operations/DAILY_REFRESH.md
    - Regime Pipeline: operations/REGIME_PIPELINE.md
    - Backtest Pipeline: operations/BACKTEST_PIPELINE.md
    - New Asset Onboarding: operations/NEW_ASSET_ONBOARDING.md
    - Disaster Recovery: operations/DISASTER_RECOVERY.md
    - State Management: operations/STATE_MANAGEMENT.md
```

Preserve all existing nav entries unchanged. Only add the Operations section.

IMPORTANT: Use plain page paths (e.g., `operations/DAILY_REFRESH.md`), NOT anchored paths (e.g., `operations/DAILY_REFRESH.md#section`). mkdocs --strict treats anchored nav entries as missing file references (per Phase 31-03 decision).
  </action>
  <verify>
- Run `python -m mkdocs build --strict` from the project root and verify it exits 0 with no errors about missing pages.
- If mkdocs build fails due to the two new files from Plan 01 not existing yet (REGIME_PIPELINE.md, BACKTEST_PIPELINE.md), that is expected if Plan 02 runs before Plan 01. In that case, verify the nav YAML syntax is correct by checking mkdocs.yml manually.
- Confirm "Operations" appears in the nav with all 6 entries.
  </verify>
  <done>
mkdocs.yml has an Operations nav section with entries for DAILY_REFRESH.md, REGIME_PIPELINE.md, BACKTEST_PIPELINE.md, NEW_ASSET_ONBOARDING.md, DISASTER_RECOVERY.md, and STATE_MANAGEMENT.md. mkdocs build --strict passes (when all files exist).
  </done>
</task>

</tasks>

<verification>
Phase-level verification for this plan:
1. Both new runbook files exist under docs/operations/
2. mkdocs.yml has Operations section with all 6 entries
3. Onboarding SOP has 6 steps with ETH (id=2) examples
4. DR guide has two scenarios with complete rebuild procedure
5. `mkdocs build --strict` passes (may need both Plan 01 and Plan 02 complete)
</verification>

<success_criteria>
- docs/operations/NEW_ASSET_ONBOARDING.md is a complete 6-step SOP for adding a new crypto asset (RUNB-03)
- docs/operations/DISASTER_RECOVERY.md covers backup/restore and full rebuild from scratch (RUNB-04)
- mkdocs.yml nav includes Operations section with all 6 operations docs
- All commands and SQL from runbooks match the verified research in 32-RESEARCH.md
</success_criteria>

<output>
After completion, create `.planning/phases/32-runbooks/32-02-SUMMARY.md`
</output>
