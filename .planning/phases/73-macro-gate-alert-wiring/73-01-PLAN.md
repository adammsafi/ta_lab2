---
phase: 73-macro-gate-alert-wiring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ta_lab2/executor/paper_executor.py
  - src/ta_lab2/scripts/executor/run_paper_executor.py
  - src/ta_lab2/scripts/run_daily_refresh.py
  - .planning/REQUIREMENTS.md
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "PaperExecutor constructs RiskEngine with MacroGateEvaluator injected"
    - "run_daily_refresh.py runs evaluate_macro_gates after macro regime refresh"
    - "run_daily_refresh.py runs run_macro_alerts after macro regime refresh"
    - "FRED-01..07 and MREG-01..09 checkboxes marked complete in REQUIREMENTS.md"
  artifacts:
    - path: "src/ta_lab2/executor/paper_executor.py"
      provides: "MacroGateEvaluator injected into RiskEngine"
      contains: "MacroGateEvaluator"
    - path: "src/ta_lab2/scripts/run_daily_refresh.py"
      provides: "Gate evaluation and macro alerts in pipeline"
      contains: "evaluate_macro_gates"
---

<objective>
Close all 3 integration gaps from v1.0.1-MILESTONE-AUDIT.md:
1. Inject MacroGateEvaluator into PaperExecutor so Gate 1.7 is live
2. Add evaluate_macro_gates to run_daily_refresh.py
3. Add run_macro_alerts to run_daily_refresh.py
4. Fix requirements checkbox formatting

These are small wiring fixes (5-10 lines each), not new features.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Inject MacroGateEvaluator into PaperExecutor</name>
  <files>
    src/ta_lab2/executor/paper_executor.py
    src/ta_lab2/scripts/executor/run_paper_executor.py
  </files>
  <action>
In paper_executor.py __init__:
- Import MacroGateEvaluator
- Create instance and pass to RiskEngine

In run_paper_executor.py:
- No changes needed (PaperExecutor handles internally)
  </action>
</task>

<task type="auto">
  <name>Task 2: Add gate evaluation and macro alerts to daily pipeline</name>
  <files>
    src/ta_lab2/scripts/run_daily_refresh.py
  </files>
  <action>
Add run_evaluate_macro_gates() and run_macro_alerts() functions following existing patterns.
Wire into pipeline after macro_regimes step.
  </action>
</task>

<task type="auto">
  <name>Task 3: Fix requirements checkboxes</name>
  <files>
    .planning/REQUIREMENTS.md
  </files>
  <action>
Mark FRED-01..07 and MREG-01..09 checkboxes as [x] (traceability table already shows Complete).
  </action>
</task>

</tasks>
