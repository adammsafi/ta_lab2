---
phase: 78-table-drops-script-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ta_lab2/features/m_tf/views.py
  - src/ta_lab2/scripts/regimes/regime_data_loader.py
  - src/ta_lab2/scripts/regimes/refresh_regimes.py
  - src/ta_lab2/scripts/desc_stats/refresh_asset_stats.py
  - src/ta_lab2/scripts/desc_stats/refresh_cross_asset_corr.py
  - src/ta_lab2/scripts/features/run_all_feature_refreshes.py
  - src/ta_lab2/scripts/features/validate_features.py
  - src/ta_lab2/scripts/features/stats/refresh_features_stats.py
  - src/ta_lab2/macro/cross_asset.py
  - src/ta_lab2/macro/lead_lag_analyzer.py
  - src/ta_lab2/experiments/runner.py
autonomous: true

must_haves:
  truths:
    - "all_emas view points at ema_multi_tf_u (not ema_multi_tf)"
    - "No runtime Python code queries siloed data table names -- all queries use _u tables"
    - "Category E files (regime_data_loader, refresh_asset_stats, etc.) work against _u tables"
  artifacts:
    - path: "src/ta_lab2/features/m_tf/views.py"
      provides: "Updated VIEW_ALL_EMAS_SQL pointing at ema_multi_tf_u"
      contains: "ema_multi_tf_u"
    - path: "src/ta_lab2/scripts/regimes/regime_data_loader.py"
      provides: "Queries redirected to price_bars_multi_tf_u"
      contains: "price_bars_multi_tf_u"
    - path: "src/ta_lab2/scripts/desc_stats/refresh_asset_stats.py"
      provides: "SOURCE_TABLE redirected to returns_bars_multi_tf_u"
      contains: "returns_bars_multi_tf_u"
  key_links:
    - from: "src/ta_lab2/features/m_tf/views.py"
      to: "public.ema_multi_tf_u"
      via: "VIEW_ALL_EMAS_SQL constant"
      pattern: "FROM.*ema_multi_tf_u"
    - from: "src/ta_lab2/scripts/regimes/regime_data_loader.py"
      to: "public.price_bars_multi_tf_u"
      via: "load_bars_for_tf SQL queries"
      pattern: "FROM public\\.price_bars_multi_tf_u"
---

<objective>
Migrate the all_emas view and fix all runtime Python code that queries siloed table names, redirecting them to _u tables before any table drops occur.

Purpose: Without this, dropping the 30 siloed tables will break the all_emas view (blocking the ema_multi_tf DROP entirely) and break ~10 runtime scripts that hardcode siloed table names in SQL queries.

Output: Updated view definition in DB and Python source, all runtime SELECT queries redirected from siloed tables to _u equivalents.

**NOTE on ta_feature.py and daily_features_view.py (Category E):** These files were checked and do NOT reference siloed tables at runtime. ta_feature.py inherits SOURCE_TABLE = "public.price_bars_multi_tf_u" from base_feature.py (already _u). daily_features_view.py uses SOURCE_TABLES dict with "price_bars_multi_tf_u" and "returns_bars_multi_tf_u" (already _u). Both have siloed table names only in docstrings/comments, which are harmless and do not need updating.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/78-table-drops-script-cleanup/78-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate all_emas view to ema_multi_tf_u</name>
  <files>src/ta_lab2/features/m_tf/views.py</files>
  <action>
Two changes required:

1. **Update VIEW_ALL_EMAS_SQL constant in views.py**: Change `FROM public.ema_multi_tf` to `FROM public.ema_multi_tf_u`. The schema is compatible -- ema_multi_tf_u has all the same columns (id, ts, tf, tf_days, period, ema, roll) plus alignment_source which the view does not select, so no column changes needed in the SELECT list.

2. **Execute the view recreation in the live DB**: Run the following SQL against the marketdata database:
   ```sql
   DROP VIEW IF EXISTS public.all_emas;
   CREATE OR REPLACE VIEW public.all_emas AS
   SELECT id, ts, tf, tf_days, period, ema, roll
   FROM public.ema_multi_tf_u;
   ```
   Use the project's DB connection (TARGET_DB_URL env var or db_config.env).

Do NOT add an alignment_source filter -- the view should expose all alignment sources (matching the old behavior where each siloed table had its own rows).

Important: The view MUST be recreated BEFORE Plan 03 drops ema_multi_tf, otherwise the DROP will fail with a dependency error.
  </action>
  <verify>
Run: `python -c "from sqlalchemy import create_engine, text; import os; e = create_engine(os.environ.get('TARGET_DB_URL', 'postgresql://localhost/marketdata')); r = e.connect().execute(text(\"SELECT * FROM public.all_emas LIMIT 5\")); print(r.fetchall())"` -- should return rows from ema_multi_tf_u.

Also verify: `python -c "from sqlalchemy import create_engine, text; import os; e = create_engine(os.environ.get('TARGET_DB_URL', 'postgresql://localhost/marketdata')); r = e.connect().execute(text(\"SELECT definition FROM pg_views WHERE viewname = 'all_emas'\")); print(r.fetchone()[0])"` -- should show `FROM public.ema_multi_tf_u`.

Grep views.py for `ema_multi_tf_u` -- must appear in VIEW_ALL_EMAS_SQL.
  </verify>
  <done>all_emas view in DB and VIEW_ALL_EMAS_SQL in views.py both reference ema_multi_tf_u</done>
</task>

<task type="auto">
  <name>Task 2: Redirect all runtime siloed-table references to _u tables</name>
  <files>
    src/ta_lab2/scripts/regimes/regime_data_loader.py
    src/ta_lab2/scripts/regimes/refresh_regimes.py
    src/ta_lab2/scripts/desc_stats/refresh_asset_stats.py
    src/ta_lab2/scripts/desc_stats/refresh_cross_asset_corr.py
    src/ta_lab2/scripts/features/run_all_feature_refreshes.py
    src/ta_lab2/scripts/features/validate_features.py
    src/ta_lab2/scripts/features/stats/refresh_features_stats.py
    src/ta_lab2/macro/cross_asset.py
    src/ta_lab2/macro/lead_lag_analyzer.py
    src/ta_lab2/experiments/runner.py
  </files>
  <action>
For each file below, change SQL table references from siloed names to _u equivalents. These are all runtime queries that will break when siloed tables are dropped.

**IMPORTANT column compatibility notes:**
- `price_bars_multi_tf_u` has the same columns as `price_bars_multi_tf` PLUS `alignment_source` and uses `timestamp` (not `ts`) as time column -- same as siloed tables.
- `returns_bars_multi_tf_u` has the same columns as `returns_bars_multi_tf` PLUS `alignment_source` and uses `timestamp` as time column.
- When redirecting queries, add `AND alignment_source = 'default'` for queries that previously hit the base table (non-cal), OR omit the filter if the query should see all alignment sources. Use judgment: if the query was hitting the base siloed table, it wanted the default/standard alignment, so filter to `alignment_source = 'default'`. If the query was hitting a cal variant, use the corresponding alignment_source.

**Specific changes per file:**

1. **regime_data_loader.py** (~line 193): `FROM public.price_bars_multi_tf` -> `FROM public.price_bars_multi_tf_u`. Add `AND alignment_source = 'default'` to the daily bars WHERE clause. For the cal table reference (~line 202): `price_bars_multi_tf_cal_{cal_scheme}` -> `price_bars_multi_tf_u` with `AND alignment_source = 'cal_{cal_scheme}'` (alignment_source values are 'default', 'cal_us', 'cal_iso', 'cal_anchor_us', 'cal_anchor_iso').

2. **refresh_regimes.py** (~line 745): `JOIN public.price_bars_multi_tf p` -> `JOIN public.price_bars_multi_tf_u p`. Add alignment_source filter if the query is scoping to default bars.

3. **refresh_asset_stats.py**: Change `SOURCE_TABLE = "returns_bars_multi_tf"` (~line 67) to `SOURCE_TABLE = "returns_bars_multi_tf_u"`. Review all SQL queries using SOURCE_TABLE and add `AND alignment_source = 'default'` where appropriate (~lines 261, 275, 529).

4. **refresh_cross_asset_corr.py**: Change `SOURCE_TABLE = "returns_bars_multi_tf"` (~line 55) to `SOURCE_TABLE = "returns_bars_multi_tf_u"`. Review all queries and add alignment_source filter.

5. **run_all_feature_refreshes.py** (~line 711): `FROM public.price_bars_multi_tf` -> `FROM public.price_bars_multi_tf_u`. Add alignment_source filter. Also update help text on ~line 744, ~line 757.

6. **validate_features.py** (~lines 597, 641, 679): Change `returns_bars_multi_tf` and `price_bars_multi_tf` to their _u equivalents with alignment_source filters.

7. **stats/refresh_features_stats.py** (~line 47): Change `BAR_TABLE = "public.price_bars_multi_tf"` to `BAR_TABLE = "public.price_bars_multi_tf_u"`. Review all queries (~lines 315, 363) and add alignment_source filter.

8. **macro/cross_asset.py** (~lines 288, 928): Change `FROM returns_bars_multi_tf` to `FROM returns_bars_multi_tf_u` with alignment_source filter.

9. **macro/lead_lag_analyzer.py** (~lines 133, 223): Change `returns_bars_multi_tf` to `returns_bars_multi_tf_u` in both the `_get_table_columns` call and the SQL query. Add alignment_source filter.

10. **experiments/runner.py** (~lines 42-61): Remove siloed table names from `_ALLOWED_TABLES` (keep only the _u variants). Also clean `_TABLES_WITH_TIMESTAMP_COL` (~lines 73-79) to remove siloed entries.

After all changes, run a project-wide grep to confirm NO remaining runtime SQL references to any of the 30 siloed table names (excluding comments, docstrings, and the audit scripts in Category D which will be handled in Plan 78-02):
```
grep -rn "FROM.*price_bars_multi_tf[^_]" src/ta_lab2/ --include="*.py" | grep -v "audit_" | grep -v "#" | grep -v '"""' | grep -v "'''"
```
Repeat for returns_bars_multi_tf, ema_multi_tf, returns_ema_multi_tf, ama_multi_tf, returns_ama_multi_tf.
  </action>
  <verify>
Run the following grep commands -- each must return zero matches (excluding audit scripts, comments, and docstrings):

```bash
# Check for runtime SQL references to siloed tables (not _u variants)
for table in price_bars_multi_tf returns_bars_multi_tf ema_multi_tf returns_ema_multi_tf ama_multi_tf returns_ama_multi_tf; do
  echo "=== Checking $table ==="
  grep -rn "FROM.*public\\.${table}[^_]" src/ta_lab2/ --include="*.py" | grep -v "audit_" | grep -v "^.*#" || echo "CLEAN"
done
```

For runner.py: `grep "price_bars_multi_tf\"" src/ta_lab2/experiments/runner.py` should not match (only _u variant should remain).

Run a quick smoke test on one changed file: `python -c "from ta_lab2.scripts.desc_stats.refresh_asset_stats import SOURCE_TABLE; assert '_u' in SOURCE_TABLE, f'Expected _u table, got {SOURCE_TABLE}'"` -- must pass.
  </verify>
  <done>All ~10 runtime files redirect SQL queries from siloed table names to _u equivalents with appropriate alignment_source filters. Zero grep matches for runtime siloed table references.</done>
</task>

</tasks>

<verification>
1. `SELECT definition FROM pg_views WHERE viewname = 'all_emas'` shows `FROM public.ema_multi_tf_u`
2. Project-wide grep for `FROM.*{siloed_table_name}[^_]` in Python files returns zero runtime matches
3. All modified files pass `python -m py_compile {file}` (syntax check)
</verification>

<success_criteria>
- all_emas view recreated pointing at ema_multi_tf_u in both DB and views.py
- All Category E runtime files (10 files) redirect to _u tables
- Zero runtime references to the 30 siloed data table names remain
- No syntax errors introduced
- ta_feature.py and daily_features_view.py confirmed clean (already use _u via base_feature.py and SOURCE_TABLES dict)
</success_criteria>

<output>
After completion, create `.planning/phases/78-table-drops-script-cleanup/78-01-SUMMARY.md`
</output>
