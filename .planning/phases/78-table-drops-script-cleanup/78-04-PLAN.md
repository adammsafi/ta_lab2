---
phase: 78-table-drops-script-cleanup
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ta_lab2/executor/position_sizer.py
  - src/ta_lab2/dashboard/pages/10_macro.py
  - src/ta_lab2/drift/data_snapshot.py
  - src/ta_lab2/scripts/exchange/refresh_exchange_price_feed.py
  - src/ta_lab2/scripts/validation/run_preflight_check.py
  - src/ta_lab2/scripts/run_all_audits.py
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "position_sizer.py fallback price lookup queries price_bars_multi_tf_u with alignment_source=multi_tf"
    - "10_macro.py overlay chart query uses price_bars_multi_tf_u with alignment_source=multi_tf"
    - "data_snapshot.py staleness query uses price_bars_multi_tf_u with alignment_source=multi_tf"
    - "refresh_exchange_price_feed.py bar lookup uses price_bars_multi_tf_u with alignment_source=multi_tf"
    - "run_preflight_check.py check 9 uses price_bars_multi_tf_u with alignment_source=multi_tf"
    - "run_all_audits.py ALL_AUDIT_SCRIPTS list contains zero references to deleted audit scripts"
  artifacts:
    - path: "src/ta_lab2/executor/position_sizer.py"
      provides: "Fallback price lookup from _u table"
      contains: "price_bars_multi_tf_u"
    - path: "src/ta_lab2/dashboard/pages/10_macro.py"
      provides: "Overlay chart query from _u table"
      contains: "price_bars_multi_tf_u"
    - path: "src/ta_lab2/drift/data_snapshot.py"
      provides: "Data snapshot staleness query from _u table"
      contains: "price_bars_multi_tf_u"
    - path: "src/ta_lab2/scripts/exchange/refresh_exchange_price_feed.py"
      provides: "Exchange price feed bar lookup from _u table"
      contains: "price_bars_multi_tf_u"
    - path: "src/ta_lab2/scripts/validation/run_preflight_check.py"
      provides: "Preflight check 9 staleness query from _u table"
      contains: "price_bars_multi_tf_u"
    - path: "src/ta_lab2/scripts/run_all_audits.py"
      provides: "Clean audit script list with no deleted references"
  key_links:
    - from: "src/ta_lab2/executor/position_sizer.py"
      to: "public.price_bars_multi_tf_u"
      via: "SQL SELECT with alignment_source filter"
      pattern: "price_bars_multi_tf_u.*alignment_source.*multi_tf"
    - from: "src/ta_lab2/scripts/run_all_audits.py"
      to: "audit scripts on disk"
      via: "ALL_AUDIT_SCRIPTS list entries"
      pattern: "Only references scripts that exist"
---

<objective>
Fix 5 runtime files that still SELECT FROM dropped price_bars_multi_tf, and clean run_all_audits.py of 14 deleted audit script references.

Purpose: These files are currently broken in production because the 30 siloed tables were dropped in Plan 78-03. This is an urgent fix to restore functionality.
Output: 6 files patched with correct _u table references.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/78-table-drops-script-cleanup/78-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Redirect 5 runtime files from price_bars_multi_tf to price_bars_multi_tf_u</name>
  <files>
    src/ta_lab2/executor/position_sizer.py
    src/ta_lab2/dashboard/pages/10_macro.py
    src/ta_lab2/drift/data_snapshot.py
    src/ta_lab2/scripts/exchange/refresh_exchange_price_feed.py
    src/ta_lab2/scripts/validation/run_preflight_check.py
  </files>
  <action>
    Each file has a SQL query that references `price_bars_multi_tf` (the dropped table).
    Change the table name to `price_bars_multi_tf_u` and add `AND alignment_source = 'multi_tf'` filter.

    Specific changes:

    1. **executor/position_sizer.py** (line ~381):
       Change `FROM public.price_bars_multi_tf` to `FROM public.price_bars_multi_tf_u`
       Add `AND alignment_source = 'multi_tf'` to the WHERE clause (after `AND tf = '1D'`).
       Also update the logger.debug source string on line ~390 from `source=price_bars_multi_tf` to `source=price_bars_multi_tf_u`.

    2. **dashboard/pages/10_macro.py** (line ~199):
       Change `FROM public.price_bars_multi_tf pb` to `FROM public.price_bars_multi_tf_u pb`
       Add `AND pb.alignment_source = 'multi_tf'` to the WHERE clause (after the tf filter).

    3. **drift/data_snapshot.py** (line ~44):
       Change `FROM price_bars_multi_tf` to `FROM price_bars_multi_tf_u`
       Add `AND alignment_source = 'multi_tf'` to the WHERE clause (after `AND tf = '1D'`).

    4. **scripts/exchange/refresh_exchange_price_feed.py** (line ~114):
       Change `FROM public.price_bars_multi_tf b` to `FROM public.price_bars_multi_tf_u b`
       Add `AND b.alignment_source = 'multi_tf'` to the WHERE clause.

    5. **scripts/validation/run_preflight_check.py** (line ~255):
       Change `FROM price_bars_multi_tf` to `FROM price_bars_multi_tf_u`
       Add `AND alignment_source = 'multi_tf'` to the WHERE clause (after `AND tf='1D'`).

    Pattern established in Plan 78-01: always use alignment_source='multi_tf' for base table data (not 'default').
  </action>
  <verify>
    Run: `grep -rn "FROM.*price_bars_multi_tf[^_]" src/ta_lab2/executor/ src/ta_lab2/dashboard/ src/ta_lab2/drift/ src/ta_lab2/scripts/exchange/ src/ta_lab2/scripts/validation/`
    Should return zero matches (no unqualified price_bars_multi_tf references).

    Run: `python -c "import ast; ast.parse(open('src/ta_lab2/executor/position_sizer.py').read()); print('OK')"` (and similar for all 5 files)
    All 5 files should parse without syntax errors.
  </verify>
  <done>All 5 runtime files query price_bars_multi_tf_u with alignment_source='multi_tf' filter. Zero references to the dropped price_bars_multi_tf table remain in these files.</done>
</task>

<task type="auto">
  <name>Task 2: Remove deleted audit script entries from run_all_audits.py</name>
  <files>src/ta_lab2/scripts/run_all_audits.py</files>
  <action>
    In `src/ta_lab2/scripts/run_all_audits.py`, the `ALL_AUDIT_SCRIPTS` list (lines ~48-155) contains AuditScript entries referencing 14 scripts deleted in Plan 78-02. These deleted scripts are:

    Bars (3):
    - bars/audit_price_bars_integrity.py
    - bars/audit_price_bars_samples.py
    - bars/audit_price_bars_tables.py

    EMAs (4):
    - emas/audit_ema_integrity.py
    - emas/audit_ema_samples.py
    - emas/audit_ema_tables.py
    - emas/audit_ema_expected_coverage.py

    Returns (7):
    - returns/audit_returns_bars_multi_tf_integrity.py
    - returns/audit_returns_bars_multi_tf_cal_iso_integrity.py
    - returns/audit_returns_bars_multi_tf_cal_us_integrity.py
    - returns/audit_returns_bars_multi_tf_cal_anchor_iso_integrity.py
    - returns/audit_returns_bars_multi_tf_cal_anchor_us_integrity.py
    - returns/audit_returns_ema_multi_tf_cal_integrity.py
    - returns/audit_returns_ema_multi_tf_cal_anchor_integrity.py

    Remove ALL 14 AuditScript entries from the ALL_AUDIT_SCRIPTS list.

    Keep the remaining entries that reference scripts still on disk:
    - returns/audit_returns_d1_integrity.py (if it exists)
    - returns/audit_returns_ema_multi_tf_integrity.py (if it exists)
    - returns/audit_returns_ema_multi_tf_u_integrity.py (if it exists)

    Before removing, verify which scripts actually exist on disk:
    `ls src/ta_lab2/scripts/bars/audit_* src/ta_lab2/scripts/emas/audit_* src/ta_lab2/scripts/returns/audit_* 2>/dev/null`
    Only keep entries whose script_path points to an existing file.
  </action>
  <verify>
    Run: `python -c "import ast; ast.parse(open('src/ta_lab2/scripts/run_all_audits.py').read()); print('OK')"`
    Should print OK.

    Run: `python -c "
import importlib.util, sys
spec = importlib.util.spec_from_file_location('audits', 'src/ta_lab2/scripts/run_all_audits.py')
mod = importlib.util.module_from_spec(spec)
spec.loader.exec_module(mod)
for s in mod.ALL_AUDIT_SCRIPTS:
    print(s.script_path)
"`
    Every printed script_path should correspond to a file that exists on disk.
  </verify>
  <done>run_all_audits.py ALL_AUDIT_SCRIPTS list contains zero references to deleted audit scripts. All remaining entries point to scripts that exist on disk.</done>
</task>

</tasks>

<verification>
1. `grep -rn "FROM.*price_bars_multi_tf[^_]" src/ta_lab2/executor/ src/ta_lab2/dashboard/ src/ta_lab2/drift/ src/ta_lab2/scripts/exchange/ src/ta_lab2/scripts/validation/` returns zero matches
2. All 6 modified files parse without syntax errors
3. run_all_audits.py ALL_AUDIT_SCRIPTS entries all reference existing scripts
</verification>

<success_criteria>
- 5 runtime files query price_bars_multi_tf_u with alignment_source='multi_tf'
- run_all_audits.py has no references to deleted audit scripts
- Zero syntax errors in modified files
</success_criteria>

<output>
After completion, create `.planning/phases/78-table-drops-script-cleanup/78-04-SUMMARY.md`
</output>
