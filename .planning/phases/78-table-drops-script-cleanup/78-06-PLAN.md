---
phase: 78-table-drops-script-cleanup
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ta_lab2/scripts/emas/refresh_ema_multi_tf_from_bars.py
  - src/ta_lab2/scripts/emas/base_ema_refresher.py
  - src/ta_lab2/scripts/emas/refresh_ema_multi_tf_cal_from_bars.py
  - src/ta_lab2/scripts/emas/refresh_ema_multi_tf_cal_anchor_from_bars.py
  - src/ta_lab2/scripts/emas/ema_state_manager.py
  - src/ta_lab2/features/m_tf/ema_multi_timeframe.py
  - src/ta_lab2/features/m_tf/ema_multi_tf_cal.py
  - src/ta_lab2/features/m_tf/ema_multi_tf_cal_anchor.py
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "EMA base builder reads bars from price_bars_multi_tf_u with alignment_source=multi_tf"
    - "EMA cal builders read bars from price_bars_multi_tf_u with alignment_source=multi_tf_cal_{scheme}"
    - "EMA cal_anchor builders read bars from price_bars_multi_tf_u with alignment_source=multi_tf_cal_anchor_{scheme}"
    - "EMA feature class bar-read SQL includes alignment_source filter when config.alignment_source is set"
    - "EMAStateConfig.bars_table in cal/cal_anchor builders points to price_bars_multi_tf_u"
  artifacts:
    - path: "src/ta_lab2/scripts/emas/refresh_ema_multi_tf_from_bars.py"
      provides: "Base EMA builder with bars_table=price_bars_multi_tf_u"
      contains: "price_bars_multi_tf_u"
    - path: "src/ta_lab2/scripts/emas/base_ema_refresher.py"
      provides: "Base class docstring example updated to _u"
      contains: "price_bars_multi_tf_u"
    - path: "src/ta_lab2/scripts/emas/refresh_ema_multi_tf_cal_from_bars.py"
      provides: "Cal EMA builder with bars_table=price_bars_multi_tf_u"
      contains: "price_bars_multi_tf_u"
    - path: "src/ta_lab2/scripts/emas/refresh_ema_multi_tf_cal_anchor_from_bars.py"
      provides: "Cal anchor EMA builder with bars_table=price_bars_multi_tf_u"
      contains: "price_bars_multi_tf_u"
    - path: "src/ta_lab2/scripts/emas/ema_state_manager.py"
      provides: "State manager bar_metadata SQL reads from _u table"
    - path: "src/ta_lab2/features/m_tf/ema_multi_timeframe.py"
      provides: "Base EMA feature class with _u default and alignment_source-aware SQL"
      contains: "price_bars_multi_tf_u"
    - path: "src/ta_lab2/features/m_tf/ema_multi_tf_cal.py"
      provides: "Cal EMA feature class with _u bar reads"
      contains: "price_bars_multi_tf_u"
    - path: "src/ta_lab2/features/m_tf/ema_multi_tf_cal_anchor.py"
      provides: "Cal anchor EMA feature class with _u bar reads"
      contains: "price_bars_multi_tf_u"
  key_links:
    - from: "refresh_ema_multi_tf_from_bars.py _process_id_worker"
      to: "public.price_bars_multi_tf_u"
      via: "bars_table default in extra_config.get()"
      pattern: "price_bars_multi_tf_u"
    - from: "ema_multi_timeframe.py _load_canonical_closes"
      to: "public.price_bars_multi_tf_u"
      via: "SQL SELECT with alignment_source filter"
      pattern: "alignment_source"
    - from: "ema_state_manager.py bar_metadata CTE"
      to: "price_bars_multi_tf_u"
      via: "EMAStateConfig.bars_table in state bootstrap SQL"
      pattern: "price_bars_multi_tf_u"
---

<objective>
Redirect all EMA builders and feature classes from dropped siloed bar tables to price_bars_multi_tf_u with alignment_source filters.

Purpose: EMA builders read price bars as input. The base builder (refresh_ema_multi_tf_from_bars.py) defaults to price_bars_multi_tf (dropped), and cal-variant builders construct bars_table as price_bars_multi_tf_cal_{scheme} (also dropped). All daily EMA refreshes currently fail.
Output: All EMA builders, feature classes, and state manager read from price_bars_multi_tf_u with correct alignment_source filters.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/78-table-drops-script-cleanup/78-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update EMA feature classes to read from price_bars_multi_tf_u with alignment_source filter</name>
  <files>
    src/ta_lab2/features/m_tf/ema_multi_timeframe.py
    src/ta_lab2/features/m_tf/ema_multi_tf_cal.py
    src/ta_lab2/features/m_tf/ema_multi_tf_cal_anchor.py
  </files>
  <action>
    These 3 files contain EMA feature classes with bars_table defaults pointing to dropped tables AND SQL queries that read from those tables.

    **A. Update default bars_table parameters:**

    1. **ema_multi_timeframe.py**:
       - Line ~65: Change `bars_table: str = "price_bars_multi_tf"` to `bars_table: str = "price_bars_multi_tf_u"` in MultiTFEMAFeature.__init__
       - Line ~596: Change `bars_table_tf_day: str = "price_bars_multi_tf"` to `bars_table_tf_day: str = "price_bars_multi_tf_u"` in the compat wrapper function

    2. **ema_multi_tf_cal.py**:
       - Line ~84: Change `self.bars_table = "public.price_bars_multi_tf_cal_us"` to `self.bars_table = "public.price_bars_multi_tf_u"`
       - Line ~86: Change `self.bars_table = "public.price_bars_multi_tf_cal_iso"` to `self.bars_table = "public.price_bars_multi_tf_u"`

    3. **ema_multi_tf_cal_anchor.py**:
       - Line ~75: Change `self.bars_table = "public.price_bars_multi_tf_cal_anchor_us"` to `self.bars_table = "public.price_bars_multi_tf_u"`
       - Line ~77: Change `self.bars_table = "public.price_bars_multi_tf_cal_anchor_iso"` to `self.bars_table = "public.price_bars_multi_tf_u"`

    **B. Add alignment_source filter to bar-read SQL queries:**

    The EMA feature classes access self.config (EMAFeatureConfig) which has an alignment_source field. Use self.config.alignment_source to add filters.

    1. **ema_multi_timeframe.py** has 2 bar-read SQL queries:
       - `_load_canonical_closes()` (line ~443-449): SELECT FROM {self.bars_schema}.{self.bars_table} WHERE id = ANY(:ids) AND is_partial_end = FALSE
         Add: conditional alignment_source filter using self.config.alignment_source
       - `_load_canonical_closes_for_tf()` (line ~498-504): SELECT FROM {self.bars_schema}.{self.bars_table} WHERE tf = :tf AND id = ANY(:ids) AND is_partial_end = FALSE
         Add: conditional alignment_source filter using self.config.alignment_source

       Pattern:
       ```python
       alignment_filter = ""
       params = {existing params...}
       if self.config.alignment_source:
           alignment_filter = "AND alignment_source = :alignment_source"
           params["alignment_source"] = self.config.alignment_source
       ```
       Then include {alignment_filter} in the SQL WHERE clause.

    2. **ema_multi_tf_cal.py** has 2 bar-read SQL queries (one for all TFs, one for specific TFs):
       - Line ~429-435: SELECT FROM {self.bars_table} WHERE id = ANY(:ids)...
       - Line ~470-476: SELECT FROM {self.bars_table} WHERE id = ANY(:ids) AND tf = ANY(:tfs)...
       Both need alignment_source filter. The CalEMAFeature stores scheme info; determine alignment_source from self.scheme:
       ```python
       alignment_source = f"multi_tf_cal_{self.scheme.lower()}"
       ```
       Add `AND alignment_source = :alignment_source` with params.

    3. **ema_multi_tf_cal_anchor.py** has 2 bar-read SQL queries:
       - Line ~292-297: SELECT FROM {self.bars_table} WHERE id = ANY(:ids)...
       - Line ~340-346: SELECT FROM {self.bars_table} WHERE id = ANY(:ids) AND tf = ANY(:tfs)...
       Both need alignment_source filter. Determine from self.scheme:
       ```python
       alignment_source = f"multi_tf_cal_anchor_{self.scheme.lower()}"
       ```
       Add `AND alignment_source = :alignment_source` with params.

    Total: 6 SQL queries to update across 3 files.
  </action>
  <verify>
    Run for each file:
    ```
    python -c "import ast; ast.parse(open('src/ta_lab2/features/m_tf/ema_multi_timeframe.py').read()); print('OK')"
    python -c "import ast; ast.parse(open('src/ta_lab2/features/m_tf/ema_multi_tf_cal.py').read()); print('OK')"
    python -c "import ast; ast.parse(open('src/ta_lab2/features/m_tf/ema_multi_tf_cal_anchor.py').read()); print('OK')"
    ```
    All should print OK.

    Run: `grep -n "price_bars_multi_tf[^_]" src/ta_lab2/features/m_tf/ema_multi_timeframe.py src/ta_lab2/features/m_tf/ema_multi_tf_cal.py src/ta_lab2/features/m_tf/ema_multi_tf_cal_anchor.py`
    Should return zero matches.

    Run: `grep -c "alignment_source" src/ta_lab2/features/m_tf/ema_multi_timeframe.py`
    Should be >= 2 (alignment_source filter in both bar-read queries).
  </verify>
  <done>
    All EMA feature classes (base + cal + cal_anchor) default to price_bars_multi_tf_u and all 6 bar-read SQL queries include alignment_source filters. Zero references to dropped siloed bar tables remain.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update EMA builder scripts and state manager to use price_bars_multi_tf_u</name>
  <files>
    src/ta_lab2/scripts/emas/refresh_ema_multi_tf_from_bars.py
    src/ta_lab2/scripts/emas/base_ema_refresher.py
    src/ta_lab2/scripts/emas/refresh_ema_multi_tf_cal_from_bars.py
    src/ta_lab2/scripts/emas/refresh_ema_multi_tf_cal_anchor_from_bars.py
    src/ta_lab2/scripts/emas/ema_state_manager.py
  </files>
  <action>
    These 5 files specify bars_table values that point to dropped tables.

    1. **refresh_ema_multi_tf_from_bars.py** -- 3 locations:
       - Line ~71: `task.extra_config.get("bars_table", "price_bars_multi_tf")` -> change default to `"price_bars_multi_tf_u"`
       - Line ~159: `config.extra_config.get("bars_table", "price_bars_multi_tf")` -> change default to `"price_bars_multi_tf_u"`
       - Line ~247: `p.add_argument("--bars-table", default="price_bars_multi_tf")` -> change default to `"price_bars_multi_tf_u"`

    2. **base_ema_refresher.py** (line ~857):
       Update docstring example from `{"bars_table": "price_bars_multi_tf", ...}` to `{"bars_table": "price_bars_multi_tf_u", ...}`
       This is the get_source_table_info() abstract method docstring.

    3. **refresh_ema_multi_tf_cal_from_bars.py** -- 4 locations:
       - Line ~166: `self.bars_table = f"price_bars_multi_tf_cal_{scheme}"` -> change to `self.bars_table = "price_bars_multi_tf_u"`
       - Line ~311: `bars_table=f"price_bars_multi_tf_cal_{scheme}"` in EMAStateConfig -> change to `bars_table="price_bars_multi_tf_u"`
       - Line ~357: `bars_table=f"price_bars_multi_tf_cal_{scheme}"` in EMAStateConfig -> change to `bars_table="price_bars_multi_tf_u"`
       Note: The EMAStateConfig.bars_table is used in ema_state_manager.py for the state bootstrap SQL query which reads bar_metadata from the bars table.

    4. **refresh_ema_multi_tf_cal_anchor_from_bars.py** -- 4 locations:
       - Line ~170: `self.bars_table = f"price_bars_multi_tf_cal_anchor_{scheme}"` -> change to `self.bars_table = "price_bars_multi_tf_u"`
       - Line ~297: `bars_table=f"price_bars_multi_tf_cal_anchor_{scheme}"` in EMAStateConfig -> change to `bars_table="price_bars_multi_tf_u"`
       - Line ~343: `bars_table=f"price_bars_multi_tf_cal_anchor_{scheme}"` in EMAStateConfig -> change to `bars_table="price_bars_multi_tf_u"`

    5. **ema_state_manager.py** -- The bar_metadata CTE in the state bootstrap SQL (line ~288-298) reads from `{bars_schema}.{bars_table}` WITHOUT any alignment_source filter. When bars_table is now price_bars_multi_tf_u, this will aggregate bar_metadata across ALL 5 alignment sources, which is incorrect for state bootstrapping.

       Add an alignment_source parameter to the `bootstrap_state_from_output` method signature (or pass it via config). The EMAStateConfig already stores bars_table but does NOT store alignment_source.

       Solution: Accept alignment_source as an optional parameter in the bootstrap method. In the bar_metadata CTE, add `AND alignment_source = :alignment_source` when alignment_source is provided.

       The callers pass alignment_source via extra_config. Check how `bootstrap_state_from_output` is called and thread alignment_source through.

       Search for calls: `grep -rn "bootstrap_state_from_output" src/ta_lab2/scripts/emas/`
       The base_ema_refresher.py likely calls this. Thread the alignment_source from the refresher config.

       If the state manager API change is too invasive, an alternative: add alignment_source as an optional field to EMAStateConfig and use it in the SQL. This is cleaner since EMAStateConfig already groups bars-related config.

       **Recommended approach:** Add `alignment_source: Optional[str] = None` to EMAStateConfig (line ~69, after bars_partial_filter). Then in ema_state_manager.py bar_metadata CTE (~line 288-298), conditionally add `AND alignment_source = :alignment_source` when `self.config.alignment_source is not None`, and include it in the params dict.

       Then update the 4 places in cal/cal_anchor builders where EMAStateConfig is constructed to pass alignment_source:
       - refresh_ema_multi_tf_cal_from_bars.py line ~311: add `alignment_source=f"multi_tf_cal_{scheme}"`
       - refresh_ema_multi_tf_cal_from_bars.py line ~357: add `alignment_source=f"multi_tf_cal_{scheme}"`
       - refresh_ema_multi_tf_cal_anchor_from_bars.py line ~297: add `alignment_source=f"multi_tf_cal_anchor_{scheme}"`
       - refresh_ema_multi_tf_cal_anchor_from_bars.py line ~343: add `alignment_source=f"multi_tf_cal_anchor_{scheme}"`

       Also add alignment_source to the EMAStateConfig in refresh_ema_multi_tf_from_bars.py if it constructs one (check -- it may not use EMAStateConfig).
  </action>
  <verify>
    Run for each file:
    ```
    python -c "import ast; ast.parse(open('src/ta_lab2/scripts/emas/refresh_ema_multi_tf_from_bars.py').read()); print('OK')"
    python -c "import ast; ast.parse(open('src/ta_lab2/scripts/emas/base_ema_refresher.py').read()); print('OK')"
    python -c "import ast; ast.parse(open('src/ta_lab2/scripts/emas/refresh_ema_multi_tf_cal_from_bars.py').read()); print('OK')"
    python -c "import ast; ast.parse(open('src/ta_lab2/scripts/emas/refresh_ema_multi_tf_cal_anchor_from_bars.py').read()); print('OK')"
    python -c "import ast; ast.parse(open('src/ta_lab2/scripts/emas/ema_state_manager.py').read()); print('OK')"
    ```
    All should print OK.

    Run: `grep -rn "\"price_bars_multi_tf\"" src/ta_lab2/scripts/emas/refresh_ema_multi_tf_from_bars.py src/ta_lab2/scripts/emas/refresh_ema_multi_tf_cal_from_bars.py src/ta_lab2/scripts/emas/refresh_ema_multi_tf_cal_anchor_from_bars.py`
    Should return zero matches.

    Run: `grep -rn "price_bars_multi_tf_cal_" src/ta_lab2/scripts/emas/refresh_ema_multi_tf_cal_from_bars.py src/ta_lab2/scripts/emas/refresh_ema_multi_tf_cal_anchor_from_bars.py`
    Should return zero matches (all cal-variant table names replaced with _u).
  </verify>
  <done>
    All EMA builder scripts default to price_bars_multi_tf_u. EMAStateConfig carries alignment_source for state bootstrap SQL. Cal/cal_anchor builders pass their scheme-specific alignment_source. Zero references to dropped siloed bar tables remain.
  </done>
</task>

</tasks>

<verification>
1. `grep -rn "\"price_bars_multi_tf\"" src/ta_lab2/scripts/emas/ src/ta_lab2/features/m_tf/ema_multi_timeframe.py src/ta_lab2/features/m_tf/ema_multi_tf_cal.py src/ta_lab2/features/m_tf/ema_multi_tf_cal_anchor.py` returns zero matches
2. `grep -rn "price_bars_multi_tf_cal_" src/ta_lab2/scripts/emas/ src/ta_lab2/features/m_tf/ema_multi_tf_cal.py src/ta_lab2/features/m_tf/ema_multi_tf_cal_anchor.py` returns zero matches
3. All 8 modified files parse without syntax errors
4. grep confirms alignment_source appears in bar-read queries of all 3 EMA feature class files
5. EMAStateConfig has alignment_source field
</verification>

<success_criteria>
- EMA base builder reads bars from price_bars_multi_tf_u (alignment_source=multi_tf)
- EMA cal builders read bars from price_bars_multi_tf_u (alignment_source=multi_tf_cal_{scheme})
- EMA cal_anchor builders read bars from price_bars_multi_tf_u (alignment_source=multi_tf_cal_anchor_{scheme})
- All feature class SQL queries include alignment_source filter
- EMAStateConfig carries alignment_source for state bootstrap
- Zero references to dropped siloed bar tables remain in EMA code
</success_criteria>

<output>
After completion, create `.planning/phases/78-table-drops-script-cleanup/78-06-SUMMARY.md`
</output>
