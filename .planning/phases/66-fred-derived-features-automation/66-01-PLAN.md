---
phase: 66-fred-derived-features-automation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - alembic/versions/c4d5e6f7a8b9_fred_phase66_derived_columns.py
  - src/ta_lab2/macro/fred_reader.py
  - src/ta_lab2/macro/forward_fill.py
autonomous: true

must_haves:
  truths:
    - "fred.fred_macro_features table has 25 new nullable columns for Phase 66 features"
    - "SERIES_TO_LOAD includes all 7 new FRED series for Phase 66"
    - "FFILL_LIMITS has correct per-frequency limits for all 7 new series"
  artifacts:
    - path: "alembic/versions/c4d5e6f7a8b9_fred_phase66_derived_columns.py"
      provides: "ALTER TABLE migration adding 25 columns to fred.fred_macro_features"
      contains: "op.add_column"
    - path: "src/ta_lab2/macro/fred_reader.py"
      provides: "Extended SERIES_TO_LOAD with 7 new FRED series"
      contains: "BAMLH0A0HYM2"
    - path: "src/ta_lab2/macro/forward_fill.py"
      provides: "Extended FFILL_LIMITS with 7 new series entries"
      contains: "DEXJPUS"
  key_links:
    - from: "alembic/versions/c4d5e6f7a8b9_fred_phase66_derived_columns.py"
      to: "fred.fred_macro_features"
      via: "Alembic op.add_column"
      pattern: "add_column.*fred_macro_features"
    - from: "src/ta_lab2/macro/fred_reader.py"
      to: "fred.series_values"
      via: "SERIES_TO_LOAD list used in load_series_wide() SQL query"
      pattern: "BAMLH0A0HYM2.*NFCI.*M2SL.*DEXJPUS.*DFEDTARU.*DFEDTARL.*CPIAUCSL"
---

<objective>
Add 25 new columns to fred.fred_macro_features via Alembic migration and extend the data loading pipeline to read 7 new FRED series with correct forward-fill limits.

Purpose: Phase 66 features (FRED-08 through FRED-16) need both database columns to write into and raw series data to compute from. This plan provides both foundations before computation logic is added in Plan 02.

Output: One Alembic migration file, updated fred_reader.py with 7 new series, updated forward_fill.py with 7 new FFILL_LIMITS entries.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/66-fred-derived-features-automation/66-RESEARCH.md
@.planning/phases/66-fred-derived-features-automation/66-CONTEXT.md

# Existing source files to modify
@src/ta_lab2/macro/fred_reader.py
@src/ta_lab2/macro/forward_fill.py

# Existing migration to reference for pattern
@alembic/versions/a1b2c3d4e5f6_fred_macro_features.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Alembic ALTER TABLE migration for 25 new columns</name>
  <files>alembic/versions/c4d5e6f7a8b9_fred_phase66_derived_columns.py</files>
  <action>
Create a new Alembic migration file with:
- revision = "c4d5e6f7a8b9"
- down_revision = "b3c4d5e6f7a8" (current head, Phase 65 migration)
- Title: "fred_phase66_derived_columns"

upgrade() adds 25 columns to fred.fred_macro_features (schema="fred") using op.add_column():

7 raw series columns (all sa.Float(), nullable=True):
  - bamlh0a0hym2 (HY OAS spread, daily)
  - nfci (Chicago Fed NFCI, weekly)
  - m2sl (M2 money supply, monthly)
  - dexjpus (USD/JPY exchange rate, daily)
  - dfedtaru (Fed funds target upper bound, daily)
  - dfedtarl (Fed funds target lower bound, daily)
  - cpiaucsl (CPI All Items, monthly)

18 derived feature columns:
  - hy_oas_level (sa.Float) -- FRED-08
  - hy_oas_5d_change (sa.Float) -- FRED-08
  - hy_oas_30d_zscore (sa.Float) -- FRED-08
  - nfci_level (sa.Float) -- FRED-09
  - nfci_4wk_direction (sa.Text) -- FRED-09 (rising/falling/NULL)
  - m2_yoy_pct (sa.Float) -- FRED-10
  - dexjpus_level (sa.Float) -- FRED-11
  - dexjpus_5d_pct_change (sa.Float) -- FRED-11
  - dexjpus_20d_vol (sa.Float) -- FRED-11
  - dexjpus_daily_zscore (sa.Float) -- FRED-11
  - net_liquidity_365d_zscore (sa.Float) -- FRED-12
  - net_liquidity_trend (sa.Text) -- FRED-12 (expanding/contracting/NULL)
  - fed_regime_structure (sa.Text) -- FRED-13 (zero-bound/single-target/target-range)
  - fed_regime_trajectory (sa.Text) -- FRED-13 (hiking/holding/cutting)
  - carry_momentum (sa.Float) -- FRED-14
  - cpi_surprise_proxy (sa.Float) -- FRED-15
  - target_mid (sa.Float) -- FRED-16
  - target_spread (sa.Float) -- FRED-16

downgrade() drops all 25 columns using op.drop_column() with schema="fred".

Group columns by FRED requirement number in comments for clarity.
  </action>
  <verify>
Run `cd C:/Users/asafi/Downloads/ta_lab2 && python -c "from alembic.config import Config; from alembic.script import ScriptDirectory; c = Config('alembic.ini'); sd = ScriptDirectory.from_config(c); print('heads:', sd.get_heads())"` -- should show c4d5e6f7a8b9.

Then run `alembic upgrade head` to apply the migration. Verify columns exist:
`python -c "from ta_lab2.io import get_engine; from sqlalchemy import text; e = get_engine(); r = e.connect().execute(text(\"SELECT column_name FROM information_schema.columns WHERE table_schema='fred' AND table_name='fred_macro_features' ORDER BY ordinal_position\")); print([row[0] for row in r])"`
Should show all 25 new column names among the results.
  </verify>
  <done>Alembic migration applied. fred.fred_macro_features has 25 new nullable columns. `alembic heads` shows c4d5e6f7a8b9.</done>
</task>

<task type="auto">
  <name>Task 2: Extend SERIES_TO_LOAD and FFILL_LIMITS for 7 new FRED series</name>
  <files>src/ta_lab2/macro/fred_reader.py, src/ta_lab2/macro/forward_fill.py</files>
  <action>
In fred_reader.py, add 7 new series to SERIES_TO_LOAD list after the existing Phase 65 entries. Group them with comments by FRED requirement:

```python
# Phase 66 additions:
# FRED-08: Credit stress
"BAMLH0A0HYM2",   # HY OAS spread (daily)
# FRED-09: Financial conditions
"NFCI",            # Chicago Fed NFCI (weekly)
# FRED-10: M2 money supply
"M2SL",            # M2 (monthly)
# FRED-11: Carry trade FX
"DEXJPUS",         # USD/JPY exchange rate (daily)
# FRED-13/16: Fed regime (DFEDTARU/DFEDTARL already synced from VM)
"DFEDTARU",        # Fed Funds target upper bound (daily)
"DFEDTARL",        # Fed Funds target lower bound (daily)
# FRED-15: CPI proxy
"CPIAUCSL",        # CPI All Items (monthly)
```

Update the module docstring and SERIES_TO_LOAD comment to reflect "FRED-03 through FRED-16" (was "FRED-03 through FRED-07").

In forward_fill.py, add 7 new entries to FFILL_LIMITS dict:
```python
# Phase 66 additions
"BAMLH0A0HYM2": 5,    # daily
"NFCI": 10,            # weekly (same as WALCL, WTREGEN)
"M2SL": 45,            # monthly (same as IRSTCI01JPM156N)
"DEXJPUS": 5,          # daily
"DFEDTARU": 5,         # daily (policy rate)
"DFEDTARL": 5,         # daily (policy rate)
"CPIAUCSL": 45,        # monthly
```

NOTE: SOURCE_FREQ in forward_fill.py already has NFCI, CPIAUCSL, and M2SL entries (added for forward-compatibility in Phase 65). Verify these exist and are correct. No changes needed to SOURCE_FREQ if they are already present. If STLFSI4 is listed in SOURCE_FREQ but not used in SERIES_TO_LOAD, leave it (harmless forward-compat entry).
  </action>
  <verify>
Run: `cd C:/Users/asafi/Downloads/ta_lab2 && python -c "from ta_lab2.macro.fred_reader import SERIES_TO_LOAD; print(f'{len(SERIES_TO_LOAD)} series'); assert len(SERIES_TO_LOAD) == 18; assert 'BAMLH0A0HYM2' in SERIES_TO_LOAD; assert 'CPIAUCSL' in SERIES_TO_LOAD; print('OK')"` -- should show 18 series, OK.

Run: `python -c "from ta_lab2.macro.forward_fill import FFILL_LIMITS; print(f'{len(FFILL_LIMITS)} entries'); assert 'DEXJPUS' in FFILL_LIMITS; assert FFILL_LIMITS['NFCI'] == 10; assert FFILL_LIMITS['M2SL'] == 45; print('OK')"` -- should show 18 entries, OK.

Quick smoke test: `python -c "from ta_lab2.macro.fred_reader import load_series_wide; from ta_lab2.io import get_engine; e = get_engine(); df = load_series_wide(e, start_date='2025-01-01'); print(f'Columns: {sorted(df.columns.tolist())}'); print(f'Shape: {df.shape}')"` -- should show all 18 series as columns.
  </verify>
  <done>fred_reader.py has 18 series in SERIES_TO_LOAD (11 Phase 65 + 7 Phase 66). forward_fill.py has 18 FFILL_LIMITS entries. load_series_wide() successfully loads all 18 series from fred.series_values.</done>
</task>

</tasks>

<verification>
1. Alembic head is c4d5e6f7a8b9 and migration applied to database
2. fred.fred_macro_features has 25 new columns (7 raw + 18 derived)
3. SERIES_TO_LOAD contains 18 series (11 + 7)
4. FFILL_LIMITS contains 18 entries with correct limits per frequency
5. load_series_wide() returns a DataFrame with all 18 series columns
</verification>

<success_criteria>
- Migration applied: `SELECT count(*) FROM information_schema.columns WHERE table_schema='fred' AND table_name='fred_macro_features'` returns ~52 (original ~27 + 25 new)
- All 7 new series load from fred.series_values (they are already synced from VM)
- Forward-fill limits match: daily=5, weekly=10, monthly=45
</success_criteria>

<output>
After completion, create `.planning/phases/66-fred-derived-features-automation/66-01-SUMMARY.md`
</output>
