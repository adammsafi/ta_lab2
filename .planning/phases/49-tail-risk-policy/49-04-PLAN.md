---
phase: 49-tail-risk-policy
plan: 04
type: execute
wave: 4
depends_on: ["49-02", "49-03"]
files_modified:
  - src/ta_lab2/scripts/analysis/generate_tail_risk_policy.py
autonomous: true

must_haves:
  truths:
    - "TAIL_RISK_POLICY.md covers all 3 TAIL requirements: sizing comparison, flatten triggers, escalation procedure"
    - "tail_risk_config.yaml is machine-readable with vol_sizing, escalation_thresholds, re_entry, regime_interaction sections"
    - "Policy references historical crash analysis: COVID March 2020 (3-day vol lag), FTX Nov 2022 (exchange halt), May 2021"
    - "Policy documents regime interaction: down_regime * reduce_state multiplicative sizing"
    - "Policy includes override guidance: when and how operator can override automated decisions"
  artifacts:
    - path: "src/ta_lab2/scripts/analysis/generate_tail_risk_policy.py"
      provides: "CLI to generate policy document + YAML config from backtest results"
      contains: "TAIL_RISK_POLICY.md"
  key_links:
    - from: "src/ta_lab2/scripts/analysis/generate_tail_risk_policy.py"
      to: "reports/tail_risk/SIZING_COMPARISON.md"
      via: "reads backtest results to populate recommendations"
      pattern: "SIZING_COMPARISON"
    - from: "src/ta_lab2/scripts/analysis/generate_tail_risk_policy.py"
      to: "reports/tail_risk/tail_risk_config.yaml"
      via: "writes machine-readable config"
      pattern: "tail_risk_config.yaml"
---

<objective>
Create the policy document generator that produces both the human-readable TAIL_RISK_POLICY.md memo and the machine-readable tail_risk_config.yaml from backtest results and calibrated thresholds.

Purpose: TAIL-03 deliverable. The policy document is the capstone output of Phase 49 -- it ties together sizing comparison results (TAIL-01), flatten trigger definitions (TAIL-02), and escalation procedures into a comprehensive tail-risk policy with both human-readable analysis and machine-consumable configuration.

Output: CLI script + TAIL_RISK_POLICY.md + tail_risk_config.yaml + vol_spike_history.html chart in reports/tail_risk/.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/milestones/v1.0.0-ROADMAP.md
@.planning/STATE.md
@.planning/phases/49-tail-risk-policy/49-CONTEXT.md
@.planning/phases/49-tail-risk-policy/49-RESEARCH.md

# Plan 02 SUMMARY for flatten trigger thresholds and RiskEngine integration
@.planning/phases/49-tail-risk-policy/49-02-SUMMARY.md

# Plan 03 SUMMARY for sizing comparison results
@.planning/phases/49-tail-risk-policy/49-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Policy document and YAML config generator</name>
  <files>src/ta_lab2/scripts/analysis/generate_tail_risk_policy.py</files>
  <action>
Create `src/ta_lab2/scripts/analysis/generate_tail_risk_policy.py`.

**Usage:**
```
python -m ta_lab2.scripts.analysis.generate_tail_risk_policy
python -m ta_lab2.scripts.analysis.generate_tail_risk_policy --output-dir reports/tail_risk/
python -m ta_lab2.scripts.analysis.generate_tail_risk_policy --dry-run
```

**Arguments:**
- `--output-dir`: Output directory (default: reports/tail_risk/)
- `--dry-run`: Print what would be generated and exit
- `--asset-ids`: Assets for historical analysis (default: 1 1027)

**This script produces 3 output files:**

1. **reports/tail_risk/TAIL_RISK_POLICY.md** -- Human-readable policy memo
2. **reports/tail_risk/tail_risk_config.yaml** -- Machine-readable config for RiskEngine
3. **reports/tail_risk/charts/vol_spike_history.html** -- Historical vol spike chart

**TAIL_RISK_POLICY.md structure:**

```markdown
# Tail-Risk Policy

Version: 1.0
Generated: {timestamp}
Phase: 49 (Tail-Risk Policy)

## Executive Summary

This document defines when and how the trading system responds to extreme market
conditions. It covers three areas: position sizing approach (hard stops vs
volatility-based sizing), flatten-all criteria (what triggers full risk exit),
and the escalation procedure (normal -> reduce -> flatten -> re-entry).

---

## 1. Position Sizing Approach (TAIL-01)

### Methodology

Compared 3 variants across 4 bakeoff strategies, 2 assets (BTC/ETH), and 2 vol
metrics (ATR-14 and 20-day realized vol):

- **Variant A:** Fixed 30% allocation + hard stop at {stop_pct}%
- **Variant B:** Vol-sized (risk budget / volatility) + no stops
- **Variant C:** Vol-sized + hard stop at {stop_pct}%

### Results Summary

{If SIZING_COMPARISON.md exists, read and embed the summary table}
{If not, include calibration reference values from research}

### Recommendation

Default approach: {recommended_approach}
Default risk budget: 1% of portfolio per trade
Risk budget sweep: [0.5%, 1%, 2%]
Max position cap: 30% (hard limit regardless of risk budget)

**Rationale:**
- At 1% risk budget with normal ATR (3%): position = 33% -> capped at 30%
- At 1% risk budget with crisis ATR (15%): position = 6.7% (automatic delever)
- The position cap ensures vol-sizing doesn't produce oversized positions in low-vol regimes

---

## 2. Flatten-All Criteria (TAIL-02)

### Trigger Suite

Four complementary triggers, evaluated in priority order:

| Priority | Trigger | Condition | Level | Rationale |
|----------|---------|-----------|-------|-----------|
| 1 | Exchange Halt | API health check fails | FLATTEN | Infrastructure failure requires immediate exit |
| 2 | Extreme Single-Day Return | |daily return| > 15% | FLATTEN | COVID March 12: -37% would trigger same day |
| 3 | Vol Spike (3-sigma) | 20d rolling vol > 11.94%/day | FLATTEN | Prolonged crisis detection (catches COVID March 15+) |
| 4 | Correlation Breakdown | BTC/ETH 30d corr < -0.20 | FLATTEN | Unusual divergence (lower priority for crypto) |
| 5 | Vol Spike (2-sigma) | 20d rolling vol > 9.23%/day | REDUCE | Elevated conditions -- halve positions |

### Threshold Calibration

Thresholds calibrated from BTC daily data, 2010-2025 (n=5,613 bars):

| Metric | Mean | Std | Threshold | Trigger Rate |
|--------|------|-----|-----------|--------------|
| 20d rolling vol | 3.70%/day | 2.65% | 9.23% (mean+2std) = REDUCE | ~19 days/year |
| 20d rolling vol | 3.70%/day | 2.65% | 11.94% (mean+3std) = FLATTEN | ~8.5 days/year |
| |daily return| | 4.73%/day | - | 15% = FLATTEN | ~6.6 days/year |
| BTC/ETH 30d corr | 0.674 | 0.344 | -0.20 = FLATTEN | ~5th percentile |

### Historical Crash Validation

| Event | Date | |Return| | 20d Vol | Vol Trigger? | Primary Trigger |
|-------|------|---------|---------|--------------|-----------------|
| COVID crash | 2020-03-12 | 37.2% | 3.1% (pre-crash) | No (3-day lag) | Abs return (same day) |
| COVID elevated | 2020-03-15+ | - | 9.2%+ | REDUCE (day 3) | Vol spike |
| FTX collapse | 2022-11-08-11 | 10-14% | 5.1% (peak) | No | Exchange halt |
| May 2021 dip | 2021-05-19 | 12.5% | 6.7% | No | Circuit breaker |

**Key finding:** No single trigger catches all events. The multi-trigger approach
covers COVID (abs return), FTX (exchange halt), and prolonged crises (vol spike).
May 2021 is correctly handled by the Phase 46 circuit breaker (not a tail event).

### Correlation Caveat

BTC/ETH correlation during COVID was 0.93 (both crashed together). Correlation
breakdown is LOW priority for crypto-only portfolios where assets are structurally
correlated. It becomes more relevant for multi-asset portfolios with uncorrelated legs.

---

## 3. Escalation Procedure

### Three Levels

| Level | State | Position Sizing | New Orders |
|-------|-------|-----------------|------------|
| Normal | `normal` | Full signal processing | Allowed |
| Reduce | `reduce` | Buy quantities halved (0.5x) | Allowed (reduced) |
| Flatten | `flatten` | Exit all positions | Blocked |

### Escalation Rules

- **Normal -> Reduce:** Automatic when 20d vol > 9.23%/day (2-sigma)
- **Normal -> Flatten:** Automatic on exchange halt, |return| > 15%, or vol > 11.94%/day
- **Reduce -> Flatten:** Automatic on exchange halt, |return| > 15%, or vol > 11.94%/day
- Escalation is IMMEDIATE -- no delay, no confirmation needed

### De-Escalation Rules (Re-Entry)

- **Flatten -> Reduce:**
  - Minimum 21-day cooldown after flatten was triggered
  - AND 20d rolling vol below 9.23%/day for 3 consecutive days
- **Reduce -> Normal:**
  - Minimum 14-day cooldown after reduce was triggered
  - AND 20d rolling vol below 9.23%/day for 3 consecutive days

**Rationale:** Empirical vol spike persistence is median 18-20 days. 21-day cooldown
covers median + safety buffer. 3-consecutive-day requirement prevents premature
re-entry on brief vol dips.

### Manual Override

Operator can override the automated state directly in the database:
```sql
-- Manual override: set tail risk state to normal
UPDATE dim_risk_state
SET tail_risk_state = 'normal',
    tail_risk_cleared_at = NOW(),
    tail_risk_trigger_reason = 'Manual override by operator',
    updated_at = NOW()
WHERE state_id = 1;

-- Log the override event
INSERT INTO cmc_risk_events (event_type, trigger_source, reason)
VALUES ('tail_risk_cleared', 'manual', 'Manual override by operator');
```
This updates dim_risk_state.tail_risk_state and logs an override event.

---

## 4. Regime Interaction

The tail-risk escalation system operates independently of the Phase 27 regime
detection (bull/bear/sideways). Both systems can reduce position sizes simultaneously:

| Regime | Base Multiplier | + REDUCE State | Combined |
|--------|----------------|----------------|----------|
| Bull | 1.00x | 0.50x | 0.50x |
| Sideways | 1.00x | 0.50x | 0.50x |
| Down | 0.55x | 0.50x | 0.275x |

**Design choice:** Multiplicative combination. Down regime (0.55x from Phase 27)
+ REDUCE state (0.50x from Phase 49) = 0.275x base position. This is conservative
by design -- during down regimes with elevated vol, aggressive deleveraging is correct.

---

## 5. Implementation Reference

### Architecture

- **flatten_trigger.py:** check_flatten_trigger() evaluates all triggers, returns EscalationState
- **risk_engine.py:** Gate 1.5 in check_order(), evaluate_tail_risk_state() for daily eval
- **dim_risk_state:** tail_risk_state column (normal/reduce/flatten) with audit columns
- **cmc_risk_events:** tail_risk_escalated/tail_risk_cleared event logging

### Configuration

See `tail_risk_config.yaml` for machine-readable thresholds.
RiskEngine reads thresholds from this file or uses hardcoded defaults.

### Daily Evaluation

Called by run_daily_refresh.py:
1. Load latest 20d returns for BTC (market proxy)
2. Compute rolling vol and latest daily return
3. Run check_flatten_trigger()
4. Apply cooldown logic for de-escalation (cooldown days + 3 consecutive days vol below threshold)
5. Update dim_risk_state if state changed
6. Log event to cmc_risk_events

---

## Appendix: Override Guidance

| Scenario | Recommended Override | Reason |
|----------|---------------------|--------|
| Known scheduled event (e.g., ETF decision) | Set REDUCE manually before event | Proactive risk reduction |
| False positive (vol spike from exchange glitch) | Set NORMAL after investigation | Documented false positive |
| Flash crash recovery (vol subsiding) | Wait for automatic de-escalation | Cooldown prevents premature re-entry |
| Extended FLATTEN (> 30 days) | Review and set REDUCE if justified | Balance between safety and opportunity cost |
```

**tail_risk_config.yaml content:**

```yaml
version: "1.0"
generated_at: "{timestamp}"
source: "Phase 49 tail-risk policy analysis"

vol_sizing:
  default_approach: "atr_14"
  risk_budget_default: 0.01
  risk_budget_sweep: [0.005, 0.01, 0.02]
  max_position_pct: 0.30
  realized_vol_window: 20

escalation_thresholds:
  reduce_vol_20d_threshold: 0.0923
  flatten_vol_20d_threshold: 0.1194
  flatten_abs_return_threshold: 0.15
  flatten_correlation_threshold: -0.20

re_entry:
  mechanism: "graduated"
  cooldown_days_reduce: 14
  cooldown_days_flatten: 21
  vol_clear_threshold: 0.0923
  vol_clear_consecutive_days: 3

regime_interaction:
  down_regime_size_mult: 0.55
  reduce_state_additional_mult: 0.50

trigger_priority:
  - {name: "exchange_halt", level: "flatten", priority: 1}
  - {name: "abs_return", level: "flatten", priority: 2, threshold: 0.15}
  - {name: "vol_spike_3sig", level: "flatten", priority: 3, threshold: 0.1194}
  - {name: "correlation_breakdown", level: "flatten", priority: 4, threshold: -0.20}
  - {name: "vol_spike_2sig", level: "reduce", priority: 5, threshold: 0.0923}
```

**vol_spike_history.html chart:**
Load BTC daily returns from cmc_returns_bars_multi_tf_u (column `"timestamp"`, NOT `ts`).
Compute 20d rolling std. Plot:
- Line chart: 20d rolling vol over time
- Horizontal dashed lines at REDUCE (9.23%) and FLATTEN (11.94%) thresholds
- Vertical shaded regions for COVID (2020-03-01 to 2020-04-15), FTX (2022-11-01 to 2022-12-01), May 2021 (2021-05-10 to 2021-06-01)
- Title: "BTC 20-Day Rolling Volatility vs Tail Risk Thresholds"
Save to {output_dir}/charts/vol_spike_history.html

**Conditional SIZING_COMPARISON.md embedding:**
Check if reports/tail_risk/SIZING_COMPARISON.md exists. If so, read the "Summary Recommendations" section and embed it in the policy memo Section 1. If not (e.g., Plan 03 not yet run), use research calibration values as defaults and note "Run run_tail_risk_comparison.py for full comparison data."

**Project root detection:** `Path(__file__).resolve().parents[4]`
**Engine:** `from ta_lab2.io import get_engine` (NullPool for one-shot, only needed for vol chart data)
  </action>
  <verify>
```bash
python -m ta_lab2.scripts.analysis.generate_tail_risk_policy --dry-run
```
Should print planned outputs and exit.

Full run:
```bash
python -m ta_lab2.scripts.analysis.generate_tail_risk_policy --output-dir reports/tail_risk/
ls reports/tail_risk/TAIL_RISK_POLICY.md
ls reports/tail_risk/tail_risk_config.yaml
ls reports/tail_risk/charts/vol_spike_history.html
```

Verify content:
```bash
python -c "
import yaml
with open('reports/tail_risk/tail_risk_config.yaml') as f:
    config = yaml.safe_load(f)
assert config['version'] == '1.0'
assert config['vol_sizing']['risk_budget_default'] == 0.01
assert config['escalation_thresholds']['reduce_vol_20d_threshold'] == 0.0923
assert config['re_entry']['mechanism'] == 'graduated'
assert config['re_entry']['cooldown_days_flatten'] == 21
assert config['re_entry']['vol_clear_consecutive_days'] == 3
print('YAML config validation PASSED')
"
```

Check TAIL_RISK_POLICY.md contains required sections:
```bash
python -c "
with open('reports/tail_risk/TAIL_RISK_POLICY.md') as f:
    content = f.read()
assert '## 1. Position Sizing' in content
assert '## 2. Flatten-All Criteria' in content
assert '## 3. Escalation Procedure' in content
assert '## 4. Regime Interaction' in content
assert 'COVID' in content
assert 'FTX' in content
assert 'tail_risk_config.yaml' in content
assert 'UPDATE dim_risk_state' in content, 'Manual override must use SQL command, not CLI'
assert 'kill_switch --set-tail-risk-state' not in content, 'Must NOT reference nonexistent CLI flag'
print('Policy document structure PASSED')
"
```
  </verify>
  <done>
Policy document generator produces TAIL_RISK_POLICY.md (human memo with all 3 TAIL requirements, historical analysis, regime interaction, SQL-based override guidance), tail_risk_config.yaml (machine config for RiskEngine with vol_clear_consecutive_days), and vol_spike_history.html (historical vol chart with threshold lines).
  </done>
</task>

</tasks>

<verification>
- TAIL_RISK_POLICY.md covers all sections: sizing, flatten criteria, escalation, regime, override guidance
- Manual override section uses direct SQL commands (not nonexistent kill_switch CLI flag)
- tail_risk_config.yaml parseable by PyYAML with correct threshold values including vol_clear_consecutive_days
- vol_spike_history.html chart shows BTC rolling vol with threshold lines and crash event annotations
- Policy references COVID, FTX, May 2021 historical events
- Regime interaction documented with multiplicative combination
</verification>

<success_criteria>
- TAIL-01: Sizing comparison results referenced in policy (from Plan 03 output or research defaults)
- TAIL-02: Flatten criteria fully documented with trigger suite, calibration, and historical validation
- TAIL-03: Complete policy document published with machine config + human memo + SQL-based override guidance
</success_criteria>

<output>
After completion, create `.planning/phases/49-tail-risk-policy/49-04-SUMMARY.md`
</output>
