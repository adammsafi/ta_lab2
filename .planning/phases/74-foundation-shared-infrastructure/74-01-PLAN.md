---
phase: 74-foundation-shared-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ta_lab2/db/__init__.py
  - src/ta_lab2/db/psycopg_helpers.py
  - src/ta_lab2/scripts/bars/refresh_price_bars_1d.py
  - src/ta_lab2/scripts/bars/refresh_tvc_price_bars_1d.py
  - src/ta_lab2/scripts/bars/refresh_hl_price_bars_1d.py
autonomous: true

must_haves:
  truths:
    - "All 3 bar builders use shared psycopg helpers instead of per-file copies"
    - "The shared module handles psycopg v3 (preferred) and psycopg2 (fallback) identically to the current CMC implementation"
    - "No behavioral change -- builders produce identical results before and after extraction"
  artifacts:
    - path: "src/ta_lab2/db/__init__.py"
      provides: "Package marker for ta_lab2.db"
    - path: "src/ta_lab2/db/psycopg_helpers.py"
      provides: "Shared psycopg v3/v2 helper functions"
      exports: ["PSYCOPG3", "PSYCOPG2", "normalize_db_url", "connect", "execute", "fetchone", "fetchall"]
      min_lines: 60
    - path: "src/ta_lab2/scripts/bars/refresh_price_bars_1d.py"
      provides: "CMC 1D bar builder importing from ta_lab2.db.psycopg_helpers"
    - path: "src/ta_lab2/scripts/bars/refresh_tvc_price_bars_1d.py"
      provides: "TVC 1D bar builder importing from ta_lab2.db.psycopg_helpers"
    - path: "src/ta_lab2/scripts/bars/refresh_hl_price_bars_1d.py"
      provides: "HL 1D bar builder importing from ta_lab2.db.psycopg_helpers"
  key_links:
    - from: "src/ta_lab2/scripts/bars/refresh_price_bars_1d.py"
      to: "src/ta_lab2/db/psycopg_helpers.py"
      via: "from ta_lab2.db.psycopg_helpers import ..."
      pattern: "from ta_lab2\\.db\\.psycopg_helpers import"
    - from: "src/ta_lab2/scripts/bars/refresh_tvc_price_bars_1d.py"
      to: "src/ta_lab2/db/psycopg_helpers.py"
      via: "from ta_lab2.db.psycopg_helpers import ..."
      pattern: "from ta_lab2\\.db\\.psycopg_helpers import"
    - from: "src/ta_lab2/scripts/bars/refresh_hl_price_bars_1d.py"
      to: "src/ta_lab2/db/psycopg_helpers.py"
      via: "from ta_lab2.db.psycopg_helpers import ..."
      pattern: "from ta_lab2\\.db\\.psycopg_helpers import"
---

<objective>
Extract duplicated psycopg helper functions from 3 bar builder scripts into a shared module at `src/ta_lab2/db/psycopg_helpers.py`.

Purpose: Eliminate ~200 lines of identical code duplicated across CMC, TVC, and HL 1D bar builders (BAR-05). The shared module becomes the single canonical source for raw psycopg v3/v2 operations, used by all current and future bar builders.

Output: New `ta_lab2.db` package with `psycopg_helpers.py` module; all 3 bar builders rewired to import from it; per-file copies deleted.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/74-foundation-shared-infrastructure/74-RESEARCH.md
@src/ta_lab2/scripts/bars/refresh_price_bars_1d.py
@src/ta_lab2/scripts/bars/refresh_tvc_price_bars_1d.py
@src/ta_lab2/scripts/bars/refresh_hl_price_bars_1d.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create shared psycopg_helpers module</name>
  <files>src/ta_lab2/db/__init__.py, src/ta_lab2/db/psycopg_helpers.py</files>
  <action>
Create the `src/ta_lab2/db/` package with `__init__.py` (empty, just the package marker).

Create `src/ta_lab2/db/psycopg_helpers.py` by extracting from `refresh_price_bars_1d.py` (CMC builder, the most complete implementation). The module must contain:

1. **Dual-driver detection block** (module-level, done once at import time):
   - `import psycopg` with try/except, set `PSYCOPG3 = True/False`
   - `import psycopg2` with try/except, set `PSYCOPG2 = True/False`
   - Export both flags for consumers that need conditional behavior

2. **Five public functions** (drop leading underscore since they're now a public API):
   - `normalize_db_url(url: str) -> str` -- strips SQLAlchemy dialect prefixes. Copy verbatim from CMC builder lines 78-92.
   - `connect(db_url: str)` -- creates psycopg connection with autocommit=True. Uses v3 preferred, v2 fallback. Copy from lines 95-104.
   - `execute(conn, sql: str, params=None) -> None` -- executes SQL. Uses `with conn.cursor()` for v3, manual close for v2. Copy from lines 107-116.
   - `fetchall(conn, sql: str, params=None) -> List[Tuple]` -- executes and returns all rows. Copy from lines 119-132.
   - `fetchone(conn, sql: str, params=None) -> Optional[Tuple]` -- executes and returns one row. Copy from lines 135-148.

3. **Module docstring** explaining this is extracted from bar builders to eliminate duplication, and that all raw-SQL scripts should import from here.

4. **Type annotations** matching the CMC builder: `Optional[Sequence[Any]]` for params, `List[Tuple[Any, ...]]` for fetchall return, `Optional[Tuple[Any, ...]]` for fetchone return.

Do NOT change any logic -- these are exact copies with only the leading underscore removed from function names.
  </action>
  <verify>
Run: `python -c "from ta_lab2.db.psycopg_helpers import PSYCOPG3, PSYCOPG2, normalize_db_url, connect, execute, fetchone, fetchall; print('OK', PSYCOPG3, PSYCOPG2)"`

Confirm it prints "OK True True" (or True/False depending on installed drivers).

Run: `python -c "from ta_lab2.db.psycopg_helpers import normalize_db_url; assert normalize_db_url('postgresql+psycopg2://localhost/db') == 'postgresql://localhost/db'; print('normalize OK')"`
  </verify>
  <done>
`ta_lab2.db.psycopg_helpers` module exists, is importable, exports all 5 functions + 2 flags, and `normalize_db_url` correctly strips dialect prefixes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Rewire all 3 bar builders to use shared helpers</name>
  <files>src/ta_lab2/scripts/bars/refresh_price_bars_1d.py, src/ta_lab2/scripts/bars/refresh_tvc_price_bars_1d.py, src/ta_lab2/scripts/bars/refresh_hl_price_bars_1d.py</files>
  <action>
For each of the 3 bar builder files, make these changes:

**1. Replace the psycopg import block** (the `try: import psycopg` / `try: import psycopg2` block at the top) with a single import:
```python
from ta_lab2.db.psycopg_helpers import (
    PSYCOPG3, PSYCOPG2, normalize_db_url, connect, execute, fetchone, fetchall,
)
```

Import `PSYCOPG3`/`PSYCOPG2` only if the file references them elsewhere (e.g., in `_exec` branching within methods not being extracted). Check each file before deciding.

Also import `psycopg` and/or `psycopg2` directly ONLY if the file uses them beyond the 5 helper functions (e.g., for exception types like `psycopg2.OperationalError`). If so, keep those specific imports.

**2. Delete the per-file helper functions:**
- CMC (`refresh_price_bars_1d.py`): Delete `_normalize_db_url` (L78-92), `_connect` (L95-104), `_exec` (L107-116), `_fetchall` (L119-132), `_fetchone` (L135-148). Also delete the preceding `# Database utilities` section comment.
- TVC (`refresh_tvc_price_bars_1d.py`): Delete the equivalent functions (see RESEARCH.md appendix for line numbers: L56-70, L73-82, L85-90, L93-100). Note TVC does NOT have `_fetchall`.
- HL (`refresh_hl_price_bars_1d.py`): Delete the equivalent functions (L66-79, L83-92, L95-100, L103-110, L113-120).

**3. Update all call sites** in each file to use the new function names (no leading underscore):
- `_normalize_db_url(` -> `normalize_db_url(`
- `_connect(` -> `connect(`
- `_exec(` -> `execute(`
- `_fetchone(` -> `fetchone(`
- `_fetchall(` -> `fetchall(`

Search each file thoroughly for all occurrences. The CMC builder uses these extensively in its `_build_and_insert_bars` and `_get_last_src_ts` methods. TVC and HL have similar patterns.

**4. Verify no orphaned references:** After edits, grep each file for `_normalize_db_url`, `_connect`, `_exec`, `_fetchone`, `_fetchall` to ensure no old references remain. The only remaining underscore-prefixed functions should be file-specific methods like `_build_insert_bars_sql`, `_get_last_src_ts`, etc.
  </action>
  <verify>
Run for each file to confirm no syntax errors:
- `python -c "import ta_lab2.scripts.bars.refresh_price_bars_1d"`
- `python -c "import ta_lab2.scripts.bars.refresh_tvc_price_bars_1d"`
- `python -c "import ta_lab2.scripts.bars.refresh_hl_price_bars_1d"`

Run ruff check on modified files:
- `ruff check src/ta_lab2/db/psycopg_helpers.py src/ta_lab2/scripts/bars/refresh_price_bars_1d.py src/ta_lab2/scripts/bars/refresh_tvc_price_bars_1d.py src/ta_lab2/scripts/bars/refresh_hl_price_bars_1d.py`

Grep for any remaining old references:
- `grep -n "_normalize_db_url\|_connect\|_exec\|_fetchone\|_fetchall" src/ta_lab2/scripts/bars/refresh_price_bars_1d.py src/ta_lab2/scripts/bars/refresh_tvc_price_bars_1d.py src/ta_lab2/scripts/bars/refresh_hl_price_bars_1d.py`
  (Should return zero matches for the helper functions -- any hits should only be for unrelated methods like `_get_last_src_ts`)
  </verify>
  <done>
All 3 bar builders import from `ta_lab2.db.psycopg_helpers`, have zero per-file psycopg helper copies, all call sites use the new names, ruff passes clean, and all 3 files import successfully.
  </done>
</task>

</tasks>

<verification>
1. `python -c "from ta_lab2.db.psycopg_helpers import normalize_db_url, connect, execute, fetchone, fetchall; print('Module OK')"` succeeds
2. All 3 builder files import cleanly without errors
3. `ruff check` passes on all 4 modified/created files
4. No remaining `_normalize_db_url`, `_connect`, `_exec`, `_fetchone`, `_fetchall` function definitions in any bar builder file (grep returns empty)
5. Line count reduction: each builder should be ~60-70 lines shorter after extraction
</verification>

<success_criteria>
- `ta_lab2.db.psycopg_helpers` exists and exports 5 functions + 2 flags
- All 3 bar builders import from the shared module
- Zero duplicated psycopg helper definitions remain in bar builder files
- No behavioral change -- all builders function identically to before
</success_criteria>

<output>
After completion, create `.planning/phases/74-foundation-shared-infrastructure/74-01-SUMMARY.md`
</output>
