---
phase: 14-tools-integration
plan: 08
type: execute
wave: 4
depends_on: ["14-03", "14-04", "14-05", "14-06", "14-07"]
files_modified:
  - .archive/data_tools/2026-02-XX/manifest.json
  - .archive/data_tools/2026-02-XX/prototypes/
  - .archive/data_tools/2026-02-XX/one_offs/
autonomous: true

must_haves:
  truths:
    - "Excluded scripts archived with manifest"
    - "Archive follows Phase 12 patterns (manifest.json, SHA256 checksums)"
    - "git mv preserves history for archived files"
  artifacts:
    - path: ".archive/data_tools/2026-02-XX/manifest.json"
      provides: "Archive manifest with file metadata"
      contains: "schema"
    - path: ".archive/data_tools/2026-02-XX/00-README.md"
      provides: "Archive documentation"
      contains: "Data_Tools"
  key_links:
    - from: ".archive/data_tools/*/manifest.json"
      to: "ta_lab2.tools.archive"
      via: "manifest format"
      pattern: "schema.*1.0.0"
---

<objective>
Archive excluded Data_Tools scripts (prototypes, one-offs, duplicates)

Purpose: Preserve scripts that weren't migrated following Phase 12 archive patterns. This ensures nothing is lost while keeping the migrated package clean.

Output: Archived scripts in .archive/data_tools/ with manifest
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/14-tools-integration/14-CONTEXT.md
@.planning/phases/14-tools-integration/14-01-SUMMARY.md

# Archive patterns from Phase 12
@src/ta_lab2/tools/archive/__init__.py
@.archive/documentation/2026-02-02/manifest.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create archive directory structure</name>
  <files>
    .archive/data_tools/2026-02-XX/00-README.md
  </files>
  <action>
Create archive structure for Data_Tools:

```
.archive/data_tools/
└── YYYY-MM-DD/             # Use actual date
    ├── 00-README.md
    ├── manifest.json
    ├── prototypes/         # Test/experimental scripts
    │   └── (archived .py files)
    └── one_offs/           # One-off instruction/utility scripts
        └── (archived .py files)
```

Create 00-README.md:
```markdown
# Archived Data_Tools Scripts

Scripts from Data_Tools that were not migrated to ta_lab2/tools/data_tools/.

## Archive Date
YYYY-MM-DD

## Reason for Archiving

| Category | Scripts | Reason |
|----------|---------|--------|
| prototypes | chatgpt_script_look*.py, chatgpt_script_keep_look*.py | Numbered variations indicate prototyping |
| one_offs | github instruction.py | One-off instruction file |
| duplicates | (any) | Functionality exists in ta_lab2 already |
| test_files | run_instantiate_final_memories_tests.py | Test file, not production tool |

## Retrieval

These files are preserved in git history. To access:
```bash
# View file at archive time
git show HEAD:.archive/data_tools/YYYY-MM-DD/prototypes/script.py

# Or checkout specific file
git checkout HEAD -- .archive/data_tools/YYYY-MM-DD/prototypes/script.py
```

## Related

- Migrated scripts: src/ta_lab2/tools/data_tools/
- Migration phase: 14 (Tools Integration)
- Discovery manifest: .planning/phases/14-tools-integration/14-01-discovery.json
```
  </action>
  <verify>Archive directory and README exist</verify>
  <done>Archive structure created with documentation</done>
</task>

<task type="auto">
  <name>Task 2: Archive prototype and one-off scripts</name>
  <files>
    .archive/data_tools/2026-02-XX/prototypes/
    .archive/data_tools/2026-02-XX/one_offs/
    .archive/data_tools/2026-02-XX/manifest.json
  </files>
  <action>
Based on 14-01-discovery.json, archive scripts marked for archiving:

**Expected prototypes (from chatgpt/):**
- chatgpt_script_look.py
- chatgpt_script_keep_look.py
- chatgpt_script_keep_look1.py
- chatgpt_script_keep_look2.py
- run_instantiate_final_memories_tests.py (test file)
- main.py (if just a placeholder/test)

**Expected one-offs (from root):**
- github instruction.py (instruction file, not a real script)

For each file:
1. Copy file to archive (use cp, not git mv, since these are external files)
2. Compute SHA256 checksum
3. Add entry to manifest.json

Use manifest format from Phase 12:
```json
{
  "$schema": "https://ta-lab2.example.com/schemas/archive-manifest-1.0.0.json",
  "version": "1.0.0",
  "created": "YYYY-MM-DDTHH:MM:SSZ",
  "category": "data_tools",
  "source": "C:/Users/asafi/Downloads/Data_Tools",
  "files": [
    {
      "original_path": "chatgpt/chatgpt_script_look.py",
      "archive_path": "prototypes/chatgpt_script_look.py",
      "checksum_sha256": "...",
      "size_bytes": 452,
      "action": "archived",
      "reason": "Prototype script - numbered variations indicate experimentation",
      "archived_at": "YYYY-MM-DDTHH:MM:SSZ"
    }
  ],
  "summary": {
    "total_files": N,
    "prototypes": X,
    "one_offs": Y
  }
}
```
  </action>
  <verify>All archived files listed in manifest.json with checksums</verify>
  <done>Scripts archived with proper manifest following Phase 12 patterns</done>
</task>

<task type="auto">
  <name>Task 3: Commit archive</name>
  <files>None (git operations only)</files>
  <action>
Stage and commit archive:

```bash
git add .archive/data_tools/
git commit -m "archive(14): preserve non-migrated Data_Tools scripts

Archived to .archive/data_tools/YYYY-MM-DD/:
- prototypes/: N experimental scripts (chatgpt_script_look*.py, etc.)
- one_offs/: M one-off utilities (github instruction.py, etc.)

Manifest includes SHA256 checksums for validation.
See 00-README.md for retrieval instructions.

Migrated scripts in: src/ta_lab2/tools/data_tools/"
```

Verify git history:
```bash
git log --oneline -1 .archive/data_tools/
```
  </action>
  <verify>Commit exists with archive changes</verify>
  <done>Archive committed with manifest and documentation</done>
</task>

</tasks>

<verification>
- `.archive/data_tools/YYYY-MM-DD/manifest.json` exists and is valid JSON
- `cat .archive/data_tools/YYYY-MM-DD/manifest.json | python -m json.tool` succeeds
- All archived files listed in manifest have matching checksums
- 00-README.md documents archive contents
</verification>

<success_criteria>
1. Archive directory created with date-stamped subdirectory
2. All scripts marked "archive" in discovery manifest are archived
3. manifest.json follows Phase 12 schema with checksums
4. 00-README.md documents archive contents and retrieval
5. Archive committed to git
</success_criteria>

<output>
After completion, create `.planning/phases/14-tools-integration/14-08-SUMMARY.md`
</output>
