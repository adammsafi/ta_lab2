---
phase: 63-tech-debt-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ta_lab2/experiments/promoter.py
  - src/ta_lab2/scripts/experiments/batch_promote_features.py
autonomous: true

must_haves:
  truths:
    - "FeaturePromoter can load IC results from cmc_ic_results table (not just cmc_feature_experiments)"
    - "batch_promote_features.py can promote bar-level features whose IC data lives only in cmc_ic_results"
    - "Existing cmc_feature_experiments promotion path continues to work unchanged"
  artifacts:
    - path: "src/ta_lab2/experiments/promoter.py"
      provides: "_load_ic_results method that queries cmc_ic_results and maps column names"
      contains: "cmc_ic_results"
    - path: "src/ta_lab2/scripts/experiments/batch_promote_features.py"
      provides: "--source flag to select ic_results vs feature_experiments"
      contains: "ic_results"
  key_links:
    - from: "src/ta_lab2/experiments/promoter.py"
      to: "cmc_ic_results"
      via: "_load_ic_results SQL query"
      pattern: "FROM public\\.cmc_ic_results"
    - from: "src/ta_lab2/experiments/promoter.py"
      to: "promote_feature"
      via: "_load_experiment_results tries both sources"
      pattern: "_load_ic_results|_load_experiment_results"
---

<objective>
Bridge the bar-level feature promotion path so FeaturePromoter can promote features whose IC data lives in cmc_ic_results (written by run_ic_sweep.py), not just cmc_feature_experiments (written by ExperimentRunner).

Purpose: 60 bar-level features from promotion_decisions.csv cannot be promoted via the automated path because their IC data is in cmc_ic_results while FeaturePromoter only reads cmc_feature_experiments. This closes that gap.

Output: Updated promoter.py with dual-source IC loading, updated batch_promote_features.py with --source flag.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

Key files:
@src/ta_lab2/experiments/promoter.py
@src/ta_lab2/scripts/experiments/batch_promote_features.py
@sql/features/080_cmc_ic_results.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add cmc_ic_results loading to FeaturePromoter</name>
  <files>src/ta_lab2/experiments/promoter.py</files>
  <action>
Add a new private method `_load_ic_results(self, feature_name: str) -> pd.DataFrame` that queries cmc_ic_results and returns a DataFrame with the same column names that `_load_experiment_results` returns. The key column mapping is:

- cmc_ic_results.`feature` -> rename to `feature_name` (to match promoter expectations)
- cmc_ic_results columns to SELECT: feature AS feature_name, asset_id, tf, horizon, return_type, regime_col, regime_label, ic, ic_t_stat, ic_p_value, ic_ir, n_obs
- WHERE feature = :name, ORDER BY horizon, asset_id, tf

Then update `_load_experiment_results` to accept an optional `source: str = "auto"` parameter:
- `"feature_experiments"`: original behavior (query cmc_feature_experiments only)
- `"ic_results"`: query cmc_ic_results only via _load_ic_results
- `"auto"` (default): try cmc_feature_experiments first; if empty, fall back to cmc_ic_results

Update `promote_feature` to pass `source` through. Add a `source` parameter to `promote_feature` with default `"auto"`. The error message in promote_feature when ic_df is empty should mention both tables when source="auto".

Do NOT change the check_bh_gate method or _write_to_registry or _generate_migration_stub -- those are source-agnostic and work with any DataFrame that has ic_p_value column.
  </action>
  <verify>
`python -c "from ta_lab2.experiments.promoter import FeaturePromoter; print('import OK')"` succeeds.
Inspect the code to confirm _load_ic_results exists, _load_experiment_results accepts source param, and promote_feature passes source through.
  </verify>
  <done>
FeaturePromoter._load_experiment_results("some_feature", source="auto") tries cmc_feature_experiments then falls back to cmc_ic_results. source="ic_results" queries cmc_ic_results directly. Existing callers with no source arg get "auto" behavior (backward compatible).
  </done>
</task>

<task type="auto">
  <name>Task 2: Add --source flag to batch_promote_features.py</name>
  <files>src/ta_lab2/scripts/experiments/batch_promote_features.py</files>
  <action>
Add a `--source` CLI argument with choices `["auto", "feature_experiments", "ic_results"]` and default `"auto"`.

Pass `source=args.source` to both `promoter.promote_feature(name, confirm=False, source=args.source)` and `promoter._load_experiment_results(name, source=args.source)` (in the dry-run path, which calls _load_experiment_results directly).

Update the dry-run path: it currently calls `promoter._load_experiment_results(name)` directly. Change this to pass `source=args.source`. Also update the "NO DATA" message to indicate which source was checked.

Do NOT change the CSV filtering logic or the overall structure of the script.
  </action>
  <verify>
`python -m ta_lab2.scripts.experiments.batch_promote_features --help` shows the --source flag with choices.
`python -c "from ta_lab2.scripts.experiments.batch_promote_features import main; print('import OK')"` succeeds.
  </verify>
  <done>
batch_promote_features.py accepts --source flag. Default "auto" checks both tables. --source ic_results enables promoting bar-level features whose IC data is only in cmc_ic_results.
  </done>
</task>

</tasks>

<verification>
1. `python -c "from ta_lab2.experiments.promoter import FeaturePromoter; print('OK')"` -- import succeeds
2. `python -m ta_lab2.scripts.experiments.batch_promote_features --help` -- shows --source with auto/feature_experiments/ic_results
3. Code review: _load_ic_results queries cmc_ic_results with correct column alias (feature AS feature_name)
4. Code review: _load_experiment_results source="auto" tries feature_experiments first, falls back to ic_results
5. Code review: promote_feature passes source parameter through
</verification>

<success_criteria>
- FeaturePromoter can load IC results from cmc_ic_results (bridging the gap for bar-level features)
- Existing promotion path from cmc_feature_experiments unchanged (backward compatible)
- batch_promote_features.py supports --source flag for explicit source selection
- No breaking changes to existing callers (source defaults to "auto")
</success_criteria>

<output>
After completion, create `.planning/phases/63-tech-debt-cleanup/63-01-SUMMARY.md`
</output>
