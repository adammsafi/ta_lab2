---
phase: 63-tech-debt-cleanup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ta_lab2/executor/position_sizer.py
  - src/ta_lab2/executor/paper_executor.py
  - sql/executor/088_dim_executor_config.sql
  - alembic/versions/<new>_add_initial_capital.py
  - src/ta_lab2/scripts/run_daily_refresh.py
autonomous: true

must_haves:
  truths:
    - "ExecutorConfig.initial_capital is loaded from dim_executor_config.initial_capital when the column exists"
    - "When initial_capital column is NULL or missing, ExecutorConfig falls back to Decimal('100000')"
    - "run_daily_refresh.py --all prints a visible WARNING when drift monitoring is skipped due to missing --paper-start"
  artifacts:
    - path: "src/ta_lab2/executor/paper_executor.py"
      provides: "_load_active_configs reads initial_capital from DB"
      contains: "initial_capital"
    - path: "src/ta_lab2/scripts/run_daily_refresh.py"
      provides: "WARN-level drift skip message"
      contains: "[WARN]"
  key_links:
    - from: "src/ta_lab2/executor/paper_executor.py"
      to: "dim_executor_config"
      via: "_load_active_configs SQL SELECT"
      pattern: "initial_capital"
    - from: "src/ta_lab2/executor/position_sizer.py"
      to: "paper_executor.py"
      via: "ExecutorConfig.initial_capital field"
      pattern: "initial_capital.*Decimal"
---

<objective>
Wire ExecutorConfig.initial_capital from dim_executor_config DB column and upgrade the drift monitor skip message from [INFO] to [WARN] with actionable guidance.

Purpose: (1) initial_capital is hardcoded to 100,000 — adding a DB column lets operators configure it per strategy without code changes. (2) Drift monitoring is silently skipped when --paper-start is absent during --all runs — operators need a visible warning.

Output: Alembic migration adding initial_capital column, updated executor config loading, upgraded drift warning message.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

Key files:
@src/ta_lab2/executor/position_sizer.py
@src/ta_lab2/executor/paper_executor.py
@sql/executor/088_dim_executor_config.sql
@src/ta_lab2/scripts/run_daily_refresh.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add initial_capital column to dim_executor_config</name>
  <files>
    sql/executor/088_dim_executor_config.sql
    alembic/versions/<new>_add_initial_capital.py
    src/ta_lab2/executor/paper_executor.py
  </files>
  <action>
**Step A: Create Alembic migration.**

Query the current Alembic head revision from the database (or from the latest migration file if DB is unavailable). Create a new migration file in alembic/versions/ named `{rev_id}_add_initial_capital_to_executor_config.py`.

The migration should:
- upgrade(): `op.add_column("dim_executor_config", sa.Column("initial_capital", sa.Numeric(), nullable=True, server_default=sa.text("100000")), schema="public")`
- downgrade(): `op.drop_column("dim_executor_config", "initial_capital", schema="public")`

Use the standard Alembic migration template. Do NOT use UTF-8 box-drawing characters (Windows cp1252 safety). Query the live head via `SELECT version_num FROM public.alembic_version LIMIT 1` -- if that fails, check the most recent migration file's `revision` field.

**Step B: Update reference DDL.**

Add the `initial_capital` column to `sql/executor/088_dim_executor_config.sql` in the position sizing section (after `max_position_fraction`):
```sql
initial_capital         NUMERIC         NOT NULL DEFAULT 100000,
```
Also add a CHECK constraint: `CONSTRAINT chk_exec_config_initial_capital CHECK (initial_capital > 0)`

**Step C: Update _load_active_configs in paper_executor.py.**

In the SQL SELECT query at line ~186-197, add `initial_capital` to the column list.

In the ExecutorConfig construction (line ~204-216), add:
```python
initial_capital=Decimal(str(row.initial_capital)) if row.initial_capital is not None else Decimal("100000"),
```

Import Decimal at the top of paper_executor.py if not already imported (check first -- it may already be imported via position_sizer).
  </action>
  <verify>
1. The new migration file exists in alembic/versions/ and has correct up/down operations.
2. `python -c "from ta_lab2.executor.paper_executor import PaperExecutor; print('import OK')"` succeeds.
3. Code review: _load_active_configs SELECT includes initial_capital, ExecutorConfig construction reads it with fallback to Decimal("100000").
  </verify>
  <done>
dim_executor_config has initial_capital column (NUMERIC, DEFAULT 100000). PaperExecutor._load_active_configs reads it into ExecutorConfig.initial_capital. NULL or missing column gracefully falls back to Decimal("100000").
  </done>
</task>

<task type="auto">
  <name>Task 2: Upgrade drift monitor skip message to WARNING</name>
  <files>src/ta_lab2/scripts/run_daily_refresh.py</files>
  <action>
Locate the `elif run_drift and not paper_start:` block around line 2131-2132.

Replace the current single-line info message:
```python
print("[INFO] Drift monitor skipped: --paper-start not provided")
```

With a more visible warning block:
```python
print()
print("[WARN] Drift monitoring SKIPPED: --paper-start not provided")
print("       To enable drift monitoring, re-run with: --paper-start YYYY-MM-DD")
print("       (Drift guard compares paper vs backtest to detect execution divergence)")
```

Also update the help text for the --paper-start argument (around line 1759-1764) to emphasize this is required for drift monitoring. Change the help text to:
```python
help=(
    "ISO date for paper trading start (e.g. 2025-01-01). "
    "REQUIRED for drift monitoring — when absent, drift stage "
    "is skipped with a warning even if --drift or --all is specified."
),
```

Do NOT change the actual drift monitoring logic, the condition checks, or any other part of run_daily_refresh.py. Only the message text and help text change.
  </action>
  <verify>
`python -m ta_lab2.scripts.run_daily_refresh --help` shows updated --paper-start help text mentioning "REQUIRED for drift monitoring".
Code review: the elif block now prints [WARN] with actionable guidance.
  </verify>
  <done>
run_daily_refresh.py --all without --paper-start prints a visible [WARN] message with instructions on how to enable drift monitoring. Help text for --paper-start clarifies it is required for drift monitoring.
  </done>
</task>

</tasks>

<verification>
1. New Alembic migration exists and has correct add_column/drop_column operations for initial_capital
2. Reference DDL (088_dim_executor_config.sql) includes initial_capital column
3. PaperExecutor._load_active_configs SELECT includes initial_capital, construction handles NULL fallback
4. `python -c "from ta_lab2.executor.paper_executor import PaperExecutor; print('OK')"` succeeds
5. run_daily_refresh.py drift skip prints [WARN] with actionable guidance (not silent [INFO])
6. `python -m ta_lab2.scripts.run_daily_refresh --help` shows updated --paper-start help text
</verification>

<success_criteria>
- ExecutorConfig.initial_capital loaded from dim_executor_config when column exists, falls back to 100000
- Alembic migration can be applied and rolled back cleanly
- Drift skip message upgraded to [WARN] with operator guidance
- All existing tests pass (no breaking changes to ExecutorConfig or PaperExecutor)
</success_criteria>

<output>
After completion, create `.planning/phases/63-tech-debt-cleanup/63-02-SUMMARY.md`
</output>
