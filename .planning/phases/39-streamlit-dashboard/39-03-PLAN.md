---
phase: 39-streamlit-dashboard
plan: 03
type: execute
wave: 2
depends_on: ["39-01"]
files_modified:
  - src/ta_lab2/dashboard/pages/1_landing.py
  - src/ta_lab2/dashboard/pages/2_pipeline_monitor.py
autonomous: true

must_haves:
  truths:
    - "Landing page shows pipeline health summary (table count, latest refresh, stats pass rate)"
    - "Landing page shows top IC scores from recent evaluations"
    - "Pipeline Monitor shows table freshness with traffic light badges (green/yellow/red)"
    - "Pipeline Monitor shows expandable table families with per-table breakdown"
    - "Pipeline Monitor shows stats PASS/FAIL counts per stats table"
    - "Pipeline Monitor shows asset coverage grid (assets x table families)"
    - "Pipeline Monitor shows recent alert history (FAIL/WARN rows from last 7 days)"
  artifacts:
    - path: "src/ta_lab2/dashboard/pages/1_landing.py"
      provides: "Landing page with summary metrics from both modes"
      min_lines: 60
    - path: "src/ta_lab2/dashboard/pages/2_pipeline_monitor.py"
      provides: "Pipeline Monitor page with freshness, stats, coverage, alerts"
      min_lines: 120
  key_links:
    - from: "src/ta_lab2/dashboard/pages/2_pipeline_monitor.py"
      to: "ta_lab2.dashboard.queries.pipeline"
      via: "import load_table_freshness, load_stats_status, load_asset_coverage, load_alert_history"
      pattern: "from ta_lab2\\.dashboard\\.queries\\.pipeline import"
    - from: "src/ta_lab2/dashboard/pages/1_landing.py"
      to: "ta_lab2.dashboard.queries.pipeline"
      via: "import freshness/stats for summary"
      pattern: "from ta_lab2\\.dashboard\\.queries\\.pipeline import"
    - from: "src/ta_lab2/dashboard/pages/1_landing.py"
      to: "ta_lab2.dashboard.queries.research"
      via: "import IC results for top scores"
      pattern: "from ta_lab2\\.dashboard\\.queries\\.research import"
---

<objective>
Build the Landing page (summary dashboard) and Pipeline Monitor page with full interactivity -- traffic light badges, expandable table families, stats panel, coverage grid, and alert history.

Purpose: These two pages complete Mode B (Pipeline Monitor) and the summary landing page (DASH-01). After this plan, users can monitor pipeline health through the dashboard.
Output: Two fully functional page files replacing the Plan 01 placeholders.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-streamlit-dashboard/39-CONTEXT.md
@.planning/phases/39-streamlit-dashboard/39-RESEARCH.md
@.planning/phases/39-streamlit-dashboard/39-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Landing page with summary metrics</name>
  <files>src/ta_lab2/dashboard/pages/1_landing.py</files>
  <action>
Replace the placeholder `1_landing.py` with the full landing page. Do NOT call st.set_page_config() -- that is only in app.py.

Import from:
- `ta_lab2.dashboard.db` -> `get_engine`
- `ta_lab2.dashboard.queries.pipeline` -> `load_table_freshness`, `load_stats_status`
- `ta_lab2.dashboard.queries.research` -> `load_ic_results`, `load_asset_list`

Layout:
1. `st.header("Dashboard Home")`
2. `st.caption("Pipeline health + research highlights at a glance")`

3. **Pipeline Health Summary** (left column in 2-column layout):
   - `st.subheader("Pipeline Health")`
   - Load freshness data via `load_table_freshness(get_engine())`
   - Show 3 metric cards using `st.metric()`:
     - "Tables Tracked": count of source_table rows
     - "Latest Refresh": formatted timestamp of most recent last_refresh
     - "Avg Staleness": mean of staleness_hours, formatted as "Xh"
   - Load stats via `load_stats_status(get_engine())`
   - Calculate total PASS/FAIL/WARN across all tables
   - Show stats summary: `st.metric("Stats Pass Rate", f"{pass_rate:.0%}")` where pass_rate = total_pass / (total_pass + total_fail + total_warn) or 0 if no data

4. **Research Highlights** (right column):
   - `st.subheader("Top IC Scores")`
   - Load asset list, pick the first available asset (or BTC id=1 as default)
   - Load IC results for that asset at tf="1D"
   - If results exist:
     - Sort by absolute IC descending
     - Show top 10 features as a DataFrame with columns: feature, horizon, ic, ic_p_value
     - Use `st.dataframe(top_ic_df, width="stretch")` (NOT use_container_width)
   - If no results: `st.info("No IC results available. Run: python -m ta_lab2.scripts.ic.run_ic_eval")`

5. **Quick Links** section at bottom:
   - `st.divider()`
   - Two columns with `st.page_link()` buttons pointing to Pipeline Monitor and Research Explorer pages

Handle all errors gracefully with try/except -> st.warning("Could not load data: {error}")
  </action>
  <verify>
`python -c "import ast; ast.parse(open('src/ta_lab2/dashboard/pages/1_landing.py', encoding='utf-8').read()); print('Syntax OK')"`
Verify file has st.header, st.metric calls, st.dataframe, and imports from queries modules.
  </verify>
  <done>Landing page shows pipeline health metrics (tables tracked, latest refresh, staleness, stats pass rate) and top IC scores table. Quick links to Pipeline Monitor and Research Explorer at bottom.</done>
</task>

<task type="auto">
  <name>Task 2: Pipeline Monitor page</name>
  <files>src/ta_lab2/dashboard/pages/2_pipeline_monitor.py</files>
  <action>
Replace the placeholder `2_pipeline_monitor.py` with the full Pipeline Monitor page. Do NOT call st.set_page_config().

Import from:
- `ta_lab2.dashboard.db` -> `get_engine`
- `ta_lab2.dashboard.queries.pipeline` -> `load_table_freshness`, `load_stats_status`, `load_asset_coverage`, `load_alert_history`

Layout -- 4 sections:

**Section 1: Data Freshness** (`st.subheader("Data Freshness")`)
- Load freshness via `load_table_freshness(get_engine())`
- Define TABLE_FAMILIES dict mapping display names to source_table prefixes:
  ```python
  TABLE_FAMILIES = {
      "Price Bars": "cmc_price_bars",
      "Bar Returns": "cmc_returns_bars",
      "EMA Values": "cmc_ema",
      "EMA Returns": "cmc_returns_ema",
      "AMA Values": "cmc_ama",
      "Volatility": "cmc_vol",
      "TA Indicators": "cmc_ta",
      "Regimes": "cmc_regime",
      "Features": "cmc_features",
  }
  ```
- For each family, group source_tables matching the prefix
- Display as expandable rows using `st.expander()`:
  - Summary row (outside expander or as expander label): family name + traffic light badge + n_assets count
  - Traffic light logic using `st.badge()`:
    - staleness_hours < 24: `st.badge("Fresh", color="green")`
    - 24 <= staleness_hours < 72: `st.badge("Stale", color="yellow")`
    - staleness_hours >= 72 or None: `st.badge("Very Stale", color="red")`
  - Inside expander: per-table breakdown as a small DataFrame showing source_table, n_assets, latest_data_ts, staleness_hours
  - Add tooltip with exact timestamp via the `help=` parameter on the badge

**Section 2: Stats Runner Status** (`st.subheader("Stats Runner Status")`)
- Load stats via `load_stats_status(get_engine())`
- Display as a grid of columns (3 per row) using `st.columns(3)`:
  - For each stats table: show table name (cleaned), PASS/WARN/FAIL counts
  - Color: green text for PASS, yellow for WARN, red for FAIL
  - Use `st.metric()` with delta for FAIL count (negative delta_color)
- If no stats data: `st.info("No stats data in the last 24 hours")`

**Section 3: Asset Coverage Grid** (`st.subheader("Asset Coverage")`)
- Load coverage via `load_asset_coverage(get_engine())`
- Build a pivot table: rows = symbol (from dim_assets join), columns = source_table families, values = n_rows
- Display using `st.dataframe()` with conditional formatting:
  - Use pandas Styler to color cells: 0 or NaN = red background, >0 = green background
  - Apply `.style.map()` or `.style.applymap()` for cell coloring (use `map` if applymap is deprecated)
- Add CSV download button: `st.download_button("Download Coverage CSV", data=coverage_df.to_csv(index=True).encode("utf-8"), file_name="asset_coverage.csv", mime="text/csv")`

**Section 4: Alert History** (`st.subheader("Recent Alerts")`)
- Load alerts via `load_alert_history(get_engine())`
- If alerts exist:
  - Display as a table with columns: stats_table, status, checked_at
  - Color status column: FAIL=red, WARN=yellow
  - Use `st.dataframe()` with column configuration if available
- If no alerts: `st.success("No FAIL or WARN alerts in the last 7 days")` (green success box)

All sections wrapped in try/except -> st.error("Failed to load {section}: {error}")
  </action>
  <verify>
`python -c "import ast; ast.parse(open('src/ta_lab2/dashboard/pages/2_pipeline_monitor.py', encoding='utf-8').read()); print('Syntax OK')"`
Verify file has all 4 sections: freshness with st.badge, stats with st.metric, coverage grid pivot, alert history table.
Verify imports from ta_lab2.dashboard.queries.pipeline.
  </verify>
  <done>Pipeline Monitor page has 4 sections: data freshness with traffic light badges and expandable families, stats runner status grid, asset coverage pivot matrix with conditional coloring, and alert history table. All data sourced from cached query functions.</done>
</task>

</tasks>

<verification>
1. Landing page shows pipeline health metrics in left column and top IC scores in right column
2. Pipeline Monitor data freshness uses st.badge with green/yellow/red based on staleness_hours thresholds
3. Pipeline Monitor stats section shows PASS/FAIL/WARN per stats table
4. Coverage grid is a pivot table (symbol x source_table_family) with conditional coloring
5. Alert history shows recent FAIL/WARN from stats tables
6. Neither page calls st.set_page_config()
7. All data loaded via cached query functions from Plan 01
8. CSV download available for coverage grid
</verification>

<success_criteria>
- Pipeline Monitor displays run status, data freshness per table, and stats runner PASS/FAIL (DASH-01)
- Landing page provides at-a-glance summary from both modes
- Traffic light badges accurately reflect staleness thresholds
- All sections handle empty data gracefully
</success_criteria>

<output>
After completion, create `.planning/phases/39-streamlit-dashboard/39-03-SUMMARY.md`
</output>
