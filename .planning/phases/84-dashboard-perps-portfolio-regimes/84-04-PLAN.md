---
phase: 84-dashboard-perps-portfolio-regimes
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ta_lab2/dashboard/pages/15_portfolio.py
autonomous: true

must_haves:
  truths:
    - "User sees a portfolio allocation page with mock/placeholder data demonstrating intended layout"
    - "User sees a treemap AND stacked bar chart with toggle between them for exposure visualization"
    - "User sees weight history as both stacked area chart and timestamped table"
    - "User sees position sizing with bet size per asset and risk budget utilization"
    - "Page clearly indicates it is a placeholder pending Phase 86 BL pipeline"
    - "Page auto-refreshes every 15 minutes via @st.fragment(run_every=900)"
  artifacts:
    - path: "src/ta_lab2/dashboard/pages/15_portfolio.py"
      provides: "Portfolio Allocation placeholder page with mock data"
      min_lines: 120
  key_links:
    - from: "src/ta_lab2/dashboard/pages/15_portfolio.py"
      to: "ta_lab2.dashboard.db"
      via: "import get_engine (for future use)"
      pattern: "from ta_lab2\\.dashboard\\.db import get_engine"
---

<objective>
Build the Portfolio Allocation placeholder page with mock data and full layout structure.

Purpose: Create the complete portfolio page layout with sample data so the structure is ready to wire when Phase 86 delivers the Black-Litterman pipeline. No query layer needed -- all data is generated inline as mock data.

Output: pages/15_portfolio.py (placeholder page with mock data, ready for Phase 86 wiring)
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/84-dashboard-perps-portfolio-regimes/84-RESEARCH.md
@src/ta_lab2/dashboard/db.py
@src/ta_lab2/dashboard/charts.py
@src/ta_lab2/dashboard/pages/13_asset_hub.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create pages/15_portfolio.py -- Portfolio Allocation placeholder</name>
  <files>src/ta_lab2/dashboard/pages/15_portfolio.py</files>
  <action>
Create `src/ta_lab2/dashboard/pages/15_portfolio.py` following the exact page pattern:
- `from __future__ import annotations` at top
- NO `st.set_page_config()` call
- Sidebar controls OUTSIDE `@st.fragment`
- Unique `key=` on every widget and chart
- Import `get_engine` from db.py (for future use -- don't call it if no DB queries needed)
- Import `chart_download_button` from charts.py

**Placeholder banner at top:**
```python
st.header("Portfolio Allocation")
st.caption("Black-Litterman weights, position sizing, and exposure")
st.info(
    "This page uses mock data. Live portfolio data will be available after "
    "Phase 86 (Portfolio Construction Pipeline) is complete.",
    icon=":material/construction:",
)
```

**Mock data generation (inline, using numpy.random with seed=42 for reproducibility):**
Generate representative sample data using `numpy.random.default_rng(42)`:
- 10 assets: BTC, ETH, SOL, AVAX, LINK, DOT, UNI, AAVE, MATIC, ARB
- BL weights: realistic allocation (BTC 35%, ETH 25%, SOL 10%, others 3-8%)
- 30-day weight history: daily snapshots with small random walk perturbations
- Position sizing: bet_size_usd, risk_budget_pct, max_position_pct per asset
- Strategy labels: "momentum", "mean_reversion", "trend_following"

**Section 1: Current Allocation**
Two sub-views via `st.radio("View", ["Treemap", "Stacked Bar"], key="portfolio_view_toggle")`:

Treemap:
- `go.Treemap` with labels=asset symbols, values=weight_pct, parents=strategy
- Color by weight (higher=greener)
- plotly_dark template

Stacked Bar:
- `go.Bar` stacked: x=strategy, y=weight per asset within strategy
- One bar per strategy, segments colored by asset
- plotly_dark template

**Section 2: Weight History**
Two sub-views via `st.radio("Format", ["Area Chart", "Table"], key="portfolio_history_toggle")`:

Area Chart:
- `go.Scatter` with fill='tonexty' for each asset, stacked area
- x=date (30 days), y=weight
- plotly_dark template

Table:
- `st.dataframe` with columns: date, then one column per asset showing weight_%
- Format weights as "35.2%" using `format` config

**Section 3: Position Sizing**
Two columns layout:

Left column -- Bet Size per Asset:
- `go.Bar` horizontal: y=asset, x=bet_size_usd
- Color by risk tier (green=low risk, yellow=medium, red=high)
- Add rationale text as hover: "Momentum allocation based on IC-IR=0.85"

Right column -- Risk Budget Utilization:
- `go.Indicator` gauges (or `st.progress` bars) showing % of risk budget used per asset
- Total risk budget utilization as a summary metric

**Section 4: Exposure Summary**
- `st.dataframe` with columns: asset, strategy, weight_pct, bet_size_usd, risk_budget_pct, max_position_pct
- Summary row at bottom with totals

**Sidebar controls (outside fragment):**
- Date range: `st.date_input("As of Date", value=datetime.date.today())` -- decorative for now
- Strategy filter: `st.multiselect` with all 3 strategies, default all selected

**Auto-refresh:**
Wrap content in `@st.fragment(run_every=900)`.

IMPORTANT:
- Use `numpy.random.default_rng(42)` for reproducibility (decision from Phase 83)
- All charts use plotly_dark template
- Mock data should look realistic and demonstrate the intended final behavior
- Comments in code marking where live queries will replace mock data (e.g., `# TODO(Phase-86): Replace with load_bl_weights(engine)`)
  </action>
  <verify>
`ruff check src/ta_lab2/dashboard/pages/15_portfolio.py` passes.
`python -c "import ast; ast.parse(open('src/ta_lab2/dashboard/pages/15_portfolio.py').read()); print('OK')"` prints OK.
  </verify>
  <done>Portfolio page renders with mock data: treemap/bar toggle for allocation, area chart/table toggle for weight history, position sizing bars + risk gauges, and exposure summary table. Placeholder banner clearly indicates Phase 86 dependency.</done>
</task>

</tasks>

<verification>
- `ruff check src/ta_lab2/dashboard/pages/15_portfolio.py` -- clean
- No `st.set_page_config()` in file
- All `st.plotly_chart` calls have unique `key=` argument
- `numpy.random.default_rng(42)` used for reproducible mock data
- Placeholder info banner visible at page top
- TODO comments mark where Phase 86 live data will replace mocks
- All charts use plotly_dark template
</verification>

<success_criteria>
1. pages/15_portfolio.py renders placeholder page with realistic mock data
2. Treemap and stacked bar toggle works
3. Weight history area chart and table toggle works
4. Position sizing and risk budget sections render
5. Placeholder banner clearly visible
6. @st.fragment(run_every=900) wraps content
7. ruff check passes
</success_criteria>

<output>
After completion, create `.planning/phases/84-dashboard-perps-portfolio-regimes/84-04-SUMMARY.md`
</output>
