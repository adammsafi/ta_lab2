---
phase: 84-dashboard-perps-portfolio-regimes
plan: 05
type: execute
wave: 2
depends_on: ["84-01", "84-02", "84-03", "84-04"]
files_modified:
  - src/ta_lab2/dashboard/app.py
autonomous: false

must_haves:
  truths:
    - "All 4 new pages appear in the sidebar navigation under correct groups"
    - "Perps, Portfolio, AMA Inspector pages appear in Analysis group"
    - "Regime Heatmap appears in Analysis group"
    - "User can navigate to each new page via sidebar and see rendered content"
    - "Existing pages remain functional and unchanged"
  artifacts:
    - path: "src/ta_lab2/dashboard/app.py"
      provides: "Updated navigation with 4 new pages"
      contains: "14_perps.py"
  key_links:
    - from: "src/ta_lab2/dashboard/app.py"
      to: "src/ta_lab2/dashboard/pages/14_perps.py"
      via: "st.Page registration"
      pattern: "pages/14_perps\\.py"
    - from: "src/ta_lab2/dashboard/app.py"
      to: "src/ta_lab2/dashboard/pages/15_portfolio.py"
      via: "st.Page registration"
      pattern: "pages/15_portfolio\\.py"
    - from: "src/ta_lab2/dashboard/app.py"
      to: "src/ta_lab2/dashboard/pages/16_regime_heatmap.py"
      via: "st.Page registration"
      pattern: "pages/16_regime_heatmap\\.py"
    - from: "src/ta_lab2/dashboard/app.py"
      to: "src/ta_lab2/dashboard/pages/17_ama_inspector.py"
      via: "st.Page registration"
      pattern: "pages/17_ama_inspector\\.py"
---

<objective>
Register all 4 new pages in app.py sidebar navigation and verify the complete dashboard works end-to-end.

Purpose: Wire the new pages into the Streamlit multipage navigation so users can access them. Then verify all pages render without errors.

Output: Updated app.py with 4 new page registrations + human verification of all pages
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/84-dashboard-perps-portfolio-regimes/84-RESEARCH.md
@src/ta_lab2/dashboard/app.py
@.planning/phases/84-dashboard-perps-portfolio-regimes/84-01-SUMMARY.md
@.planning/phases/84-dashboard-perps-portfolio-regimes/84-02-SUMMARY.md
@.planning/phases/84-dashboard-perps-portfolio-regimes/84-03-SUMMARY.md
@.planning/phases/84-dashboard-perps-portfolio-regimes/84-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register 4 new pages in app.py</name>
  <files>src/ta_lab2/dashboard/app.py</files>
  <action>
Read `src/ta_lab2/dashboard/app.py` and add 4 new page entries to the "Analysis" group in the `pages` dict.

Add these 4 entries AFTER the existing "Asset Statistics" entry in the "Analysis" group:

```python
st.Page(
    "pages/14_perps.py",
    title="Perps",
    icon=":material/currency_exchange:",
),
st.Page(
    "pages/15_portfolio.py",
    title="Portfolio",
    icon=":material/account_balance:",
),
st.Page(
    "pages/16_regime_heatmap.py",
    title="Regime Heatmap",
    icon=":material/grid_view:",
),
st.Page(
    "pages/17_ama_inspector.py",
    title="AMA Inspector",
    icon=":material/ssid_chart:",
),
```

Do NOT modify any existing page entries. Only ADD the 4 new ones.
Ensure the Analysis group now has 10 entries total (6 existing + 4 new).

Also verify:
- No other files need changes
- The pages dict structure is valid Python
- All 4 new page filenames match the files created in Plans 01-04
  </action>
  <verify>
`ruff check src/ta_lab2/dashboard/app.py` passes.
`python -c "import ast; ast.parse(open('src/ta_lab2/dashboard/app.py').read()); print('OK')"` prints OK.
Grep for all 4 new page paths in app.py: `grep -c "14_perps\|15_portfolio\|16_regime_heatmap\|17_ama_inspector" src/ta_lab2/dashboard/app.py` returns 4.
  </verify>
  <done>app.py has all 4 new pages registered in Analysis group. Existing pages unchanged.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
4 new dashboard pages:
1. Perps (14_perps.py) -- Hyperliquid perpetuals: top perps, funding rates, heatmap, candles, OI
2. Portfolio (15_portfolio.py) -- Portfolio allocation placeholder with mock data
3. Regime Heatmap (16_regime_heatmap.py) -- Cross-asset regime visualization
4. AMA Inspector (17_ama_inspector.py) -- AMA/EMA analysis with derivative curves
  </what-built>
  <how-to-verify>
1. Start the dashboard: `streamlit run src/ta_lab2/dashboard/app.py`
2. Verify sidebar shows all 4 new pages under "Analysis" group
3. Click "Perps" -- verify top perps table loads, funding heatmap renders, candle chart works
4. Click "Portfolio" -- verify placeholder banner visible, mock charts render with treemap/bar toggle
5. Click "Regime Heatmap" -- verify heatmap loads for top 30 assets, regime stats cards show
6. Click "AMA Inspector" -- verify AMA curves load for KAMA, ER chart appears. Switch to DEMA and verify ER chart hides with info message.
7. Verify all existing pages (Landing, Asset Hub, Backtest Results, etc.) still work
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
- app.py has 4 new st.Page entries in Analysis group
- All 4 pages appear in sidebar when dashboard is running
- Each page renders without errors
- Existing pages unaffected
- ruff check passes on app.py
</verification>

<success_criteria>
1. app.py updated with all 4 new page registrations
2. Dashboard sidebar shows Perps, Portfolio, Regime Heatmap, AMA Inspector
3. Each new page loads and renders its primary content
4. Existing pages continue to function
5. Human verification confirms visual correctness
</success_criteria>

<output>
After completion, create `.planning/phases/84-dashboard-perps-portfolio-regimes/84-05-SUMMARY.md`
</output>
