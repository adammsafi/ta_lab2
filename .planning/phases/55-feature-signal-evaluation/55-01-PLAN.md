---
phase: 55-feature-signal-evaluation
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ta_lab2/scripts/analysis/run_ic_sweep.py
  - reports/evaluation/ic_ranking_full.csv
  - reports/evaluation/methodology_verification.csv
autonomous: true

must_haves:
  truths:
    - "Phase 42 IC results verified as methodologically identical (recomputed sample matches to 6+ decimal places)"
    - "IC scores exist in cmc_ic_results for all qualifying (asset, tf) pairs at all 109 TFs"
    - "Regime-conditional IC breakdown exists for BTC and ETH at 1D"
    - "Feature ranking CSV written to reports/evaluation/ with all features ranked by |IC-IR|"
  artifacts:
    - path: "reports/evaluation/methodology_verification.csv"
      provides: "Side-by-side IC comparison: Phase 42 vs recomputed sample"
    - path: "reports/evaluation/ic_ranking_full.csv"
      provides: "All-TF feature ranking by |IC-IR| aggregated across assets"
  key_links:
    - from: "run_ic_sweep.py"
      to: "cmc_ic_results"
      via: "save_ic_results upsert"
      pattern: "save_ic_results"
    - from: "run_ic_sweep.py"
      to: "cmc_features"
      via: "batch_compute_ic on feature columns"
      pattern: "batch_compute_ic"
---

<objective>
Verify Phase 42 IC methodology is unchanged, then run the full IC sweep across all assets x all 109 TFs with regime breakdown, populating cmc_ic_results and producing a comprehensive feature ranking CSV.

Purpose: EVAL-01 requires IC scores for all canonical features across all assets x all TFs. Phase 42 only covered 5 TFs (47,614 rows). This plan extends to all 109 TFs while preserving existing results.
Output: ~1M+ IC rows in cmc_ic_results; reports/evaluation/ic_ranking_full.csv; methodology verification artifact.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/55-feature-signal-evaluation/55-CONTEXT.md
@.planning/phases/55-feature-signal-evaluation/55-RESEARCH.md
@src/ta_lab2/scripts/analysis/run_ic_sweep.py
@src/ta_lab2/analysis/ic.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Methodology verification + output directory setup</name>
  <files>reports/evaluation/methodology_verification.csv</files>
  <action>
1. Create reports/evaluation/ directory (matching reports/bakeoff/ pattern from Phase 42).

2. Run methodology verification: recompute IC for BTC (id=1) on 1D for 3-5 well-known features (rsi_14, ret_arith, bb_ma_20 or similar available features) using run_ic_eval.py. Compare the recomputed values against existing cmc_ic_results rows for the same (asset_id=1, tf='1D', feature, horizon) combinations.

   Steps:
   a. Query existing IC values from cmc_ic_results for asset_id=1, tf='1D', feature IN ('rsi_14', 'ret_arith', ...), horizon IN (1, 5, 10, 20).
   b. Recompute using: `python -m ta_lab2.scripts.analysis.run_ic_eval --asset-id 1 --tf 1D --feature rsi_14 ret_arith --train-start 2014-01-01 --train-end 2026-02-01`
   c. Compare results. If IC values match to 6+ decimal places, methodology is identical -- log PASS.
   d. If values differ by more than 1e-6, log WARNING with discrepancy details but proceed (existing data may use different train bounds).
   e. Write comparison to reports/evaluation/methodology_verification.csv with columns: feature, horizon, phase42_ic, recomputed_ic, delta, status (MATCH/WARN).

3. Run dry-run to count qualifying pairs: `python -m ta_lab2.scripts.analysis.run_ic_sweep --dry-run --all --min-bars 500` to estimate scope and wall-clock time.

IMPORTANT: Do NOT use --overwrite for the verification step. Use --no-overwrite (append semantics) to preserve Phase 42 data.
  </action>
  <verify>
- reports/evaluation/ directory exists
- reports/evaluation/methodology_verification.csv has rows with MATCH or WARN status
- Dry-run output shows the count of qualifying (asset, tf) pairs
  </verify>
  <done>Methodology verified (MATCH or documented WARN); qualifying pair count known; reports/evaluation/ directory created</done>
</task>

<task type="auto">
  <name>Task 2: Full IC sweep across all assets x all TFs</name>
  <files>reports/evaluation/ic_ranking_full.csv, src/ta_lab2/scripts/analysis/run_ic_sweep.py</files>
  <action>
1. Run the full IC sweep. If the --output-dir flag is already supported by run_ic_sweep.py, use it to write the ranking CSV to reports/evaluation/. If not, add a simple --output-dir argument to run_ic_sweep.py that controls where the feature_ic_ranking.csv is written (default remains reports/bakeoff/ for backward compat).

   Command: `python -m ta_lab2.scripts.analysis.run_ic_sweep --all --regime --min-bars 500`

   This will:
   - Sweep cmc_features columns across all qualifying (asset, tf) pairs
   - Sweep AMA columns if cmc_ama_multi_tf_u has data (graceful skip if empty)
   - Run regime breakdown for BTC (id=1) and ETH (id=1027) on 1D
   - Write results to cmc_ic_results via upsert (preserves Phase 42 data, updates if same keys)

   NOTE: This sweep may take several hours (estimated 9-10 hours for all 109 TFs). The script already has per-pair transaction isolation so it can be interrupted and resumed (upserted rows persist). If timing is prohibitive, it is acceptable to run in stages:
   - Stage 1: Key TFs first (1D, 7D, 14D, 30D, 90D) -- these already exist from Phase 42
   - Stage 2: Remaining TFs in batches

2. After the sweep completes (or first stage), generate the full ranking CSV:
   - Query cmc_ic_results: SELECT feature, AVG(ABS(ic_ir)) as mean_abs_ic_ir, COUNT(*) as n_pairs GROUP BY feature ORDER BY mean_abs_ic_ir DESC
   - Write to reports/evaluation/ic_ranking_full.csv
   - Print top-20 features by |IC-IR| to console

3. Verify dashboard visibility: query cmc_ic_results to confirm rows exist for a sample of non-Phase-42 TFs (e.g., 3D, 5D, 2W).
  </action>
  <verify>
- `python -c "from sqlalchemy import create_engine, text; ... SELECT COUNT(*) FROM cmc_ic_results"` shows significantly more rows than 47,614 (Phase 42 baseline)
- reports/evaluation/ic_ranking_full.csv exists and has rows
- At least one non-Phase-42 TF (e.g., 3D) has IC rows in cmc_ic_results
  </verify>
  <done>cmc_ic_results populated with IC scores across all qualifying asset x TF pairs; ranking CSV generated in reports/evaluation/; regime breakdown present for BTC/ETH 1D</done>
</task>

</tasks>

<verification>
- cmc_ic_results row count >> 47,614 (Phase 42 baseline)
- reports/evaluation/methodology_verification.csv confirms methodology consistency
- reports/evaluation/ic_ranking_full.csv contains feature rankings across all TFs
- Research Explorer dashboard page shows IC data for non-Phase-42 TFs
</verification>

<success_criteria>
EVAL-01 satisfied: IC scores computed for all canonical features across all assets x all TFs, ranked by IC-IR. Phase 42 data preserved and verified.
</success_criteria>

<output>
After completion, create `.planning/phases/55-feature-signal-evaluation/55-01-SUMMARY.md`
</output>
