---
phase: 37-ic-evaluation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ta_lab2/analysis/feature_eval.py
  - alembic/versions/XXXX_ic_results_table.py
  - sql/features/080_cmc_ic_results.sql
autonomous: true

must_haves:
  truths:
    - "fillna(method='ffill') no longer appears in feature_eval.py"
    - "alembic upgrade head creates cmc_ic_results table with correct columns and constraints"
    - "alembic downgrade -1 drops cmc_ic_results cleanly"
    - "cmc_ic_results has unique constraint on (asset_id, tf, feature, horizon, return_type, regime_col, regime_label, train_start, train_end)"
    - "cmc_ic_results has indexes on (asset_id, tf, feature) and (computed_at)"
  artifacts:
    - path: "src/ta_lab2/analysis/feature_eval.py"
      provides: "Fixed fillna deprecation"
      contains: ".ffill()"
    - path: "alembic/versions/XXXX_ic_results_table.py"
      provides: "Alembic migration creating cmc_ic_results table"
      contains: "cmc_ic_results"
    - path: "sql/features/080_cmc_ic_results.sql"
      provides: "Reference DDL documentation for cmc_ic_results"
      contains: "CREATE TABLE"
  key_links:
    - from: "alembic/versions/XXXX_ic_results_table.py"
      to: "alembic/versions/5f8223cfbf06_psr_results_table.py"
      via: "down_revision chain"
      pattern: "down_revision.*5f8223cfbf06"
---

<objective>
Fix the fillna deprecation in feature_eval.py and create the Alembic migration for the cmc_ic_results table.

Purpose: The fillna fix is a blocking prerequisite (prior decision: "v0.9.0 fix fillna deprecation before IC"). The cmc_ic_results table is needed by IC-08 for persisting evaluation results. Both are small, independent tasks that clear the path for IC implementation.

Output: Fixed feature_eval.py, Alembic migration file, and reference DDL.

NOTE: `XXXX` in filenames is a placeholder. Use `alembic revision -m "ic_results_table"` to generate the actual revision hash.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-ic-evaluation/37-RESEARCH.md

@src/ta_lab2/analysis/feature_eval.py
@alembic/env.py
@alembic/versions/5f8223cfbf06_psr_results_table.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix fillna deprecation in feature_eval.py</name>
  <files>src/ta_lab2/analysis/feature_eval.py</files>
  <action>
On line 78 of feature_eval.py, replace:

```python
X = df.loc[y.index, cols].fillna(method="ffill").fillna(0.0)
```

with:

```python
X = df.loc[y.index, cols].ffill().fillna(0.0)
```

This is the only change needed. Do NOT modify any other code in feature_eval.py.

Why: `fillna(method='ffill')` is deprecated in pandas and will become an error in future versions. The prior decision explicitly requires this fix before adding IC code.
  </action>
  <verify>
Run `python -c "from ta_lab2.analysis.feature_eval import quick_logit_feature_weights; print('import OK')"` to confirm no import errors.
Run `grep -n "fillna(method" src/ta_lab2/analysis/feature_eval.py` and confirm zero matches.
Run `grep -n ".ffill()" src/ta_lab2/analysis/feature_eval.py` and confirm at least one match on line 78.
  </verify>
  <done>
fillna(method='ffill') replaced with .ffill() on line 78 of feature_eval.py. No other changes. Module imports cleanly.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Alembic migration for cmc_ic_results table + reference DDL</name>
  <files>alembic/versions/XXXX_ic_results_table.py, sql/features/080_cmc_ic_results.sql</files>
  <action>
Generate a new Alembic revision using `alembic revision -m "ic_results_table"` from the project root. The filename will contain an auto-generated revision hash (XXXX is a placeholder).

Set `down_revision = '5f8223cfbf06'` (Phase 36 head, psr_results_table).

In upgrade(), create `cmc_ic_results` table in the `public` schema with these columns:
- `result_id` UUID PK with `server_default=sa.text('gen_random_uuid()')`, nullable=False
- `asset_id` INTEGER NOT NULL
- `tf` TEXT NOT NULL
- `feature` TEXT NOT NULL
- `horizon` INTEGER NOT NULL (bar count)
- `horizon_days` INTEGER nullable (calendar days, derived from horizon * tf_days_nominal)
- `return_type` TEXT NOT NULL ('arith' or 'log')
- `regime_col` TEXT NOT NULL ('trend_state', 'vol_state', or 'all')
- `regime_label` TEXT NOT NULL (e.g. 'Up', 'High', 'all')
- `train_start` TIMESTAMP(timezone=True) NOT NULL
- `train_end` TIMESTAMP(timezone=True) NOT NULL
- `ic` NUMERIC nullable
- `ic_t_stat` NUMERIC nullable
- `ic_p_value` NUMERIC nullable
- `ic_ir` NUMERIC nullable
- `ic_ir_t_stat` NUMERIC nullable
- `turnover` NUMERIC nullable
- `n_obs` INTEGER nullable
- `computed_at` TIMESTAMP(timezone=True) NOT NULL, `server_default=sa.text('now()')`

Constraints:
- PrimaryKeyConstraint on `result_id`
- UniqueConstraint named `uq_ic_results_key` on `(asset_id, tf, feature, horizon, return_type, regime_col, regime_label, train_start, train_end)`

Indexes:
- `idx_ic_results_asset_feature` on `(asset_id, tf, feature)` in schema public
- `idx_ic_results_computed_at` on `(computed_at)` in schema public

In downgrade(), drop both indexes then drop the table (all in public schema).

CRITICAL: Do NOT use any UTF-8 box-drawing characters in comments (Windows encoding pitfall per MEMORY.md). Use plain ASCII dashes or equals signs only.

Also create `sql/features/080_cmc_ic_results.sql` as reference DDL with an equivalent CREATE TABLE statement plus COMMENT ON TABLE. This file is NOT executed by Alembic -- it documents the schema for human readers, matching the project convention (040, 041, 042 already exist).
  </action>
  <verify>
Run `alembic history` and confirm the new revision appears chained from 5f8223cfbf06.
Run `alembic upgrade head` and confirm it completes without error.
Run the following SQL to verify the table exists and has the correct columns:
```sql
SELECT column_name, data_type FROM information_schema.columns
WHERE table_name='cmc_ic_results' ORDER BY ordinal_position;
```
Confirm all 19 columns exist.
Run `alembic downgrade -1` and confirm the table is dropped.
Run `alembic upgrade head` again to leave DB at head for downstream plans.
Confirm `sql/features/080_cmc_ic_results.sql` exists.
  </verify>
  <done>
Alembic migration creates cmc_ic_results table chained from 5f8223cfbf06 with correct schema: UUID PK, unique constraint on 9-column natural key, two indexes. Reference DDL exists at sql/features/080_cmc_ic_results.sql. Upgrade and downgrade both work cleanly.
  </done>
</task>

</tasks>

<verification>
- `grep -rn "fillna(method" src/ta_lab2/analysis/feature_eval.py` returns zero matches
- `alembic history` shows: baseline -> psr_column_rename -> psr_results_table -> ic_results_table (4 revisions, linear chain)
- `alembic current` shows ic_results_table revision as head
- cmc_ic_results table exists with 19 columns, UUID PK, unique constraint, and two indexes
- `alembic downgrade -1 && alembic upgrade head` round-trips cleanly
- `sql/features/080_cmc_ic_results.sql` exists with matching CREATE TABLE
</verification>

<success_criteria>
- fillna deprecation fixed (prior decision satisfied)
- IC-08 partially satisfied: cmc_ic_results table exists and is ready for data
- Alembic chain intact: 5f8223cfbf06 -> new ic_results revision
- Reference DDL documents the schema
</success_criteria>

<output>
After completion, create `.planning/phases/37-ic-evaluation/37-01-SUMMARY.md`
</output>
