---
phase: 110-feature-parallel-subphases
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ta_lab2/scripts/features/run_all_feature_refreshes.py
autonomous: true

must_haves:
  truths:
    - "Wave 1 runs all 4 sub-phases (vol, ta, cycle_stats, rolling_extremes) simultaneously with 4 threads"
    - "--workers CLI flag controls Wave 1 thread count (default 4)"
    - "Worker budget warning fires when tf_workers * wave1_workers > 8"
    - "Wave labels in log output identify each stage (Wave 1, Wave 2, Wave 2b, Wave 2c, Wave 3)"
    - "Microstructure remains in Wave 2b (after features unified), NOT in Wave 1"
  artifacts:
    - path: "src/ta_lab2/scripts/features/run_all_feature_refreshes.py"
      provides: "Parallelized feature refresh orchestrator with --workers flag"
      contains: "max_workers=wave1_workers"
  key_links:
    - from: "parse_args()"
      to: "run_all_refreshes()"
      via: "wave1_workers parameter threaded through main() and _run_single_tf()"
      pattern: "wave1_workers"
    - from: "main()"
      to: "logger.warning"
      via: "worker budget guard"
      pattern: "tf_workers.*wave1_workers.*8"
---

<objective>
Parallelize all 4 Wave 1 feature sub-phases and add CLI control for worker count.

Purpose: Reduce full feature recompute from ~100min to ~65min by running vol, ta, cycle_stats,
and rolling_extremes with 4 threads instead of 3 (FEAT-04, FEAT-05).

Output: Updated run_all_feature_refreshes.py with --workers flag, budget guard, and wave logging.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/110-feature-parallel-subphases/110-RESEARCH.md
@src/ta_lab2/scripts/features/run_all_feature_refreshes.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add --workers CLI flag, wave1_workers parameter, and budget guard</name>
  <files>src/ta_lab2/scripts/features/run_all_feature_refreshes.py</files>
  <action>
Three changes to run_all_feature_refreshes.py:

1. **Add `wave1_workers` parameter to `run_all_refreshes()` (line 416):**
   - Add `wave1_workers: int = 4` to the function signature
   - Change `ThreadPoolExecutor(max_workers=3)` at line 450 to `ThreadPoolExecutor(max_workers=wave1_workers)`

2. **Add `--workers` CLI argument in `parse_args()` (after --tf-workers block, around line 852):**
   ```python
   parser.add_argument(
       "--workers",
       type=int,
       default=4,
       dest="wave1_workers",
       help="Number of parallel workers for Wave 1 sub-phases (vol/ta/cycle_stats/rolling_extremes). Default 4, max 4.",
   )
   ```

3. **Thread `wave1_workers` through all call sites:**
   - In `main()` sequential path (~line 996): pass `wave1_workers=args.wave1_workers` to `run_all_refreshes()`
   - In `_run_single_tf()` (~line 674): add `wave1_workers` to the args_tuple unpacking (10th element), pass it to `run_all_refreshes()`
   - In `main()` parallel path (~line 942): add `args.wave1_workers` to the work_items tuple
   - Clamp the value: `wave1_workers = min(args.wave1_workers, len(phase1_tasks))` — never more workers than tasks

4. **Add worker budget guard in `main()` (after args parsing, before engine creation):**
   ```python
   if args.tf_workers > 1:
       total_workers = args.tf_workers * args.wave1_workers
       if total_workers > 8:
           logger.warning(
               "Total concurrent workers=%d (tf_workers=%d x wave1_workers=%d) may cause "
               "memory pressure with large asset sets. Consider reducing --tf-workers or --workers.",
               total_workers,
               args.tf_workers,
               args.wave1_workers,
           )
   ```

IMPORTANT: Do NOT move microstructure to Wave 1. It MUST remain after features unified (Phase 2b).
The code comment at line 508 ("MUST run after Phase 2") is correct and must be preserved.
  </action>
  <verify>
  - `python -c "from ta_lab2.scripts.features.run_all_feature_refreshes import run_all_refreshes; import inspect; sig = inspect.signature(run_all_refreshes); assert 'wave1_workers' in sig.parameters, 'missing wave1_workers param'"` passes
  - `python -m ta_lab2.scripts.features.run_all_feature_refreshes --help` shows `--workers` flag with default 4
  - `grep -c "wave1_workers" src/ta_lab2/scripts/features/run_all_feature_refreshes.py` returns >= 8 (signature, ThreadPoolExecutor, parse_args, main sequential, main parallel, _run_single_tf unpack, _run_single_tf pass, budget guard)
  - `grep "max_workers=3" src/ta_lab2/scripts/features/run_all_feature_refreshes.py` returns NO matches (old hardcoded value replaced)
  </verify>
  <done>
  - run_all_refreshes() accepts wave1_workers parameter (default 4)
  - ThreadPoolExecutor uses wave1_workers instead of hardcoded 3
  - --workers CLI flag exposed and threaded through both sequential and parallel paths
  - Budget guard warns when tf_workers * wave1_workers > 8
  - Microstructure remains in Phase 2b position (unchanged)
  </done>
</task>

<task type="auto">
  <name>Task 2: Update wave logging labels throughout pipeline</name>
  <files>src/ta_lab2/scripts/features/run_all_feature_refreshes.py</files>
  <action>
Update all log messages in `run_all_refreshes()` to use consistent "Wave N" labels
that match the documented wave structure. This is a log-message-only change (no logic changes).

Current labels -> New labels:
- Line 448 `"Phase 1: Running vol/ta in parallel"` -> `"Wave 1: Running vol/ta/cycle_stats/rolling_extremes in parallel (%d workers)" % wave1_workers`
- Line 478 `"Phase 1: Running vol/ta sequentially"` -> `"Wave 1: Running vol/ta/cycle_stats/rolling_extremes sequentially"`
- Line 494 `"Phase 2: Running features (unified view)"` -> `"Wave 2: Running features (unified view) -- depends on Wave 1"`
- Line 511 `"Phase 2b: Running microstructure feature UPDATE on features"` -> `"Wave 2b: Running microstructure UPDATE -- depends on Wave 2"`
- Line 529 `"Phase 2c: Running CTF features (cross-timeframe)"` -> `"Wave 3: Running CTF features (cross-timeframe) -- depends on Wave 2b"`
- Line 548 `"Phase 2c: Skipping CTF features (module not available)"` -> `"Wave 3: Skipping CTF features (module not available)"`
- Line 553/563 CS norms messages: prefix with `"Wave 4:"` instead of `"Phase 3:"`

Also update the module docstring (lines 1-24) to reflect the Wave 1/2/2b/3/4 naming and
mention the --workers flag.

Do NOT change any logic, control flow, or error handling. Only log strings and docstring.
  </action>
  <verify>
  - `grep -c "Wave 1:" src/ta_lab2/scripts/features/run_all_feature_refreshes.py` returns >= 2 (parallel + sequential paths)
  - `grep -c "Wave 2:" src/ta_lab2/scripts/features/run_all_feature_refreshes.py` returns >= 1
  - `grep -c "Wave 2b:" src/ta_lab2/scripts/features/run_all_feature_refreshes.py` returns >= 1
  - `grep -c "Wave 3:" src/ta_lab2/scripts/features/run_all_feature_refreshes.py` returns >= 1
  - `grep -c "Wave 4:" src/ta_lab2/scripts/features/run_all_feature_refreshes.py` returns >= 1
  - `grep "Phase 1:" src/ta_lab2/scripts/features/run_all_feature_refreshes.py` returns NO matches (all renamed)
  - `grep "Phase 2:" src/ta_lab2/scripts/features/run_all_feature_refreshes.py` returns NO matches
  - `grep "Phase 3:" src/ta_lab2/scripts/features/run_all_feature_refreshes.py` returns NO matches
  </verify>
  <done>
  - All log messages use consistent Wave N labeling (Wave 1, 2, 2b, 3, 4)
  - Wave 1 parallel log shows worker count
  - Module docstring updated with wave structure and --workers flag
  - No logic changes, only log strings and docstring
  </done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `python -m ta_lab2.scripts.features.run_all_feature_refreshes --help` shows --workers flag
2. `python -c "from ta_lab2.scripts.features.run_all_feature_refreshes import run_all_refreshes; import inspect; sig = inspect.signature(run_all_refreshes); print(sig)"` shows wave1_workers param
3. No references to old `max_workers=3` remain
4. No references to old "Phase 1/2/3:" log labels remain
5. Microstructure position unchanged (still after features unified)
6. `--sequential` flag still works (escape hatch preserved)
</verification>

<success_criteria>
- FEAT-04: Wave 1 runs vol, ta, cycle_stats, rolling_extremes in parallel with 4 workers (all independent sub-phases grouped)
- FEAT-05: Estimated full recompute ~65min (within 70min target) -- actual measurement deferred to operational testing
- --workers CLI flag controls Wave 1 parallelism (default 4)
- Worker budget guard warns on excessive parallelism (tf_workers * wave1_workers > 8)
- Log output uses consistent Wave 1/2/2b/3/4 labels for observability
</success_criteria>

<output>
After completion, create `.planning/phases/110-feature-parallel-subphases/110-01-SUMMARY.md`
</output>
