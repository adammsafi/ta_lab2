---
phase: 93-fix-integration-breaks
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ta_lab2/scripts/integration/smoke_test.py
  - src/ta_lab2/scripts/executor/run_parity_check.py
autonomous: true

must_haves:
  truths:
    - "Smoke test GARCH stage queries use `id` column (not `asset_id`)"
    - "All 26 smoke test checks pass including both GARCH checks"
    - "_STRATEGY_SIGNAL_MAP covers all 9 bakeoff winner strategies"
    - "Parity check attempts all 9 strategies (not just 3)"
  artifacts:
    - path: "src/ta_lab2/scripts/integration/smoke_test.py"
      provides: "Fixed GARCH queries using correct PK column"
      contains: "WHERE id=1"
    - path: "src/ta_lab2/scripts/executor/run_parity_check.py"
      provides: "Complete strategy-to-signal mapping"
      contains: "rsi_mean_revert"
  key_links:
    - from: "smoke_test.py _build_garch_checks()"
      to: "garch_forecasts table"
      via: "SQL query with id=1"
      pattern: "WHERE id=1"
    - from: "run_parity_check.py _STRATEGY_SIGNAL_MAP"
      to: "dim_signals table"
      via: "signal_type lookup"
      pattern: "ema_trend.*ema_crossover|rsi_mean_revert|atr_breakout|macd_crossover"
---

<objective>
Fix two integration breaks found in v1.2.0 audit: (1) smoke test GARCH queries use wrong column name `asset_id` instead of `id`, causing ProgrammingError; (2) parity check only maps 3/9 strategies, silently skipping 6.

Purpose: Close REQ-15 (at risk), Break 1 (HIGH), Break 3 (MEDIUM) from v1.2.0 milestone audit.
Output: Both scripts work correctly with no column errors and full strategy coverage.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/ta_lab2/scripts/integration/smoke_test.py
@src/ta_lab2/scripts/executor/run_parity_check.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix GARCH column bug in smoke_test.py</name>
  <files>src/ta_lab2/scripts/integration/smoke_test.py</files>
  <action>
In `_build_garch_checks()` (around lines 349-368), replace `asset_id=1` with `id=1` in both GARCH query strings:

1. Line 356: Change `"WHERE asset_id=1 "` to `"WHERE id=1 "`
2. Line 364: Change `"SELECT cond_vol FROM garch_forecasts_latest WHERE asset_id=1 LIMIT 1"` to `"SELECT cond_vol FROM garch_forecasts_latest WHERE id=1 LIMIT 1"`

The garch_forecasts PK is `(id, venue_id, ts, tf, model_type, horizon)` — there is no `asset_id` column. The `id` column maps to cmc_da_ids.id (BTC=1).

This is a 2-line fix. Do NOT change anything else in the file.
  </action>
  <verify>
Run: `python -c "import ast; ast.parse(open('src/ta_lab2/scripts/integration/smoke_test.py').read()); print('syntax ok')"` to confirm no syntax errors.

Then grep to confirm the fix: `grep -n 'asset_id' src/ta_lab2/scripts/integration/smoke_test.py` should return NO matches in the GARCH section (lines 345-370). The string `id=1` should appear on both lines.
  </verify>
  <done>Both GARCH queries use `id=1` instead of `asset_id=1`. No other changes to the file.</done>
</task>

<task type="auto">
  <name>Task 2: Expand _STRATEGY_SIGNAL_MAP in run_parity_check.py</name>
  <files>src/ta_lab2/scripts/executor/run_parity_check.py</files>
  <action>
Expand `_STRATEGY_SIGNAL_MAP` (lines 49-53) from 3 entries to cover all 9 bakeoff strategies. Replace the existing dict with:

```python
_STRATEGY_SIGNAL_MAP: dict[str, str] = {
    "ama_momentum": "ema_crossover",
    "ama_mean_reversion": "ema_crossover",
    "ama_regime_conditional": "ema_crossover",
    "ema_trend": "ema_crossover",
    "macd_crossover": "ema_crossover",
    "rsi_mean_revert": "rsi_mean_revert",
    "breakout_atr": "atr_breakout",
}
```

That is 7 distinct strategies. The remaining 2 of the "9 bakeoff winners" may be expression-engine strategies from Phase 82. To handle those gracefully, also add a fallback after the existing `if signal_type is None` block (around line 148-154). Change the warning+skip to attempt a direct lookup in dim_signals using the strategy_name as signal_type before skipping:

```python
signal_type = _STRATEGY_SIGNAL_MAP.get(strategy_name)
if signal_type is None:
    # Fallback: try strategy_name as signal_type directly
    signal_type = strategy_name
    logger.info(
        "No explicit mapping for strategy=%s -- trying as signal_type",
        strategy_name,
    )
```

This way expression-engine or future strategies that match their dim_signals signal_type by name will work automatically, while truly unknown ones will fail at the signal_row lookup and be logged as a warning (the existing None-check on signal_row at line ~160 handles this).

Do NOT change any other logic in the file.
  </action>
  <verify>
Run: `python -c "import ast; ast.parse(open('src/ta_lab2/scripts/executor/run_parity_check.py').read()); print('syntax ok')"` to confirm no syntax errors.

Grep to confirm: `grep -c 'ema_trend\|rsi_mean_revert\|breakout_atr\|macd_crossover' src/ta_lab2/scripts/executor/run_parity_check.py` should return 4+ matches (the new entries).
  </verify>
  <done>_STRATEGY_SIGNAL_MAP has 7 explicit entries covering all known strategies. Unknown strategies fall back to using strategy_name as signal_type instead of silently skipping.</done>
</task>

</tasks>

<verification>
1. `python -c "import ast; ast.parse(open('src/ta_lab2/scripts/integration/smoke_test.py').read())"` -- syntax valid
2. `python -c "import ast; ast.parse(open('src/ta_lab2/scripts/executor/run_parity_check.py').read())"` -- syntax valid
3. `grep -n 'WHERE id=1' src/ta_lab2/scripts/integration/smoke_test.py` -- shows 2 matches in GARCH section
4. `grep -n 'asset_id' src/ta_lab2/scripts/integration/smoke_test.py` -- zero matches in GARCH section (lines 345-370)
5. `python -c "from ta_lab2.scripts.executor.run_parity_check import _STRATEGY_SIGNAL_MAP; assert len(_STRATEGY_SIGNAL_MAP) >= 7, f'only {len(_STRATEGY_SIGNAL_MAP)} strategies'; print(f'{len(_STRATEGY_SIGNAL_MAP)} strategies mapped')"` -- confirms 7+ strategies
</verification>

<success_criteria>
- Smoke test GARCH queries use `id=1` (not `asset_id=1`)
- _STRATEGY_SIGNAL_MAP has 7+ entries (was 3)
- Unknown strategies fall back gracefully instead of silently skipping
- Both files parse without syntax errors
</success_criteria>

<output>
After completion, create `.planning/phases/93-fix-integration-breaks/93-01-SUMMARY.md`
</output>
